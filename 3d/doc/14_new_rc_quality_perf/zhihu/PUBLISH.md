# 知乎专栏粘贴说明

知乎不渲染 mermaid，也不显示 SVG。配图已是 PNG。按顺序在编辑器里上传后，再贴正文。

知乎开放平台 CLI 没有创建或更新专栏的命令。仓库里的 push 只更新粘贴包；上线仍要在专栏编辑器里手动贴。

## 读者

图形学研究生：知道路径追踪、探针 GI、立体角、图集，但不熟悉本仓库的门禁名、CLI 和阶段编号。正文已按此重写。

## 标题

不要拿「看起来像路径追踪」当全局光照正确

## 导语（约 40 字）

实现等于定义、近似够不够、这一帧付不付得起，是三层证据。混用它们，会把算法自己的上限当成 bug。

## 配图上传顺序

1. `images/01-two-paths.png` — 表面附着 vs 体网格，同名不是同一算法
2. `images/02-three-layers.png` — 实现 / 近似 / 成本，红了停在本层
3. `images/03-quality-knobs.png` — 空间与角度绑在同一幂次上
4. `images/04-split-dispatch.png` — 空转是图集留白，按矩形少启动线程
5. `images/05-roadmap.png` — 先能验收，再换真实网格

正文里的本地图片路径粘贴后不会自动带上文件；在对应位置插入刚上传的图。

## 和已有专栏的关系

账号里已有 2D 辐射度级联课程。本文是 3D 表面路径的方法论续篇，不替换那些课。建议发新专栏。

## 标签建议

图形学、全局光照、Radiance Cascades、实时渲染、路径追踪
