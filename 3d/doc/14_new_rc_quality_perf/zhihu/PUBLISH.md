# 知乎专栏粘贴说明

知乎不渲染 mermaid，也不显示 SVG。配图已是 PNG。按顺序在编辑器里上传后，再贴正文。

## 读者

图形学研究生。正文按能顺着读的口语写。不出现仓库内部的门禁名和阶段编号。旧的体空间路径只提一句。

## 标题

不要拿「看起来像路径追踪」当全局光照正确

## 导语

实现没跑偏、亮得合不合理、这一帧跑不跑得起，是三问。混用它们，会把算法自己能细到的上限当成 bug。

## 配图上传顺序

1. `images/00-cascades.png` — 近处排得密，远处看得细
2. `images/02-three-layers.png` — 对 / 亮 / 快 三问
3. `images/03-quality-knobs.png` — 切成 4 块 vs 16 块
4. `images/04-split-dispatch.png` — 空白角落不必算
5. `images/05-roadmap.png` — 先盒子对上，再换大厅

`01-two-paths.png` 仍在目录里，正文不再用。旧路径只留一句，不单独占一张图。

正文里的本地图片路径粘贴后不会自动带上文件；在对应位置插入刚上传的图。

## 标签建议

图形学、全局光照、Radiance Cascades、实时渲染、路径追踪
