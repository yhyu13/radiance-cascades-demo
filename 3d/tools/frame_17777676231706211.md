# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777676231706211_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777676239285913_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777676246893937_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17777676231706211.json`  
**Analyzed:** 2026-05-03T08:20:56  
**Model:** claude-opus-4-7

---

# Visual Triage Report — Cornell Box GI

## 1. GI Contribution Analysis

Comparing **mode 0 (final)** against **mode 6 (GI only)**:

- **Color bleed onto floor**: In mode 6, the floor near the red wall (lower-left interior) shows a clear pinkish tint, and the floor near the green wall (lower-right) shows a faint green tint. This is genuine indirect bounce — visible but weak.
- **Ceiling**: Mode 6 shows the ceiling is dimly lit by bounced light (the light source is implied to be ceiling-mounted given the bright spot), with reddish wash on the left half and greenish on the right half. Good directional separation.
- **Tall block (left)**: The right-facing side of the tall block shows a faint green spill in mode 6 — correct, since the green wall is closer on that side. The left-facing side has a subtle red tint.
- **Where GI is *absent***: The shadow under/behind the short block (right side) in mode 0 is nearly black. Mode 6 confirms very little fill light reaches into that occluded pocket — the GI is failing to wrap around the short block.

## 2. Banding in Mode 6

I can see:
- **Soft vertical/horizontal blockiness** on the back wall and floor — roughly 6–8 large patches across the wall width. Spacing is consistent with a **low-resolution probe grid** rather than directional bins (directional bins would produce angular streaks radiating from probe centers; these are axis-aligned blocks).
- **Stair-step boundary** visible along the ceiling-to-back-wall seam and around the tall block silhouette — characteristic of **probe interpolation across a geometric discontinuity** (probes straddling the wall leak into the interior).
- **No clear ring-shaped or radial banding**, so directional bin count (`dirRes=8`) is not the dominant artifact source.
- **No visible cascade-boundary seam** (no concentric square ring around the camera) — cascade transitions appear blended.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: Looks **very smooth, almost too smooth** — no high-frequency noise, but contact shadows under the blocks have completely disappeared. Indirect term is essentially a low-pass version of the scene. This is consistent with `temporalAlpha=0.05` (heavy temporal accumulation) plus low probe resolution.
- **Mode 0 (final)**: The bilateral blur has **not over-smoothed direct-lit edges** — block silhouettes, the wall corners, and the cast shadow of the tall block on the floor remain crisp. Good edge preservation. However, the contact shadow at the base of the short block looks slightly softened.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| Mode 0 (final) | **Good** | Crisp direct, plausible bleed, edges preserved |
| Mode 3 (indirect ×5) | **Fair** | Smooth but lacks any small-scale structure; near-field occlusion missing |
| Mode 6 (GI only) | **Fair** | Visible probe-grid blockiness on back wall and ceiling |

## 5. Stats Cross-Check & Recommendation

Key observations from stats:
- **Cascade 0 surfPct = 29%** — only ~29% of finest-cascade probes hit a surface. This is the under-sampled cascade, and it's the one responsible for near-field bleed and contact GI. **This explains the missing occlusion fill behind the short block.**
- **Cascade 3 variance = 0.00016** — essentially flat, contributing only a uniform ambient bias. Confirms no cascade-seam artifact (matches visual).
- **Cascade 1–2 variance ~0.006** — moderate, consistent with the soft blockiness seen on the back wall.
- `temporalAlpha=0.05` is aggressive smoothing → matches the over-smoothed mode 3.

### Concrete parameter change

**Increase probe spatial resolution for cascade 0** (or equivalently shrink cascade 0's footprint so its probes are denser near surfaces). The 29% surfPct on the finest cascade indicates probes are wasted in empty space while near-surface regions are under-sampled — this is the root cause of both the back-wall blockiness in mode 6 and the missing contact GI behind the short block.

If probe-resolution tuning isn't
