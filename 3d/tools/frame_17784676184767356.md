# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784676184767356_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784676185935217_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784676186814360_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784676184767356.json`  
**Analyzed:** 2026-05-11T10:47:30  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing mode 0 (final) against modes 3 and 6:

- **Ceiling region (top of frame)**: The reddish/brown stair-step patches on the ceiling appear in all three modes with similar intensity, indicating these are direct-lit emissive or strongly bounce-lit features carried by the GI buffer.
- **Right wall (foreground bulge ~x=900–1280)**: Strong warm reddish indirect contribution visible in mode 6 and amplified heavily in mode 3. The right wall is receiving most of its color from bounce light — there is essentially no direct illumination here, only GI.
- **Left wall (~x=0–300)**: Very dim in mode 6, only mildly brighter in mode 3 (still near white/neutral). This wall is under-lit by indirect — the final composite gets most of its left-wall brightness from direct lighting.
- **Floor center strip**: The bright specks running down the floor centerline in mode 0 are *absent* from modes 3/6 — those are direct light hits (light source visible at end of corridor), not GI.
- **End of corridor (~x=450, y=300)**: The bright doorway/light source is dim in mode 6 and only modestly visible in mode 3, confirming it is a direct emitter, not an indirect feature.

## 2. Banding in Mode 6

- **Ceiling stair-step patterns**: Discrete horizontal/diagonal "staircase" bands across the ceiling at roughly uniform spacing. The spacing matches a low-resolution probe grid being trilinearly sampled across an angled surface — characteristic of **probe-grid aliasing** (baseRes=32 with volumeSize=4 → ~0.125 unit probe spacing, visible at this distance).
- **Right wall soft vertical bands**: Smooth vertical gradient with subtle stepping at roughly even intervals down the wall. This is consistent with **cascade-boundary transitions** (4 cascades with c0MinRange=0.5, c1MinRange=1) — the falloff gradient where cascade 0 hands off to cascade 1.
- **No clear directional-bin streaks**: With dirRes=8 (64 directions) I do not see distinct radial spoke artifacts, so directional binning is not the dominant artifact source.

## 3. Smoothness of Mode 3 vs Over-smoothing in Mode 0

- **Mode 3** is spatially smooth on walls and floor — almost too smooth, with no high-frequency noise. Combined with temporalAlpha=0.05 (heavy temporal accumulation), this confirms the indirect term is well-integrated but **lagged**.
- **Mode 0**: The bilateral blur has clearly over-smoothed the **right wall edge** — the foreground occluder bleeds into the corridor with no crisp silhouette. Similarly, the **left wall edge** at ~x=200 is mushy. Ceiling features remain pixelated/blocky because they originate from the low-res probe grid itself, not blur-able detail.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Fair** | Lighting reads correctly, but bilateral blur over-softens silhouettes; ceiling probe blockiness leaks through composite. |
| 3 (Indirect ×5) | **Good** | Smooth, low-noise; useful for diagnosis. Reveals probe-grid stepping clearly. |
| 6 (GI only) | **Fair** | Visible probe-grid banding on ceiling and cascade transition on right wall. |

## 5. Stats Cross-Check & Recommendation

- `surfPct` for cascade 0 = **48.6%** — only half of cascade-0 probes hit a surface. This is the lowest of the four and explains why near-field GI (right wall foreground) shows the most banding.
- `variance` decreases monotonically across cascades (1.0e-3 → 8.9e-6), consistent with the visible cascade-handoff banding on the right wall.
- `maxLum` 0.54 in cascade 0 vs 0.13 in cascade 2 shows large dynamic range across cascades — another driver of visible boundaries.
- Raymarch cost is modest (1.55 ms), so we have headroom.

**Concrete change**: **Increase `c0MinRange` from 0.5 → 0.75** (and/or raise `
