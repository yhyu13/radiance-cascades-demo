# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777725728216653_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777725735631856_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777725742877690_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17777725728216653.json`  
**Analyzed:** 2026-05-03T09:43:29  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Render Modes

## 1. GI Contribution Isolation

Comparing mode 0 (final) against mode 6 (GI only), the indirect light is doing the following work:

- **Color bleed onto floor**: visible in mode 6 as a faint reddish tint on the left half of the floor and a greenish tint on the right half — confirmed by mode 0 where the floor near the red wall is subtly warmer.
- **Ceiling fill**: mode 6 shows the only illumination on the ceiling near the light fixture (small bright spot center-top), bleeding outward.
- **Box side-lighting**: in mode 6, the right-facing side of the tall box picks up a green tint, and the left-facing side of the short cube picks up a faint warm tint. This is the classic Cornell color-bleed signal.
- **GI is absent / very weak** on the back wall — mode 6 shows it as nearly flat dim gray, when a real bounce solution should show a brighter band where the floor reflects upward.

## 2. Banding in Mode 6

Looking at mode 6 carefully:

- **Soft blocky halo around the box silhouette** (gray fringe extending ~1 probe-cell beyond the geometry on all sides). Spacing is coarse and aligned to world axes — consistent with the **probe grid at baseRes=32 / volumeSize=4** (≈ 0.125 units per probe, which matches the visible cell size).
- **No fine pinwheel or radial banding** that would indicate dirRes=8 directional-bin aliasing.
- **No abrupt brightness step partway across the room** that would indicate a cascade-boundary seam.

Diagnosis: the dominant artifact is **probe-grid leakage/dilation**, not directional binning or cascade transitions.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: spatially **smooth, not noisy** — almost too smooth. Walls show clean gradients with no stochastic speckle. This indicates temporal accumulation is doing heavy lifting (temporalAlpha=0.05 is very low → ~20-frame EMA).
- **Mode 0 (final)**: bilateral blur looks **well-tuned on large surfaces** but has slightly **over-smoothed the contact shadow** under the tall box (the AO darkening is softer than the geometry edge would predict). Box edges themselves are preserved.
- The geometry "halo" outside the box silhouette in modes 3/6 is **not** blur over-reach — it's the probe volume bleeding past surfaces, then being faithfully reproduced.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 — Final | **Good** | Clean, plausible Cornell. Minor over-soft contact shadow. |
| 3 — Indirect ×5 | **Fair** | Smooth but reveals probe-grid halo around geometry. |
| 6 — GI only | **Fair** | Confirms color bleed works, but back-wall bounce is weak and grid leakage is obvious. |

## 5. Stats Cross-Check & Recommendation

Stats summary:
- `surfPct` 98.3 → 99.2 → 100 → 100: **good convergence**, all probes hit something.
- Variance drops monotonically across cascades (0.0085 → 0.00016): **healthy cascade falloff**, no boundary discontinuity expected — matches what I see.
- C0 variance 0.0085 with maxLum 0.56 vs meanLum 0.053 → **~10× peak-to-mean ratio** in the finest cascade. That's the source of the visible halo: a few bright probes near the light/walls are dilating into empty cells.
- `temporalAlpha=0.05` is aggressive smoothing → explains the lack of noise in mode 3.

### Concrete change

**Increase `probeJitterScale` from 0.06 to ~0.15** (and optionally raise `baseRes` from 32 to 48 if budget allows; raymarch is only 0.11 ms).

Rationale: the probe-grid halo around geometry in mode 6 is the dominant artifact, and C0's high peak-to-mean variance (0.0085 with 10× outliers) means individual probes are leaking. Larger jitter will spatially decorrelate which probes sample the bright bounce surfaces frame-to-frame, and the existing low temporalAlpha (0.
