# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785569299045831_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785569301961658_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785569305648502_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17785569299045831.json`  
**Analyzed:** 2026-05-12T11:36:01  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Hallway GI

## 1. GI Contribution Isolation

Comparing mode 0 (final) against mode 6 (GI only), the indirect term is doing most of the heavy lifting in this scene — the two images are nearly identical in the dark regions, indicating direct light contribution is small here (the scene is lit primarily by the ceiling lamp emitters bouncing off the orange/copper walls).

**Where GI is bright:**
- The **right wall** (lower half, copper/orange band) shows strong indirect bounce — clearly visible in mode 3 as a continuous bright strip running down the corridor.
- The **ceiling around each lamp fixture** receives strong contribution — visible as halos in mode 3.
- The **left window/alcove area** (left third of frame) shows the brightest GI response, consistent with light spilling from openings.

**Where GI is absent/weak:**
- The **far end of the corridor** (vanishing point) is noticeably dark in mode 6, suggesting cascade coverage falls off with distance.
- The **floor center stripe** is darker than the side walls in mode 6, despite being closer to the lamps — likely a probe sampling gap.

## 2. Banding Artifacts in Mode 6

Visible banding patterns:
- **Concentric arc bands on the right wall** — large, smooth curves spaced roughly every ~80–120 px in the lower right quadrant. The radius and softness suggest **cascade boundary transitions** (cascade 2 → 3 falloff), not directional bins.
- **Step discontinuities along the floor/wall seam** on the right — small periodic blocks. These align with **probe-grid spacing** (baseRes 64, volumeSize 4 → ~16 cm probe spacing in world units).
- No obvious rotational/wedge banding that would indicate dirRes=8 directional binning is the dominant artifact.

## 3. Smoothness Assessment

**Mode 3** is spatially **smooth to the point of being washed out** — the 5× amplification reveals very low-frequency information with almost no high-frequency detail. This indicates either heavy temporal accumulation (consistent with `temporalAlpha: 0.05`) or aggressive pre-filtering. The far end fades to a soft gradient with no visible probe noise, but also no defined contact shadows.

**Mode 0**: The bilateral blur appears **slightly over-aggressive** on the right wall — the orange band's top edge bleeds upward into the dark wall above it (visible halo). The lamp fixture edges on the ceiling are also softened more than the geometry itself. However, contact darkening at the floor/wall junction is preserved.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Good** | Cohesive, atmospheric; mild edge bleed on right wall |
| 3 (Indirect ×5) | **Fair** | Too smooth — suggests under-resolved spatial detail masked by temporal blur |
| 6 (GI only) | **Fair** | Visible cascade arcs and probe-grid stepping on right wall; far corridor underlit |

## 5. Stats Cross-Check & Recommendation

The cascade stats show serious problems:
- **Cascade 0: anyPct=0** — the nearest cascade has no hits at all. This is why contact lighting looks soft.
- **Cascades 1–2: surfPct=0, NaN luminance** — probes are firing but recording invalid data; `maxLum: inf` in cascade 1 indicates a fireflies/division-by-zero in accumulation.
- **Cascade 3: anyPct=96.9%** — only the coarsest cascade is well-populated, which explains why the visible banding is large smooth arcs (cascade 3 dominates) rather than fine probe noise.

### Concrete parameter change:

**Increase `c0MinRange` from 0.5 to ~1.5–2.0** (and correspondingly raise `c1MinRange`). Cascade 0 currently captures nothing because its range is too short relative to scene scale (volumeSize=4). Extending c0's reach will populate the near cascade, fix the contact-lighting flatness, and reduce reliance on cascade 3 — which should eliminate the concentric arc banding on the right wall.

Secondary fix: investigate the NaN/inf in cascades 1–2 before tuning anything else — those invalid luminance values are likely being
