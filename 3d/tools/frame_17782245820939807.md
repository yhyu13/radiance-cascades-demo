# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782245820939807_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782245821766124_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782245822383024_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17782245820939807.json`  
**Analyzed:** 2026-05-08T15:16:54  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Renders

## 1. GI Contribution Analysis

Comparing the three views, the indirect light is concentrated almost entirely in a **narrow horizontal band along the top rim** of the box (roughly y≈180–230 px), with a slight wraparound onto the upper side walls. This is visible in all three modes but isolated cleanly in mode 6.

- **Bright GI**: top lip/rim, upper inner walls just below the rim, and faint vertical falloff streaks down the left and right inner walls (~x=210 and x=1070).
- **Absent GI**: the entire lower two-thirds of the interior (below y≈400) is essentially black even in mode 3 (5× amplified). Either the probes there see no bounce contribution, or the floor/lower cascade has collapsed to near-zero.

The scene is clearly lit from above (sky/area light through the open top), and indirect bounces are not propagating down into the cavity.

## 2. Banding in Mode 6

The most visible structure in mode 6:
- A **soft horizontal seam** at roughly y≈230–250 px, just below the bright rim — this looks like a **cascade-boundary** transition (C0→C1 handoff near the top of the volume).
- **Faint vertical bright streaks** at the left/right inner walls, regularly spaced — consistent with **probe-grid aliasing** at baseRes=32 over a 4-unit volume (probe spacing ≈ 0.125 units, projecting to ~8–12 px columns at this camera distance).
- No obvious **directional-bin** pinwheels or angular discontinuities are visible — dirRes=8 (64 dirs) appears adequate for this nearly-diffuse scene.

## 3. Smoothness / Blur Assessment

- **Mode 3** looks **spatially smooth**, almost too smooth — the 5× amplification reveals no per-pixel noise, only large-scale gradients. This is consistent with the very low temporalAlpha (0.05) heavily averaging frames, and with the low variance numbers (C3 variance = 6.5e-6).
- **Mode 0** shows no obvious over-blur on geometric edges — the rim highlight and cup silhouette remain crisp. Bilateral blur is behaving.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Fair** | Crisp edges, but the interior is crushed to black — GI is not filling the cavity. |
| 3 (Indirect ×5) | **Fair** | Smooth and noise-free, but reveals the GI signal is essentially absent below the rim. |
| 6 (GI only) | **Poor** | Confirms raw GI carries almost no energy into the lower volume; faint cascade seam visible. |

## 5. Stats Cross-Check & Recommendation

Key observations from stats:
- `surfPct` is only **81.6% on C0** and **85.2% on C1** — ~15–18% of the finest probes are missing surface hits. This matches the dark lower interior.
- `meanLum` **decreases monotonically** across cascades (0.0159 → 0.0068), which is backwards — coarser cascades should aggregate *more* energy, not less. C3 looks under-contributing.
- `skyPct = 0` everywhere — **no probe is seeing the sky**, even though the box is open at top. This is the smoking gun: sky/miss rays are not being credited, so only surface bounces contribute, and they decay rapidly with depth.
- Variance is low (6.5e-6 on C3), consistent with the smooth mode-3 appearance — banding is structural, not noise.

**Concrete change**: The biggest leverage is the missing sky contribution, but assuming sky sampling is a separate fix, the most actionable probe-side change is:

> **Increase `volumeSize` from 4 → 6 (or raise `c0MinRange`/`c1MinRange` to 2)** so that C0/C1 probes near the floor can actually reach the lit rim/sky within their march range. The monotonically-decreasing meanLum across cascades and the 81% surfPct on C0 both indicate finer cascades are starving — they're not reaching the lit geometry. Extending range will let lower-cavity probes pick up the rim bounce and lift the black interior.

If sky misses *should* be contributing, also verify the miss shader is writing to the radiance target — `skyPct=0` across all cascades
