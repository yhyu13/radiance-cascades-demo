# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785702672216367_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785702673321601_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785702675020973_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17785702672216367.json`  
**Analyzed:** 2026-05-12T15:18:17  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing the three views, the indirect light contribution is concentrated in:

- **Floor directly beneath the hanging lanterns** (center-left band running down the corridor): bright pooled splotches in mode 6, confirming bounce light from the lamps is being captured.
- **Right wall lower section** near the orange/copper baseboard: warm bounce visible in mode 6, contributing the saturated rust tones in the mode 0 composite.
- **End of corridor (vanishing point)**: the orange doorway/back wall in mode 0 owes its visibility almost entirely to GI — mode 3 confirms strong indirect there, and direct-only would leave it black.
- **GI is largely absent on the right wall mid-to-upper region**: it stays dim brown in all three modes, meaning bounces aren't reaching it (likely occluded from the lamps by the ceiling overhang).

## 2. Banding in Mode 6

Visible artifacts:
- **Concentric "onion-ring" arcs on the right wall**, spaced roughly every 40–60 px in screen space, centered roughly on the camera-forward axis. Their **radial, view-dependent geometry** points to **cascade boundaries** (probe volumes are camera-anchored at 4³ size with 4 cascades doubling in range).
- **Repeating blob pattern on the floor** (~30–50 px spacing) aligned with world axes — this is the **probe grid** at `baseRes=64` showing through; bilateral filtering can't fully hide it because temporalAlpha is so low that probes barely move per-frame.
- I do **not** see clear directional-bin striping (no angular pinwheels), so `dirRes=8` is adequate here.

## 3. Spatial Smoothness

- **Mode 3** is quite smooth — almost over-blurred, with soft halos around lanterns and no high-frequency noise. The temporal accumulation (alpha=0.05) is doing heavy lifting.
- **Mode 0** shows the bilateral blur has **over-smoothed the floor/wall crease on the right** (the baseboard transition is mushy) and **softened the lantern silhouettes** into bokeh-like blobs. Direct lighting edges that should be crisp are bleeding.
- No salt-and-pepper noise visible, so sampling is stable; the issue is structural (banding) not stochastic.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Fair** | Plausible mood, but bilateral blur over-smooths edges and probe banding leaks through |
| 3 (Indirect ×5) | **Good** | Smooth, low-noise, useful for diagnosis |
| 6 (GI only) | **Fair** | Reveals clear concentric cascade rings and floor probe-grid structure |

## 5. Stats Cross-Check & Recommendation

- `surfPct` climbs from **49.9% (C0) → 100% (C3)** — **C0 is severely under-sampled**, only half its probes hit a surface. This matches the visible probe-grid blobs on the nearby floor (C0 covers the close range).
- C0 `variance=0.0046` is ~8× C3's — consistent with the banding being concentrated in the near field.
- `temporalAlpha=0.05` is very low → blends out noise but **bakes in the sparse C0 sampling pattern**, producing the static rings.

**Concrete change:** Increase `c0MinRange` from **0.5 → 0.8** (or raise `baseRes` for C0 specifically). C0's volume is too small relative to the probe spacing, so each probe represents too much surface and the grid aliases. Alternatively, **raise `probeJitterScale` from 0.06 → 0.12** to break the static near-field pattern — temporalAlpha=0.05 will still smooth the added jitter noise effectively, trading visible bands for invisible flicker.
