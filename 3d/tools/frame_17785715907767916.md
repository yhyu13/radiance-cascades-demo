# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785715907767916_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785715915484624_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785715922777623_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17785715907767916.json`  
**Analyzed:** 2026-05-12T15:40:25  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Render

## 1. GI Contribution Isolation

Comparing the three views, indirect light is **present and behaving plausibly**:
- **Red bleed onto floor** (left of the tall block) — visible in mode 6, faintly carried into mode 0 as a warm tint on the lower-left floor.
- **Green bleed on the right side of the tall block** — clearly visible in mode 6, present in mode 0.
- **Ceiling fill** — mode 6 shows the ceiling lit only by bounce (no direct light reaches it from below the area light), confirming GI is doing real work there.
- **Shadow region behind the tall block (floor)** — mode 6 shows soft fill where mode 0's direct term would be black; this is the indirect lifting shadows.
- GI is **absent / very dim on the patterned short cube's front face** in mode 6 — it sits in a position that should receive bounce off the green wall and floor, but reads nearly black.

## 2. Banding in Mode 6

The mode 6 image shows a clear **regular dotted/grid pattern** overlaying the ceiling, floor margins, and the front face of the short cube. Characteristics:
- **Spacing**: roughly uniform across the ceiling, ~6–8 dots per wall edge.
- **Shape**: round/square dots, not stripes — this rules out directional-bin banding (which would appear as angular wedges or directional streaks).
- **No visible cascade-boundary discontinuity** (no concentric "shells" around the camera or scene center where cascade Cn hands off to Cn+1).
- **Conclusion**: this is **probe-grid aliasing** from the `baseRes: 32` / `volumeSize: 4` probe lattice (effective probe spacing ≈ 0.125 units), made visible because the jitter pattern (`jitterPatternSize: 8`, `probeJitterScale: 0.06`) is too small to dither the grid out, and `temporalAlpha: 0.05` is slow enough that the pattern persists frame-to-frame.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: spatially **smooth, almost too smooth** — walls show clean color gradients, no high-frequency noise. This indicates heavy temporal accumulation is doing its job, but at the cost of responsiveness.
- **Mode 0 (final)**: bilateral blur looks **well-tuned on large surfaces** but has slightly softened the **shadow contact edge under the tall block** and the **left edge of the patterned cube** — the contact shadow is a touch mushy. Texture detail on the short cube survives, so the bilateral guide is mostly working.
- The **probe-grid dots from mode 6 do not survive into mode 0**, which means the bilateral blur is successfully hiding them in the composite.

## 4. Ratings

| Mode | Rating | Reason |
|---|---|---|
| Mode 0 (final) | **Good** | Clean composite, plausible color bleed, minor contact-shadow softening |
| Mode 3 (indirect ×5) | **Good** | Smooth, no noise, but hides the underlying grid that mode 6 reveals |
| Mode 6 (GI only) | **Fair** | Clear probe-grid dot pattern visible on ceiling/floor; short cube under-lit |

## 5. Cascade Stats Cross-Check

- `surfPct` for **C0 = 68.9%** is the standout — ~31% of cascade-0 probes are not finding surfaces. This matches the **under-lit front face of the short cube** in mode 6: nearby high-resolution probes are missing geometry hits.
- C1–C3 are at 99–100% surfPct → far-field is fine.
- `variance` is highest in C0/C1 (~0.008), consistent with the visible dot pattern being a near-field probe issue, not a far cascade issue.
- `maxLum 0.56` in C0 vs `0.054` in C3 shows good cascade luminance falloff — no cascade-boundary popping expected.

## Concrete Parameter Change

**Increase `probeJitterScale` from 0.06 → 0.15** (roughly 1× the C0 probe spacing of ~0.125).

Rationale: the dot banding in mode 6 is probe-grid aliasing, and C0's low surfPct (68.9%) indicates near-field probes are systematically missing geometry at fixed positions. A lar
