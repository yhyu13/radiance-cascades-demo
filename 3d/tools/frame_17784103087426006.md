# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784103087426006_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784103089911014_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784103092402107_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784103087426006.json`  
**Analyzed:** 2026-05-10T18:52:20  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Renders

## 1. GI Contribution Isolation

Comparing the three views, indirect light is concentrated in these regions:

- **Left wall (foreground)**: Strong bright vertical streak around x≈100, y≈100–550. Visible in all three modes — this is GI bleeding from the bright doorway/light source down the corridor onto the near-camera wall. Mode 6 confirms it's purely indirect (no direct light reaches that surface at that angle).
- **Ceiling (right half, far)**: The reddish/brown patches along the ceiling at y≈0–200, x≈600–1100 appear in mode 3 amplified and faintly in mode 6 — bounce light from the warm floor/walls onto the ceiling.
- **Floor down corridor center**: The bright streaks along the floor (x≈600–900, y≈400–700) appear strongest in mode 0 — these are largely **direct** light through the doorway; mode 6 shows the floor is mostly dark, confirming GI contributes little here.
- **Far doorway (center, x≈640, y≈300)**: Dark in mode 6, bright in mode 0 → direct-light dominated.
- **Right wall**: Almost entirely GI-driven (warm reddish tint in mode 6 matches mode 0).

## 2. Banding Artifacts in Mode 6

- **Blocky stair-step patterns on the ceiling** (x≈700–1100, y≈30–200): Sharp rectangular patches roughly **40–80 px wide**, axis-aligned. With `baseRes=32` over `volumeSize=4`, probe spacing projects to roughly this pixel size at mid-corridor depth — consistent with **probe-grid (spatial) aliasing**, not directional bins.
- **Vertical "doorway" banding** in the far corridor (x≈600–760, y≈250–400): Concentric darker rectangles stepping inward — classic **cascade boundary** transitions. With 4 cascades and the camera looking down a long corridor, you're seeing C0→C1→C2 handoffs as discrete brightness steps.
- **No obvious directional-bin streaks** (no radial spokes around bright spots), so `dirRes=8` is adequate for this scene's lighting frequency.

## 3. Smoothness Assessment

- **Mode 3 (×5 amplified)**: Surprisingly smooth in flat wall regions but reveals the blocky ceiling probe footprint clearly. No high-frequency speckle noise → temporal accumulation is working. However, geometry edges (door frames) show *halo softening* — the indirect term leaks across silhouettes.
- **Mode 0**: The bilateral blur has over-smoothed the **near-camera left wall**, where the bright GI streak has feathered into a wide gradient with no detail. The doorway edges in the far corridor are also slightly muddy. Direct-light specular streaks on the floor survived, so the blur's depth/normal weights are at least partly working.

## 4. Ratings

| Mode | Rating | Reason |
|---|---|---|
| Mode 0 (final) | **Fair** | Plausible look but over-blurred GI on the left wall; cascade seams hidden but not fixed. |
| Mode 3 (×5) | **Fair** | Useful for diagnosis; reveals probe-grid blockiness clearly. Amplification exposes that mid-cascades are unstable. |
| Mode 6 (GI only) | **Poor** | Visible probe-grid banding on ceiling, cascade-boundary steps in corridor, and the stats confirm broken cascades. |

## 5. Stats Cross-Check & Recommendation

The cascade stats are alarming:
- **C0**: only 2.1% `anyPct` — severely under-sampled near-field probes.
- **C1**: `meanLum=NaN`, `maxLum=Inf` — **numerical blowup**, almost certainly the source of the `maxLum=169` and `20202.7` spikes propagating as the bright streaks.
- **C2**: 35% coverage but `variance=-NaN` — corruption from C1 NaNs propagating.
- **C3**: `meanLum=-375`, `maxLum=0`, negative variance — completely broken (likely uninitialized or sign-flipped accumulator).

The NaN/Inf in C1 is being clamped on display but is the root cause of the banding intensity differences between cascades visible in
