# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784073138469132_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784073139479866_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784073140886979_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784073138469132.json`  
**Analyzed:** 2026-05-10T18:02:24  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing mode 0 (final) against mode 6 (GI only), the indirect light is doing most of the heavy lifting in this scene — there is no obvious direct-only contribution that "pops" between the two views. The corridor is lit primarily by bounce light coming from the bright doorway/opening at the end of the hallway (center of frame, slightly above the floor midline).

**Bright GI regions:**
- Far end of the corridor near the doorway aperture (center, ~y=300–380): warm reddish-brown bounce, clearly visible in modes 0 and 6, blown out white in mode 3 (consistent with 5× amplification of an already bright region).
- Ceiling beams/slats at the top of frame: pick up reddish bounce on their undersides.
- Floor near the doorway: faint warm wash visible in mode 6.

**GI-absent / dark regions:**
- Right wall mid-to-foreground (x>900): nearly pure dark brown in mode 6, suggesting probes there receive little bounce — this is a wall facing away from the light source.
- Left wall foreground (x<250): brighter cream tone, receiving more bounce because it faces the lit aperture.

## 2. Banding Artifacts in Mode 6

The most visible structure in mode 6 is on the **ceiling beams** (top-center, around y=50–250), which show stepped/blocky color transitions following the voxel grid. With `baseRes: 32` and `volumeSize: 4`, that's ~8 cm probe spacing in cascade 0 — the block size in the image is consistent with **probe-grid spacing**, not directional bins. There are no smooth angular fan-shaped bands (which would indicate `dirRes:8` directional binning), and no sharp linear discontinuities crossing the image (which would indicate cascade-boundary transitions).

The right wall and far corridor end show **soft large-scale luminance gradients** rather than discrete bands — these are probe-trilinear interpolation artifacts, not directional aliasing.

## 3. Smoothness Assessment

**Mode 3 (indirect ×5):** Spatially **very smooth**, almost too smooth — looks heavily filtered. The 5× amplification would expose temporal noise if it were present, but I see almost none. This corroborates `temporalAlpha: 0.05` (heavy temporal smoothing) and the low variance numbers (0.0011 → 9e-6 across cascades).

**Mode 0 (final):** Bilateral blur has **over-smoothed the right wall** — the transition from the lit aperture region to the dark right wall is mushy with no geometric edge definition. The doorframe edges at the end of the corridor are also softened. The ceiling beams retain their crisp silhouettes (likely because they're a separate direct-render pass), so the blur is hitting the GI buffer specifically.

## 4. Ratings

| Mode | Rating | Reason |
|---|---|---|
| Mode 0 (final) | **Fair** | Lighting plausible, but bilateral over-blur erases wall/floor boundaries on the right side. |
| Mode 3 (indirect ×5) | **Good** | Clean, smooth, no temporal noise — but reveals how flat/uniform the GI is. |
| Mode 6 (GI only) | **Fair** | Visible probe-grid blockiness on ceiling beams; right-wall darkening looks like under-sampled bounce, not real occlusion. |

## 5. Stats Cross-check & Parameter Recommendation

- `anyPct: 100` everywhere and `surfPct: 81–100%` — probes are converging fine, no under-sampling.
- Variance drops cleanly across cascades (0.00109 → 9.2e-6) — cascade hierarchy is healthy.
- `cascadeTimeMs: 12.5` vs `raymarchTimeMs: 0.15` — cascade build dominates cost.
- `meanLum` is low (~0.017) — scene is genuinely dim, justifying the ×5 debug multiplier.

**Concrete change: increase `baseRes` from 32 → 48 (or 64).**

Justification: the only real visible artifact is **probe-grid blockiness on the ceiling beams** in mode 6. Cascade 0's variance (0.00109) is ~3× cascade 1's, which is consistent with spatial structure at the finest level being under-resolved rather than noisy. Tem
