# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784847313005571_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784847313129708_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784847313700983_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784847313005571.json`  
**Analyzed:** 2026-05-11T15:32:42  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Isolation

Comparing mode 0 (final) with mode 6 (GI only):

- **Ceiling strip (top center):** Mode 6 shows the brightest indirect contribution here — a warm orange band running down the corridor's ceiling/upper-wall junction. This is the dominant bounce source (likely a warm light fixture or emissive ceiling panel).
- **Floor down the corridor center:** Strong warm indirect glow in mode 6, peaking near the vanishing point. Mode 0's floor highlights are largely *direct* light reflections; the GI fills the mid-distance floor with warm fill.
- **Left wall (near to mid):** Almost no indirect contribution in mode 6 — it reads as a flat dim gray. The brightness in mode 0's left wall is therefore mostly direct lighting.
- **Right side / foreground blob:** The large blurry shape on the right (a foreground object very close to camera) receives essentially zero indirect light in mode 6 — it stays near-black. In mode 0 it's lit only by direct illumination.

## 2. Banding in Mode 6

I can see two distinct artifact patterns in mode 6:

- **Vertical/longitudinal streaks along the ceiling-wall seam** (the warm orange band): the orange isn't continuous — it has discrete soft "patches" spaced roughly every 25–35 px along the corridor axis. Spacing is consistent with **probe-grid interpolation** at the inner cascade (baseRes 64, volumeSize 4 ⇒ ~6 cm probe spacing projected down the hallway).
- **A soft horizontal seam** roughly 60% down the right wall, where warm fill abruptly transitions to cool gray. This looks like a **cascade boundary** between c0 and c1 (c0MinRange 0.5, c1MinRange 1.0) — the falloff radius lands on the right-wall mid-depth.
- I do **not** see clear directional-bin (dirRes=8) ringing — no 8-fold rosette around bright spots.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5):** Visibly **noisy/blotchy** in the mid-corridor and on the right blur object. The amplification reveals temporal lag — the smear on the right is consistent with the very low `temporalAlpha = 0.05` (slow convergence on moving/disoccluded regions).
- **Mode 0 (final):** The bilateral blur has done its job on the walls, but the **right-foreground object** is over-smoothed into an indistinct blob — its silhouette has been eaten by the filter. The ceiling detail (red/dark speckles) is preserved well, indicating the bilateral edge term is working on luminance but failing on the depth/normal discontinuity at the foreground object.

## 4. Ratings

| Mode | Rating | Reason |
|---|---|---|
| 0 Final | **Fair** | Plausible warm corridor look, but foreground is mush. |
| 3 Indirect ×5 | **Fair** | Reveals real GI shape but noticeably noisy/laggy. |
| 6 GI only | **Good** | Clean enough to diagnose; banding is mild and localized. |

## 5. Stats Cross-Check & Recommendation

Stats observations:
- `anyPct = 100` everywhere → no under-sampling at the probe level.
- `surfPct` rises 49.8 → 83.7 → 84.8 → 100 across cascades — **c0 is only hitting surfaces on half its rays**, which matches the patchy ceiling banding (c0 covers the close ceiling).
- `variance` drops cleanly with cascade index (0.00107 → 0.00014); the visible banding lives in c0/c1, consistent with the highest-variance cascades.
- `maxLum 0.64` in c0 vs `meanLum 0.016` → very high dynamic range in the inner cascade, amplifying any sample-count deficit into visible blotches.

**Concrete change:** Raise `temporalAlpha` from **0.05 → 0.12** (or alternatively increase `probeJitterScale` from 0.06 → 0.10 with `jitterHoldFrames` reduced 2 → 1). 

Rationale: c0's low surfPct + high maxLum + the smeared right-foreground in mode 3 all point to **temporal accumulation lag on disocclusion**, not a probe-
