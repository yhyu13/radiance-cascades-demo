# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17781417372960089_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17781417380569249_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17781417388079740_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17781417372960089.json`  
**Analyzed:** 2026-05-07T16:16:11  
**Model:** claude-opus-4-7

---

## Visual Triage Report

### 1. GI Contribution Analysis

Comparing mode 0 (final) against mode 6 (GI only) reveals where indirect light is doing real work:

- **Strong GI presence**: The ceiling near the back wall (mode 6 shows a bright bloom directly under the light position, indicating the only real light source is on the ceiling and bouncing). The floor and back wall show clear color bleed — pinkish tint on the left side of the floor/back wall from the red wall, and a faint green tint on the right side of the tall block's left face (visible in mode 6).
- **GI absent / weak**: The outer "frame" border (the gray rim outside the box interior) receives essentially no indirect light in mode 6 — it stays near-black. This is correct since those faces point away from the interior.
- **Color bleed verification**: Mode 6 clearly shows red bleed onto the tall block's left face and floor-left, and green bleed onto the short block's right face and floor-right. Mode 0 carries this bleed subtly.

### 2. Banding Artifacts in Mode 6

Visible structures in mode 6:
- **Blocky square halos around the box silhouette** (the gray rectangles extending past the frame top/bottom and left/right): these align with the **probe grid spacing**. With `baseRes: 32` over `volumeSize: 4`, probes sit ~0.125 units apart, and the halo thickness matches roughly one probe cell projected to screen — this is **probe-grid leakage**, not directional bins.
- **Soft radial falloff on the back wall** centered above the light: smooth, no concentric rings → directional bins (`dirRes: 8`) are not the dominant artifact source here.
- **No visible cascade-boundary seams** (no abrupt brightness step at a fixed radius from camera). Cascades appear to blend cleanly.

So the dominant artifact is **probe-grid blockiness at silhouette edges**, consistent with trilinear probe sampling without sufficient geometric occlusion weighting.

### 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: Spatially **smooth**, almost too smooth — no per-pixel noise visible, which means temporal accumulation (`temporalAlpha: 0.05`) is doing heavy lifting. The interior walls show a clean gradient. The cost is the visible blocky halo around the box exterior (same probe-grid leak).
- **Mode 0 (final)**: The bilateral blur has **over-smoothed the contact shadow** under the short block on the right — its base shadow is much softer than the tall block's left shadow, and the tall block's own shadow on the floor is blurred wider than the geometry warrants. The block edges themselves are preserved, so the blur's range weight is fine; it's the **shadow term** that's getting bled across.

### 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 Final | **Good** | Convincing Cornell look, color bleed reads correctly; minor shadow over-blur |
| 3 Indirect ×5 | **Fair** | Smooth interior but obvious probe-grid halo outside silhouette |
| 6 GI only | **Fair** | Correct color bleed direction, but blocky leakage dominates the read |

### 5. Stats Cross-Check

- `surfPct` is 98.3 / 99.2 / 100 / 100 across cascades — **well-converged**, no under-sampling.
- `anyPct: 100` everywhere — no empty probes.
- Variance climbs from 0.00016 (C3) → 0.0085 (C0); the C0/C1 variance (~0.008) matches the visible structured bleed at silhouette edges. This is **spatial structure, not noise**, confirming the artifact is geometric (probe leak) rather than sampling-rate.
- `temporalAlpha: 0.05` is very low → explains the smoothness in mode 3 but also means **probe leaks persist** because nothing is washing them out frame-to-frame.

### Concrete Parameter Recommendation

**Increase `probeJitterScale` from 0.06 → ~0.15**, and pair with **raising `temporalAlpha` from 0.05 → 0.08**.

Rationale: the probe grid is converged (high surfPct, low C3 variance) but the trilinear footprint is leaking past the box walls in a grid-aligned pattern. Larger jitter will randomize which probes cont
