# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784104670417376_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784104672898089_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784104675947853_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784104670417376.json`  
**Analyzed:** 2026-05-10T18:54:59  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing the three views to isolate where indirect light is doing work:

- **Floor (foreground, lower third)**: Mode 6 shows the floor receiving moderate warm bounce — visible as a soft brown wash. In mode 0 this is what gives the floor its mid-tone; without GI it would read nearly black since the only direct light source appears to be the distant doorway at the end of the corridor.
- **Left wall (near camera)**: Mode 6 shows almost no indirect contribution (it stays dark/neutral), while mode 3 ×5 reveals a faint cool-white fill. This wall is essentially unlit in the final.
- **Right side (the large blurry brown shape — likely the player's arm/hand or a foreground prop)**: Strong warm indirect in both mode 3 and mode 6. This is the brightest GI region in the frame.
- **Distant doorway / vanishing point**: Mode 3 reveals a bright bloom of indirect light bouncing back from whatever surface is past the door. Mode 6 shows it dimmer but still the dominant emitter for the corridor.
- **Ceiling stair-step geometry (top center)**: GI is essentially absent here — these pixels are direct-lit only (you can see them retain crisp red/black detail across all three modes).

## 2. Banding in Mode 6

Looking at mode 6 specifically:
- A **soft horizontal seam** runs across the corridor floor roughly one-third up from the bottom — the tone shifts from warm-brown (foreground) to a cooler grey-brown (mid-corridor). The spacing matches what I'd expect from a **cascade boundary** between c0 and c1 (volumeSize=4, baseRes=32 → c0 covers ~4 units, then c1 takes over).
- **Blocky 8–16 px patches** are visible on the left wall and the right foreground blob. The patch size is consistent with the **probe grid footprint** (baseRes=32 over volumeSize=4 → ~8 cm probes projected to screen at this distance).
- I do **not** see clear directional-bin striping (no angular pinwheel artifacts), so dirRes=8 is at least not the dominant failure mode.

## 3. Smoothness Assessment

- **Mode 3** is spatially **smooth, almost too smooth** — it looks like a low-frequency wash with no surface detail recovered. This is consistent with very heavy temporal accumulation (alpha=0.05) plus the probe blur kernel.
- **Mode 0** shows the bilateral blur has **over-smoothed the right-side foreground object** — its silhouette is gone, dissolved into a brown blob. The ceiling stair geometry survived because direct-light edges anchor the bilateral weights, but the indirect-dominated regions lost all geometric edge.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Fair** | Readable but mushy; foreground silhouettes destroyed by blur |
| 3 (Indirect ×5) | **Fair** | Smooth but featureless — confirms GI is low-frequency only |
| 6 (GI only) | **Poor** | Cascade seam visible; large dark zones suggest under-sampled probes |

## 5. Stats Cross-Check & Recommendation

The probe statistics are **alarming**:
- **c0 anyPct=2.2%** — cascade 0 is almost entirely empty. Only ~2% of near-field probes have any hit data.
- **c1 anyPct=77.6%** but **surfPct=0** — probes are hitting *something* but registering zero surface contribution, suggesting a classification bug or that only sky/miss is being recorded.
- **c2/c3 show NaN/Inf luminance and negative variance** — these cascades are mathematically broken (uninitialized memory or divide-by-zero in the reduction).
- **NaN/Inf in meanLum** for c0/c1 corroborates: the GI you see in mode 6 may be coming from a single dirty path, not from converged probes.

The visible cascade seam in mode 6 is consistent with c0 being empty and c1 taking over abruptly.

### Concrete change

**Raise `temporalAlpha` from 0.05 → 0.15 and increase `probeJitterScale` from 0.06 → 0.15.**

Justification: with c0 at 2% any-hit
