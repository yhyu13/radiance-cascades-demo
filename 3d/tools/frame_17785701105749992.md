# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785701105749992_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785701109954130_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785701114202038_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17785701105749992.json`  
**Analyzed:** 2026-05-12T15:15:45  
**Model:** claude-opus-4-7

---

# Visual Triage Report — Cornell Box Corridor

## 1. GI Contribution Analysis

Comparing **mode 0 (final)** against **mode 6 (raw GI)** — they look nearly identical, which tells me **the "final" composite is dominated by indirect light**; direct lighting contribution is minimal in this view (likely the lanterns are emissive surfaces feeding GI rather than being treated as direct analytical lights).

**Where GI is bright:**
- The **left wall lantern column** (left third of frame) — strong bounce onto the left wall and floor directly beneath each lantern, producing the row of bright floor "puddles."
- The **right wall** carries a warm orange/copper bounce — clearly the wall material reflecting lantern emission back into the corridor.
- The **floor strip** between the camera and ~mid-corridor, where bounce light from both walls converges.

**Where GI is absent / under-lit:**
- The **far end of the corridor** (center vanishing point) falls off to near-black in modes 0 and 6 — lanterns there contribute little because cascade falloff and probe sparsity at distance.
- The **upper-right ceiling region** is very dark despite proximity to lanterns — suggests probes above the lantern row are not capturing the upward-emitting hemisphere well.
- The **deep right-side wall** in the foreground shows strong bokeh-like circular blobs rather than continuous illumination — this is a probe-footprint artifact, not real shading.

## 2. Banding Artifacts in Mode 6

Three distinct artifact families are visible:

- **Circular/hexagonal "bokeh" blobs on the right wall** (most obvious in the foreground right): roughly uniform-sized rounded patches, ~150–200 px across at near distance, shrinking with depth. Spacing matches a regular 3D grid → **probe-grid aliasing from cascade 0** (baseRes 64, volumeSize 4 → ~16 probes/unit, footprint visible because temporalAlpha=0.05 freezes jitter patterns slowly).
- **Concentric ring banding inside each lantern halo** (visible on left lantern in mode 3 especially): step-like contour rings → **directional-bin quantization**, dirRes=8 is too coarse for the small solid angles subtended by point-like lanterns.
- **Faint vertical stripes on the right wall in the deep distance**: aligned with cascade transitions → **cascade-boundary seam** between c0 and c1 (c0MinRange 0.5, c1MinRange 1.0).

## 3. Mode 3 Smoothness & Mode 0 Over-blur

- **Mode 3 (indirect ×5)** is spatially **very smooth, almost too smooth** — the bilateral / temporal accumulation has eliminated high-frequency noise but at the cost of **edge bleeding**: the lantern halos extend visibly past the lantern geometry into the surrounding wall, and the floor "puddles" merge into soft ovals rather than crisp light spots. The left pillar/column edge is washed out.
- **Mode 0** inherits this same softness. The bilateral blur has **over-smoothed** the contact shadow under each lantern and the floor-wall crease on the right (no sharp corner darkening visible). However, geometry edges (lantern silhouettes) survive, so depth/normal weights are working — only the radiance term is over-filtered.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Fair** | Plausible mood, but indistinguishable from raw GI → direct light pipeline is barely contributing; over-smoothed contacts. |
| 3 (Indirect ×5) | **Good** | Smooth, low-noise, useful for diagnostics; reveals dirRes ringing clearly. |
| 6 (GI only) | **Fair** | Visible probe-grid bokeh on right wall and dark falloff at corridor end. |

## 5. Stats Cross-check & Recommendation

**Convergence read:**
- `anyPct` 92–97% across cascades — healthy.
- `surfPct` cascade 0 = **49.9%** — *half* of c0 probes are not hitting surfaces. This is the source of the **bokeh blobs on the right wall**: c0 covers near-field (0.5u), and many probes fire into empty corridor air, leaving sparse hits that show as discrete footprints.
- `vari
