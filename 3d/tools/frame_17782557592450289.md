# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782557592450289_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782557594591477_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782557596637624_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17782557592450289.json`  
**Analyzed:** 2026-05-08T23:56:30  
**Model:** claude-opus-4-7

---

# Visual Triage Report — Cornell Box GI

## 1. GI Contribution Isolation

Comparing mode 0 (final) against mode 6 (GI only):

- **Color bleeding** is clearly present and correctly localized: red bleed visible on the left portion of the back wall, floor, and the left face of the tall block (mode 6, mid-image). Green bleed mirrors this on the right side of the back wall and right face of the short block.
- **Ceiling indirect** (mode 6, top center) shows a soft bright lobe under the light — that's first-bounce return from the floor/walls, as expected.
- **Floor indirect** is mostly uniform grey with subtle warm/cool gradient toward the colored walls — correct.
- **Direct-only contribution** (mode 0 minus mode 6) accounts for the sharp light disc on the ceiling and the crisp shadows under the boxes — those are absent in mode 6, confirming GI doesn't carry the direct term.
- **Outside the box** (the dark grey/red/green rectangles framing the opening): these receive GI leakage in mode 6, indicating probes outside the room are being lit by the colored walls — a mild **light-leak through the box exterior**.

## 2. Banding in Mode 6

Looking carefully at the GI-only image:

- **Back wall**: I can see a faint **horizontal step** roughly 1/3 down from the ceiling and another near the floor line. Spacing is consistent with the `baseRes:32` probe grid sampled in a `volumeSize:4` volume → ~0.125 units between probes, which projects to roughly the step size visible. This is **probe-grid origin**, not directional binning.
- **Left/right colored walls**: gradients are smooth top-to-bottom; no directional-bin pinwheel artifacts visible (good — `dirRes:8` = 64 directions seems sufficient here).
- **No cascade-boundary seam** is visible as a discrete ring/edge in the interior — cascade transitions appear well blended.
- **Outside frame** (the colored leakage zones): edges are blocky and aligned to probe cells, again pointing to grid resolution rather than directional artifacts.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: very smooth, almost no high-frequency noise. The `temporalAlpha:0.05` (heavy temporal accumulation) is doing its job — variance stats confirm (0.0085 → 0.0001 across cascades). Possibly *too* smooth: the tall block's left-face red bleed looks washed-out and lacks contact-darkening near the floor.
- **Mode 0 (final)**: bilateral blur appears appropriate on flat walls. However, **the contact shadow between the tall block and the floor is softened** — the AO crease that should anchor the block to the ground is faint. The shadow of the tall block on the floor (left side) also has slightly mushy edges. Block silhouettes themselves are preserved, so the bilateral filter's range term is working, but the radius may be a touch wide.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| Mode 0 (final) | **Good** | Clean composite, correct color bleed, mild over-smoothing of contact shadows |
| Mode 3 (×5 indirect) | **Good** | Smooth, low noise, but slightly flat — temporal lag visible |
| Mode 6 (GI raw) | **Fair** | Faint probe-grid banding on back wall + light leak outside the box exterior |

## 5. Stats Cross-check & Recommendation

- `surfPct` is 98–100% across all cascades → probes are well-sampled, not the bottleneck.
- `variance` drops cleanly across cascades (0.0085 → 0.00016) → cascade hierarchy is converging properly.
- C0 has the highest variance (0.0085) and `maxLum:0.56` — consistent with the visible banding being in the finest cascade's footprint (back wall detail region).
- `temporalAlpha:0.05` is aggressive; combined with `jitterHoldFrames:2` and small `probeJitterScale:0.06`, jitter isn't decorrelating grid samples enough to hide the C0 banding.

**Concrete change**: Increase `probeJitterScale` from `0.06` → `0.15` (and optionally drop `jitterHoldFrames` to `1`). This will break up the visible probe-grid st
