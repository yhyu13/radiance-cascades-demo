# AI Visual Triage — Sequence (8 frames)

**f0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777600682039361_f0.png`  
**f1:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777600682745522_f1.png`  
**f2:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777600683528160_f2.png`  
**f3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777600684243896_f3.png`  
**f4:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777600684986887_f4.png`  
**f5:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777600685685752_f5.png`  
**f6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777600686533706_f6.png`  
**f7:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777600687227923_f7.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17777600682039361.json`  
**Analyzed:** 2026-05-03T06:14:52  
**Model:** claude-opus-4-7

---

## Analysis

### 1. Visually Stable Elements
The Cornell Box geometry — red left wall, green right wall, white back/floor/ceiling, two grey blocks, and the dark grey horizontal bars framing top/bottom — remains fixed in position, scale, and base albedo across all 8 frames.

### 2. Regions That Change

- **Red left wall (interior face)**: Subtle tonal shifts in pinkish saturation between f0–f3 (slightly warmer/brighter) and f4–f7 (a touch cooler/darker). Small magnitude, but visible as a low-frequency wobble.
- **Green right wall (interior face)**: Mirror behavior — green appears marginally more vivid in f2/f3 and slightly desaturated in f4/f5. Subtle.
- **Outer red and green wall slabs (the strips visible outside the box opening)**: The most noticeable change. The red outer slab (left edge) and green outer slab (right edge) shift in brightness and apparent thickness/coverage between f0–f3 vs f4–f7 — looks like a step change roughly every ~4 frames rather than per-frame noise.
- **Floor near the front edge / under the cubes**: Faint shimmer in the bounce tint (red/green color bleed onto the floor) — very low amplitude.
- **Ceiling and back wall**: Essentially stable; no visible flicker.

**Temporal pattern**: Not random per-frame noise — changes appear to cluster into two states (f0–f3 vs f4–f7), consistent with a **jitter cycle of period 4** that EMA at α=0.2 has not fully damped. This is the "periodic every 4 frames" signature.

### 3. Stability Rating
**Marginal** — geometry is rock solid and interior GI is mostly converged, but the colored wall slabs visibly step between two states on a 4-frame cadence.

### 4. Stats Cross-check
- Cascade 0 `surfPct = 29.9%` with `variance = 0.00566` — under-sampled probes in the finest cascade are the likely source of the colored-wall wobble (red/green bounce probes hit/miss surfaces depending on jitter offset).
- Cascades 2–3 are fully converged (`surfPct = 100`, very low variance in C3) — explains why ceiling/back wall are stable.
- `temporalAlpha = 0.2` is moderate; combined with `probeJitterScale = 0.25` (fairly large) and a 4-position jitter pattern, the per-jitter-phase bake differs enough that EMA leaves a residual 4-frame oscillation.

### 5. Recommended Parameter Change
**Reduce `probeJitterScale` from 0.25 → 0.12** (roughly halve it). This directly attacks the cascade-0 hit/miss variance driving the periodic shift on the colored walls, without sacrificing responsiveness. If shimmer persists after that, also lower `temporalAlpha` to ~0.12 to give the EMA more averaging across the 4-phase cycle.
