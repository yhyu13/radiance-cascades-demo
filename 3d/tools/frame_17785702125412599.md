# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785702125412599_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785702126564289_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785702131150863_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17785702125412599.json`  
**Analyzed:** 2026-05-12T15:17:23  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Hallway Render Modes

## 1. GI Contribution Isolation

Comparing mode 0 (final) vs mode 6 (GI only): the two are **nearly identical** in the dim hallway regions, which tells me direct light contributes very little here — the scene is essentially GI-driven. Specific observations:

- **Left wall window/light source** (x≈100–350, y≈150–550): brightest in all three modes. Mode 3 reveals this is the dominant indirect emitter bouncing into the corridor.
- **Floor reflections** (center, y≈400–700): the bright "footprint" splotches down the center of the floor are entirely GI — they appear in mode 6 and are amplified in mode 3. These are bounce contributions from the ceiling lights.
- **Right wall** (x≈600–1280): falls off into near-black in mode 0/6, while mode 3 shows it should have a soft warm wash. **The GI is under-contributing here** — the right wall is starved of indirect bounce despite being adjacent to ceiling emitters.
- **Ceiling fixtures**: bright in all modes; these are the primary emitters.

## 2. Banding Artifacts in Mode 6

Visible banding patterns:

- **Right wall concentric arcs** (centered roughly off-screen lower-right): Large, smooth, curved bands ~80–150 px apart. These follow a **spherical falloff pattern around a probe center**, strongly indicating **probe-grid interpolation** at low spatial resolution. With `baseRes:64` and `volumeSize:4`, probe spacing is ~6cm world units — the arcs match probe-cell boundaries projected into screen space.
- **Floor checker-like splotches** (y≈420–720, regularly spaced down the corridor): periodic bright/dark patches every ~60–80 px along the depth axis. Spacing is too regular for noise — this is **directional-bin aliasing** (`dirRes:8` = only 8 directions per probe is very coarse).
- **No sharp cascade-boundary discontinuity** is visible (no abrupt ring at a fixed radius from camera), so cascade transitions appear OK.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: spatially **smooth, almost too smooth** — no high-frequency noise, but ceiling light shapes are soft blobs rather than crisp fixtures. Indicates heavy temporal accumulation (consistent with `temporalAlpha:0.05`) and/or aggressive prefiltering.
- **Mode 0 (final)**: bilateral blur has **over-smoothed the right wall and far corridor**, producing the streaky horizontal smears on the right wall (~x>700). Edge details on ceiling fixtures are also melted into the surrounding ceiling. The left-side window edge survives well, but only because of the strong luminance gradient anchoring the bilateral kernel.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (final) | **Fair** | Atmospheric but right side is over-blurred; loss of geometry definition |
| 3 (indirect ×5) | **Good** | Clean, useful for diagnosis; reveals GI shape clearly |
| 6 (GI only) | **Poor** | Banding visible (probe + directional); right wall under-lit; large dark holes |

## 5. Stats Cross-Check & Recommendation

The stats are alarming:
- **Cascade 0: anyPct=0** — completely empty, no probes have data
- **Cascades 1–3: NaN/Inf luminance**, `maxLum=inf`, `variance=200626` on cascade 3 — there is a **fireflies/NaN injection problem**, almost certainly from a divide-by-zero in probe normalization (matches the bright speckles visible on the floor in mode 0 around y≈500–700)
- **anyPct max of 5.8%** — over 94% of probes are unpopulated, explaining the heavy reliance on interpolation and the spherical banding
- **cascadeTimeMs=37.8** vs **raymarchTimeMs=0.15** — almost all time is in cascade update, not lookup

### Concrete parameter change

**Increase `dirRes` from 8 → 16** (primary fix). The directional-bin aliasing on the floor and the empty `surfPct=0` across all cascades both point to insufficient angular sampling
