# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784842468059217_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784842470649871_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784842473805224_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784842468059217.json`  
**Analyzed:** 2026-05-11T15:24:40  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Renders

## 1. GI Contribution Analysis

Comparing mode 0 (final) against mode 6 (GI only), the indirect light is doing **almost all the heavy lifting** in this frame — mode 0 and mode 6 are nearly identical in tonality, which suggests direct lighting is contributing very little to visible surfaces (the camera is in a corridor with no direct light source in view).

**Where GI is bright:**
- The far end of the corridor (center-frame vanishing point) shows a warm orange/red glow — clearly bounce light from a lit surface beyond the visible geometry.
- The right wall picks up a saturated brownish-red tint, consistent with color bleeding from a red/warm surface.
- Floor near the camera receives mid-tone bounce.

**Where GI is absent / weak:**
- The ceiling shows hard dark cutouts (the brown "comb" / "zipper" patterns) — these are regions where GI sampling has clearly failed or returned near-zero.
- The left wall is washed-out pale, indicating it's receiving mostly ambient/sky-tinted bounce with little directional contribution.

## 2. Banding Artifacts in Mode 6

The most prominent artifacts are on the **ceiling** — repeating tooth-like / chevron stripes running along the corridor's depth axis. Characteristics:

- **Shape:** short horizontal dashes arranged in staggered rows, becoming denser toward the vanishing point.
- **Spacing:** discrete chunks roughly aligned to a regular grid in screen space, with spacing that compresses with perspective — strongly indicative of a **world-space probe grid** being sampled at grazing angles on the ceiling plane.
- **Not** smooth angular bands (which would suggest dirRes/directional-bin issues), and **not** abrupt tonal jumps at fixed screen-space distances (which would indicate cascade boundaries).

Diagnosis: **probe-grid aliasing** at grazing-angle surfaces. The ceiling is nearly parallel to the view ray, so a small world-space probe spacing maps to a tight screen-space stripe pattern. Cascade 0 has only `surfPct: 48.6%` — nearly half of c0 probes are in empty space, meaning the ceiling probes that *do* hit are sparse and produce these holes.

## 3. Smoothness Assessment

**Mode 3 (indirect ×5):** Spatially **smooth on walls and floor** — the bilateral/temporal filtering is doing its job on large flat surfaces. However, the **ceiling stripes survive amplification**, meaning these are not noise but structured undersampling — temporal accumulation can't fix them because the same probes keep missing.

**Mode 0 (final):** The bilateral blur has **not** over-smoothed obvious edges — the doorway/pillar silhouettes on the left side are still crisp. If anything, the blur is under-aggressive on the ceiling artifacts.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| Mode 0 (Final) | **Fair** | Plausible warm corridor mood, but ceiling artifacts leak through to the composite |
| Mode 3 (Indirect ×5) | **Good** | Confirms wall/floor GI is smooth; cleanly isolates the ceiling problem |
| Mode 6 (GI only) | **Poor** | Severe structured banding on ceiling; large dark voids that shouldn't exist in a closed box |

## 5. Stats Cross-check & Recommendation

Key red flags in the stats:
- **C0 surfPct = 48.6%** — half the finest cascade is wasted on empty-space probes
- **C0 variance = 1.04e-3** vs C3 = 9.86e-6 — two orders of magnitude — spatial structure in c0 matches the visible banding
- **C0 maxLum = 0.55** but meanLum = 0.017 — extreme outlier probes (the bright far-end glow) are dominating a few cells
- **temporalAlpha = 0.05** is fine; **cascadeTimeMs = 30.9** is the real cost driver

### Concrete change: **increase `probeJitterScale` from 0.06 → 0.15** (and consider raising `jitterHoldFrames` from 2 → 1 to refresh more often).

Rationale: the ceiling banding is a *fixed*
