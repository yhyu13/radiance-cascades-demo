# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17781554269170324_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17781554270231586_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17781554271059118_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17781554269170324.json`  
**Analyzed:** 2026-05-07T20:04:21  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing modes 0 and 6 against the implied direct-only baseline:

- **Brightest GI** sits along the **upper rim/lip band** (~y=190–230), where the inner walls catch bounce light from the (off-frame) ceiling/light fixture. This is visible as the warm-tinted highlight strip in all three modes.
- **GI is essentially absent on the main back wall** below the rim — mode 6 shows it as nearly black (mean luminance ~0.01–0.02 from stats), and mode 0 looks almost identical to a direct-only render across that region.
- A **faint vertical brighter strip on the left edge** (~x=255, y=300–700) in mode 6 indicates some side-wall bounce reaching the back wall corner, but it is very dim.
- The lower-center region (y > 500) receives almost no indirect light — the floor bounce isn't reaching here, suggesting the probe volume may not extend low enough or the floor isn't in the cascade footprint.

## 2. Banding Artifacts (mode 6)

- **Horizontal band** at the rim (y≈195–225): a sharp bright stripe ~30 px tall with a softer falloff below. This aligns with a **probe-grid boundary** — at `baseRes=32` over `volumeSize=4`, probe spacing is ~0.125 units, which projects to roughly the observed stripe width.
- **Concentric arc faintly visible** centered around image center (y≈450), barely above black floor. Spacing is large and smooth → consistent with **cascade 0→1 boundary** transitioning at `c0MinRange=1`.
- **No directional-bin starbursts** visible; with `dirRes=8` (64 directions) the angular resolution is sufficient at this luminance level.
- The left-edge vertical strip has a hard inner edge — likely a **cascade footprint clip** rather than a probe seam.

## 3. Smoothness Assessment

- **Mode 3 (5× indirect)** looks **spatially smooth, almost too smooth** — the bilateral/temporal filter (α=0.05) is doing heavy work. No speckle noise is visible, but the rim highlight has a slightly **gauzy halo** extending ~40 px below the geometric edge, suggesting filter bleed.
- **Mode 0** shows the same rim halo but darker; the bilateral blur **has not destroyed the rim edge**, but the soft transition from the lit lip to the dark wall is suspiciously gradient-like (could be GI, could be over-blur — hard to disambiguate without an unblurred mode 0).
- No firefly/spike artifacts. `maxLum=0.64` in C0 vs `meanLum=0.027` indicates a 24× peak-to-mean ratio, which is high but not pathological.

## 4. Ratings

| Mode | Rating | Notes |
|------|--------|-------|
| 0 (Final) | **Fair** | Reads correctly but GI contribution is so weak the result looks flat/direct-only. |
| 3 (Indirect ×5) | **Good** | Clean, smooth, useful for diagnosis. Rim halo bleed noted. |
| 6 (GI only) | **Fair** | Reveals a probe-grid stripe at the rim and a cascade arc; useful but shows real artifacts. |

## 5. Stats Cross-Check & Recommendation

Key observations from stats:
- `surfPct` is only **81.6% in C0** and **85.2% in C1** — ~15–18% of cascade-0 probes are not hitting any surface, meaning under-sampled probes near the cup interior.
- C3 `meanLum=0.012, variance=1.9e-05` — essentially uniform, contributing almost nothing distinct.
- C0 variance (0.002) is ~100× C3's, matching the visible banding being concentrated near the rim (C0 territory).
- `temporalAlpha=0.05` is very low → 20-frame effective integration → explains the smoothness in mode 3 but also means slow response that masks under-sampling rather than fixing it.

**Concrete change:** Increase `c0MinRange` from **1 → 1.5** (or raise `baseRes` from 32 → 48). The C0 surfPct of 81% combined with the visible probe-grid stripe at the rim indicates C0 probes are too sparse / too short-range to cover the high-frequency near-field bounce at the l
