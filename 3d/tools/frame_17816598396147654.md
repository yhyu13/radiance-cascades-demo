# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17816598396147654_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17816598398195459_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17816598400303820_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17816598396147654.json`  
**Analyzed:** 2026-06-17T09:31:12  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Render Modes

## 1. GI Contribution Analysis

Comparing modes, indirect light is **present and behaving correctly** in the expected places:

- **Color bleeding onto the floor**: visible as faint pink tint near the red wall (left floor) and faint green tint near the green wall (right floor) in mode 0. Mode 6 confirms this — strong red/green saturation hugging the side walls.
- **Ceiling bounce**: mode 6 shows a warm, slightly desaturated wash on the ceiling — consistent with light reflecting off the white floor back up.
- **Behind/between the boxes**: the back wall in mode 6 shows mixed red/green crosstalk where both wall bounces meet, which is correct.
- **Indirect is *absent* (correctly)** on the front-facing tops of the two blocks in mode 0 — those are lit primarily by direct light from the ceiling area light. Mode 6 confirms the block tops are quite dim in GI.

## 2. Banding in Mode 6

I can see:
- **Rectangular dark frame around the box exterior** (top and bottom horizontal bars, ~80 px tall): this is a **cascade-boundary** artifact. The outer cascade (c3, with `maxLum: 0.054`, ~10× dimmer than c0) is taking over outside the inner volume's footprint, producing a visible step in luminance. Spacing matches a cascade-volume edge, not a probe grid.
- **Soft blocky falloff at the room's outer corners** (where wall meets ceiling/floor at the box silhouette): consistent with the `baseRes: 32` probe grid at `volumeSize: 4` (~0.125 units/probe) — I do not see clear per-probe checkerboard on interior surfaces, so the probe grid is well-resolved here.
- I do **not** see clean directional-bin banding (no radial pinwheel patterns), so `dirRes: 8` is adequate for this smooth-diffuse scene.

The dominant artifact is the **cascade transition**, not probe grid or angular bins.

## 3. Smoothness Assessment

- **Mode 3 (5× indirect)**: spatially **smooth**, almost too smooth — gradients on walls are clean, no speckle. This indicates `temporalAlpha: 0.05` is integrating heavily (slow-moving but very stable). Variance numbers (0.008 on c0/c1) are low, consistent with the smooth look.
- **Mode 0 (final)**: contact shadows under the boxes and at wall–floor seams are **preserved well** — bilateral blur has *not* over-smoothed those edges. The shadow boundary of the tall block on the floor is crisp.
- One mild concern: the soft halo on the ceiling around the light in mode 0 looks slightly blurry, but that's expected from indirect bounce, not a blur failure.

## 4. Ratings

| Mode | Rating | Reason |
|---|---|---|
| Mode 0 (final) | **Good** | Clean composite, shadows preserved, color bleed present but subtle |
| Mode 3 (×5 indirect) | **Excellent** | Very smooth GI, no fireflies, gradients read clearly |
| Mode 6 (GI only) | **Fair** | Visible cascade-boundary frame around the box; outer cascade contribution is too dim relative to inner |

## 5. Stats Cross-Check & Recommendation

Key stat observations:
- `surfPct` rises 84.9 → 97.9 → 100 → 100 across cascades: **c0 is under-sampling surfaces** (15% of c0 probes hit nothing useful). This matches the visible cascade-edge banding — c0's footprint is too tight or its range too short.
- `maxLum` collapses from 0.56 (c0) → 0.054 (c3): nearly 10× drop, which is the **luminance step** producing the visible frame artifact in mode 6.
- Variance on c0/c1 (~0.008) is modest; banding is not from noisy probes but from the cascade transition.

### Concrete parameter change

**Increase `c0MinRange` from 0.5 → 0.8** (and/or bump `volumeSize` for c0 slightly).

Rationale: c0's `surfPct` of 84.9% means many c0 probes are missing the geometry within their reach — the cascade is too small/short for the room. Extending c0's range will (a) raise c0
