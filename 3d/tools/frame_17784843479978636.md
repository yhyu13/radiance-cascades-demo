# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784843479978636_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784843480102443_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784843480422715_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784843479978636.json`  
**Analyzed:** 2026-05-11T15:26:18  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Render

## 1. GI Contribution Analysis

Comparing the three views:

- **Bright indirect zones** (mode 6): the warm orange glow at the **end of the corridor / vanishing point center** dominates. This is bounce light from the lit far wall spilling onto the side walls and floor near the back.
- **Faint indirect** on the **left wall** (cool greyish-brown) and **right wall** (slightly warmer) — typical Cornell-style colored bounce, but very subtle here.
- **Indirect absent / near-zero** on the **ceiling** and **foreground floor** in mode 6 — these areas in mode 0 are lit almost entirely by direct light.
- The **bright speckled cluster near the top-center** of all three modes (most visible in mode 3) is GI from a ceiling light fixture or emissive geometry — it appears as a vertical streak of bright probes.

## 2. Banding Artifacts in Mode 6

- **Horizontal/diagonal blocky bands** are visible along the **left wall** and across the **floor**, spaced roughly every ~30–40 px. The block size is consistent with **probe-grid spacing** (baseRes 8, volumeSize 4 → coarse probe footprint). This is a **probe-grid source**, not directional.
- The **speckled vertical streak at top-center** (3–4 isolated bright cells) looks like **individual probe cells** receiving the emitter directly while neighbors miss it — again probe-grid, aggravated by low dirRes=8 (only 64 directions, so a small emitter is hit by few bins).
- I do **not** see clear ring/halo patterns that would indicate cascade-boundary transitions, nor concentric directional-bin rosettes.

## 3. Smoothness Assessment

- **Mode 3** (5× amplified): noticeably **noisy/blotchy** in the upper region around the emitter; the rest of the frame is smooth-to-flat (because the underlying signal is near zero, amplification reveals little). The emitter speckle is the main concern — high spatial variance there.
- **Mode 0**: bilateral blur appears to have **over-smoothed the emitter cluster** at the top of the frame — what should be a crisp bright fixture becomes a smeared brown-orange splotch with broken edges. Wall/floor edges at the corridor vanishing point look acceptable.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Fair** | Readable scene, but emitter region smeared and overall GI feels muddy/dim. |
| 3 (Indirect ×5) | **Fair** | Reveals the speckle problem clearly; useful diagnostic but noisy. |
| 6 (GI only) | **Poor** | Probe-grid blocking visible; signal so dim (maxLum ~0.08) that structure dominates over content. |

## 5. Stats Cross-Check & Recommendation

Key observations from stats:
- **Cascade 3 is completely empty** (anyPct=0) — wasted cascade, misconfigured range.
- **Cascade 0 surfPct=51.6%** — only half the probes hit a surface; the rest miss. This explains the speckled/blocky look.
- **Cascade 2 variance=1.3e-6** (essentially flat) vs **Cascade 1 variance=3.3e-4** (highest) — the visible banding correlates with cascade 1 carrying most of the structured signal.
- **temporalAlpha=0.05** is already low (heavy temporal smoothing), so noise isn't from lack of accumulation — it's from **spatial under-sampling**.

### Concrete parameter change

**Increase `dirRes` from 8 → 12 (or 16)** and **raise `c0MinRange` from 0.5 → ~1.0** so cascade 0 probes actually reach surfaces (lifting surfPct above ~80%). 

Rationale: the dominant artifact is per-probe directional aliasing on the small emitter (only 64 dirs at dirRes=8 means a small light is hit by 1–2 bins, producing the speckled vertical streak). Raising dirRes attacks the root cause; extending c0 range fixes the 51% surfPct miss. Also reclaim cascade 3 by widening its range or dropping `cascadeCount` to 3.
