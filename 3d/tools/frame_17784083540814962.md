# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784083540814962_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784083541999140_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784083542936908_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784083540814962.json`  
**Analyzed:** 2026-05-10T18:19:46  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing mode 0 (final) against mode 6 (GI only), the indirect light is doing most of the work in this scene — the corridor has no visible direct sun hits except for the bright specular streaks on the floor near the center vanishing point and the small light source visible through the doorway at frame center (~x=460, y=320). 

**Where GI is bright:**
- The ceiling near the doorway/vanishing point picks up warm orange bounce from the lit far room (mode 6, top-center)
- The right wall carries a strong reddish-brown indirect tint, brightest in the mid-right region (~x=850, y=350)
- Floor in mid-distance has a soft warm fill

**Where GI is absent/dim:**
- Left wall in the foreground (~x=100–250) is nearly unlit in mode 6 — it reads as a flat beige with no directional gradient
- Floor in the very near foreground (bottom edge) is dark in GI-only, suggesting the probe cascade isn't delivering bounce there

## 2. Banding in Mode 6

I can see **horizontal/diagonal soft bands** along the right wall, roughly 5–7 bands spaced ~40–60 px apart, running with the perspective of the corridor. Their alignment with world-space rather than screen-space, and their coarse spacing, points to **probe-grid trilinear interpolation seams** (baseRes=32, volumeSize=4 → ~8 cm probe spacing in world units, which would project to exactly that band size at this depth).

I do **not** see sharp angular discontinuities that would indicate directional-bin (dirRes=8) aliasing, nor do I see the abrupt brightness step characteristic of cascade boundaries — cascade transitions appear smooth.

## 3. Smoothness Assessment

**Mode 3 (indirect ×5):** Looks **spatially very smooth**, almost too smooth — the walls are nearly flat colored fields with only the doorway showing structure. Given temporalAlpha=0.05 (heavy temporal accumulation) and the 5× amplification, I'd expect to see noise if it were present; I don't. This suggests the indirect term is well-converged but possibly **over-blurred or under-resolved** (low dirRes=8 limits angular detail).

**Mode 0 (final):** The bilateral blur has clearly **over-smoothed the right wall** — there's a large soft brown blob on the right (~x=900, y=400) with no surface detail, despite the geometry being a flat wall that should show subtle shading variation. The floor specular streaks survived the blur (good edge preservation there), but the wall lost its meso-structure.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| Mode 0 (final) | **Fair** | Reads correctly but right-wall over-blur kills detail |
| Mode 3 (×5 GI) | **Good** | Smooth, low noise, but suspiciously featureless |
| Mode 6 (GI only) | **Fair** | Visible probe-grid banding on right wall; left foreground under-lit |

## 5. Stats Cross-check & Recommendation

The stats confirm the visual read:
- **anyPct=100, surfPct rising 81→100** across cascades: probes are well-populated, no under-sampling
- **Variance drops 10× per cascade** (1.1e-3 → 9.2e-6): higher cascades are very flat, consistent with the smooth mode 3
- **Cascade 0 maxLum=0.54 vs meanLum=0.017** — high dynamic range in c0, which is where the banding would originate
- **raymarchTimeMs=24.1** is the dominant cost

### Concrete recommendation: **Increase `dirRes` from 8 to 12 (or 16)**

The probe density is fine (surfPct=100 in higher cascades), and temporal accumulation is already aggressive (alpha=0.05). The smoothness in mode 3 combined with banding in mode 6 along the right wall is the signature of **insufficient angular resolution** — 8 directional bins is too few to resolve the directional gradient of bounce light along a glancing wall. Raising dirRes will sharpen wall shading and reduce the trilinear-seam appearance without needing to touch the bilateral blur radius (which is currently masking the symptom).

If raymarch cost is a concern, alternatively **reduce bilateral blur sig
