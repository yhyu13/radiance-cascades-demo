# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784775890013387_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784775890852757_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784775892125345_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784775890013387.json`  
**Analyzed:** 2026-05-11T13:33:37  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing mode 0 (composite) to mode 6 (GI only):
- **Floor down the corridor center**: Mode 0 shows bright specular/direct streaks (the dashed white highlights) that are completely absent in mode 6 — confirming those are pure direct light from ceiling fixtures.
- **Right wall (foreground bulge)**: The warm reddish-brown tint dominating the right half of mode 0 comes almost entirely from GI (visible as the same reddish mass in mode 6). This is bounce light from an off-screen warm source.
- **Left wall**: Cooler, near-white in all three modes — GI contribution is low here, dominated by direct illumination.
- **Far doorway**: The dark reddish recess in mode 0 matches mode 6, meaning the back area is lit almost exclusively by indirect bounce.

## 2. Banding in Mode 6

Visible artifacts:
- **Ceiling near top center**: Stair-stepped/ribbon-like dark bands following the ceiling fixtures. Spacing is roughly aligned with texel boundaries — consistent with **probe-grid resolution** (baseRes=32, volumeSize=4 → ~0.125u spacing) rather than directional bins.
- **Right wall**: Large soft blob with no sharp ringing — looks like low-resolution probe interpolation (cascade 2 or 3 dominating that region), not directional aliasing.
- **No visible concentric rings** that would indicate cascade boundary seams, and no radial fan patterns that would indicate dirRes=8 directional aliasing.

The banding is therefore **probe-grid spatial undersampling**, consistent with cascade 0's variance (0.00103) being ~100× higher than cascade 3's (8.9e-6).

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: Spatially very smooth — almost milky. No high-frequency noise, which suggests temporal accumulation is working (alpha=0.05 is aggressive smoothing). However, edge detail at the doorway and ceiling fixtures has been blurred away — the indirect term cannot resolve the geometry boundary cleanly.
- **Mode 0 (final)**: The bilateral blur has **over-smoothed the right wall** — it appears as a featureless gradient with no surface detail, and the foreground right edge has a soft "ghost bulge" with no defined silhouette. The floor highlights survived (depth/normal edges preserved), but lateral wall detail did not.

## 4. Ratings

| Mode | Rating | Reason |
|---|---|---|
| Mode 0 | **Fair** | Readable, but right side over-blurred; loses spatial definition |
| Mode 3 | **Fair** | Smooth but lacks structure; amplification reveals lack of detail |
| Mode 6 | **Good** | Clearly shows GI contribution and reveals probe-grid banding for diagnosis |

## 5. Stats Cross-Check & Recommendation

Key observations from stats:
- `surfPct` for cascade 0 is only **48.6%** — half of c0 probes are missing surface hits, which explains the high variance (0.00103) and visible banding.
- `temporalAlpha=0.05` is very low → strong temporal smoothing, which combined with bilateral blur produces the over-smoothed right wall.
- `dirRes=8` appears adequate (no directional fan artifacts visible).

**Concrete change**: Increase `baseRes` from 32 → 48 (or reduce `volumeSize` to 3) to densify cascade 0 probes and raise its `surfPct` above ~75%. This should reduce the ceiling band artifacts and let you ease the bilateral blur radius, recovering the lost right-wall detail. If cost is a concern, alternatively raise `temporalAlpha` to 0.08–0.10 to let new sample data refresh probes faster, reducing reliance on heavy spatial filtering.
