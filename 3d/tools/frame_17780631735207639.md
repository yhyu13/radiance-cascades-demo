# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17780631735207639_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17780631735873237_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17780631736743065_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17780631735207639.json`  
**Analyzed:** 2026-05-06T18:26:45  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Render Modes

## 1. GI Contribution Analysis

Comparing mode 0 (final) against modes 3 and 6, the indirect light contribution is most visible in:

- **Color bleeding on the floor**: The left side of the floor in mode 6 shows a faint red wash near the red wall; the right side shows a corresponding green tint near the green wall. This is the classic Cornell signature and it *is* present, just weak.
- **Ceiling light spot**: Mode 6 shows a distinct bright circular hotspot on the ceiling center — this is light bouncing back up from the floor/blocks.
- **Shadow fill on the tall block's left face**: In mode 0 the tall block's shaded side is not pitch black; mode 6 confirms GI is filling it with a soft reddish tint.
- **Absent GI**: The black border/frame regions (top and bottom horizontal bars) receive essentially zero indirect light in mode 6, consistent with them being outside the probe volume's interior.

## 2. Banding Artifacts in Mode 6

I can see the following structured artifacts:

- **Soft blocky gradients on the back wall and ceiling** — large, low-frequency cells roughly matching what a 32³ base resolution would project. No sharp stair-stepping, but the reddish/greenish wash on the back wall transitions in broad patches rather than smoothly. This is consistent with **probe-grid spacing** at `baseRes: 32, volumeSize: 4` (≈12.5 cm probe spacing).
- **A faint rectangular halo around the outer cube frame** in mode 6 (the slightly darker rim outside the box interior) — this looks like a **cascade boundary** between c0/c1, where the higher-resolution cascade ends.
- I do **not** see clear directional-bin banding (no angular pinwheel/spoke patterns around the blocks), so `dirRes: 8` appears adequate for this scene's smoothness.

## 3. Smoothness / Over-blur Assessment

- **Mode 3 (indirect ×5)**: Spatially very smooth — no salt-and-pepper noise, no temporal sparkle visible in a still. The 5× amplification reveals only gentle gradients, which suggests `temporalAlpha: 0.05` is doing heavy averaging (consistent with the very low cascade variance values, especially c3 at 0.00016).
- **Mode 0 (final)**: The bilateral blur looks **slightly aggressive** — the contact shadow under the small right-hand block is soft and the tall block's cast shadow on the floor has a fuzzy edge that should be crisper given a single point/area light. Geometry edges (block silhouettes against the back wall) are still preserved, so the blur is guided correctly, just wide.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 Final | **Good** | Plausible Cornell, correct color bleed direction, but shadow contact is soft. |
| 3 Indirect×5 | **Good** | Clean and smooth; confirms GI is converged, not noisy. |
| 6 GI only | **Fair** | Visible low-frequency probe-grid blockiness on back wall/ceiling; cascade-boundary halo around frame. |

## 5. Stats Cross-Check & Recommendation

Stats look healthy: `surfPct` is 98–100% across all cascades (probes are finding geometry, not under-sampled), `anyPct` is 100%, and variance drops cleanly from c0 (0.0085) to c3 (0.00016) — exactly the expected falloff. No convergence problem.

The **visible issue is spatial resolution**, not temporal noise. The low-frequency banding on the back wall plus the soft over-blurred shadows in mode 0 both point to probe spacing being the bottleneck.

**Concrete change:** Increase `baseRes` from **32 → 48** (or alternatively reduce `volumeSize` from 4 → 3 if the scene fits). This tightens c0 probe spacing from ~12.5 cm to ~8 cm, which should sharpen the back-wall color bleed gradient and reduce reliance on the wide bilateral blur — letting you also dial the blur radius down so the small block's contact shadow regains its crisp edge.

If perf is tight (current `cascadeTimeMs: 0.093` leaves plenty of headroom), this change is essentially free.
