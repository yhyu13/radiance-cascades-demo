# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784854402257553_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784854403095816_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784854404252808_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784854402257553.json`  
**Analyzed:** 2026-05-11T15:44:30  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Frame

## 1. GI Contribution Analysis

Comparing modes to isolate where indirect light dominates:

- **Ceiling stripes (top of frame)**: The reddish-brown hatched markings on the ceiling are visible identically in all three modes — these are *direct-lit* features (likely emissive/lit ceiling fixtures) bleeding through.
- **Right wall**: Strong warm brown tint in mode 0 and mode 6, and clearly bright in mode 3. This is **dominantly indirect light** — the wall has no direct illumination but receives bounce from the ceiling lights.
- **Left wall**: Cooler/whiter cast in mode 6, with faint vertical structure visible in mode 3. Indirect contribution is weaker here than the right wall, suggesting asymmetric source placement.
- **Floor center streak (the bright dashed trail)**: Present in mode 0 only, absent in mode 3 and mode 6 — this is pure **direct lighting** from the ceiling fixtures hitting the floor.
- **End-of-corridor doorway**: Bright warm glow in mode 0; in mode 3 it appears as a soft bright rectangle; in mode 6 it's a dim warm patch. GI correctly carries the distant emitter contribution forward.

## 2. Banding in Mode 6

Visible banding/structure:
- **Right wall**: Smooth horizontal-ish gradient with **soft blocky patches** roughly 40–60 px across, particularly mid-height. Spacing is consistent with the **probe grid** (baseRes 32 / volumeSize 4 → ~8 probes/unit; the patch size matches probe-cell projections at this depth).
- **Ceiling near vanishing point**: Faint stair-stepped luminance transitions where the corridor recedes — these align with **cascade boundaries** (cascade 0→1 transition near mid-distance, where the c0MinRange 0.5 / c1MinRange 1.0 handoff would land roughly where the corridor narrows).
- **No directional pinwheel/rosette artifacts** are visible, so dirRes=8 appears adequate for this scene's smoothness.

## 3. Smoothness Assessment

- **Mode 3 (×5 indirect)**: Looks **spatially smooth**, almost over-smoothed — large soft gradients with no high-frequency noise. The probe grid is doing its job, and temporalAlpha=0.05 (heavy temporal accumulation) is suppressing per-frame noise. The blocky probe footprints noted above are the only structural defect.
- **Mode 0 (final)**: The bilateral blur **has over-smoothed the right wall** — it appears as a featureless brown gradient. The direct-lit floor streaks remain crisp (good edge preservation), but the wall-to-ceiling boundary on the right is mushy. Left-side foreground geometry (the railing/wall edge in the lower-left) is also blurred into a soft smear.

## 4. Ratings

| Mode | Rating | Reason |
|---|---|---|
| Mode 0 (final) | **Fair** | Plausible composite, but bilateral blur is too aggressive on indirect-dominant surfaces |
| Mode 3 (×5) | **Good** | Smooth, low-noise; reveals probe-cell footprints but no severe artifacts |
| Mode 6 (GI only) | **Fair** | Banding visible on right wall; cascade transition faintly steps near vanishing point |

## 5. Stats Cross-Check & Recommendation

Stats observations:
- **Cascade 0 surfPct = 48.6%** — under half the probes hit a surface. This is the source of the right-wall blockiness: many c0 probes are sampling empty space or mis-aligned to geometry.
- **Variance drops 100× from c0 (1.03e-3) to c3 (8.9e-6)** — consistent with the smooth far-field and structured near-field seen in mode 6.
- **maxLum on c0 = 0.54 vs c1 = 0.26** — large spike differential supports the cascade-boundary visibility.
- **temporalAlpha = 0.05** is very low (heavy smoothing), explaining mode 3's smoothness but also why probe-cell footprints persist (they're locked in across frames).

**Concrete change**: Increase **`probeJitterScale` from 0.06 → 0.15** (and opt
