# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784844226399298_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784844226600942_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784844226761263_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784844226399298.json`  
**Analyzed:** 2026-05-11T15:27:32  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing the three views, indirect light is concentrated in:
- **Tunnel/doorway in mid-distance** (center-left): warm orange bounce dominates here — clearly visible in modes 3 and 6 as the brightest GI region. This is the strongest indirect contributor since it has no direct light reaching the camera-side surfaces.
- **Right wall foreground**: receives moderate warm bounce, visible as the soft pink/peach tint in mode 6.
- **Ceiling near light fixtures** (top, mode 0): bright dotted highlights are *direct* — they are absent in mode 6, confirming GI is doing little there.
- **Floor**: GI contribution is weak/dark in mode 6; the visible floor brightness in mode 0 is largely direct.

GI is essentially **absent on the left wall** in mode 6 — that surface stays muddy/flat, suggesting either probe occlusion or that no nearby bouncer is feeding it.

## 2. Banding in Mode 6

I can see:
- **Soft horizontal/blocky gradient transitions** on the right wall — roughly 3–4 broad bands stacked vertically, spacing consistent with the `baseRes: 16` probe grid projected at this view distance. This points to **probe-grid interpolation** rather than directional bins (no radial pinwheel pattern visible).
- **A faint vertical seam** between the bright doorway region and the darker right wall — likely a **cascade boundary** between c0 and c1 (consistent with `c0MinRange: 0.5` falling near that depth).
- No visible 8-fold angular pinwheels, so `dirRes: 8` is not the dominant artifact source.

## 3. Smoothness / Over-blur

- **Mode 3** (indirect ×5) looks **very smooth, almost too smooth** — no high-frequency noise, but also no contact detail. This is consistent with `temporalAlpha: 0.05` (heavy temporal smoothing) successfully killing variance.
- **Mode 0** shows clear **bilateral over-smoothing on the right wall** — the wall/floor junction in the foreground is mushy and the edge of the right pillar bleeds into the floor. Edge-stopping weights look too permissive.
- The doorway frame in mode 0 retains its silhouette well; over-blur is localized to grazing/shallow-angle surfaces.

## 4. Ratings

| Mode | Rating | Notes |
|------|--------|-------|
| 0 Final | **Fair** | Readable, but bilateral has eaten the right-wall/floor edge |
| 3 Indirect ×5 | **Good** | Smooth, low-noise, plausible bounce distribution |
| 6 GI only | **Fair** | Banding on right wall, cascade seam visible |

## 5. Stats Cross-Check & Recommendation

Key observations from stats:
- **Cascade 3 is completely empty** (`anyPct: 0`) — you are paying for 4 cascades but only using 3. Either the scene is too small for c3's range or `c1MinRange: 1` doubles too aggressively for `volumeSize: 4`.
- Cascades 0–2 show healthy `anyPct: 100` and rising `surfPct` (48 → 89 → 100), so coverage itself is fine.
- **Variance drops 10× from c0 (0.00107) to c2 (0.0001)** — the right-wall banding I see matches c0's higher variance; it's the near-field cascade producing the structured artifact, not the far cascades.

### Concrete suggestion

**Drop `cascadeCount` from 4 → 3 and increase `baseRes` from 16 → 24** (or alternatively raise `probeJitterScale` from 0.06 → 0.10).

Rationale: c3 contributes nothing (wasted ~25% of `cascadeTimeMs`). Reinvesting that budget into denser near-field probes will directly attack the c0 variance/banding visible on the right wall, without touching the temporal filter (which is doing its job — mode 3 is clean). If you'd rather not change resolution, bumping `probeJitterScale` will dither the probe grid pattern that's currently aliasing into those broad bands.
