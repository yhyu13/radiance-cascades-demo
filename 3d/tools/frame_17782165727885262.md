# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782165727885262_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782165728452547_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782165729514080_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17782165727885262.json`  
**Analyzed:** 2026-05-08T13:03:25  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing mode 0 (final) against mode 6 (GI only) and mode 3 (5× indirect):

- **Top rim band** (~y=190–230): The brightest GI contribution is here. Mode 3 shows it as a near-white horizontal strip; mode 6 confirms it as the dominant indirect signal. This is consistent with the rim catching bounced light from the (unseen) opening above.
- **Lower-left vertical streak** (~x=255, y=400–720): A faint brighter vertical line is visible in all three modes — likely the front-left edge of the box receiving indirect bounce from the adjacent wall.
- **Center floor / back wall** (the large gray expanse, y=300–720): GI is essentially flat and very dim. Mode 6 shows almost no variation across the entire interior — indirect light is reaching the surface but with negligible spatial modulation.
- **Above rim** (y < 180): Completely black in all modes — no scene geometry / no GI, as expected.

## 2. Banding in Mode 6

Looking carefully at mode 6, I can see:

- **A faint horizontal seam at ~y=230**, just below the bright rim, where luminance steps down abruptly into the dark interior. Spacing is single — this looks like a **cascade-boundary transition** (c0 → c1 falloff at the rim's near-field).
- **A very subtle curved arc visible mid-image** (~y=450, centered horizontally), broad and low-contrast. Shape is smooth and large-scale — consistent with **probe-grid interpolation** at base resolution 32 over volumeSize 4 (i.e., probes ~12.5 cm apart projecting as broad lobes).
- I do **not** see distinct directional-bin (8-tap) striping; the interior is too uniformly dim for binning artifacts to surface.

## 3. Smoothness Assessment

- **Mode 3 (×5 indirect)**: Spatially **very smooth** — no salt-and-pepper noise, no temporal sparkle frozen in. The rim gradient is clean. This indicates temporal accumulation is doing its job (temporalAlpha 0.05 is heavy smoothing).
- **Mode 0 (final)**: Bilateral blur has **not** over-smoothed visible edges. The rim highlight is preserved sharply, and the lower-left vertical edge is still distinguishable. No edge bleeding evident.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| Mode 0 | **Good** | Clean composite, edges preserved, but interior reads almost flat-direct because GI is very weak |
| Mode 3 | **Good** | Smooth, low-noise indirect; rim contribution clearly resolved |
| Mode 6 | **Fair** | Reveals that raw GI magnitude is very low (interior nearly black); subtle cascade seam at rim |

## 5. Stats Cross-check & Recommendation

Key observations from stats:
- `surfPct` drops to 81.6% on **c0** — ~18% of finest-cascade probes aren't hitting surfaces, which under-samples near-field bounce exactly where the interior of the box needs it.
- `variance` falls off sharply per cascade (c0=0.0021 → c3=1.9e-5), and `meanLum` is only 0.027 even at c0 — matches the visibly dim interior.
- `maxLum` 0.64 at c0 vs 0.17 at c2 confirms the rim hotspot is driven almost entirely by c0; coarser cascades contribute almost nothing.

**Concrete change: increase `c0MinRange` from 1 → 2** (or alternatively raise `baseRes` from 32 → 48).

Rationale: c0 is the only cascade carrying meaningful luminance into the interior, but its 81.6% surfPct shows probes are missing geometry — likely because c0's range is too tight to reach the box walls from interior probe positions. Extending c0's range will raise surfPct toward 100%, push more bounce light into the dark center floor, and reduce reliance on the near-zero c3 contribution. The faint cascade seam under the rim should also soften.

If the dim interior persists after that, secondary tweak: bump `dirRes` 8 → 12 to better resolve the rim's directional contribution into the cavity.
