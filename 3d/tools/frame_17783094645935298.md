# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783094645935298_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783094648108811_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783094650177616_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17783094645935298.json`  
**Analyzed:** 2026-05-09T14:51:36  
**Model:** claude-opus-4-7

---

# Visual Triage Report — Cornell Box GI

## 1. GI Contribution Analysis

Comparing mode 0 (final) against modes 3/6, the indirect term contributes:

- **Color bleeding onto the side walls and floor**: visible in mode 6 as red wash on the left wall/floor near the red panel, and green wash on the right wall/floor near the green panel. In mode 0 these tints are subtle but present on the floor seam at the left/right edges.
- **Top of the tall box (left)**: mode 6 shows a soft red/warm fill on its left-facing side — this is light bouncing from the red wall. Mode 0 preserves it as a faint warmth.
- **Ceiling spot near the back-center**: mode 6 has a small bright lobe on the ceiling, indicating the GI is treating that as the strongest indirect contributor (likely the area light's first bounce return).
- **Indirect is essentially absent in the direct shadow pockets** (under/behind the short box on the right) — that region stays dark in mode 0, consistent with mode 6 showing low luminance there.

## 2. Banding Artifacts in Mode 6

I can see:
- **Soft rectangular halo around the room silhouette** — the gray top/bottom bars and the colored side panels extend slightly outside the room geometry. This is consistent with **probe-grid leakage at the volume boundary** (volumeSize=4, baseRes=32 → ~0.125 unit probe spacing leaks through the thin walls).
- **No clear directional-bin striping** is visible on the back wall or floor — surfaces look smoothly graded, not faceted into 8 sectors. So `dirRes=8` appears adequate for this geometry.
- **No sharp cascade-boundary rings** are visible as discrete steps; transitions front-to-back on the floor look continuous.

So the dominant artifact is **probe-volume leakage / boundary bleed**, not directional or cascade banding.

## 3. Smoothness Assessment

- **Mode 3 (×5 indirect)**: spatially **very smooth**, almost over-blurred — no speckle, no probe-cell checkering. With temporalAlpha=0.05 (heavy temporal accumulation) and the 5× amplification not revealing noise, the indirect signal is well-converged but **low frequency** (probes can't resolve the box edges; the tall box appears as a soft blob).
- **Mode 0 bilateral blur**: edges of the boxes and wall seams remain crisp — direct lighting carries the high-frequency detail. **No over-smoothing of geometric edges is visible.** However the contact shadow under the short box looks slightly softened compared to the hard shadow under the tall box.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 Final | **Good** | Clean composite, plausible bleeding, edges intact; minor boundary halo. |
| 3 Indirect ×5 | **Good** | Smooth, low-noise, but reveals low spatial resolution of probe field. |
| 6 GI only | **Fair** | Boundary leakage halo around the box exterior is the clearest defect. |

## 5. Stats Cross-Check & Recommendation

- `surfPct` 98.3 / 99.2 / 100 / 100 — **excellent convergence**, no under-sampled probes.
- `variance` drops 0.0085 → 0.0044 → 0.00016 across cascades — clean falloff, consistent with no visible cascade banding ✓.
- C0 maxLum 0.563 vs meanLum 0.053 → ~10× peak/mean ratio at finest cascade, which matches the bright ceiling lobe seen in mode 6.
- `temporalAlpha=0.05` + `probeJitterScale=0.06` + `jitterHoldFrames=2` → very stable but the small jitter radius combined with `baseRes=32` over `volumeSize=4` is what's letting probes near the wall sample through it (the leak halo).

**Concrete change**: increase **`probeJitterScale` from 0.06 → ~0.15** (and optionally drop `jitterHoldFrames` to 1). This widens the stochastic probe offset so wall-adjacent probes average out the back-face leak, which should remove the gray/red/green halo around the box silhouette in mode 6 without hurting the already-strong temporal stability (alpha=0.05 will still smooth the added jitter noise).
