# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784096323799317_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784096326018612_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784096328033452_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784096323799317.json`  
**Analyzed:** 2026-05-10T18:41:05  
**Model:** claude-opus-4-7

---

# Cornell Box GI Triage

## 1. GI Contribution Isolation

Comparing direct-only expectation (a single ceiling light) vs. the three modes:

- **Color bleeding** is clearly present in mode 6: the left wall throws a warm red wash onto the floor near it (visible left-of-center floor) and the right wall casts a green tint onto the right side of the tall block and the floor. In mode 0 this bleed is subtle but visible on the floor and on the side faces of both blocks.
- **Ceiling indirect**: in mode 6 the ceiling shows a faint bright blob directly under/around the light (light leaking back through the indirect term — expected from second bounce off the floor).
- **Shadow fill**: the back-facing sides of the two blocks (right face of tall block, left face of short block) get most of their illumination from indirect — visible as soft non-black shading in mode 6.
- **GI absent**: the outer black region beyond the box frame is correctly zero in all modes; the gray top/bottom slabs of the frame receive negligible indirect (correct, they face away from the interior).

## 2. Banding in Mode 6

I can see:
- **Soft rectangular halos** around the gray top and bottom frame slabs — their edges are blurred outward by roughly 8–12 px in a blocky, axis-aligned fashion. Spacing matches a coarse probe footprint, consistent with the **base 32³ probe grid at volumeSize=4** (probe spacing ≈ 0.125 units, projecting to ~one block per ~10 px at this view).
- **Soft vertical seam** faintly visible on the back wall between the red-tinted left half and green-tinted right half — this is a smooth gradient, not a hard cascade boundary, so it reads as **probe-grid interpolation**, not cascade-boundary banding.
- I do **not** see clear directional-bin pinwheeling (no 8-fold rosette around the light), so dirRes=8 is adequate for this low-frequency lighting.
- No hard ring/shell discontinuities that would indicate cascade transitions.

Conclusion: dominant artifact is **probe-grid (spatial) blockiness**, not directional or cascade.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: spatially very smooth, almost no high-frequency noise. This is consistent with `temporalAlpha=0.05` (heavy temporal accumulation) and the low variance numbers (0.0085, 0.0083, 0.0044, 0.00016). Possibly *over-smoothed* — the wall-corner transitions are mushy.
- **Mode 0 (final)**: the bilateral blur has slightly softened the contact shadow under the short block (right block's base shadow is feathered) and the crisp red/green wall edges show a faint halo bleed onto the floor. The small block's shadow on the floor is acceptable; the tall block's shadow on the left wall is fine.
- No firefly/speckle noise anywhere — variance stats confirm.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| Mode 0 (final) | **Good** | Plausible Cornell, mild over-blur on shadow edges |
| Mode 3 (×5 indirect) | **Fair** | Smooth but blocky around frame slabs; probe footprint visible |
| Mode 6 (GI only) | **Fair** | Clear color bleed direction correct, but probe-grid halos around occluders |

## 5. Stats Cross-check & Recommendation

- `surfPct` 98.3 / 99.2 / 100 / 100 → cascades well-converged, no under-sampling.
- `anyPct=100` everywhere → no empty probes.
- Variance drops cleanly per cascade (0.0085 → 0.00016) → cascade hierarchy is healthy.
- `meanLum` ~0.05 is consistent across c0–c2, then halves at c3 → expected falloff.
- `temporalAlpha=0.05` + low variance = **heavily filtered**, matching the observed mushiness.

**Concrete change**: increase **`baseRes` from 32 → 48** (or `volumeSize` 4 → 3 to tighten probe spacing). The dominant visible artifact is probe-grid blockiness around the frame slabs and at wall/floor seams; directional resolution and temporal filtering are already adequate, so spending the budget on spatial probe density will shar
