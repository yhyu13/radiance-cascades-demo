# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783085299756279_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783085307284223_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783085315027345_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17783085299756279.json`  
**Analyzed:** 2026-05-09T14:36:05  
**Model:** claude-opus-4-7

---

## Visual Triage: Cornell Box GI Cascade

### 1. GI Contribution Isolation

Comparing mode 0 (final) vs mode 6 (GI only):

- **Color bleed onto floor**: Mode 6 shows clear red tint on the left half of the floor and green tint on the right half — strongest within ~1 box-width of each colored wall. This bleed is preserved in mode 0 but heavily diluted by the bright direct term.
- **Tall block (left)**: Its right face picks up faint **green** bounce in mode 6; its left face picks up **red**. Both are visible but very subtle.
- **Short block (right)**: Top face shows soft green wash from the right wall in mode 6.
- **Ceiling**: Mode 6 shows a bright spot directly under/around the area light position — this is the light fixture itself bleeding through, not true bounce. Surrounding ceiling is near-black, indicating **second-bounce light reaching the ceiling is essentially missing**.
- **Outside the box**: The purple sidebar regions remain in all three modes — that's a viewport/letterbox artifact, not GI.

### 2. Banding in Mode 6

- **Halo/footprint around the box exterior**: A clear gray rectangular "skirt" extends ~30–50 px beyond the box silhouette on top and bottom, and the red/green walls bleed outward as soft rectangles past the actual wall geometry. The shape is **axis-aligned and step-edged**, with one visible step roughly matching a probe spacing — this is the **cascade 0 probe footprint leaking outside surface bounds** (probes near the box boundary sampling empty space and writing radiance into adjacent texels).
- **Block edges**: The two interior blocks have soft, slightly blocky shadow boundaries on the floor in mode 6 — spacing is uniform and on the order of the base probe grid (baseRes 32 over volumeSize 4 ⇒ ~8 cm probes), consistent with **probe-grid aliasing**, not directional-bin striping (no radial spokes visible) and not cascade-boundary rings (no concentric jumps).
- No directional-bin "pinwheel" patterns are visible, so dirRes=8 appears adequate for this diffuse scene.

### 3. Smoothness & Over-blur

- **Mode 3 (indirect ×5)**: Spatially **smooth, low-noise**. No fireflies, no temporal speckle visible in the still. The walls show clean gradients. Good convergence.
- **Mode 0 bilateral blur**: The block silhouettes and the wall-corner seams are crisp; the contact shadow under the tall block is preserved. **No over-smoothing of edges**. However, the *outside-the-box halo* (gray skirt) from the GI texture is also present in mode 0 — the bilateral filter is not masking out empty-space probes.

### 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 Final | **Good** | Crisp edges, plausible shading, but exterior halo leaks. |
| 3 Indirect ×5 | **Good** | Smooth, low variance, color bleed clearly resolved. |
| 6 GI only | **Fair** | Correct color bleed, but exterior probe-footprint banding and very dim ceiling indicate weak multi-bounce. |

### 5. Stats Cross-check

- `surfPct` 98.3 / 99.2 / 100 / 100 — excellent probe coverage; no under-sampling.
- `anyPct` 100 across all cascades — no empty probes.
- **Variance trend**: 0.0085 → 0.0083 → 0.0044 → 0.00016. Cascade 3 is essentially flat (maxLum 0.054), which matches the dim ceiling/second-bounce weakness in mode 6. The far cascade is contributing almost nothing.
- `meanLum` is roughly constant across c0–c2 then collapses at c3 — the largest cascade isn't carrying enough energy to feed back as ambient fill.
- `temporalAlpha` 0.05 is low (≈20-frame history), consistent with the smooth mode 3.

### Concrete Parameter Change

**Increase `c1MinRange` (or the cascade-3 range / contribution weight) so the outer cascade carries more of the long-range bounce.** Cascade 3's variance (1.5e-4) and maxLum (0.054) are an
