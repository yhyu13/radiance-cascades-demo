# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783217078871677_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783217086181041_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783217093788376_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17783217078871677.json`  
**Analyzed:** 2026-05-09T18:15:42  
**Model:** claude-opus-4-7

---

## Visual Triage

### 1. GI contribution isolation
Comparing mode 0 (final) to mode 6 (GI only):
- **Color bleed onto the floor and back wall**: The red and green tints visible near the side walls in mode 6 (especially the red glow on the floor-left and green glow on the floor-right) confirm indirect bounce is reaching the diffuse white surfaces. In mode 0 these are subtle but present near the wall-floor seams.
- **Tall block (left)**: GI-only shows a faint red wash on its left face and a green tint on its right face — correct cross-bleed from the colored walls. Direct-only would leave these faces uniformly grey.
- **Short block (right)**: similar but greener on the right side.
- **Ceiling**: GI mode 6 shows a bright hotspot directly under the area light's position on the ceiling — this is GI catching the light source itself (or first-bounce off the area light's neighborhood). The dark band around the ceiling perimeter shows GI is **absent or very weak in the ceiling corners**, which is where direct light dominates in mode 0.
- **Outside the box**: The purple/violet side bars show GI leaking outside the room volume into the sky background — indirect probes are sampling beyond the geometry boundary.

### 2. Banding in mode 6
- **Halo/extension around the box silhouette**: In both modes 3 and 6, the red, green, and grey wall colors extend ~30–50 px *outside* the actual geometry edges. The extensions are **axis-aligned rectangular blocks** with sharp corners and uniform color — classic signature of **probe-grid footprint leakage**: the lowest-resolution cascade's probes (32³ base, 4-unit volume → ~12.5 cm spacing) are interpolating GI values across the boundary between "inside room" and "outside room" probes.
- **Spacing**: the halo thickness is roughly constant on all four sides of the box, consistent with a single cascade's voxel size rather than a directional-bin artifact (which would show angular streaks) or a cascade-boundary artifact (which would show concentric rings of different blur radii).
- **No visible directional-bin banding** (no angular pinwheel/star pattern around the light), so dirRes=8 is sufficient angularly.

### 3. Smoothness assessment
- **Mode 3 (indirect ×5)**: Spatially **very smooth**, almost no high-frequency noise. This is consistent with the very low temporalAlpha (0.05 = heavy temporal accumulation) and the low per-cascade variance (0.0085 down to 0.00016).
- **Mode 0 bilateral blur**: Edges of the blocks and the wall-floor seam look **crisp and well-preserved**; the blur has not bled red onto the white walls or smeared the block silhouettes. No over-smoothing detected on geometry. The only "smoothing" issue is the probe halo outside the box, but that's a probe-extent issue, not a blur issue.

### 4. Ratings
- **Mode 0 (final)**: **Good** — clean composite, correct shadows, color bleed is plausible, only minor halo leakage at the silhouette.
- **Mode 3 (indirect ×5)**: **Fair** — smooth and noise-free, but the probe-grid halo is very obvious here and shows the GI field is "fatter" than the geometry.
- **Mode 6 (GI only)**: **Fair** — color bleed is correct in direction, but the leakage halo and the soft, blocky silhouette confirm the probe footprint is too coarse near surface boundaries.

### 5. Stats cross-check & recommended change
- `surfPct` is 98–100% across all cascades and `anyPct=100%` → probes are **fully converged**, no under-sampling.
- Variance drops cleanly from cascade 0 (0.0085) to cascade 3 (0.00016) → cascade hierarchy is healthy.
- `temporalAlpha=0.05` is quite low; combined with smooth mode 3 it confirms we are **trading responsiveness for smoothness** successfully.
- The visible artifact is **not** noise or directional aliasing — it is **probe-grid spatial resolution at the geometry boundary**.

**Concrete change**: Increase `baseRes` from 32 → 48 (or 64). At volumeSize=4, current spacing is 12.5 cm; halving it to ~6 cm would shrink the leakage halo around the box by ~
