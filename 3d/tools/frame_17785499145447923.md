# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785499145447923_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785499145760786_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785499146202063_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17785499145447923.json`  
**Analyzed:** 2026-05-12T09:39:03  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing mode 0 (final) against modes 3 and 6, the indirect light is doing significant work in this scene:

- **Bright GI zones**: The right-hand wall (foreground, lower-right ~60% of frame) shows strong warm orange/brown bounce light visible in all three modes — clearly indirect, since no direct light source illuminates that surface from the camera angle. The left wall mid-distance also picks up warm bounce.
- **GI absence**: The doorway/opening dead-center (the dark rectangle ~x=230, y=160) reads black across all modes, as expected — no surface behind it to bounce from.
- **Ceiling strip**: The ceiling lights (top of frame) appear in all modes; mode 6 shows their indirect halo on adjacent ceiling tiles, confirming GI is contributing softened light around emitters rather than just the direct hit.

## 2. Banding in Mode 6

Visible banding patterns:
- **Ceiling**: Blocky, roughly square patches along the ceiling row (~24-32 px wide tiles). Spacing matches `baseRes: 32` / `volumeSize: 4` = **probe grid spacing** — this is cascade 0 probe footprint, not directional binning.
- **Right wall**: Smooth gradient with no obvious stepping — directional bins (`dirRes: 8`) are not producing visible angular discontinuities here.
- **No cascade boundary ring** is visible as a discrete seam — cascades appear to blend cleanly.

So banding source = **probe-grid resolution**, primarily on the ceiling where high-frequency emitter contrast meets coarse probes.

## 3. Smoothness vs Blur

- **Mode 3 (×5 indirect)**: Spatially smooth overall, but the ceiling shows visible blocky probe footprints and the right wall has a soft horizontal seam mid-frame. Not noisy in a stochastic sense — the structure is grid-aligned, consistent with `temporalAlpha: 0.05` doing heavy temporal smoothing (variance in cascades is low: 0.004 → 0.00007).
- **Mode 0 (final)**: Bilateral blur has slightly over-smoothed the **right-wall/floor crease** (lower-right diagonal) — the edge softens into a gradient rather than a crisp geometric line. Ceiling emitter highlights are preserved well.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Good** | Pleasant, atmospheric; minor edge softening on floor/wall crease |
| 3 (Indirect ×5) | **Fair** | Reveals probe-grid blockiness on ceiling; smoothness elsewhere is acceptable |
| 6 (GI only) | **Fair** | Ceiling banding clearly visible; right-wall bounce reads correctly |

## 5. Stats Cross-check & Recommendation

- `surfPct` for cascade 0 = **48.6%** — only half of cascade-0 probes hit a surface. This is the under-sampled cascade and explains the ceiling blockiness (probes near emitters are missing hits).
- Cascades 1–3 surfPct = 85/100/100% — converged.
- Variance on cascade 0 = **0.004** (60× cascade 3) — matches the visible spatial structure on the ceiling.
- `anyPct` ~99% everywhere → rays aren't escaping; it's a sampling-density issue, not range.

**Concrete change**: Increase `baseRes` from **32 → 48** (or halve `c0MinRange` from 0.5 → 0.35 to densify cascade 0 spatially). This directly attacks the cascade-0 surfPct deficit and the ceiling probe-grid banding without touching temporal smoothing (which is already tuned conservatively at α=0.05 and looks stable).

If memory is tight, alternative: bump `probeJitterScale` from 0.06 → 0.12 to dither the grid footprint across frames — cheaper but trades banding for slight temporal shimmer given `jitterHoldFrames: 2`.
