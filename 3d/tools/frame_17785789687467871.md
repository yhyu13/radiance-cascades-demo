# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785789687467871_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785789694721019_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785789702420297_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17785789687467871.json`  
**Analyzed:** 2026-05-12T17:43:24  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Cascade

## 1. GI Contribution Isolation

Comparing mode 0 (final) against mode 6 (GI only):

- **Color bleed onto floor**: Mode 6 shows clear red wash on the left half of the floor and green wash on the right half, strongest near the respective colored walls. This bleed is visible but subtle in mode 0 (the floor reads near-white because direct light dominates).
- **Tall block (left)**: Right face picks up faint green from the far wall reflection; left face shows red bleed from the red wall — visible in mode 6, washed out in mode 0.
- **Ceiling**: Mode 6 shows the ceiling is dimly lit by bounce only (correct — no direct light reaches it from a ceiling-mounted area light), with a small bright hotspot near the light position. Mode 0's ceiling brightness comes entirely from this GI term.
- **Back wall**: In mode 6, picks up subtle red on the left edge and green on the right edge — proper bounce.
- **GI absent**: Outside the room volume (the gray "frame" surfaces visible around the box) — correct, those are outside the cascade volume.

## 2. Banding in Mode 6

Inspecting mode 6 carefully:

- **Soft blocky steps along the back wall and ceiling** — roughly 6–8 broad cells across the wall width. Spacing matches a **probe-grid** footprint at `baseRes: 32` over `volumeSize: 4` (effective ~8 visible cells per cascade-0 face after trilinear filtering).
- **No sharp directional pinwheel/star artifacts** are visible, so `dirRes: 8` is adequate for this low-frequency lighting.
- **No visible cascade-boundary seam** (no concentric ring/step where c0→c1 hands off). Consistent with surfPct climbing smoothly 67→99→100→100.
- The faint horizontal seam near the top of the back wall (mode 6) looks like a **probe-grid** artifact, not a cascade transition.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: Spatially **smooth, not noisy** — no salt-and-pepper, no flicker-shaped speckle. The indirect term is well-converged temporally (consistent with `temporalAlpha: 0.05`). The price is visible **over-blur**: the two blocks in mode 3 have lost their silhouette crispness and the colored walls bleed past their geometric edges (red/green halos extend ~20 px beyond the wall plane).
- **Mode 0 (final)**: The bilateral blur has **over-smoothed the contact shadow** under the tall block (the shadow on the floor is soft and lacks a defined ground-contact darkening). The block edges themselves remain sharp because direct lighting carries the geometry, but ambient occlusion-like detail at corners is missing.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 Final | **Good** | Clean, plausible Cornell; minor loss of contact shadow detail |
| 3 Indirect ×5 | **Fair** | Smooth and stable, but bleeds past geometry edges |
| 6 GI only | **Fair** | Correct color bleed direction, but probe-grid blockiness on flat walls |

## 5. Stats Cross-Check & Recommendation

Key observations from stats:
- **Cascade 0 surfPct = 67.8%** — ~32% of c0 probes hit nothing useful. This is the under-sampled cascade and likely the source of the residual blockiness on close surfaces.
- Variance is low and uniform across c0–c2 (~0.008), consistent with the smooth (non-noisy) appearance of mode 3 — banding is **structural, not stochastic**.
- c3 variance collapses to 0.00016 (very flat) — the largest cascade is essentially DC, fine.

**Concrete change**: **Increase `baseRes` from 32 → 48** (or alternatively **lower `c0MinRange` from 0.5 → 0.35** to tighten c0 around the geometry it actually covers). The c0 surfPct of 67% indicates c0 is wasting probes on empty space; either denser probes or a tighter c0 footprint will directly reduce the probe-grid blockiness visible on the back wall and ceiling in mode 6 without needing more directional bins (`dirR
