# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783226334239373_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783226341666313_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783226349023046_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17783226334239373.json`  
**Analyzed:** 2026-05-09T18:31:08  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Renders

## 1. GI Contribution Analysis

Comparing modes 0, 3, and 6:

- **Color bleed** is clearly present and correctly localized: the floor near the red wall picks up a subtle warm tint, and the right side of the tall block shows faint green spill (visible in mode 6 around the block's right edge against the green wall).
- **Ceiling indirect** (mode 6) shows the expected bright spot directly under the area light's reflection, fading toward corners — physically plausible.
- **Behind/under the blocks**: mode 6 shows GI fills the shadow regions (back of tall block, under the short block), which is exactly what indirect should do — direct shadows in mode 0 are not pitch black thanks to this fill.
- **Outside the box** (the gray "frame" border and lavender outer strips): these regions receive GI in mode 6 despite being outside the lit volume. This is leakage — the cascade volume extends beyond the box walls and probes outside are sampling through/around the geometry.

## 2. Banding Artifacts in Mode 6

Looking at mode 6 carefully:

- **Blocky halo around the box exterior**: the gray frame and the colored side panels show clear **rectangular step boundaries** with sharp edges — these are aligned with the **probe grid** at `baseRes: 32` over `volumeSize: 4` (probe spacing ~0.125 units). The steps are roughly uniform in size, consistent with probe-grid quantization, not directional bins.
- **Soft horizontal seam** across the ceiling/upper walls where intensity transitions — likely a **cascade boundary** between c0 and c1 (visible as the brightness falloff doesn't match a smooth 1/r² profile).
- No obvious *radial* banding (which would indicate `dirRes: 8` directional binning being too coarse on glossy/edge regions) — the diffuse-only Cornell hides directional aliasing well.

## 3. Smoothness of Mode 3 vs Bilateral Blur in Mode 0

- **Mode 3** (indirect ×5) looks **very smooth** — almost no high-frequency noise. The temporal accumulation (`temporalAlpha: 0.05` = ~20 frame history) is doing heavy lifting.
- **Mode 0** shows the bilateral blur has **over-smoothed the block edges**: the tall block's silhouette against the back wall is mushy, and its contact shadow on the floor is washed out and bleeds outward. The short block's shadow is similarly soft-edged with no clear contact darkening.
- The leakage in the gray frame area (mode 0) confirms blur is pulling indirect light across geometric discontinuities.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| Mode 0 (Final) | **Fair** | Color bleed correct, but contact shadows lost; visible halo leakage outside box |
| Mode 3 (Indirect ×5) | **Good** | Smooth, low noise, expected color distribution |
| Mode 6 (GI only) | **Fair** | Banding around box exterior reveals probe quantization and leakage |

## 5. Stats Cross-Check & Recommendation

Stats look healthy on convergence: `anyPct=100`, `surfPct≥98%` everywhere, variance drops monotonically across cascades (0.0085 → 0.00016) — **cascades are converged and well-behaved**. The low variance in c3 (0.00016) confirms the visible banding is *not* from noisy probe values; it's from **spatial reconstruction / leak**, i.e. probes outside walls trilinearly interpolating into visible regions.

**Concrete change**: **Reduce bilateral blur reach OR add a stricter normal/depth weight**, and **enable probe occlusion / visibility weights** when sampling. If only one parameter knob: **lower the bilateral blur radius** (or its color-range sigma) — this directly addresses the over-soft block edges and the halo leakage in mode 0 without sacrificing the already-smooth mode 3 indirect.

If a probe-side knob is preferred: **increase `baseRes` from 32 to 48** (finer probe grid → smaller leakage cells around walls). Temporal/jitter settings are fine as-is given the low variance; touching `temporalAlpha` would only hurt responsiveness without fixing the spatial leak that's the actual problem
