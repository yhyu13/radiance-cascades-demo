# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782841125548285_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782841127540568_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782841129650856_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17782841125548285.json`  
**Analyzed:** 2026-05-09T07:49:05  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Pipeline

## 1. GI Contribution Analysis

Comparing the three views isolates indirect light in these regions:

- **Color bleeding onto floor & ceiling**: Mode 6 shows clear red wash on the left half of the floor and faint green on the right — confirmed in mode 0 as the subtle pinkish tint on the floor near the red wall and greenish tint near the green wall. The ceiling in mode 6 shows a warm spot directly under the light, indicating the back-bounce from the bright floor.
- **Tall block left face**: Picks up red bleed in mode 6 (visible as pinkish cast), barely surviving into mode 0.
- **Shadow regions** (behind tall block, under both blocks): Mode 6 shows these are not pitch black — GI fills them with ~0.02–0.05 luminance, which matches `meanLum ≈ 0.05` in the stats.
- **GI is essentially absent** outside the box frame (the dark gray bars top/bottom and the colored wall backs at the edges of the frame remain near-black in mode 6), as expected — those surfaces don't see the interior light.

## 2. Banding Artifacts in Mode 6

I can see:
- **Soft blocky gradient on the back wall and floor** in mode 6 — roughly 4–6 broad luminance "cells" across the back wall. Spacing is consistent with the **base probe grid (32³ at volume 4 → ~0.125 units/probe)**, projected to screen this gives ~6–8 cell widths across the box interior. This points to **probe-grid undersampling**, not directional binning.
- **No visible pinwheel/star patterns** that would indicate directional-bin (`dirRes=8`) aliasing.
- **No sharp seams** between cascade boundaries — cascade transitions appear smooth, so c0/c1 MinRange handoff is fine.

So: **probe-grid spatial resolution is the dominant artifact source**, consistent with cascade 0 variance of 0.0085 (highest of all cascades) and `surfPct=98.3%` (slightly under-converged vs cascades 2–3 at 100%).

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: Looks **spatially smooth** — no high-frequency noise, no speckle. Temporal accumulation (`alpha=0.05`, very strong) is doing its job. However, smoothness here is bordering on "blurry" — edges of the blocks bleed into the floor noticeably.
- **Mode 0**: The bilateral blur has **over-smoothed the tall block's left edge and shadow boundary** — the cast shadow on the floor behind the tall block is soft and lacks a defined contact line. Direct shadow contact under the short block is acceptable but slightly lifted.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Good** | Plausible Cornell, but soft contacts and slight color-bleed loss |
| 3 (Indirect ×5) | **Fair** | Smooth but featureless — low-frequency probe grid visible |
| 6 (GI raw) | **Fair** | Reveals broad probe-cell structure and weak color bleed |

## 5. Recommended Parameter Change

The stats show cascade 0 has the **highest variance (0.0085)** and lowest `surfPct (98.27%)`, while temporal alpha is already very low (0.05) — meaning we're trading responsiveness for smoothness, but spatial resolution is the actual bottleneck.

**Concrete change: increase `probeJitterScale` from 0.06 → 0.15** (or alternatively raise `baseRes` from 32 → 48).

Rationale: The blocky low-frequency structure on the back wall in mode 6 is spatial-grid aliasing in cascade 0. With `temporalAlpha=0.05` you have ~20 frames of accumulation budget — larger jitter will dither across probe cells and let temporal integration resolve sub-cell detail, reducing variance without touching the already-tight time budget (0.21 ms total). Keep `dirRes=8` — no directional artifacts visible.
