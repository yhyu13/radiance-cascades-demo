# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17800338186059493_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17800338193415041_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17800338200790363_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17800338186059493.json`  
**Analyzed:** 2026-05-29T13:50:46  
**Model:** claude-opus-4-7

---

# Visual Triage Report — Cornell Box GI

## 1. GI Contribution Analysis

Comparing mode 0 (final) against mode 6 (GI only), indirect light is doing real work in these locations:

- **Ceiling**: Mode 6 shows the ceiling lit with a faint warm/neutral tint despite the light source being on the ceiling itself — this is bounce from the floor (visible as the brightest GI region after the walls).
- **Floor center**: Picks up a mixed red/green tint from both side walls, visible as a desaturated grey-pink-green wash in mode 6.
- **Tall block, left face**: Clearly tinted **pink/red** from the red wall bounce (mode 6, very obvious).
- **Short block, right face**: Clearly tinted **green** from the green wall bounce.
- **Back wall**: Receives a soft neutral GI fill in mode 6, which in mode 0 lifts it from pure shadow.
- **GI is essentially absent** on the outside (black) regions of the frame and on the direct-lit ceiling cap above the room — correct behavior, since those surfaces are outside the probe volume or face away.

## 2. Banding Artifacts in Mode 6

Visible artifacts on the **back wall** in mode 6:
- A faint **swirled/contour-like pattern** in the upper-center back wall, roughly horizontal sweeps with ~30–50 px spacing in image space.
- Shape is curved/organic rather than axis-aligned grid lines → **not probe-grid aliasing** (which would look like a regular lattice) and **not cascade boundaries** (which would be sharp concentric step changes).
- The organic swirl shape is most consistent with **directional-bin undersampling** at `dirRes: 8` (only 8 directions per probe), where the SH/octahedral interpolation produces low-frequency lobed patterns on smooth walls.
- No visible cascade ring/boundary discontinuity anywhere in the frame — cascade blending looks clean.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: Spatially **very smooth** — almost over-smooth. No high-frequency noise, no speckle. The 5× amplification reveals the indirect is dominated by low-frequency probe interpolation, not stochastic noise. Color bleed onto the blocks is clearly visible and softly graded.
- **Mode 0 (final)**: The bilateral blur has **over-softened the contact shadow under the short block** (right side) — the shadow has a fuzzy halo rather than a crisp ground-contact line. The tall block's left shadow on the floor is also slightly bloomed. Block silhouettes themselves remain sharp, so edge-stopping is mostly working, but the shadow-edge weight is too loose.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 — Final | **Good** | Reads as a Cornell Box, color bleed present, but contact shadows softened. |
| 3 — Indirect ×5 | **Good** | Smooth, plausible bounce, no noise; arguably too smooth (probe-limited). |
| 6 — GI only | **Fair** | Correct color bleed visible, but directional banding on back wall is noticeable. |

## 5. Stats Cross-Check & Recommended Change

- `anyPct = 100` and `surfPct ≥ 84%` across all cascades → probes are **well-converged**, not under-sampled. C0 at 84.8% surfPct is the lowest but still healthy.
- `variance` on C0 (0.008) and C1 (0.008) is moderate; C3 variance is essentially zero (flat far-field). The visible banding on the back wall is **not** explained by spatial probe variance — it's directional.
- `temporalAlpha: 0.05` is low (heavy temporal smoothing) which is why mode 3 looks noise-free.

**Concrete change: increase `dirRes` from 8 → 16.**

Justification: the swirled banding on the back wall in mode 6 is the signature of directional undersampling, not probe-grid or cascade issues (which the stats and image both rule out). Doubling angular resolution will cost ~4× directional storage/bandwidth but `cascadeTimeMs` is only 0.19 ms — there is ample budget. This should eliminate the back-wall swirls without needing to touch the well-tuned temporal/jitter parameters.

Second
