# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785689823959030_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785689825996083_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785689828221718_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17785689823959030.json`  
**Analyzed:** 2026-05-12T14:56:54  
**Model:** claude-opus-4-7

---

# Visual Triage Report — Cornell Box GI

## 1. GI Contribution Analysis

Comparing mode 6 (raw GI) against mode 0 (final), the indirect light is doing the expected work:

- **Color bleeding**: The red wall casts a clear pinkish wash onto the ceiling-left and the left face of the tall white block (visible in mode 6, subtly preserved in mode 0). The green wall does the same on the right side of the short cube and ceiling-right.
- **Ceiling**: In mode 6 the ceiling shows a red→green gradient left-to-right, confirming wall-to-ceiling bounce is working.
- **Shadow fill**: The contact shadow under the tall block in mode 0 is softened (not pitch black) — mode 3 shows indirect light reaching that pocket.
- **GI-absent regions**: The four extruded "tab" regions outside the box (top/bottom gray bars, side panels) receive **no GI** (mode 6 shows them as flat checker pattern with no bleed) — these appear to be out-of-volume geometry where probes return zero.

## 2. Banding in Mode 6

Visible artifacts:

- **Coarse checker pattern** on the outer frame (top/bottom gray bars and the red/green side extensions). Spacing is uniform, ~8–10 px per cell, aligned to world-space — this is **probe-grid aliasing** at the volume boundary, where the inner cascades don't cover and only the coarsest cascade (or fallback) is sampled. Consistent with `cascade[3].variance = 0.00015` (nearly flat) but very low resolution.
- **No directional-bin ringing** visible on curved/angled bounces — `dirRes:8` appears adequate for this diffuse scene.
- **No sharp cascade-boundary seam** visible inside the box interior — c0/c1/c2 blend appears smooth.

The checker is therefore **probe-grid extrapolation outside the volume**, not directional or cascade-transition banding.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: Very smooth inside the box — no temporal flicker signature, no probe dots. The blur is so heavy that the tall block's own GI silhouette has bled ~15 px past its geometric edge (visible halo around the block in mode 3).
- **Mode 0 (final)**: Bilateral blur has **over-smoothed the contact shadow** under the tall block — the AO darkening that should hug the base is washed out. The corner crease where back wall meets floor is also softer than the geometry warrants.
- The checker tabs in mode 0 retain hard texture detail, so the bilateral is correctly edge-stopping on albedo/normal — the over-smoothing is confined to the GI term itself.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 Final | **Good** | Color bleeding reads correctly; mild over-blur on contact shadows |
| 3 Indirect ×5 | **Good** | Spatially clean, but halo around tall block reveals probe footprint |
| 6 GI only | **Fair** | Clear probe-grid checker on out-of-volume geometry; interior is fine |

## 5. Stats Cross-Check & Recommendation

Key stats observations:
- `c0.surfPct = 68.9%` — **31% of cascade-0 probes miss surfaces**, the lowest of any cascade. This is the inner/tightest cascade and is under-converged near the box walls.
- `c0.maxLum = 0.563` with `variance = 0.008` — highest variance of all cascades, consistent with the contact-shadow region near geometry.
- `temporalAlpha = 0.05` is already conservative (slow accumulation), so flicker isn't the issue; spatial coverage is.
- `volumeSize = 4` with `baseRes = 32` → ~0.125 unit probe spacing at c0, but the box geometry extends slightly past the volume (hence the GI-less tabs).

**Concrete change**: Increase `c0MinRange` from `0.5` to `~0.75`, OR enlarge `volumeSize` from `4` to `5`. The 31% c0 miss rate and the visible probe-checker on the extruded frame both point to the **volume being too tight around the Cornell box**. Expanding c0's footprint will (a) bring `c0.surfPct` up toward c1's 99%, (b) eli
