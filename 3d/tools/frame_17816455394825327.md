# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17816455394825327_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17816455396761900_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17816455398963082_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17816455394825327.json`  
**Analyzed:** 2026-06-17T05:32:52  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing the three views, indirect light is **most visible** in:
- **Ceiling near corners** (mode 3 shows soft red/green tint bleeding inward from side walls onto the ceiling)
- **Floor between the two blocks** — mode 6 shows a faint warm glow where bounce light pools
- **Shadowed faces of the blocks** — the right (green-facing) face of the tall block in mode 3 picks up a green tint; the left face of the short block shows red bleed
- **Floor under the tall block's shadow** (mode 0, lower-left of the shadow region) — mode 3 confirms GI is filling this in correctly

GI is **absent / very dim** outside the box volume — the dark frame bars in mode 0 receive almost no indirect (correct, they're outside the cascade volume's lit region).

## 2. Banding in Mode 6 (GI only)

I can see:
- **Soft rectangular blocks** of color on the back wall — roughly 4–5 wide bands across the wall width, consistent with **probe-grid spacing** at `baseRes: 32` projected through the camera (probe footprint ~1/8 of the back wall).
- **A horizontal seam** roughly 1/3 down from the top of the back wall, and another near the floor line — these align with **cascade boundaries** between c0 and c1 (the c0 inner volume edge cuts through the back wall).
- I do **not** see clear directional-bin pinwheel artifacts; the `dirRes: 8` (64 directions) appears sufficient at this view.

The probe-grid blockiness dominates over directional artifacts.

## 3. Smoothness Assessment

- **Mode 3** (5× indirect): Looks **spatially smooth** overall — no high-frequency noise, which is expected given `temporalAlpha: 0.05` (heavy temporal accumulation). Edges of color bleed transitions are soft, almost too soft.
- **Mode 0** (final): Bilateral blur looks **well-tuned** on flat surfaces, but the **shadow contact under the tall block** appears slightly washed — the hard contact shadow line is softened more than ideal. The block silhouettes themselves are preserved cleanly.
- No visible firefly noise anywhere.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| Mode 0 (Final) | **Good** | Clean composite, plausible color bleed, mild over-blur on contact shadows |
| Mode 3 (Indirect ×5) | **Good** | Smooth, correct directionality of bleed, visible probe-grid structure on back wall |
| Mode 6 (GI only) | **Fair** | Banding/probe footprint clearly visible; cascade seams discernible |

## 5. Stats Cross-Check

- `anyPct: 100` across all cascades — full coverage, no under-sampled probes.
- `surfPct` rises 84.9 → 97.9 → 100 → 100 — c0 has ~15% empty-space probes (expected, smallest volume).
- **Variance is highest in c0/c1 (~0.008)** and drops 50× by c3 (0.00016) — this matches the visible banding being concentrated in the near field (back wall, floor between blocks), where c0/c1 dominate.
- `maxLum 0.563` in c0 vs `meanLum 0.053` → strong local hotspots, consistent with the sharp color-bleed pockets seen.

## Recommended Parameter Change

**Increase `probeJitterScale` from 0.06 → 0.12** (and optionally drop `jitterHoldFrames` from 2 → 1).

Reasoning: The visible banding is **probe-grid footprint**, not directional or temporal noise. Variance is moderate, `temporalAlpha: 0.05` is already aggressive (so doubling jitter won't reintroduce flicker), and `dirRes: 8` is adequate. Larger jitter combined with the existing temporal filter will smear the grid structure across more probe positions per accumulation window, dissolving the rectangular bands on the back wall without sacrificing the clean shadow edges in mode 0.

If banding persists after that, **then** consider `baseRes: 32 → 48` — but jitter is the cheaper first move (cascade build is only 0.09 ms).
