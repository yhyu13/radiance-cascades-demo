# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777653876518322_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777653883565236_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777653890842653_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17777653876518322.json`  
**Analyzed:** 2026-05-03T07:43:42  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Render

## 1. GI Contribution Isolation

Comparing mode 0 (final) against mode 6 (GI only):

- **Color bleeding is present and correct in sign**: the red wall casts pinkish indirect onto the left face of the tall block and onto the floor near the left wall (visible in mode 6). The green wall casts greenish indirect onto the right face of the short block and the right floor region.
- **Ceiling indirect** (mode 6) shows a soft bright spot near center — this is the bounce-back from the bright floor, expected.
- **Indirect is essentially absent outside the box silhouette** — the gray frame/walls receive almost no GI (correct, they face away).
- The lavender bars on the far left/right edges show in all three modes identically — these are **outside the GI volume entirely** (no probe coverage), confirmed by the fact that mode 6 has zero contribution there.

## 2. Banding in Mode 6

Looking at mode 6 carefully:

- **Back wall**: a faint vertical gradient and a soft circular hot spot near upper-center — this is the probe-grid footprint, spacing roughly 1/4 of the wall width. Consistent with a low-resolution probe volume (likely 8³ or 16³ at the finest cascade).
- **Floor**: smooth red→green lateral gradient, no visible stair-stepping — directional bins (dirRes=8) are sufficient here.
- **Ceiling**: one soft blob, no concentric rings → **no cascade-boundary seams visible**.
- **Block faces**: the tall block's left face shows a very faint horizontal seam near mid-height — possibly a probe-plane interpolation artifact, but subtle.

Diagnosis: dominant artifact is **probe-grid sparsity**, not directional aliasing or cascade transitions.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: spatially smooth, no salt-and-pepper noise. Edges of the boxes are slightly bloomed/halo'd (the white blocks have a soft glow extending past their silhouette by ~10 px). This is the bilateral filter leaking across geometric edges.
- **Mode 0 (final)**: the bilateral blur has **over-smoothed the contact shadow** under the tall block — the ground-contact darkening that should anchor the block to the floor is washed out on its right side. Color bleed onto the blocks is also softened to the point of looking like ambient tint rather than directional bounce.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 Final | **Fair** | Direct lighting and shadows read well, but indirect contact shadows are smeared and color bleed is muted. |
| 3 Indirect ×5 | **Good** | Useful for debugging; smooth, edges identifiable, halos are mild. |
| 6 GI only | **Fair** | Correct color directions, but probe-grid blob on back wall and overall low magnitude (max ~0.05 in cascade 3). |

## 5. Stats Cross-Check & Recommendation

Key observations from stats:
- **Cascade 1 surfPct = 75.4%** — ~25% of probes in the second cascade missed surfaces. This is the under-sampled cascade and likely correlates with the soft back-wall blob.
- **Cascade 3 maxLum = 0.054, variance = 0.00015** — extremely flat/dim; this cascade is contributing almost nothing useful but still being blended in, which dilutes the sharper near-field cascades.
- **Variance drops 50× from cascade 0 → 3** — consistent with the visually soft, low-contrast GI.
- temporalAlpha=0.1 is fine (no flicker visible in a still); probeJitterScale=0.06 is modest.

**Concrete change:** increase **probeJitterScale from 0.06 → 0.15** (or alternatively raise the cascade-1 probe density). The 75% surfPct in cascade 1 indicates probes are landing in coherent dead spots; more jitter will spread misses across frames and, combined with temporalAlpha=0.1, average out the back-wall probe blob without introducing flicker.

If a second change were allowed: reduce the weight of cascade 3 in the final blend — its variance is two orders of magnitude below the others and it's flattening the GI signal.
