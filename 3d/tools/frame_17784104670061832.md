# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784104670061832_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784104672561374_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784104675589680_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784104670061832.json`  
**Analyzed:** 2026-05-10T18:54:57  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Renders

## 1. GI Contribution Analysis

Comparing the three modes, indirect light is contributing primarily in these regions:

- **Ceiling area (top of frame)**: Strong reddish-brown bounce, visible in modes 3 and 6 as the warm tint on the upper-left and upper-right walls. The "stair-step" colored patches near the top center are the dominant GI emitters (likely the lit ceiling fixtures bleeding color).
- **Right wall (mid-to-lower)**: Heavy warm/orange GI saturation in mode 6 — this is where indirect dominates over direct.
- **Left wall**: Comparatively cool/neutral; GI is weak here, suggesting it faces away from the bounce sources.
- **Floor center strip**: The bright specular/direct highlights down the floor in mode 0 are *absent* in modes 3 and 6 — confirming those are pure direct contribution, not GI.
- **Far doorway (center vanishing point)**: Dark in mode 6, meaning GI is not reaching the far end — corridor occlusion is working.

## 2. Banding in Mode 6

Visible artifacts in the GI-only view:

- **Blocky pixel patches on the ceiling** (top-center, ~8–16 px wide): These align with the `baseRes: 32` / `volumeSize: 4` probe grid spacing — classic **probe-grid banding**, not directional-bin banding (which would show angular pinwheel patterns instead).
- **Soft horizontal smear across the right wall**: Looks like a **cascade boundary** transition — the right side is in a coarser cascade (likely C3, given it's the only one with data) while the corridor center samples a different cascade level.
- No obvious directional-bin striping (which would appear as discrete fan-shaped lobes), so `dirRes: 8` is adequate angularly.

## 3. Smoothness Assessment

- **Mode 3 (×5 amplified)**: Surprisingly smooth — almost too smooth, looks washed out and low-frequency. This indicates heavy temporal accumulation (`temporalAlpha: 0.05` is very low → ~20 frame lag) is hiding noise but also hiding detail. No high-frequency speckle visible.
- **Mode 0 (final)**: The bilateral blur has **over-smoothed the floor highlights** — the bright direct-light specks down the corridor floor look stretched/dashed rather than crisp. The right-wall GI bleeds into what should be a darker corner near the lower right.
- **Edge preservation**: The doorway silhouette in mode 0 is acceptably sharp, but the ceiling fixture edges are mushy.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Fair** | Plausible look, but blur is too aggressive on floor; right-side GI feels overpowered |
| 3 (Indirect ×5) | **Fair** | Smooth but suspiciously featureless; suggests under-sampling masked by temporal blend |
| 6 (GI only) | **Poor** | Visible probe-grid blockiness on ceiling; cascade asymmetry left vs right |

## 5. Stats Cross-Check & Recommendation

The stats reveal serious issues:
- **C0: anyPct=5.15%, meanLum=NaN, maxLum=inf** → cascade 0 is almost entirely empty *and* contains a NaN/Inf poison value. This is a bug-level red flag.
- **C1: all zeros** → cascade 1 never sampled anything.
- **C2: meanLum=−375, variance=−0.109** → negative luminance and variance are mathematically impossible; another corruption indicator.
- **C3: anyPct=100%, skyPct=51%, variance=1.3e7** → only the coarsest cascade is actually producing data, and its variance is enormous, matching the visible blockiness.

**Concrete parameter change**: The system is effectively running on cascade 3 alone (coarse probes covering the whole volume), which directly explains the probe-grid banding on the ceiling and the lack of fine GI detail. The Inf in C0 is also likely propagating into the bilateral filter and contaminating mode 0.

> **Recommendation**: Increase `c0MinRange` from `1` to `4–8` so cascade 0 covers a meaningful near-field region, *and*
