# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785700946351434_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785700947531427_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785700949787837_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17785700946351434.json`  
**Analyzed:** 2026-05-12T15:15:25  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Render Modes

## 1. GI Contribution Analysis

Comparing the three views to isolate where indirect light is doing work:

- **Ceiling lanterns (top center, receding into distance)**: Strong direct emission visible in mode 0; mode 6 shows them as bright sources with halo bleed. GI is correctly capturing emitter position.
- **Floor directly beneath lanterns (center-bottom strip)**: Mode 3 reveals strong bounce light pooling on the floor between lantern positions — this is the dominant GI contribution. In mode 0 these read as soft bright pools.
- **Right wall (orange/terracotta block run)**: Almost entirely GI-lit. Mode 3 shows uniform warm wash; mode 6 confirms the brown tint comes from indirect bounce off the floor/ceiling, not direct illumination.
- **Left wall pillars (mid-left)**: Bright in mode 3, indicating they receive significant bounce. Mode 0 preserves this.
- **Far end of corridor (vanishing point)**: Mode 3 shows it falls off to dark — GI is not propagating far enough down the hallway, consistent with low cascade range or insufficient bounces.

## 2. Banding Artifacts in Mode 6

Distinct artifact patterns visible:

- **Concentric circular "bokeh" rings on the right wall** (mid-right, surrounding each lantern): Large overlapping discs roughly 80–120 px across, spacing ~40 px. Shape is too large and too circular for directional bins (8 dirs would give angular wedges, not discs). These are **probe-grid splat footprints** — each probe's contribution is being rendered as a sphere of influence, and at `baseRes:64` with `volumeSize:4`, probe spacing is coarse relative to screen.
- **Stepped contour bands on the floor** (center-bottom, perpendicular to view direction): Spacing roughly matches probe cell boundaries projected into screen space. These are **probe-grid interpolation seams**, not cascade boundaries (no clean radial transitions visible).
- **No clear cascade-boundary discontinuity** is visible as a single hard ring — the cascades appear to blend, though the falloff at the far end may hide a c0/c1 transition.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: Spatially **smooth but blobby** — temporal accumulation (alpha 0.05) is doing heavy lifting. Almost no high-frequency noise, but probe footprints are visible as soft bulges on the right wall and floor. This is under-resolved, not noisy.
- **Mode 0 (final)**: Bilateral blur has **not over-smoothed** sharp geometry edges (lantern silhouettes remain crisp, floor/wall corner is preserved). However, the GI bokeh discs on the right wall pass through the blur largely intact — blur is respecting depth/normal edges but cannot fix low-frequency probe structure.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Fair** | Readable, correct mood, but probe blobs visible on right wall |
| 3 (×5 indirect) | **Fair** | Smooth, no noise, but reveals coarse probe footprint clearly |
| 6 (GI only) | **Poor** | Severe circular banding/bokeh on right wall and ceiling |

## 5. Parameter Recommendation

**Cross-checking stats with visuals:**
- Cascade 0: `surfPct: 49.8%` is **the bottleneck** — half of c0 probes don't have surface hits, yet `variance: 0.00456` is the highest of all cascades (3–8× higher than c2/c3). High variance + low surface coverage in the finest cascade = exactly the blobby, ringed appearance on the right wall.
- `maxLum: 0.92` in c0 vs `0.37` in c1 confirms c0 is carrying isolated bright probes that aren't being smoothed by neighbors.
- `cascadeTimeMs: 3.7` vs `raymarchTimeMs: 0.11` — cascade build dominates; there's headroom.

**Concrete change**: **Increase `c0MinRange` from 0.5 → 1.0** (and/or raise `baseRes` from 64 → 96).

Rationale: c0 
