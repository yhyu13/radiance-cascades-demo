# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784666311687158_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784666314458448_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784666317040523_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784666311687158.json`  
**Analyzed:** 2026-05-11T10:31:06  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing modes 0, 3, and 6:

- **Ceiling lights** (top edge of frame, the bright orange/copper rectangular fixtures) are the only meaningful direct emitters — visible identically across all three modes.
- **Floor and right wall** in mode 6 carry a distinct warm/orange tint that is absent from a pure-direct expectation; this is GI doing its job, picking up the bounce from the ceiling lights and warm end-wall.
- **Far end of corridor** (center, around the doorway/opening) — mode 3 reveals that indirect light *should* be filling this region brightly (it's the brightest spot in the 5× view), but in mode 0 that contribution is being overwhelmed by the dark direct term, leaving the doorway looking nearly black.
- **Left wall** is conspicuously *missing* indirect contribution in mode 6 — it reads almost pure neutral/grey while the right wall is saturated warm. This is asymmetric and suggests directional-bin coverage is biased.

## 2. Banding Artifacts in Mode 6

Visible banding:
- **Ceiling light row**: the lights themselves show stair-stepped/blocky horizontal striping along their length (top of frame). Spacing is roughly uniform, ~30–40 px between steps. This matches **probe-grid voxel resolution** (baseRes 32, volumeSize 4 → ~8 cm per probe). These are probe-grid texel boundaries, not directional bins.
- **Floor**: smooth gradient, no obvious banding, but the right-wall/floor seam shows a **soft vertical discontinuity** mid-frame — consistent with a **cascade boundary** between c0 and c1.
- No clear ring-shaped or angular banding that would indicate dirRes=8 directional bin aliasing on diffuse surfaces (which is appropriate — 8 dirs is low but the scene is diffuse enough to hide it).

## 3. Smoothness / Over-blur Assessment

- **Mode 3** is *spatially smooth* on the floor and walls but shows the blocky stepping on the ceiling lights. The smoothness is consistent with heavy temporal accumulation (temporalAlpha 0.05 → ~20-frame history).
- **Mode 0** the bilateral blur has **over-smoothed the right side of the frame** — the entire right third is a continuous warm blur with no geometric edge between wall, floor, and far doorway visible. The corridor-end doorway edge is also softened to the point of being illegible.
- The left wall edge is preserved better, suggesting the bilateral kernel weights are working asymmetrically (likely depth-driven and the right-wall depth gradient is gentle).

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| Mode 0 (final) | **Fair** | Right-side over-blur destroys geometric structure; doorway clarity lost |
| Mode 3 (indirect ×5) | **Fair** | Smooth but reveals heavy probe-grid stepping on ceiling lights |
| Mode 6 (GI only) | **Poor** | Asymmetric coverage (right warm, left neutral), visible cascade seam |

## 5. Stats Cross-Check & Recommendation

The stats are alarming:
- **c0 anyPct = 3.56%, surfPct = 0%** — cascade 0 is barely hitting anything and *never* hitting a surface. meanLum is NaN, maxLum is inf → uninitialized/divide-by-zero territory.
- **c1, c2, c3 all have anyPct = 0** — cascades 1–3 are effectively dead. c2 reports `meanLum = -435` with `variance = 1e6`, which is garbage (negative luminance).
- The visible GI is being carried almost entirely by 3.5% of c0 probes, which explains the asymmetric/blotchy coverage on the left vs right walls.

### Concrete parameter change

**Increase `c0MinRange` from 1 to 4** (and `c1MinRange` to 8). The current ranges are far too short — c0 probes can only see 1 unit, so they almost never hit a surface in a 4-unit volume corridor, producing the 3.5% anyPct and starving the higher cascades of any usable input. Lengthening the per-cascade ray range should raise surfPct on c0 above ~30%, propagate real
