# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784839093701522_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784839093827952_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784839094079081_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784839093701522.json`  
**Analyzed:** 2026-05-11T15:19:00  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Renders

## 1. GI Contribution Analysis

Comparing the three modes, the indirect light contribution is concentrated in:
- **Ceiling near the light source** (top-center of frame, mid-distance): brightest indirect bounce, visible as warm orange-brown patches in modes 3 and 6
- **Far end of the corridor**: a clear warm "glow pocket" centered in the vanishing point — direct light bouncing off the back wall
- **Right wall, mid-distance**: subtle warm tint in mode 6 indicating wall-to-wall bounce

GI is **largely absent** on the left wall (it stays cool/neutral in mode 6) and on the floor in the foreground — these areas in mode 0 are essentially direct-lit only.

## 2. Banding Artifacts in Mode 6

The ceiling shows **discrete rectangular blotches** of indirect light, irregularly spaced along the corridor's length. Their shape is **axis-aligned and patchy** (not radial, not angular wedges), with apparent spacing roughly matching a coarse voxel grid — consistent with **probe-grid aliasing** at `baseRes: 32` over a `volumeSize: 4` volume (~0.125 unit probe spacing in C0).

I do **not** see clean concentric rings (which would indicate cascade boundaries) or angular pie-slice patterns (which would indicate `dirRes:8` directional binning). The artifact signature is **probe-grid sourced**, not directional or cascade-boundary.

## 3. Smoothness Assessment

- **Mode 3** (indirect ×5): Spatially **fairly smooth** on walls and floor — the amplification reveals a soft, low-frequency field with no high-frequency noise. The ceiling blotches are the only structured artifact.
- **Mode 0** (final): The bilateral blur has done its job on the floor and walls, but the **right wall mid-frame shows a smeared dark vertical streak** that looks over-blurred — an edge has been dissolved into a soft gradient where geometry detail should be.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Fair** | Pleasant overall, but right-wall over-blur and ceiling artifacts leak through |
| 3 (Indirect ×5) | **Good** | Smooth field, isolated artifact location is diagnostic |
| 6 (GI only) | **Fair** | Clear probe-grid blotches on ceiling betray under-resolved C0 |

## 5. Stats Cross-Check & Recommendation

Cascade convergence looks healthy: `anyPct=100` everywhere, `surfPct` rises 48→100 across cascades as expected. **C0 variance (1.03e-3) is ~116× C3 variance (8.9e-6)** — the spatial structure you see on the ceiling matches this: C0 is doing most of the work and carrying most of the noise/aliasing. `maxLum=0.54` in C0 vs `meanLum=0.017` indicates a few very hot probes (the light-adjacent ceiling cells) dominating.

### Concrete parameter change

**Increase `probeJitterScale` from 0.06 → 0.15** (and consider reducing `jitterHoldFrames` from 2 → 1).

Rationale: The artifact is probe-grid aliasing in C0, not directional or temporal noise. With `temporalAlpha=0.05` you have plenty of temporal integration headroom (~20-frame effective window) to absorb stronger jitter without flicker. Larger jitter will dither the C0 grid pattern across frames and let the temporal filter dissolve the ceiling blotches, without needing to raise `baseRes` (which would cost more than the current 0.386 ms cascade budget).

If the ceiling blotches persist after that, **then** raise `baseRes` to 48 — but try the cheaper jitter fix first.
