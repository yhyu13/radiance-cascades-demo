# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783005445910910_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783005447027106_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783005448188876_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17783005445910910.json`  
**Analyzed:** 2026-05-09T12:22:55  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution (where indirect light lives)

Comparing **mode 0** (final) against **mode 6** (raw GI):

- **Back wall (center)**: In mode 0 it reads neutral white; mode 6 shows it tinted **pinkish-grey on the left half and greenish on the right half** — classic Cornell color-bleed from the red/green walls. This is the cleanest, most expected GI signal in the frame.
- **Pedestals (foreground blocks)**: Mode 6 shows essentially **no indirect contribution** on their tops/sides — they read near-black. The pedestals in mode 0 are lit almost entirely by direct light from the ceiling panel.
- **Orange sphere/icosahedron on the rear pedestal**: Mode 6 shows it retaining strong orange — likely emissive being injected into the GI volume rather than receiving bounce. Confirmed by mode 3 where it shows a soft yellow halo on the back wall behind it.
- **Floor**: Mode 6 floor is nearly black. Either the floor isn't sampling probes correctly, or the volume doesn't extend low enough — see banding note below.
- **Red and green walls themselves**: Receive almost no GI in mode 6 (correct — they face the light directly and don't bounce onto themselves much).

## 2. Banding in Mode 6

Visible structures:

- **Horizontal arc-bands across the ceiling and floor** (the dotted/striped curves wrapping around the top and bottom edges). The spacing widens toward the camera and tightens toward the rear — this is **probe-grid aliasing along the depth axis**, where each probe's influence footprint is being sampled as a discrete shell. Spacing is consistent with `baseRes:32` over `volumeSize:4` (≈0.125 units per probe), and the arcs read as cascade-0 probe spheres.
- **Vertical streaking on the pedestals** in all three modes — these are **directional-bin artifacts** from `dirRes:8` (only 8×8=64 directions per probe). The streaks align with cone axes, not probe positions.
- **No clear cascade-boundary discontinuity** is visible (no sudden ring where c0→c1 hands off), which matches the smooth meanLum falloff across cascades (0.039 → 0.037 → 0.032 → 0.017).

## 3. Smoothness of Mode 3 vs Blur in Mode 0

- **Mode 3 (×5 indirect)** is **spatially smooth on the walls** — the pink/green bleed is broad and gradient-like, no high-frequency speckle. Good temporal integration (`temporalAlpha:0.05` is doing its job).
- However mode 3 shows the **same arc banding on ceiling/floor** as mode 6, just dimmer — confirming it's a sampling-structure issue, not a noise issue.
- **Mode 0's bilateral blur** does **not appear to have over-smoothed** any geometric edges — pedestal silhouettes, sphere outline, and wall corners are all crisp. The blur is correctly respecting depth/normal edges.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| Mode 0 (final) | **Good** | Clean composite, edges preserved, color bleed subtle but present |
| Mode 3 (indirect ×5) | **Fair** | Smooth on walls but probe-arc banding clearly visible on ceiling/floor |
| Mode 6 (GI only) | **Fair** | Reveals real GI structure but pedestals/floor read near-black, suggesting under-sampling there |

## 5. Stats Cross-check & Recommendation

Key observations from stats:
- `surfPct` for **cascade 0 is only 74.8%** and cascade 1 is 81.7% — meaning ~25% of c0 probes are not finding surfaces within range. This explains the dark pedestals and floor in mode 6: the near-field cascade is under-populated.
- `variance` is highest in cascade 0 (0.0053) — consistent with the visible arc banding being a c0 phenomenon.
- `dirRes:8` (64 dirs) is low and matches the vertical streaking on pedestals.

**Concrete recommendation**: Increase `c0MinRange` from `1` to `2` (or bump `baseRes` from 32 → 48). The 25% miss rate in cascade 0 indicates probe r
