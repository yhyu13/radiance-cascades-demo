# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17780615026054170_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17780615033499679_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17780615041007860_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17780615026054170.json`  
**Analyzed:** 2026-05-06T17:58:54  
**Model:** claude-opus-4-7

---

# Visual Triage Report — Cornell Box GI

## 1. GI Contribution Isolation

Comparing mode 0 (final) against mode 6 (GI only):

- **Color bleed** is clearly visible in mode 6: the floor near the red wall (left side) takes on a reddish tint, and the floor near the green wall (right side) shows a green tint. The taller box's right face picks up faint green, and the shorter cube's left face picks up a touch of red — exactly what you'd expect from a Cornell box.
- **Ceiling glow** in mode 6 is concentrated as a soft hotspot just below center on the back/ceiling area — this is light bouncing back from the bright floor toward the ceiling.
- **Indirect light is absent** behind the tall box (left-rear floor area) — the shadow region in mode 0 stays dark because GI doesn't reach into that occlusion pocket strongly.
- The dark frame/border surrounding the box in mode 6 confirms GI is correctly zero outside the lit volume.

## 2. Banding in Mode 6

Examining mode 6 closely:

- **Soft block-shaped halos** extend ~10–15 px outside the geometry silhouette on all sides (especially visible on the top/bottom gray bars and the outer edges of the red/green walls). The halo size is roughly uniform and rectangular — consistent with **probe-grid footprint leakage** at `baseRes=32` over `volumeSize=4` (one probe ≈ 0.125 units, projecting to ~that pixel halo in screen space).
- **No directional-bin streaks** (no radial spokes from probes), so `dirRes=8` is sufficient for this diffuse scene.
- **No sharp cascade-boundary rings** are visible — the cascades blend smoothly, consistent with cascade 3's very low variance (0.00016).
- The faint **rectangular silhouette discontinuity** around the colored walls in mode 6 is the trilinear probe footprint, not a binning artifact.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5):** Spatially very smooth — no salt-and-pepper noise, no flickering hotspots. The 5× amplification reveals only gentle gradients and the same probe-footprint halos seen in mode 6. Temporal accumulation (`temporalAlpha=0.05`) is doing its job.
- **Mode 0 (final):** The bilateral blur appears **slightly over-aggressive on the small cube's contact shadow** (right side of frame) — the shadow under the small cube is soft and blurred outward. The tall box's left contact shadow is also a bit feathered. Wall–ceiling and wall–floor seams remain crisp, so the blur is preserving major edges correctly.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Good** | Clean composite, correct color bleed, minor contact-shadow softening |
| 3 (Indirect ×5) | **Good** | Smooth, low noise, but probe-footprint halo is obvious at 5× |
| 6 (GI only) | **Fair** | Reveals rectangular probe-footprint leakage outside geometry |

## 5. Stats Cross-Check & Recommendation

- `surfPct` is 98.3 / 99.2 / 100 / 100 — **excellent probe convergence**, no under-sampling.
- Variance drops cleanly across cascades (0.0085 → 0.0044 → 0.00016), matching the absence of ring banding.
- The visible halo leakage outside silhouettes is a **spatial resolution issue**, not a sampling/convergence issue.

**Concrete parameter change:** Increase `baseRes` from 32 to 48 (or reduce `volumeSize` to ~3.0 to tighten the probe volume around the box). This will shrink the probe footprint and eliminate the rectangular halo bleeding past the geometry silhouette in mode 6, and tighten the contact shadows in mode 0 without needing to reduce bilateral blur radius. Cascade timings (~0.1 ms each) leave plenty of budget for the resolution bump.
