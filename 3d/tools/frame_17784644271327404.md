# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784644271327404_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784644277865591_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784644284718755_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784644271327404.json`  
**Analyzed:** 2026-05-11T09:54:21  
**Model:** claude-opus-4-7

---

# Triage Report: Cornell Box GI Cascade

## 1. GI Contribution Isolation

Comparing modes 0 → 6 → 3:

- **Top edge / ceiling band** (~y=150–320): Strong warm orange-brown contribution visible in all three modes. This is the dominant GI signal — bounce light off the top surface is depositing color along the upper rim with brightest pixels at the top corners (~x=340 and ~x=2200).
- **Left wall vertical strip** (x≈340–400, full height): A faint warm vertical streak is visible in mode 3 (×5) and barely in mode 6 — indirect bounce off the left wall.
- **Right wall** (x≈2150–2230): Symmetric faint vertical streak, slightly weaker than the left.
- **Center / lower 2/3 of the frame**: Almost pure black in mode 6, very dim brown in mode 3. Indirect contribution falls off rapidly away from the lit top edge — expected for a Cornell box where bounces decay with distance from the light/aperture.

The mode 0 final closely matches mode 6 in spatial distribution, confirming GI is the only meaningful term here (no strong direct light is visible in this crop — likely a back/shadow face).

## 2. Banding in Mode 6

- **Horizontal band at y≈300–340**: A sharp transition from the bright top stripe to the dim body. The edge is straight and runs the full width — consistent with a **cascade-boundary discontinuity** (likely C0→C1 handoff at ~1 unit from the probe origin, matching `c0MinRange=1`).
- **Vertical "shoulders" at x≈340 and x≈2200**: Mirrored bright caps protruding upward at the top corners. Their spacing (~1860 px apart) and discrete positioning suggest a **probe-grid sampling pattern** at `baseRes=32` — the corner probes happen to land directly on the lit geometry while interior probes sample shadowed faces.
- No fine directional ringing visible — `dirRes=8` (64 directions) appears adequate; banding is positional, not angular.

## 3. Smoothness Assessment

- **Mode 3 (×5 indirect)**: Spatially smooth across the body — no salt-and-pepper noise, no temporal flicker artifacts visible. The top stripe shows soft falloff. Temporal accumulation at `α=0.05` is doing its job.
- **Mode 0 (final)**: The bilateral blur has **not over-smoothed** — the top edge transition at y≈300 remains crisp and the corner highlights are preserved. If anything, the blur is conservative; the cascade-boundary band at y≈300 is still clearly visible (a more aggressive blur would hide it).

## 4. Ratings

| Mode | Rating | Notes |
|------|--------|-------|
| 0 (Final) | **Good** | Clean, no noise, edges preserved. Cascade seam slightly visible. |
| 3 (Indirect ×5) | **Good** | Smooth, useful for diagnosis, confirms GI shape. |
| 6 (GI only) | **Fair** | Reveals real banding at cascade boundary and corner probe quantization. |

## 5. Stats Cross-Check & Recommendation

Stats show:
- `surfPct` 98.3 / 99.2 / 100 / 100 — excellent probe convergence, no under-sampling.
- `variance` drops cleanly across cascades (0.0085 → 0.00016) — outer cascades are essentially flat, confirming the bright top stripe lives entirely in **C0**.
- `maxLum` 0.56 in C0 vs 0.054 in C3 — a 10× dynamic range across cascades, which explains the visible **C0→C1 seam at y≈300** matching `c0MinRange=1`.

**Concrete suggestion**: Increase `c0MinRange` from `1` → `1.5` (and correspondingly `c1MinRange`) to push the C0→C1 transition further out, and/or widen the cascade overlap blend region. This will smooth the horizontal seam at y≈300 without changing probe density or sampling cost. If a parameter must stay integer-valued, alternatively raise `baseRes` from 32 → 48 to reduce the corner-probe quantization causing the bright caps at x≈340/2200 — variance stats indicate you have headroom to spend on spatial resolution rather than directional or temporal budget
