# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777313678792269_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777313679515508_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777313680437961_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17777313678792269.json`  
**Analyzed:** 2026-05-02T22:16:38  
**Model:** claude-opus-4-7

---

# Cornell Box GI Triage

## 1. GI Contribution Analysis

Comparing mode 0 against modes 3/6, the indirect light is contributing in the expected places:

- **Color bleeding on the floor**: Slight red tint on the floor near the left (red) wall and faint green tint near the right (green) wall — visible in mode 6, more obvious in mode 3.
- **Ceiling brightness**: The ceiling in mode 6 shows a clear bright spot near the back-center, indicating the area light's bounce is being captured.
- **Box side-faces**: In mode 6, the left face of the tall box (facing the red wall) picks up subtle warm tones; the right face of the short box picks up green. This is correct GI behavior.
- **GI absence**: The shadow regions behind the tall box (visible in mode 0) are *not* receiving meaningful fill light in mode 6 — they read nearly black, which is why mode 0's contact shadows look harsh.

## 2. Banding Artifacts in Mode 6

I can see the following structured artifacts:
- **Soft blocky gradients on all three back-facing walls** — the falloff is not smooth but stepped in roughly large patches (~50–80 px wide). Spacing/shape is consistent with a **probe-grid** footprint, not directional bins (which would manifest as angular wedges) or cascade boundaries (which would be sharp concentric rings).
- **No visible cascade ring** — the variance drop from cascade 0→3 (0.0057 → 0.00016) is monotonic and the transition appears hidden by the probe blur.
- **No directional pinwheel/wedge artifacts** — `dirRes: 8` (64 directions) appears sufficient for this diffuse scene.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: Very smooth, almost *too* smooth — looks heavily low-pass filtered. No high-frequency noise visible, which suggests the temporal accumulation (`alpha=0.05`, ~20-frame EMA) is doing most of the denoising work. The tall box's shadow region against the back wall is washed out — GI is leaking slightly into the contact shadow.
- **Mode 0 (final)**: The bilateral blur has **over-smoothed the wall/ceiling seams** — note how the corners between back wall and ceiling are slightly rounded/blurry rather than crisp. The boxes themselves keep their edges, so the bilateral is respecting depth/normal discontinuities reasonably well, but the edge stop sigma may be too loose for coplanar tonal transitions.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Good** | Plausible CB image; minor edge softening at wall seams |
| 3 (Indirect ×5) | **Good** | Smooth, low noise, but probe footprint visible |
| 6 (GI only) | **Fair** | Clear probe-grid blockiness on walls; back wall GI is dim |

## 5. Stats Cross-Check & Recommendation

Key observations from stats:
- **Cascade 0 surfPct = 29%** — only ~1/3 of near-field probes hit a surface. This is the cascade most responsible for short-range bounce (color bleeding, contact GI), and its under-sampling explains both the dim back-wall GI and the visible probe blockiness.
- **Cascade 0 variance (0.0057)** is the highest — consistent with the visible spatial structure on walls in mode 6.
- **Cascade 3 maxLum (0.053)** is far below others — far cascade is contributing almost nothing, which is fine for a closed box but means it's wasted budget.

**Concrete change**: Increase **`probeJitterScale` from 0.05 → 0.15** (or ~0.2). With `temporalAlpha=0.05` you have ~20 frames of integration headroom, so larger jitter will be smoothed out temporally while filling the gaps in cascade 0's 29% surface coverage and breaking up the grid-aligned banding on the walls.

If banding persists after that, secondary suggestion: redistribute cascade range — shrink cascade 3's footprint (it's near-saturated and contributing <6% of max luminance) and tighten cascade 0 to lift its surfPct above 50%.
