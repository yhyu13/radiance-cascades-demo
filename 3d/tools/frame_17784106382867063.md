# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784106382867063_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784106385694142_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784106388129609_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784106382867063.json`  
**Analyzed:** 2026-05-10T18:57:49  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Isolation

Comparing mode 0 (final) against modes 3/6, the indirect light is doing real work in these regions:

- **Ceiling/upper-right corner** (the warm reddish wash visible in mode 6 around x≈900–1100, y≈100–400): clearly inherited from the orange/copper ceiling beams. This bounce dominates the right-wall warmth in mode 0.
- **Left wall and floor near camera** (lower-left quadrant): mode 6 shows a cool/neutral grey contribution, lifting what would otherwise be black shadow under the railing/ledge in mode 0.
- **Far doorway** (center, ~x=470, y=300): mode 3 shows a soft pinkish glow leaking out from behind the doorway, which becomes the bright orange "exit light" in mode 0. This is a genuine indirect bounce, not direct.
- **GI is essentially absent** along the very dark right-foreground blob (x≈1100, y≈500–700) — that area reads black even in mode 6, suggesting either occlusion or under-sampled probes there.

## 2. Banding in Mode 6

Visible structured artifacts:

- **Ceiling beams (top center/right)**: hard stair-stepped color blocks roughly 30–50 px wide, aligned with the ceiling geometry. Spacing matches the **probe grid** (baseRes 32 over volumeSize 4 → ~8 voxel ticks across the frame), not directional bins.
- **Far doorway interior**: a sharp rectangular dark/light boundary at ~x=440, y=280–360. The abrupt transition from warm interior to cool exterior on a near-axis-aligned plane is consistent with a **cascade boundary** — likely C0→C1 handoff, given C0 has `anyPct=4.3%` while C1 collapses to `0.024%`.
- I do **not** see the rotational pinwheel pattern that would indicate `dirRes=8` directional binning artifacts.

## 3. Smoothness Assessment

- **Mode 3 (5× amplified)**: surprisingly smooth — almost no high-frequency noise. This is suspicious given `temporalAlpha=0.05` (heavy temporal accumulation is masking variance). The image is nearly featureless across large wall areas, indicating the indirect signal is very low-magnitude and being lifted mostly by amplification.
- **Mode 0 bilateral blur**: has **over-smoothed** the right-foreground silhouette (x≈1000–1280, y=300–720) into an indistinct brown smear. Edge detail of the railing on the left (x≈250, y=450–600) is also dissolved. The bilateral kernel is too wide, or its range sigma too permissive.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 Final | **Fair** | Looks plausible but blur kills geometry edges; foreground reads as mush. |
| 3 Indirect ×5 | **Fair** | Smooth, but reveals how little real signal exists outside ceiling. |
| 6 GI only | **Poor** | Banding on ceiling + cascade seam at doorway; large dark voids. |

## 5. Stats Cross-Check & Recommendation

The stats confirm the visual diagnosis:
- C0 `anyPct=4.3%`, C1 `0.024%`, C2 `0.39%`, C3 `100% sky` — **cascades are severely under-occupied**. Most probes are not finding hits.
- `meanLum=-nan` in C0 and **negative mean luminance** in C2/C3 (-376, -320) with `maxLum=inf` indicate **NaN/inf contamination** in the probe atlas — almost certainly the source of the amplified-but-flat look in mode 3 (clamped) and the hard banding (NaN → black blocks).
- High `variance` (4104, 24221, 17051) matches the visible banding magnitude.

**Concrete change (single):** Increase `probeJitterScale` from **0.06 → 0.25** and raise `temporalAlpha` from **0.05 → 0.12**.

Rationale: with `anyPct < 5%`, probes are missing surfaces because jitter is too small to break the regular grid alignment that's producing the 32-voxel ceiling banding. More jitter expands effective coverage; the slightly higher alpha compensates for the resulting
