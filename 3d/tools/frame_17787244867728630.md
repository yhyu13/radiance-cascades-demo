# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17787244867728630_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17787244868556062_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17787244869606976_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17787244867728630.json`  
**Analyzed:** 2026-05-14T10:08:38  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing the three views to isolate where indirect light contributes:

- **Ceiling (top of frame)**: The ceiling strips/lights show strong direct emission in mode 0, and mode 6 confirms they're the primary GI emitters — bright warm reddish-brown bleeding visible around them.
- **Left wall (mid-frame)**: Mode 6 shows significant indirect contribution here — the wall is lit almost entirely by bounce light (warm cream tone). Mode 0 reflects this brightening.
- **Floor (foreground)**: GI is present but dim in mode 6; the warm orange tint at the right edge of the floor in mode 0 is clearly indirect bounce from the right wall (visible as the red-orange band in mode 6).
- **Far end of corridor (vanishing point)**: Dark in all three modes — GI does not reach the back wall, which appears as a near-black recess. This is consistent with corridor occlusion limiting bounce paths.
- **Right wall (foreground bulge)**: The large orange smear on the right is dominated by indirect light (visible in both 3 and 6).

## 2. Banding Artifacts (Mode 6)

- **Ceiling**: Blocky, axis-aligned rectangular patches roughly **uniform in size** along the corridor length — these are **probe-grid footprints** (baseRes 32 over volumeSize 4 = ~0.125 unit probe spacing, projected through perspective).
- **Left wall midground**: Soft horizontal striations near the wall/ceiling junction look like **directional-bin discretization** (dirRes 8 = only 8 directions per probe → coarse angular sampling shows as banded shading transitions).
- **No clear cascade-boundary rings** are visible as concentric discontinuities — cascade transitions appear well-blended.

## 3. Smoothness Assessment

- **Mode 3 (×5 amplified)**: Spatially **smooth overall**, but the ceiling area shows the same blocky probe footprints amplified. No high-frequency noise — temporal accumulation (α=0.05) is doing its job. Slight ghosting/smearing on left wall suggests stable but laggy convergence.
- **Mode 0 bilateral blur**: **Over-smoothed** in two places:
  - The left-wall window/door opening edges are smeared into the wall (white highlight bleeds outward).
  - The right wall's orange bounce blends so heavily into the floor that the floor/wall boundary is ambiguous in the foreground.
  - The far-end dark recess has soft, mushy edges where it should be a crisp doorway silhouette.

## 4. Stats Cross-Check

- **Cascade 0**: surfPct only **48.6%** — under half the probes are hitting surfaces. This is the source of the variance (0.00115, highest of all cascades) and likely contributes to the ceiling blockiness.
- **Cascades 1–3**: well-converged (surfPct 85→100%), variance drops cleanly.
- **anyPct=100 everywhere**: probes are inside the volume, good.
- **High variance in C0 ↔ visible blocky ceiling banding**: confirmed correlation.

## 5. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Fair** | Plausible mood, but bilateral blur eats geometric edges |
| 3 (Indirect ×5) | **Fair** | Smooth but reveals probe-grid footprints on ceiling |
| 6 (GI only) | **Poor → Fair** | Banding clearly visible; C0 under-sampled |

## 6. Recommended Parameter Change

**Increase `probeJitterScale` from 0.06 → 0.15** (and consider raising `dirRes` from 8 → 12).

Rationale: C0's 48.6% surfPct + highest variance + visible blocky probe footprints on the ceiling indicate that cascade 0 probes are spatially under-sampling the nearby geometry. A larger jitter (combined with the existing temporal α=0.05 accumulator) will dither the probe positions enough to break up the axis-aligned grid pattern without introducing flicker. If banding on the left wall persists after that, raising `dirRes` would address the directional-bin striations as a second step.
