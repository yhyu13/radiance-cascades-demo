# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782539475966409_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782539476536153_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782539477612553_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17782539475966409.json`  
**Analyzed:** 2026-05-08T23:26:14  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Renders

## 1. GI Contribution Analysis

Comparing the three views, the indirect light is concentrated almost entirely along the **top edge / ceiling band** (bright white-grey strip across the upper ~10% of the frame) and a softer **left-wall vertical streak** at roughly y=400–650. The interior of the box (the large dark grey field in mode 0) receives almost no indirect bounce — mode 6 confirms this is near-black except near those upper/side edges. So the GI is essentially capturing first-bounce off the ceiling/top-light region and a sliver off the left wall, with the box's central volume starved of multi-bounce energy.

## 2. Banding in Mode 6

In mode 6 I can see:
- A **horizontal seam** running across the ceiling band at roughly y≈75 and a fainter one at y≈115 — spacing ~40 px, consistent with a **cascade boundary** between c0 and c1 (the brightness step is sharp, not gradual).
- **Soft concentric ring structure** faintly visible in the central dark region (large oval contours) — this is the signature of **probe-grid interpolation** at the 32³ base resolution, not directional bins.
- I do **not** see angular pinwheel/wedge banding, so dirRes=8 is not the dominant artifact source here.

## 3. Smoothness Assessment

Mode 3 (5× amplified) looks **spatially smooth** — no high-frequency speckle, which is expected given temporalAlpha=0.05 (heavy temporal integration). The price is the visible probe-grid rings.

Mode 0's bilateral blur appears to have **over-smoothed the ceiling-to-wall transition** at the top corners (~x=110 and x=1170, y=70) — the bright cap bleeds into transparent/black background, suggesting the blur is leaking across the geometry silhouette.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 Final | **Fair** | Composite is readable but interior is too dark; edge bleed at top corners |
| 3 Indirect ×5 | **Good** | Smooth, low-noise, makes GI distribution legible |
| 6 GI only | **Fair** | Reveals genuine cascade-seam and probe-ring artifacts |

## 5. Stats Cross-Check & Recommendation

The stats corroborate the visual read:
- **surfPct climbs 81.6 → 85.2 → 100 → 100** across cascades — c0 and c1 have ~15–18% of probes missing surface hits, which is exactly what produces the under-lit interior and the visible seam where c0/c1 hand off to the fully-converged c2.
- **maxLum drops 0.65 → 0.37 → 0.17 → 0.02** — c3 is contributing almost nothing (meanLum 0.011, variance 1.9e-5), so the cascade pyramid is top-heavy.
- variance in c0 (2.1e-3) is ~100× c3, matching the visible ring structure being a near-field artifact.

**Concrete change:** Increase `c0MinRange` from 1 → 2 (and consider `baseRes` 32 → 48). This pushes c0 probes to actually reach surfaces in the box interior, which should lift surfPct on c0 above ~95% and close the cascade seam visible at y≈75. If you can only change one knob, **`c0MinRange = 2`** is the highest-leverage fix — temporalAlpha and jitter are already doing their jobs (the image is smooth, not noisy), so spending more samples there wouldn't help.
