# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783188053232660_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783188054341541_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783188055561623_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17783188053232660.json`  
**Analyzed:** 2026-05-09T17:27:16  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing mode 0 (final) to mode 6 (GI only):

- **Color bleeding**: The GI-only view confirms strong red bleed onto the floor near the left wall (bottom-left corner) and onto the left face of the front box. Green bleed is weaker but visible on the right face of the rear box and floor near the right wall.
- **Ceiling/light fixture area**: The orange emitter (light shroud) shows up bright in mode 6 — indicating the GI is correctly capturing emitter-direct contribution to nearby surfaces.
- **Indirect-dark zones**: The shadowed wedge on the floor (center-left, behind the front box) is nearly black in all three modes. Direct light is occluded there, and GI is not filling it in — that pocket is under-lit by indirect bounces.
- **Boxes**: The vertical-striped boxes show GI primarily on their tops and front-facing sides; the inter-stripe gaps stay dark in mode 6, suggesting the probe field isn't resolving those crevices.

## 2. Banding in Mode 6

- **Ceiling**: Concentric arc-like banding radiating from the light fixture — spacing widens toward the corners. Shape and curvature are consistent with **directional-bin aliasing** (dirRes=8 is coarse; only 8² = 64 directions per probe, so the cosine-weighted reconstruction shows visible lobes).
- **Floor**: Horizontal striations roughly aligned with the probe grid spacing (volumeSize=4 / baseRes=32 → ~0.125 unit probe spacing). These look like **probe-grid trilinear seams**, not cascade boundaries.
- **No clear cascade-boundary rings** visible — which matches the stats (all cascades at anyPct=100, surfPct ≥75%).

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: Surprisingly smooth on the large walls and floor — temporal accumulation (alpha=0.05) is doing its job. However, the orange fixture and the box stripes show **chunky pixelated patches** rather than smooth falloff, again pointing at low dirRes.
- **Mode 0 (final)**: The bilateral blur has **not over-smoothed** wall/floor edges (the red/green wall boundaries remain crisp). However, the box silhouettes against the back wall show slight halo-ing, and the fine stripe gaps on the boxes are partially smeared — borderline acceptable.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| Mode 0 | **Good** | Composite reads correctly; minor halo on box edges |
| Mode 3 | **Fair** | Smooth on flats but visible directional chunkiness near emitter |
| Mode 6 | **Fair** | Clear ceiling arcs and floor striations betray dirRes/probe-grid limits |

## 5. Stats Cross-Check & Recommendation

- Cascade 0 has **surfPct=74.9%** and **variance=0.0058** (highest of all cascades) — the finest cascade is both under-converged on surface hits *and* spatially noisy, which matches the visible floor striations.
- Cascade 3 variance is two orders of magnitude lower — far cascades are fine.
- maxLum on C0 (0.579) vs meanLum (0.040) shows a heavy-tailed distribution → a few probes are catching the emitter strongly while neighbors miss it → directional under-sampling.

**Concrete recommendation**: **Increase `dirRes` from 8 → 12** (or 16 if budget allows; raymarch is only 0.083 ms so there's headroom). This directly attacks the ceiling arc banding and the chunky emitter reconstruction in mode 3, and should reduce C0 variance by smoothing the per-probe directional integral.

If dirRes can't be raised, the secondary choice is **`probeJitterScale` 0.06 → 0.12** to better dither the C0 grid seams visible on the floor — but this trades banding for temporal noise given the already-low temporalAlpha=0.05.
