# AI Visual Triage

**Image:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777082435236350.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17777082435236350.json`  
**Analyzed:** 2026-05-02T15:51:05  
**Model:** claude-opus-4-7  
**Note:** Phase 12a triage (image + probe stats) — geometry-coupled causes stated; software-level root causes require Phase 6b pipeline inspection.

---

## Triage Report

**1. Overall:** A Cornell Box rendered through the radiance cascades GI system, showing the classic red/green side walls, white ceiling/floor/back, and two white blocks on the floor, with soft indirect illumination and visible shadows.

**2. Visible artifacts:**

- **Color bleeding error / leaked wall radiance outside the box:** The red and green wall colors are visibly bleeding *through* the exterior of the box, showing as faint red and green rectangles extending beyond the left and right outer silhouette of the box. This is geometry-coupled — radiance from the colored walls is reaching probes positioned outside/behind the box geometry, suggesting probe rays are not being occluded by the back faces of the side walls (one-sided occlusion or thin-wall leak).

- **Possible directional bin banding (mild) on the back wall:** The indirect lighting gradient on the back wall has subtle soft transitions but no hard 36° steps are visible at D=8 (45° bins would be expected if present); I do not see clear evidence of this artifact.

- **No probe-grid banding visible:** The indirect lighting on floor and walls is smooth at the cell scale.
- **No cascade ring seam visible.**
- **Shadows under both blocks look correct**, no missing shadows or shadow acne.

**3. Stats cross-check:**
- C0 surfPct is only 29% — most near-field probe rays miss surfaces, which is consistent with the outside-the-box leak (rays from exterior probes find nothing nearby and pull color from farther cascades that *do* see the colored walls).
- Variance is low across all cascades (≤0.0064), matching the visually smooth interior — no hidden banding.
- C3 meanLum and maxLum are very low (0.029 / 0.053), so the far cascade is not the dominant contributor; the leak is mid-cascade.

**4. Suggested parameter change:** Since the dominant artifact is exterior color bleed and C0 is heavily under-occupied, increase `probeJitterScale` modestly (e.g. 0.05 → 0.15) to break alignment of exterior probes with thin wall geometry, OR (preferred) leave jitter alone and address via pipeline-level occlusion — but staying within parameters: raise `dirRes` from 8 → 16 to give C0 finer angular resolution so thin-wall hits are not missed by sparse direction bins.

**Overall quality: Fair** — interior lighting is clean and plausible, but the exterior color halo is an obvious correctness issue.
