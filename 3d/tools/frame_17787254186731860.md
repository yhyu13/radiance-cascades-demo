# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17787254186731860_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17787254194791643_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17787254202553708_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17787254186731860.json`  
**Analyzed:** 2026-05-14T10:24:15  
**Model:** claude-opus-4-7

---

# Visual Triage Report — Cornell Box GI

## 1. GI Contribution Isolation

Comparing mode 0 (final) against mode 6 (GI only):

- **Color bleeding is present and correctly placed**: the floor near the red wall picks up a faint warm tint, and the floor near the green wall picks up a faint cool/green tint, visible most clearly in mode 6 along the lower-left and lower-right floor regions.
- **Ceiling in mode 6** shows a small bright hotspot directly under the light, indicating the indirect term is correctly capturing the light source's near-field bounce.
- **Tall block's left face** in mode 6 receives slight green spill from the far green wall (visible as a subtle greenish gradient on its right edge) — physically inverted from what I'd expect; the tall block's *right* face should pick up green, its *left* face red. Looking again, the bleed direction on the blocks looks weakly defined and somewhat washed.
- **Behind/around the box (outside the room walls)**: mode 0 and mode 3 both show indirect light leaking *outside* the box footprint — the gray "halo" framing the room in mode 3 is GI escaping through/around the outer wall geometry. This is a light-leak artifact.

## 2. Banding in Mode 6

- **Blocky rectangular structure** is visible along the **outer perimeter of the box** in mode 6 — the dark frame around the room has straight-edged steps roughly aligned to a coarse grid. Spacing looks consistent with the **base probe grid (32³ over volumeSize=4)**, i.e. ~1 probe every ~12.5% of the image width near the box.
- **No fine directional-bin striping** (no radial/angular streaks emanating from probes) is visible — dirRes=8 appears adequate for this diffuse-only scene.
- **No sharp cascade-boundary discontinuities** (no concentric square ring where c0→c1 hands off) are visible in the interior; the cascade transition appears smooth.
- The dominant artifact is therefore **probe-grid aliasing at the geometry silhouette**, not directional or cascade-related.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: spatially smooth across wall interiors, no high-frequency noise. Temporal accumulation (alpha=0.05) is doing its job. The red and green wall gradients are clean.
- **Mode 0 (final)**: bilateral blur has **not over-smoothed** the primary edges — the block silhouettes, wall corners, and shadow boundaries on the floor are crisp. The shadow under/left of the tall block is well preserved.
- One concern: the **outer "frame" geometry** (the gray border outside the room) shows the same blocky GI halo in all three modes, suggesting the blur is preserving the artifact rather than hiding it — correct behavior, but it exposes the underlying probe quantization.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Good** | Clean composite, edges preserved, color bleed subtle but present |
| 3 (Indirect ×5) | **Good** | Smooth, no noise, reveals light-leak halo around box exterior |
| 6 (GI only) | **Fair** | Probe-grid blockiness clearly visible at silhouettes; bleed direction on blocks looks weak |

## 5. Stats Cross-Check & Recommendation

Key observations from stats:
- **Cascade 0 surfPct = 68.9%** — nearly a third of c0 probes are not hitting surfaces. This is the under-sampled cascade and likely the source of the silhouette blockiness, since c0 dominates near-field contribution.
- c1–c3 are at 99–100% surfPct — well converged.
- Cascade 0 variance (0.008) is the highest and matches the visible spatial structure.
- maxLum drops cleanly across cascades (0.56 → 0.05) — no cascade inversion.

**Concrete recommendation**: increase `c0MinRange` from **0.5 → 0.75** (or alternatively raise `probeJitterScale` from 0.06 → 0.12). The low c0 surfPct indicates c0 rays are terminating in empty space before hitting the near walls/blocks; extending c0's reach will both raise its surfPct toward c1's level and dither out the probe-grid blockiness visible at the box silhouette in mode 6
