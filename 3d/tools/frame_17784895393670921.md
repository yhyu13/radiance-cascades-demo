# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784895393670921_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784895401045123_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784895408683646_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784895393670921.json`  
**Analyzed:** 2026-05-11T16:52:53  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Render

## 1. GI Contribution Analysis

Comparing the three modes to isolate indirect light:

- **Color bleed onto floor/ceiling**: In mode 6, the floor near the red wall picks up a clear red tint, and the area near the green wall picks up green — this is genuine indirect bounce. Mode 0 shows this bleed only weakly because the bright direct term washes it out.
- **Tall block (left)**: Its right face shows faint **green tint** in mode 6 from the green wall bounce; its left face is in shadow but receives reddish fill. This is the most diagnostic GI cue.
- **Ceiling**: Mode 6 shows the ceiling lit primarily from the floor bounce (warmish/neutral) — direct light from the area light barely reaches it, so what's visible there is almost pure GI.
- **Back wall**: Mode 6 shows it dimly lit with a slight red-left/green-right gradient, consistent with sidewall bounce.
- **Outside the box (the gray "halo" frame)**: In all three modes, light spills *outside* the actual box geometry. This is the most concerning signature — see §2.

## 2. Banding / Artifacts in Mode 6

The dominant artifact is **not** stripe banding but a **blocky halo extending ~30–50 px beyond every box edge** (top, bottom, left, right). Characteristics:

- **Shape**: Rectangular, axis-aligned, roughly the footprint of the probe volume (volumeSize=4) projected to screen.
- **Spacing**: Single uniform offset, not periodic — consistent with **probe-volume leakage** (probes outside the wall sampling interior radiance and depositing it on exterior fragments), not directional-bin striping or cascade ring boundaries.
- **No visible cascade ring** transitions are detectable inside the box — cascade blending appears smooth.
- **No directional-bin pinwheel** patterns are visible on the white walls; dirRes=8 looks adequate for this diffuse scene.

The halo color matches the adjacent interior wall (red on left exterior, green on right), confirming it's interior radiance bleeding through the wall via probe trilinear interpolation.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: Spatially **very smooth** — no high-frequency noise, no speckle. Gradients on walls are clean. This is consistent with `temporalAlpha=0.05` (heavy temporal integration) and trilinear probe sampling. Possibly *too* smooth — fine contact darkening near the block bases is missing.
- **Mode 0 (final)**: Bilateral blur has **not** noticeably over-smoothed direct-lit edges — the block silhouettes and hard shadow from the tall block are crisp. Good balance.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| Mode 0 (final) | **Good** | Crisp direct edges, plausible bleed, but exterior halo visible |
| Mode 3 (×5 indirect) | **Fair** | Smooth but halo dominates; reveals leakage clearly |
| Mode 6 (raw GI) | **Fair** | Useful diagnostic; halo and slight muddiness on back wall |

## 5. Stats Cross-Check & Recommendation

Key observations from stats:
- **C0 surfPct = 68.9%** — nearly a third of cascade-0 probes hit nothing useful. C0 has the smallest range (`c0MinRange=0.5`) and is responsible for near-field detail. Under-sampled C0 probes fall back to coarser cascades, which **causes the exterior leakage** (coarse probes straddle walls).
- **C0 variance (0.0080) ≈ C1 variance (0.0083)** — C0 is not capturing more local detail than C1, confirming it's under-resolved relative to its job.
- **C3 maxLum 0.054, variance 1.5e-4** — outermost cascade is essentially flat/empty; fine.

### Concrete parameter change

**Increase `c0MinRange` from 0.5 → ~0.75–1.0**, OR equivalently **increase `baseRes` from 32 → 48**.

Rationale: C0 probes are too sparse to resolve the wall thickness, so trilinear interpolation leaks interior radiance outward by half a probe spacing. Either
