# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17781658890107280_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17781658897603637_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17781658905230243_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17781658890107280.json`  
**Analyzed:** 2026-05-07T22:58:42  
**Model:** claude-opus-4-7

---

# Visual Triage Report: Cornell Box GI Renders

## 1. GI Contribution Isolation

Comparing the three modes, indirect light is **present and behaving correctly** in the following locations:
- **Color bleed onto the floor**: Mode 6 shows clear red tint on the left half of the floor and green tint on the right half — classic Cornell bounce.
- **Color bleed onto the boxes**: The tall box's left face picks up red; the short box's right face picks up green (visible in mode 6 between the boxes).
- **Ceiling**: Mode 6 shows the ceiling is lit only by bounce (the light source itself is outside the GI capture, since it's brightest near the center — likely the area light's near-field contribution to the ceiling).

GI is **absent or weak**:
- The outer black region surrounding the box frame in all three modes — expected, no geometry there.
- Sharp shadow contact under the tall box (left side) in mode 0 — direct shadow, no GI fill, looks correct.

## 2. Banding Artifacts in Mode 6

Visible banding/structure:
- **Blocky stair-step edges** along the outer silhouette of the dark gray frame (top, bottom, left, right margins). Spacing ≈ probe footprint at ~32³ base resolution over a volume of 4 units → ~0.125 units per probe, which matches the visible step size.
- **Soft rectangular halo** around the ceiling bright spot — consistent with the **probe grid (32³ base)** rather than directional bins. No radial pinwheel patterns that would indicate dirRes=8 aliasing.
- **No visible cascade-boundary seams** (no concentric rings or abrupt luminance steps mid-floor), so cascade transitions look well-blended.

Conclusion: artifacts are **probe-grid sourced**, not directional or cascade-boundary.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: Spatially **smooth, low noise**. The walls show clean color gradients with no high-frequency speckle. This is consistent with the very low variance in cascade 3 (0.00016) and modest variance elsewhere (~0.008). Temporal alpha of 0.05 is doing heavy lifting here.
- **Mode 0 (final)**: The bilateral blur has **over-smoothed the contact shadow** under the tall box — the shadow's leading edge is soft and the box's bottom-left corner loses geometric definition. The short box's base shadow is similarly mushy. Wall-to-wall corners (e.g., back wall meeting side walls) are slightly rounded.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| Mode 0 (Final) | **Good** | Lighting is plausible and pleasant, but bilateral blur is too aggressive on contact shadows. |
| Mode 3 (Indirect ×5) | **Excellent** | Very smooth, no noise, color bleed is physically reasonable. |
| Mode 6 (GI only) | **Fair** | Visible probe-grid blockiness on silhouettes; the structure leaks slightly into the framed image edges. |

## 5. Stats Cross-Check & Recommendation

- `surfPct` is 98–100% across all cascades → probes are well-converged on surfaces.
- `anyPct` = 100% everywhere → no empty probes.
- Variance drops monotonically by cascade (0.0085 → 0.00016) → cascade hierarchy is healthy.
- `temporalAlpha = 0.05` is very low → strong temporal smoothing, which explains mode 3's cleanliness but also means probe-grid blockiness gets *baked in* rather than dithered out by jitter.
- `probeJitterScale = 0.06` with `jitterHoldFrames = 2` → jitter amplitude is small relative to the ~0.125-unit probe spacing (only ~5% of a cell).

**Concrete recommendation**: Increase `probeJitterScale` from **0.06 → 0.15–0.20**. The probe-grid stair-stepping visible on the frame silhouette in mode 6 indicates jitter is too small to dither the grid pattern across the temporal accumulation window. Combined with the already-low `temporalAlpha = 0.05`, larger jitter will average out the blocky probe footprint without introducing visible noise (the low variance budget supports this). 

If blockiness persists after that,
