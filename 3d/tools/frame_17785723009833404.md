# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785723009833404_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785723010971346_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785723015662139_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17785723009833404.json`  
**Analyzed:** 2026-05-12T15:52:11  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box Corridor Renders

## 1. GI Contribution Analysis

Comparing the three views, indirect light is visible in the following locations:

- **Left wall (windowed side)**: Strong indirect bounce visible in mode 3, contributing the warm/bright wash on the left pillars and floor in mode 0. This is the dominant GI feeder.
- **Floor reflectance band** (center-left, the "checkered" lit patches stretching down the corridor): Mode 6 shows these as faint warm smudges, mode 3 amplifies them into the bright pools. These are the floor catching bounce light from the ceiling fixtures.
- **Right wall (orange/copper)**: Almost entirely *missing* indirect contribution. Mode 6 shows a near-flat falloff; mode 3 confirms the right wall receives very little bounce despite the lights overhead. This wall in mode 0 is essentially lit by direct only, hence the dark/uniform appearance.
- **Ceiling cavities between fixtures**: Mode 3 shows soft fill between lights; in mode 6 these are nearly black.

## 2. Banding Artifacts in Mode 6

- **Concentric/elliptical rings on the right wall** centered roughly on the vanishing point (right-mid frame): regular spacing increasing toward the periphery. Shape is smooth arcs, not blocky — this is characteristic of a **cascade boundary** transition (radial distance bins from the camera-anchored probe volume), not a probe-grid (which would be axis-aligned blocks) and not a directional-bin pattern (which would be angular wedges).
- **Faint horizontal bands on the floor** running perpendicular to the corridor axis, spacing ~80–120 px near mid-frame, compressing toward the back wall: consistent with **probe-grid interpolation** at `baseRes 64` / `volumeSize 4` (≈16 cm probe spacing) where the camera moves between cells.
- No visible angular wedges → directional binning at `dirRes: 8` is not the dominant artifact source here.

## 3. Smoothness / Over-smoothing

- **Mode 3** is *spatially smooth* — almost too smooth. The 5× amplification reveals no high-frequency noise, but it also reveals heavy temporal/spatial blur (likely from `temporalAlpha 0.05`). Edges of light pools on the floor are mushy.
- **Mode 0** shows the bilateral blur has **over-smoothed the floor light pools** into the soft chevron shapes near center; their boundaries should be sharper given the discrete fixture geometry. The left-wall sunlit area also bleeds onto the floor edge.
- The right wall in mode 0 shows GI essentially clamped to near-black — bilateral blur is preserving direct-light edges correctly there, but the missing GI signal is the real problem.

## 4. Ratings

| Mode | Rating | Notes |
|------|--------|-------|
| 0 (Final) | **Fair** | Plausible mood, but right wall is GI-starved; floor pools over-blurred. |
| 3 (Indirect ×5) | **Fair** | Smooth, but reveals asymmetric GI distribution and stale temporal accumulation. |
| 6 (GI only) | **Poor** | Clear cascade-ring banding on right wall; signal dominated by one cascade only. |

## 5. Stats Cross-Check & Recommendation

The stats confirm the visual diagnosis:
- **Cascade 0: anyPct=0** — completely empty. Near-field GI is missing entirely, which explains why the right wall (close to camera) gets no bounce.
- **Cascade 1: anyPct=2.37%, but maxLum=inf and meanLum=NaN** — numerical blow-up; this cascade is corrupting the signal it does have.
- **Cascade 2: anyPct=0.024%, maxLum=333, variance=27.1** — extremely sparse but high variance, exactly matching the visible ring banding.
- **Cascade 3: empty.**
- Only ~2.4% of probes have *any* hits — massively under-sampled.
- `cascadeTimeMs ≈ 42 ms` vs `raymarchTimeMs 0.16 ms`: the probe update is the bottleneck, but it's not producing usable data.

### Concrete parameter change

**Raise `temporalAlpha` from 0.05 → 0
