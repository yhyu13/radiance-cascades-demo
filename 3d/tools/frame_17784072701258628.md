# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784072701258628_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784072702391637_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784072703535007_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784072701258628.json`  
**Analyzed:** 2026-05-10T18:01:43  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Isolation

Comparing mode 0 (composite) against modes 3 and 6, the indirect term is doing most of the visible shading work in this scene since the corridor has no strong direct-lit surface in view aside from the bright spots on the floor mid-frame (around x≈460, y≈360–620) — those are the dominant **direct** contribution, as they appear in mode 0 but are *absent* in modes 3 and 6.

Indirect light is brightest:
- **Ceiling near the top-center** (the lit area around the dark angled fixtures at y≈0–150, x≈400–700) — clearly receiving bounce from the floor lights.
- **Far end of the corridor** (vanishing point near x≈460, y≈300) — warm reddish-brown tint visible in both mode 3 and mode 6, indicating colored bounce from a back wall.
- **Left wall mid-distance** (x≈200–350, y≈200–500) — soft warm wash.

Indirect light is essentially **absent**:
- **Right wall foreground** (x≈800–1280, y≈200–700) — almost pure unlit brown in mode 6, suggesting the camera-side surface is not being reached by probes or its facing direction has no bin coverage.
- **Floor foreground bottom-right** — same falloff.

## 2. Banding in Mode 6

Visible structures in the GI-only view:
- **Blocky stair-step patches on the ceiling fixtures** (top center, ~30–40 px wide squares) — these match a **probe-grid** signature at this resolution. With `baseRes: 32` and `volumeSize: 4`, each probe footprint projects to roughly that pixel size at mid-distance.
- **Soft horizontal seam across the back wall** at the vanishing point — likely a **cascade boundary** between c1 and c2 (the warm pocket sits in one cascade, the cooler region above in another).
- **No obvious directional-bin pinwheels** visible — `dirRes: 8` (64 directions) appears sufficient to hide individual bin discontinuities.

## 3. Smoothness & Over-Blur Assessment

- **Mode 3 (indirect ×5)** looks **spatially smooth but low-frequency blobby** — no high-frequency noise, but clearly under-resolved (large soft gradients dominate). Variance stats confirm: c0 variance 8.2e-4 is the largest, consistent with the visible blocky patches near camera.
- **Mode 0** shows the bilateral blur has **over-smoothed the floor light streaks** — they appear as broken dashes (y≈620, fragmented) rather than a continuous specular highlight. The right-wall edge against the ceiling is also softened to the point of losing geometry definition (x≈900–1100).

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| Mode 0 | **Fair** | Readable, but bilateral blur is eating direct-light detail on the floor |
| Mode 3 | **Fair** | Smooth, no noise, but probe-grid footprint visible on ceiling |
| Mode 6 | **Poor** | Right half of frame is essentially black — probes not covering camera-facing surfaces |

## 5. Stats Cross-Check & Recommendation

Key observations from stats:
- `surfPct` for c0 is only **81.6%** — ~18% of c0 probes failed to hit a surface. This explains the dark right wall.
- c0 has the highest variance (8.2e-4) and highest maxLum (0.51) — consistent with the visible blocky structure being a **c0 probe-grid** artifact, not directional or cascade.
- `temporalAlpha: 0.05` is already conservative (slow accumulation), and `raymarchTimeMs: 39.8` is dominating frame cost — so resolution/quality knobs are tight.

**Concrete change:** Increase **`c0MinRange` from 1 to 2** (or bump `baseRes` from 32 → 48). The under-sampling on near-camera surfaces (right wall blackness + 81% surfPct in c0) indicates c0 rays are terminating before reaching nearby geometry on the camera side. Extending c0's range will catch those surfaces and lift the dark right half without raising directional cost. If raymarch budget is a concern, pair this with `dirRes: 6` — the 8-bin directional resolution shows no visible benefit in this sc
