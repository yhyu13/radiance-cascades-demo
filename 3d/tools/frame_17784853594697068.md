# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784853594697068_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784853595532452_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784853596642108_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784853594697068.json`  
**Analyzed:** 2026-05-11T15:43:11  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing mode 0 (final) against modes 3/6 (indirect-only):

- **Bright indirect contribution**: The left wall in modes 3 and 6 receives strong bounce light — clearly visible as the warm cream/beige glow on the left pillars and ceiling near the camera. This is the dominant GI feature.
- **Far end of corridor**: The vanishing-point doorway shows a warm reddish bounce in mode 6 — light reflecting off the red end-wall back into the corridor. Mode 3's 5× amplification confirms this is real signal, not noise.
- **GI-absent regions**: The right wall in all three modes is noticeably darker and redder. The floor near the camera receives almost no indirect light — visible as the flat grey tone in mode 6, suggesting the floor plane has very little upward bounce reaching it (probes there are seeing mostly sky-occluded/dark hemispheres).
- **Ceiling**: The patchy dark/light squares on the ceiling are real geometry (recessed lights/openings) — direct light dominates there in mode 0.

## 2. Banding Artifacts in Mode 6

- **Concentric arc banding on the right wall**: Smooth curved bands sweeping from upper-right toward lower-right, spaced ~80–120 px apart. The shape (large radius arcs centered roughly off-screen-right) and the *progressive coarsening with distance* is characteristic of **cascade-boundary transitions** — you can see roughly 2–3 visible rings, consistent with the cascadeCount=4 transitions.
- **Blocky stair-stepping on the left wall and ceiling**: Square-edged blobs roughly 8–16 px on screen near the camera, growing larger with distance. This is the **probe grid** (baseRes=32, volumeSize=4 → 0.125 unit spacing in c0) showing through as trilinear-interpolated probe footprints.
- **No clear directional-bin pinwheel/petal artifacts** are visible — dirRes=8 (64 directions) appears sufficient to avoid obvious angular aliasing in this scene.

## 3. Spatial Smoothness Assessment

- **Mode 3 (indirect ×5)**: Reasonably **smooth, low noise** — the temporalAlpha=0.05 (heavy temporal accumulation) is doing its job. No visible flicker-grain, only structured banding. The image is almost too smooth — variance stats (c0=0.001, c3=8.9e-6) confirm a well-converged but possibly over-blurred field.
- **Mode 0 (final)**: The bilateral blur has **over-smoothed the left pillar edges** — the vertical pillar contours that should be sharp (visible as silhouettes in mode 6) are smeared into the wall. The bright specular dashes on the floor are preserved well, so the blur is correctly edge-stopping on luminance highs but failing on the low-contrast pillar/wall boundaries.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| Mode 0 (final) | **Fair** | Usable, but pillar edges smeared and right-wall banding leaks through |
| Mode 3 (indirect ×5) | **Good** | Clean, low noise, banding visible but expected at 5× gain |
| Mode 6 (GI only) | **Fair** | Cascade rings on right wall are clearly diagnostic-visible at 1× |

## 5. Stats Cross-Check & Recommendation

- `surfPct` rises 48.6 → 84.8 → 100 → 100 across cascades: **c0 is under-covering surfaces** (only ~49% of c0 probes hit a surface). This explains the blocky probe-footprint artifacts on the near left wall — c0 has too many empty probes contributing nothing, so neighbors stretch trilinearly.
- Variance drops cleanly across cascades (1e-3 → 9e-6), consistent with the smooth-but-banded right wall — no high-frequency noise, just low-frequency cascade seams.
- `temporalAlpha=0.05` is aggressive; combined with `jitterHoldFrames=2`, the field is locked-in and can't dither out the visible probe footprint.

**Concrete change**: **Increase `probeJitterScale` from 0.06 → 0.15** (and optionally lower `jitterHoldFrames`
