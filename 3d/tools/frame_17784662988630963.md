# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784662988630963_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784662991064664_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784662993020782_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784662988630963.json`  
**Analyzed:** 2026-05-11T10:25:33  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing mode 0 (final) against mode 6 (GI only):

- **Ceiling strip lights** (top of frame, running down the corridor): These appear sharp and bright in mode 0 but are *absent* in mode 6 — confirming they are direct-lit emitters/specular highlights, not reflected via GI.
- **Floor specular streaks** (bright dashes running down the corridor centerline, lower half): Present only in mode 0, absent in mode 6 → these are direct/specular contribution.
- **Wall fill light** (left wall, right wall reddish-brown wash): Almost the entire color of the right wall and the warm tone on the left wall in mode 0 comes from GI — mode 6 reproduces this nearly identically, just slightly darker.
- **Far doorway** (center, ~x=460, y=320): The dim red rectangle at the end of the corridor is almost entirely GI-driven — visible in both mode 0 and mode 6 with similar intensity.

The GI is doing most of the heavy lifting for the ambient/wall coloration; direct lighting only contributes the ceiling fixtures and floor highlights.

## 2. Mode 3 — Completely Black

Mode 3 (indirect ×5) is **entirely black**, yet mode 6 (raw GI) clearly shows a lit corridor. This is a contradiction: a 5× amplification of a non-zero signal should be brighter, not darker. This points to a **mode-3 shader path bug** (likely a sign flip, channel swap, or unbound texture) rather than a true absence of indirect light. No banding can be assessed because there is no signal.

This is corroborated by the stats: cascade 0 has `meanLum = -0.0055` and cascade 3 has `meanLum = -320` — **negative luminance values** indicate the probe storage is producing signed/garbage data that mode 3's amplification path is likely clamping to zero.

## 3. Mode 6 Smoothness / Mode 0 Blur Assessment

- **Mode 6** is *very* smooth spatially — almost too smooth. There are no visible probe-grid blocks or directional-bin pinwheels in the wall coloration. The only structure visible is a soft horizontal seam roughly halfway up the right wall (~y=350) where the reddish tint shifts intensity, possibly a **cascade boundary** between c0 and c1.
- **Mode 0**'s bilateral blur has **over-smoothed the ceiling fixtures** — they smear into horizontal streaks rather than crisp rectangular lights, especially toward the vanishing point. The right wall is also blurred to the point of losing all geometric detail (a doorway frame is just barely perceptible as a vertical lighter band around x=200).

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 Final | **Fair** | Plausible result, but bilateral blur is too aggressive on direct highlights |
| 3 Indirect ×5 | **Poor** | Completely black — broken visualization path |
| 6 GI only | **Good** | Smooth, plausible bounce lighting; matches scene colors |

## 5. Stats Cross-Check & Parameter Recommendation

Key red flags in the stats:
- **Cascade 0 `anyPct = 0.046%`** — only ~1 in 2000 probes have any hit. Severely under-sampled at the finest cascade.
- **Cascade 1 `maxLum = 21095`, variance = NaN** — a fireflies/overflow problem; one probe is blowing up the statistics.
- **Cascade 2 `anyPct = 0`** — completely empty.
- **Cascade 3 `skyPct = 100%`** but `meanLum = -320` — sky cascade is producing negative luminance, almost certainly the source of the mode-3 black-frame bug.
- **`temporalAlpha = 0.05`** is very low (slow accumulation), which combined with `anyPct ≈ 0` means c0 will take ~hundreds of frames to converge.

### Concrete recommendation
**Raise `c0MinRange` from 1 to 2–3 voxels** (or equivalently double `baseRes` ray length for c0). Cascade 0's `anyPct = 0.046%` shows its rays are terminating before hitting anything; the corridor walls are simply outside c0's reach. This will populate c0 with real surface
