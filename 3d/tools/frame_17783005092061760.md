# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783005092061760_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783005093132445_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783005094310110_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17783005092061760.json`  
**Analyzed:** 2026-05-09T12:22:19  
**Model:** claude-opus-4-7

---

# Visual Triage Report — Cornell Box GI

## 1. GI Contribution Analysis

Comparing mode 0 (final) against mode 6 (GI only) reveals where indirect light is doing real work:

- **Strong GI presence**: The orange sphere on the left wall acts as a secondary emitter — visible as a warm orange glow bleeding onto the red wall around it in mode 6, and contributing the saturated orange highlight on the sphere itself in mode 0.
- **Color bleeding**: The red wall casts faint red onto the left edge of the front-facing white wall (mode 6), and the green wall contributes a subtle green tint to the right side of the white back wall. Both are quite weak.
- **GI absent / underlit**: The ceiling reads almost pure black in mode 6 despite the area light directly below it — the ceiling is not receiving any meaningful bounce return. The floor (visible as the curved black band along the bottom) is similarly devoid of GI.
- **Pillars/box geometry**: The grey vertical pillars in the foreground show GI on their lit faces but their inter-pillar gaps are black in mode 6, suggesting probes inside those crevices aren't resolving.

## 2. Banding Artifacts in Mode 6

- **Curved horizontal bands** along the ceiling and floor — concentric arcs spaced ~4–8 px apart near the back, widening toward the camera. Shape (concentric, view-dependent curvature) is consistent with a **probe-grid sampling pattern** projected onto grazing surfaces, not directional bins (which would produce angular wedges) or cascade boundaries (which would produce a sharp ring/step at a fixed world distance).
- **Vertical streaks** on the left red wall below the sphere shadow — these align with probe column spacing (`baseRes=32`, `volumeSize=4` → 0.125 units/probe), again pointing to grid aliasing on a near-parallel surface.
- I do **not** see a clear cascade-boundary ring (no abrupt luminance step at a constant radius from camera).

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: Spatially smooth on the large wall surfaces — the pink and pale-green washes are well-integrated. However, the same curved banding from mode 6 is still visible on the ceiling/floor, just dimmer. No high-frequency noise — the temporal accumulation (`temporalAlpha=0.05`) is doing its job.
- **Mode 0 bilateral blur**: Looks reasonable on flat walls. The shadow under the sphere on the red wall has a clean edge — not over-smoothed. The pillar edges in the foreground retain their hard silhouettes. No obvious light-leak across geometry boundaries.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 — Final | **Good** | Convincing color bleed, clean shadows, no obvious leaks. Ceiling/floor blackness is the main weakness. |
| 3 — Indirect ×5 | **Fair** | Smooth on walls but exposes grid banding on grazing surfaces. |
| 6 — GI only | **Fair** | Useful diagnostic; banding clearly visible, no GI on ceiling/floor. |

## 5. Stats Cross-Check & Recommendation

Key observations from stats:
- **Cascade 0 surfPct = 74.8%** — a quarter of c0 probes aren't hitting surfaces, which matches the black ceiling/floor (probes near those surfaces marching outward find nothing within c0's short range).
- **Cascade 0 variance = 0.0056** is ~70× higher than c3 (8e-5), consistent with the visible banding being concentrated in the near-field cascade.
- **maxLum 0.572 in c0** vs meanLum 0.040 → high dynamic range within c0, amplifying any spatial undersampling into visible bands.
- `dirRes=8` (64 directions) is modest; combined with `probeJitterScale=0.06` and `jitterHoldFrames=2`, the jitter pattern may not be breaking up the grid alignment fast enough at `temporalAlpha=0.05`.

**Concrete change**: **Increase `c0MinRange` from 1 to 2** (or bump `baseRes` from 32 to 48). The c0 surfPct of 74.8% is the smoking gun — c0 probes aren't re
