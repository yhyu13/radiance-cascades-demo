# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17787245618559999_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17787245619414742_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17787245620490922_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17787245618559999.json`  
**Analyzed:** 2026-05-14T10:09:53  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Isolation

Comparing modes 0, 3, and 6:

- **Ceiling lights** (the dark stripe-shaped emitters running down the centerline of the ceiling) are the only direct emitters. In mode 6, indirect light is concentrated **on the ceiling itself around each emitter** and on the **upper portions of the left and right walls** near the ceiling line.
- **Floor (the gray strip running into the distance)**: receives noticeably weaker indirect bounce — visible in mode 3 as a faint warm wash, but in mode 6 it's nearly black except along the **right floor/wall seam** which picks up the warm reddish bounce from the right wall.
- **Right wall (rust/red colored)**: dominant color-bleed source. Mode 3 (×5) shows the warm tone bleeding onto the floor and the back of the corridor; in mode 0 this manifests as the orange tint along the right baseboard.
- **Far end of corridor (center-left dark blob)**: indirect contribution is essentially zero there in mode 6 — the back wall reads pure black, suggesting probes at depth aren't receiving meaningful bounce.
- **Left wall**: appears overexposed/washed out in mode 3, indicating strong indirect there relative to its direct illumination.

## 2. Banding in Mode 6

Visible artifacts:
- **Blocky/stair-stepped silhouettes on the ceiling emitters** — rectangular chunks roughly 8–16 px wide following the perspective foreshortening. Spacing is consistent with the **base probe grid (baseRes 32 over volumeSize 4 → ~0.125 unit spacing)**, so this is **probe-grid aliasing**, not directional binning.
- **Soft horizontal bands on the right wall** in the mid-distance — these track perspective depth, suggesting **cascade-boundary transitions** between c0/c1 (minRange 0.5 → 1.0).
- No clear radial/pinwheel pattern that would indicate dirRes=8 directional bin starvation.

## 3. Smoothness Assessment

- **Mode 3** is spatially **smooth, almost too smooth** — looks like a low-frequency irradiance field with no high-frequency noise. Combined with `temporalAlpha: 0.05` (very heavy temporal smoothing), this is consistent with the data: variance is extremely low (1.8e-5 in c3, 1.1e-3 in c0).
- **Mode 0** shows the bilateral blur **over-smoothing the floor markings** (the white stripe dashes are smeared into elongated triangles) and **bleeding the rust-red right wall onto the floor** along a diagonal in the lower right. The far-back wall transition is also mushy.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Fair** | Reads correctly but bilateral kernel is too aggressive; geometry edges (floor markings, baseboard) are smeared. |
| 3 (Indirect ×5) | **Good** | Smooth, low-noise, useful for diagnosis. Slight clipping on left wall. |
| 6 (GI only) | **Fair** | Probe-grid blockiness on ceiling clearly visible; back wall has zero GI. |

## 5. Stats Cross-check & Recommendation

Cascade stats:
- `anyPct: 100` everywhere — probes are populated.
- `surfPct: 48.6%` on c0 — **half of c0 probes saw no surface hits**. This is the root cause of the blocky ceiling artifacts and the dead back-wall region.
- Variance drops cleanly across cascades (0.0011 → 1.8e-5), consistent with the smooth-but-blocky look.
- `temporalAlpha: 0.05` is very low → explains how mode 3 looks so noise-free despite c0 under-sampling (it's averaging over ~20 frames).

**Concrete change:** Increase **`probeJitterScale` from 0.06 → 0.15** (or raise `baseRes` from 32 → 48). The c0 surfPct of 48% combined with visible probe-grid stair-stepping on the ceiling means c0 probes are missing the thin emitter geometry. More aggressive jitter will let temporal accumulation fill in those gaps without needing more rays per frame, and the existing low `temporalAlpha=0
