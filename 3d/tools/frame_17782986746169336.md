# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782986746169336_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782986747013811_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782986748192647_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17782986746169336.json`  
**Analyzed:** 2026-05-09T11:51:46  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing the three modes, indirect light is clearly contributing in these regions:

- **Back wall (center, behind sphere)**: Mode 6 shows a faint warm/orange tint and reddish bleed on the left half — color bleeding from the red wall and the orange sphere. Mode 0 looks correctly warmer here than a pure-direct render would.
- **Pillar/box facing the green wall (right side of mode 6)**: distinct green tint on the right-facing face — classic green-wall bleed.
- **Pillar facing the red wall (left side)**: red tint visible in mode 6, especially on the left-facing slabs.
- **Floor and ceiling near the walls**: gradient of red→white→green visible in mode 6, consistent with expected Cornell behavior.
- **GI is essentially absent** on the ceiling center and on surfaces in deep shadow under the boxes (those black "ribbed" regions), where direct light also doesn't reach — these stay dark in all three modes.

## 2. Banding Artifacts in Mode 6

The most prominent artifact is **horizontal/vertical ribbing on the floor and ceiling**, appearing as tightly-spaced black streaks running roughly parallel to the camera depth axis. These are visible in all three modes (so not a GI-only artifact — likely a raymarch/voxelization stepping issue rather than probe banding).

Looking specifically at GI-attributable banding in mode 6:
- **Soft horizontal bands** on the back wall, spaced at roughly 1/8 of the wall height — consistent with the **probe grid spacing** (baseRes 32 over volumeSize 4 → probes every ~0.125 units, which matches).
- **No obvious directional-bin pinwheel artifacts** (would appear as 8-fold rotational discontinuities around the sphere — sphere shading in mode 6 looks smooth).
- **No sharp cascade-boundary seams** visible — cascade transitions appear blended.

So banding is **probe-grid dominated**, not directional or cascade-boundary.

## 3. Smoothness Assessment

- **Mode 3 (5× indirect)**: spatially smooth, almost too smooth — looks like temporal accumulation is dominating (consistent with `temporalAlpha: 0.05`). Very little noise, but also slow to react.
- **Mode 0 (final)**: bilateral blur appears appropriate; **edges between the red wall and the dark ribbed floor are preserved**, and the sphere silhouette is crisp. No obvious over-smoothing of geometry edges.
- **Mode 6**: shows the underlying probe structure most clearly — slight blockiness on the back wall confirms probe-grid resolution is the limiting factor, not noise.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Good** | Clean composite, color bleeding plausible, edges intact. Ribbed floor artifact pre-existing. |
| 3 (Indirect ×5) | **Fair** | Very smooth but washed out; sphere highlight nearly gone, suggesting indirect on the sphere is under-converged. |
| 6 (GI only) | **Good** | Clearly shows expected Cornell color bleed; banding is mild and grid-aligned. |

## 5. Stats Cross-Check & Recommendation

Key observations from stats:
- `surfPct` for cascade 0 is only **75.2%** and cascade 1 is **82%** — meaning ~25% of near-field probes aren't hitting surfaces. This explains the patchy/banded appearance on the back wall in mode 6.
- `variance` is highest in cascade 0 (0.0053) — matches the visible spatial structure there.
- `temporalAlpha: 0.05` is very low, which explains the over-smooth, slow-to-converge sphere in mode 3.
- `probeJitterScale: 0.06` with `jitterHoldFrames: 2` — jitter is small and held, contributing to grid-aligned banding rather than dithering it out.

**Concrete recommendation**: **Increase `probeJitterScale` from 0.06 to ~0.15** (and optionally drop `jitterHoldFrames` to 1). This will dither the probe grid sampling enough to break up the horizontal banding visible on the back wall in mode 6, and combined with the existing low `tem
