# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17778193764065590_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17778193766205434_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17778193768253742_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17778193764065590.json`  
**Analyzed:** 2026-05-03T22:43:27  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing mode 0 (final) to mode 6 (GI only):

- **Color bleeding** is clearly present and physically plausible. The left wall's red bleeds onto the floor near the left-front corner and onto the left face of the tall block (visible in mode 6 around x≈480–560, y≈400–500). The right wall's green bleeds onto the floor right-front and right face of the short cube (mode 6, x≈700–800, y≈450–530).
- **Ceiling near the light** in mode 6 shows a soft bright spot directly under/around the light fixture position — consistent with first-bounce return from the floor and walls.
- **Direct-only expectation gap**: the ceiling away from the light, the back wall corners, and the shadowed (occluded) side of the tall block in mode 0 all retain visible illumination — that fill clearly comes from GI (mode 6 confirms it).
- **Where GI is absent / weak**: the outer black border (outside the box) is correctly zero. The dark grey frame slabs at top/bottom in mode 0 receive almost no GI (they face outward).

## 2. Banding Artifacts in Mode 6

I can see:
- **Soft axis-aligned blockiness** along the left and right colored walls in mode 6 — the red/green gradient is not perfectly smooth; there appear to be ~4–5 vertical bands of slightly different intensity along each colored wall. Spacing is roughly proportional to the wall height divided by ~8, which matches the **probe grid (baseRes=32, volumeSize=4 → ~8 probes across the box interior)**. This points to **probe-grid spatial aliasing**, not directional binning.
- **No clear ring-shaped or wedge-shaped artifacts** that would indicate directional-bin (`dirRes=8`) issues on the diffuse walls.
- **No visible cascade-boundary seam** (no abrupt step in luminance at a fixed radius from camera). Cascade transitions appear smooth, consistent with cascade 3's very low variance (0.000156).

## 3. Smoothness of Mode 3 vs Mode 0

- **Mode 3 (indirect ×5)**: spatially very smooth, almost over-smoothed — edges of the blocks are barely distinguishable from the floor. This is the expected look of a low-frequency irradiance volume but suggests heavy temporal accumulation (consistent with `temporalAlpha=0.05`). No high-frequency noise visible.
- **Mode 0 (final)**: the bilateral blur appears **slightly over-smoothed on the cube shadows** — the contact shadow of the short cube on the right blends softly rather than crisply. The tall block's left-side shadow on the floor is also somewhat washed. However, geometric silhouettes of the blocks themselves are preserved, so the bilateral filter is respecting depth/normal edges adequately.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Good** | Plausible color bleed, light shadows; minor over-blur on contact shadows |
| 3 (Indirect ×5) | **Good** | Smooth, no noise, shows GI structure clearly |
| 6 (GI only) | **Fair** | Subtle probe-grid banding visible on side walls |

## 5. Stats Cross-Check & Recommendation

- `surfPct` is 98–100% across all cascades — probes are well-converged on surfaces.
- `anyPct = 100` everywhere — no empty probes.
- Cascade 0 variance (0.0085) is ~54× cascade 3 (0.00016) — expected, but the spatial banding I see on the walls correlates with cascade 0/1 variance.
- `temporalAlpha=0.05` is very low → strong temporal smoothing, which explains mode 3's smoothness but also means residual probe-grid structure can persist if jitter doesn't cover it.
- `probeJitterScale=0.06` is small relative to a probe spacing of ~0.5 units (volumeSize=4 / baseRes=32 × 4 = 0.5).

**Concrete suggestion**: **Increase `probeJitterScale` from 0.06 to ~0.15** (roughly 30% of probe spacing). This will spatially dither the probe-grid banding I see on the colored walls without requiring more probes or a higher `dirRes`. The low `tempor
