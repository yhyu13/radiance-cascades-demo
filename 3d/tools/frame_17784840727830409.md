# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784840727830409_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784840728741603_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784840729889549_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784840727830409.json`  
**Analyzed:** 2026-05-11T15:21:45  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing the three views, indirect light is doing significant work in this scene:

- **Bright GI zones**: The right wall (foreground, ~x=900–1280) shows a strong warm reddish bounce that only appears in modes 0 and 6 — this is clearly color-bleed from a red surface out of frame. The ceiling near the back (around the bright doorway, y=200–350) also picks up warm bounce light from the lit area beyond.
- **Absent/weak GI**: The left wall (x=0–300) is nearly neutral grey/white in mode 6, indicating little colored bounce reaches it. The floor center is also dimly lit by GI — most of its brightness in mode 0 comes from direct light (note the bright specks/caustics on the floor are absent in modes 3 and 6, confirming they are direct).
- **Doorway (~x=450, y=280–380)**: Reads bright in mode 0 due to direct light from the next room, but mode 6 shows it as a warm dim glow — GI is correctly carrying the spill.

## 2. Banding Artifacts in Mode 6

Visible structure:
- **Blocky stair-step patches on the ceiling** (clearly visible around x=550–700, y=20–180) — these are *probe-grid aligned* squares, roughly 30–50 px wide at that depth. Spacing matches a coarse probe lattice (baseRes=32, volumeSize=4 → ~0.125 unit probe spacing in C0).
- **Vertical soft bands on the right wall** at roughly even spacing — consistent with **cascade-boundary** transitions where C0→C1→C2 hand off (the right wall recedes in depth, crossing cascade shells).
- No obvious *directional-bin* pinwheel/star artifacts are visible (dirRes=8 is sufficient here for diffuse).

The high C0 variance (0.00103, ~3× C3's variance scaled by mean) corroborates the visible spatial structure in the near field.

## 3. Smoothness / Over-blur Assessment

- **Mode 3 (×5 indirect)**: Mostly smooth on large surfaces but the ceiling shows clear quantization/blockiness amplified by the 5× gain. Not noisy in a stochastic sense — the artifacts are *structured*, pointing to under-resolved probes rather than insufficient samples per probe.
- **Mode 0 bilateral blur**: Has over-smoothed the right-wall silhouette — the foreground object/wall edge on the right (~x=850) bleeds softly into the corridor, losing definition. The doorway frame at the back is also slightly mushy. Floor specular/direct highlights survive, so the blur is mostly respecting luminance edges but failing on low-contrast geometric ones.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 Final | **Fair** | Plausible look, but right-edge over-blur and a slightly muddy mid-field |
| 3 Indirect ×5 | **Fair** | Reveals real GI structure but exposes ceiling probe blockiness |
| 6 GI only | **Good** | Clean diagnostic; cascade and grid structure readable |

## 5. Concrete Parameter Change

**Raise `probeJitterScale` from 0.06 → 0.12 (and/or reduce `jitterHoldFrames` from 2 → 1).**

Justification: C0 has surfPct=48.6% (only half of C0 probes hit a surface) yet anyPct=100, and its variance is the highest of all cascades — exactly matching the visible blocky ceiling banding. More aggressive jitter combined with the existing low `temporalAlpha=0.05` will dither the probe-grid quantization away without introducing flicker. Bumping `temporalAlpha` would be the wrong lever here — convergence is fine; spatial undersampling is the issue.

Secondary suggestion if blockiness persists: increase `baseRes` from 32 → 48 to densify C0 directly.
