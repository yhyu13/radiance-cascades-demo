# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784104115277194_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784104118039740_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784104120983363_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784104115277194.json`  
**Analyzed:** 2026-05-10T18:54:02  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing mode 0 (final) vs mode 6 (GI only) against what direct-only would look like:

- **Bright GI zones**: The left wall in the near-mid distance (around x=200-350, y=200-450) shows the strongest indirect bounce — visible as a warm beige glow in mode 6. This is consistent with light bouncing off the floor onto the wall.
- **Absent GI zones**: The right side of the frame (x=800+) is almost pitch-black in mode 6, despite being a similar wall surface. This asymmetry suggests probe coverage is failing on the right, or the foreground occluder (the brown shape in lower-right) is blocking probe queries.
- **Bright doorway/end-of-corridor**: In mode 0 the far end is the brightest spot (direct light source visible), but in mode 6 that region is dim — confirming it's primarily direct contribution, not bounce.

## 2. Banding in Mode 6

- **Horizontal bands on the ceiling** (y≈30-180): regularly spaced dark/light streaks aligned with ceiling beams. Spacing roughly matches one probe cell — consistent with **probe-grid aliasing** at `baseRes=32` over `volumeSize=4`.
- **Vertical blocky transitions on the left wall** (around x=300-400): chunky stair-step boundaries that look like **cascade-boundary discontinuities** between c0 and c1 (the corridor depth crosses cascade transitions).
- I do **not** see clear directional-bin pinwheel/spoke artifacts, so `dirRes=8` appears adequate angularly.

## 3. Smoothness Assessment

- **Mode 3 (×5 indirect)**: Spatially smooth on large surfaces but shows visible **low-frequency blotching** on the left wall and ceiling. No high-frequency noise — the temporal filter (`alpha=0.05`) is doing its job, perhaps too aggressively (lag visible).
- **Mode 0 (final)**: Bilateral blur has **over-smoothed the right side** — the foreground brown object's silhouette has bled into the background wall (huge soft halo around x=900-1200). The floor specular highlights survive, but mid-distance wall edges are mush.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Fair** | Readable scene, but right-side blur halo and asymmetric GI hurt it |
| 3 (×5 GI) | **Fair** | Smooth but low-frequency blotches reveal probe-grid structure |
| 6 (GI only) | **Poor** | Severe asymmetry, banding, and right-half is essentially black |

## 5. Stats Cross-Check & Recommendation

The stats confirm the visual problems are **not artistic — they're broken**:
- `c0.anyPct=1.46%`, `c1.anyPct=0%`, `c2.anyPct=0.2%` — **probes are barely hitting anything**. Cascades are starved.
- `meanLum=-nan/-373/-320` and `maxLum=inf` — **NaN/Inf contamination** in the probe buffer. This explains the black right side: poisoned probes being clamped to zero downstream.
- `variance=17051` in c3 with banding visible → matches the cascade-boundary stairstepping.
- `temporalAlpha=0.05` is also too low given how empty the buffer is — bad samples persist forever.

### Concrete parameter change

**Increase `c0MinRange` from 1 to 2-3 and raise `temporalAlpha` to 0.15.**

Rationale: `anyPct≈1.5%` on c0 means rays are terminating before hitting geometry — the cascade-0 range is too short for this corridor's scale. Extending it should populate `surfPct` and eliminate the NaN means (which arise from divide-by-zero on empty bins). Bumping `temporalAlpha` lets the buffer flush the Inf/NaN values once new valid samples arrive, instead of accumulating them for ~20 frames.

If banding persists after that, **then** consider `baseRes` 32→48 to address the ceiling probe-grid aliasing.
