# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17787251120483786_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17787251128202905_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17787251136077923_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17787251120483786.json`  
**Analyzed:** 2026-05-14T10:19:08  
**Model:** claude-opus-4-7

---

## Visual Triage

### 1. GI Contribution (cross-mode comparison)

Comparing mode 0 (final) against mode 6 (raw GI), the indirect light is concentrated in:
- **Ceiling and upper walls**: brightest GI return, consistent with the ceiling light bouncing onto adjacent surfaces. The white "hot spot" on the back wall just below the ceiling in mode 6 confirms first-bounce off the ceiling.
- **Floor between the two boxes**: noticeable warm/neutral fill — this is the multi-bounce contribution that lifts the shadow region in mode 0.
- **Tall box left face**: picks up faint **green** spill in mode 6 — but this is *wrong-sided*; the green wall is on the right. This suggests directional bin leakage (see below).
- **Short box left face**: picks up green tint in mode 6 (correct, facing green wall).
- **Red wall contribution**: visible on the left side of the tall box and on the floor near the red wall in mode 3, but weaker than the green-side response.

GI is essentially **absent** in the outer black region (correct — outside the volume), and on the dark gray top/bottom slabs (which sit outside the probe volume — see point 2).

### 2. Banding in Mode 6

- **Hard rectangular cutoff** around the entire room silhouette: the black border hugging the colored walls is a **cascade volume boundary** — geometry outside `volumeSize=4` receives zero GI. The top/bottom gray slabs and the left/right outer wall slivers fall outside the cascade-0 footprint.
- **Soft blocky gradients on the back wall and ceiling** (~6–8 cell pattern across the wall): spacing matches `baseRes=32` projected through the camera — this is **probe-grid trilinear footprint**, not directional aliasing. No fine pinwheel/star pattern that would indicate `dirRes=8` bin aliasing.
- **Green tint bleeding onto the left face of the tall box**: a directional-bin leak from the green wall through 8-direction quantization — `dirRes=8` (≈45° cones) is coarse enough that a probe near the box samples the green wall through a wide bin.

### 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: spatially **very smooth**, almost no per-pixel noise. Temporal accumulation at `α=0.05` is doing its job. Edges of boxes and wall corners are soft but readable.
- **Mode 0 (final)**: bilateral blur looks well-tuned. Box silhouettes, wall corners, and the contact shadow under the short box are crisp. **No over-smoothing** is visible on geometric edges. The only "soft" region is intentional GI on diffuse surfaces.

### 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (final) | **Good** | Clean composite, edges preserved, plausible GI fill. Slight green leak on tall box's shaded face. |
| 3 (indirect ×5) | **Good** | Smooth, low noise, clear directional structure. |
| 6 (GI only) | **Fair** | Reveals probe-grid blockiness on back wall and a wrong-side green leak on tall box; cascade boundary clips the gray slabs. |

### 5. Stats Cross-Check & Recommendation

- `surfPct`: cascade 0 = **68.9%** (≈31% of finest probes hit nothing useful), cascades 1–3 = ~100%. Cascade 0 is the under-converged one and matches the blockiness on near-camera surfaces.
- `variance`: c0 = 0.008, c3 = 0.00015 — variance drops cleanly with cascade level, consistent with the soft probe-grid banding (not high-frequency directional noise).
- `maxLum` halves from c0 (0.56) to c3 (0.05) — reasonable falloff.
- `anyPct=100, skyPct=0` everywhere — fully enclosed scene, expected.

**Concrete change**: increase `dirRes` from **8 → 12 (or 16)**. The dominant remaining artifact is the green-side directional leak onto the tall box's left face, which is a directional-bin width problem, not a spatial-density problem. `cascadeTimeMs=0.07` leaves plenty of budget. If only one knob: **`dirRes: 8 → 12`**.

Secondary (if
