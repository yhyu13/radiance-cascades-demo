# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784967485099536_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784967487184295_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784967489245515_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784967485099536.json`  
**Analyzed:** 2026-05-11T18:53:01  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Renders

## 1. GI Contribution Analysis

Comparing mode 0 (final) against mode 6 (GI only):

- **Color bleeding**: Strong red bleed visible on the left tall block's left face and floor near the red wall (mode 6). Green bleed mirrors on the right block's right face near the green wall. This is the expected Cornell signature and is present.
- **Ceiling**: In mode 6 the ceiling shows a clear hot spot directly under the light with a dim red/green tint at the lateral edges — indirect bounce from the colored walls is reaching the ceiling.
- **Block tops**: The shorter right block's top is brighter in mode 6 than the taller block's top; consistent with proximity to the light.
- **Shadow regions**: The contact shadow behind/under the tall block in mode 0 retains some fill from indirect — visible as soft gray rather than black, confirming GI is filling occluded zones.
- **Outside the box** (the dark/green/red slabs around the frame): GI leaks faintly into the gray bars behind the box (top/bottom in mode 6) — these are receiving wrap-around bounce.

## 2. Banding in Mode 6

- **Outer frame edges**: The gray top/bottom bars and red/green side slabs show **soft rectangular halos** with roughly uniform thickness — these align with the outer cascade boundaries (likely C2/C3 transition given `volumeSize: 4` and the slabs sitting near the edge of the highest cascade).
- **Inside the box**: I do not see clear striped probe-grid stepping or directional-bin pinwheels. The internal walls look smooth-but-blobby, consistent with the low `baseRes: 32` and `dirRes: 8` being adequately filtered.
- **Block silhouettes**: Slight haloing around both blocks — consistent with probe-to-pixel interpolation footprint, not directional binning.

So the dominant banding source reads as **cascade-boundary**, not probe-grid or directional-bin.

## 3. Spatial Smoothness & Over-Blur

- **Mode 3 (×5 indirect)**: Looks **spatially smooth, almost too smooth** — the wall gradients are continuous, no speckle, no per-pixel noise. The taller block's silhouette is *very* soft, suggesting the indirect resolution is well below screen res (expected with 32³ probes).
- **Mode 0 bilateral blur**: Edges of the blocks and the wall corners are still crisp in mode 0, so the bilateral filter is correctly preserving direct-lit geometry edges. However the **shadow under the tall block** in mode 0 looks slightly washed — the bilateral may be pulling indirect across the contact shadow boundary.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Good** | Clean composite, plausible color bleed, edges preserved. Slight contact-shadow softening. |
| 3 (Indirect ×5) | **Good** | Smooth, no noise, but very low-frequency — confirms heavy temporal/spatial filtering. |
| 6 (GI only) | **Fair** | Cascade-boundary halos visible around the outer frame; interior is plausible but blobby. |

## 5. Stats Cross-Check & Recommendation

Key observations from stats:
- **C0 surfPct = 68.9%** — nearly a third of cascade-0 probes hit nothing. This is the under-sampled cascade.
- C1–C3 all ≥99% surface coverage; C3 variance is tiny (0.00016) — the outer cascade is effectively flat, contributing little high-frequency info.
- **C0 variance (0.008) > C1 (0.008) ≈ C2 (0.004) ≫ C3** — variance drops with cascade level as expected, no anomaly.
- **temporalAlpha = 0.05** is very low (20-frame effective history), which explains the smoothness in mode 3 and lack of noise — but also means cascade-boundary structure gets *baked in* and persists.

### Concrete change

**Increase `c0MinRange` from 0.5 → ~0.8**, or alternatively **raise `baseRes` from 32 → 48**.

Rationale: C0's 68.9% surface hit rate means many short rays terminate in empty space before reaching geometry — the inner cascade's range is too short for the
