# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17815246552820220_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17815246553461186_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17815246553804319_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17815246552820220.json`  
**Analyzed:** 2026-06-15T19:58:10  
**Model:** claude-opus-4-7

---

# Visual Triage Report — Cornell Box GI

## 1. GI Contribution Analysis

**Where indirect light is present (visible in modes 3 & 6):**
- **Left red wall** bleeds onto the floor and the left side of the tall block — visible as a pinkish/reddish wash in mode 6, and as the strongest signal in mode 3 (the only colored region).
- **Ceiling-near regions** of the back wall pick up faint warm bounce in mode 6.
- **Front face of the short block** (right side) shows soft fill in mode 6, lifting it above pure-black shadow in mode 0.

**Where indirect light is absent / under-sampled:**
- **Floor center and area between the two blocks** — large black/empty regions in modes 3 and 6, consistent with `cascade[1].anyPct = 0.098%` (essentially no probes hit there).
- **Right wall** — almost no red bleed visible despite proximity geometry; mode 6 shows it nearly neutral gray.
- **Vertical strips on both blocks** in mode 0 — the bands of pure black between lit columns are GI dropouts, not shadows.

## 2. Banding Artifacts in Mode 6

- **Vertical column banding on both blocks**: regularly spaced light/dark stripes running floor-to-top. Spacing looks like ~8–10 px, aligned to world-space, not screen-space → **probe-grid aliasing** at `baseRes=32` over `volumeSize=4` (probe spacing = 0.125 units, matches the stripe period at this camera distance).
- **Horizontal dotted/stippled bands on the ceiling**: dense regular dot pattern across the entire ceiling in all three modes. Period matches `jitterPatternSize=8` — this is the **jitter pattern itself becoming visible** because `temporalAlpha=0.05` is too low to dissolve it within `jitterHoldFrames=2`.
- **Concentric ring on the floor** (visible center-front in mode 0/6): looks like a **cascade boundary** between c0 (`minRange=0.5`) and c1 (`minRange=1.0`) — the radius matches a ~0.5–1.0 unit transition from camera/probe origin.
- No clear directional-bin banding (no angular pinwheel artifacts), so `dirRes=8` is not the dominant issue.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: spatially **smooth where it exists** (red wall gradient is clean), but riddled with hard black holes on the blocks and floor. Not noisy — it's *missing data*, not noisy data.
- **Mode 0 (final)**: bilateral blur has **not over-smoothed** geometric edges (block silhouettes are crisp), but it cannot rescue the black GI dropouts — they show through as sharp dark vertical bars on the blocks. The blur is essentially idle on regions with no GI signal to spread.
- **Mode 6**: shows the rawest structure — the probe grid and jitter pattern dominate over actual radiance variation.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| Mode 0 (Final) | **Fair** | Direct lighting clean, but black GI gaps on blocks/floor leak through. |
| Mode 3 (Indirect ×5) | **Poor** | Huge under-sampled regions; only red-wall bounce is reliable. |
| Mode 6 (GI only) | **Poor** | Probe-grid + jitter banding visible everywhere; cascade ring on floor. |

## 5. Stats Cross-check

- `cascade[0].anyPct = 2.65%` and `cascade[1].anyPct = 0.098%` — **catastrophically under-populated** near-field cascades. This directly explains the black columns on the blocks (they live in c0/c1 range).
- `cascade[3].variance = 13.0` with `meanLum = 1.36` → variance/mean² ≈ 7, very high — matches the visible banding on far surfaces.
- `maxLum = 21525` in c0 vs `meanLum = nan` (no valid samples) → a single firefly probe with no neighbors to average against.
- `surfPct = 0` for c0–c2 confirms probes are not finding surfaces in the near field.

## Concrete Parameter Change

**Increase `temporalAlpha` from 0.05 → 0.15 AND redu
