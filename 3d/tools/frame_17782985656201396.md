# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782985656201396_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782985657225044_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782985658195637_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17782985656201396.json`  
**Analyzed:** 2026-05-09T11:49:55  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Render

## 1. GI Contribution Analysis

Comparing modes 0, 3, and 6:

- **Red wall bleed (left)**: Visible in mode 6 as a pinkish tint on the left side of the front-facing block and faint red wash on the floor near the left wall. Mode 3 confirms this — the red bleed is most concentrated at the bottom-left corner where the wall meets the floor.
- **Green wall bleed (right)**: Notably weaker than the red side. In mode 6, the green tint barely registers on the right block face. The green wall in mode 3 itself looks desaturated (pale green) compared to the red wall (pale pink), suggesting asymmetric probe sampling or the orange sphere is occluding green-side bounce.
- **Orange sphere contribution**: In mode 6 the sphere appears as a saturated orange disk against the back wall — it's reflecting/contributing GI but there's no visible orange bleed onto adjacent surfaces (the back wall behind it stays neutral gray).
- **Ceiling/light area**: The ceiling is essentially black in all three modes — GI is not reaching upward-facing surfaces away from the light panel.

## 2. Banding in Mode 6

- **Concentric arc bands on floor and ceiling**: Black hyperbolic/curved striping wrapping around the bottom and top of the frame. Spacing widens toward image edges. Shape and curvature are consistent with **probe-grid aliasing** at `baseRes: 32` over `volumeSize: 4` — these are slices of the 3D probe lattice intersecting the view frustum at grazing angles.
- **Vertical streaks on the blocks**: Regular vertical stripes on both pillar/block surfaces, evenly spaced (~6–8 px). These align with **directional bin discretization** (`dirRes: 8` → only 8 directions per probe), causing under-sampled lateral hemisphere coverage on vertical surfaces.
- **No clear cascade-boundary ring**: I don't see a discrete radial step where one cascade hands off to the next, so cascade blending appears OK.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: Spatially **smooth** on large flat surfaces (walls), but the floor/ceiling banding bleeds through. No high-frequency noise — temporal accumulation (`temporalAlpha: 0.05`) is doing its job.
- **Mode 0 (final)**: Bilateral blur has **over-smoothed the orange sphere's silhouette edge** against the green wall — note the soft dark halo around the sphere in mode 0 vs. the crisp voxel edge in mode 6. The block edges against the back wall also show slight bleeding.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 Final | **Fair** | Lighting reads correctly but blur halos around sphere; floor banding still visible |
| 3 Indirect ×5 | **Good** | Smooth GI, asymmetric R/G bleed exposed clearly |
| 6 GI only | **Fair** | Strong probe-grid and directional-bin banding dominate |

## 5. Stats Cross-check & Recommendation

- `surfPct` climbs 75% → 82% → 100% → 100% across cascades: **C0 is under-sampled** (25% of probes hit nothing useful), explaining the floor/ceiling void bands.
- `variance` on C0 (0.0055) is ~75× higher than C3 (7.4e-5) — consistent with the visible C0-scale striping.
- `maxLum 0.578` on C0 vs `meanLum 0.040` → high dynamic range within C0, amplifying any directional-bin aliasing.

**Concrete change**: Increase **`dirRes` from 8 → 12 (or 16)**. The vertical streaking on the blocks is the dominant artifact and is directly tied to angular under-sampling, which also explains why C0's high `maxLum/meanLum` ratio shows up as banding rather than smooth bleed. If perf budget is tight (`raymarchTimeMs: 0.117` has headroom), this is the cheapest quality win. Secondary option: bump `probeJitterScale` from 0.06 → 0.10 to break up the C0 grid arcs on the floor.
