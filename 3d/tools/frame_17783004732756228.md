# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783004732756228_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783004733871082_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783004735046250_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17783004732756228.json`  
**Analyzed:** 2026-05-09T12:21:43  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Render

## 1. GI Contribution Analysis

Comparing mode 0 (final) against mode 6 (GI only):

- **Back wall (white)**: Picks up a clear pinkish tint on the left half and a faint green tint on the right — classic color bleed from the red/green walls. Visible in both mode 3 and mode 6.
- **Floor under the tall slab/box**: Receives noticeable indirect bounce; the area between the two block clusters is brighter in mode 6 than the shadowed direct-only expectation would predict.
- **Ceiling**: Almost entirely black in mode 6 except a faint halo around the light panel — indirect contribution there is essentially zero (consistent with `surfPct=74.9%` in cascade 0; many upper probes miss surfaces).
- **Orange emitter block**: Shows up strongly in mode 6, confirming it's being sampled as an emissive surface, not just direct.
- **Right side of green wall (lower)**: A dark notch near the floor in mode 0 corresponds to a region where mode 6 also drops to near-black — an occlusion/probe-miss zone, not a shading bug.

## 2. Banding Artifacts in Mode 6

- **Curved horizontal bands on the ceiling and floor**: Concentric arc-like striping, spacing ~6–10 px near image center widening toward edges. Shape is **probe-grid aligned** — these are isosurfaces of trilinear probe interpolation across the `baseRes=32`, `volumeSize=4` grid (≈0.125 units/probe), seen edge-on against grazing surfaces.
- **Vertical streaks on the slab/pillar geometry**: Aligned with the voxel columns themselves — these are **directional-bin aliasing** from `dirRes=8` (only 8² = 64 directions/probe), insufficient for the small emitter solid angle.
- **No obvious sharp rectilinear seams** between cascades — cascade-boundary artifacts are not the dominant issue.

## 3. Smoothness Assessment

- **Mode 3 (×5 indirect)**: Looks **spatially smooth**, almost too smooth — the wall gradients are creamy with no high-frequency noise. This is consistent with `temporalAlpha=0.05` (heavy temporal accumulation) and `variance=0.0057` on cascade 0 — low variance means most spatial structure has been integrated away.
- **Mode 0 (final)**: Bilateral blur appears **well-behaved on flat walls** but has **over-smoothed the contact shadows** at the base of the pillars and at the wall/floor seam on the right (green wall lower edge looks washed). The pillar silhouettes against the back wall are slightly haloed.
- The probe-grid arcs visible in mode 6 are **not** fully removed by the bilateral pass in mode 0 — they remain faintly on the ceiling.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Fair** | Color bleed reads correctly; over-blur at contacts; residual ceiling banding |
| 3 (Indirect ×5) | **Good** | Smooth, plausible bounce distribution; no temporal noise |
| 6 (GI only) | **Fair** | Reveals probe-grid arcs and dir-bin streaks; cascade 0 surfPct only 74.9% |

## 5. Recommended Parameter Change

**Increase `dirRes` from 8 → 12 (or 16)**, and as a secondary change raise `temporalAlpha` from 0.05 → 0.08.

Justification:
- The vertical streaks on the pillars are angular aliasing — `dirRes=8` gives 64 directions, marginal for a small bright emitter. Bumping to 12 (144 dirs) or 16 (256) directly attacks the dominant visible artifact.
- `cascadeTimeMs=0.117` leaves headroom for ~2× direction count.
- The slightly higher `temporalAlpha` will reduce the over-smoothed feel without re-introducing noise, since current variance is already very low (0.0057 → blur is doing too much work). 
- Cascade 0's `surfPct=74.9%` suggests `probeJitterScale=0.06` may be too small to fill ceiling gaps; if dirRes alone doesn't fix the ceiling halo, raise jitter to 0
