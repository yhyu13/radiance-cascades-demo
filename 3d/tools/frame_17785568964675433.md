# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785568964675433_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785568967356535_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785568970974479_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17785568964675433.json`  
**Analyzed:** 2026-05-12T11:35:29  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Isolation

Comparing mode 0 (final) against mode 6 (raw GI) and mode 3 (5× indirect):

- **Bright GI zones**: The ceiling soffits and the window-side wall on the left (around x≈200–400, y≈100–500) receive significant indirect bounce — visible as the warm cream/white spill in mode 3 around the window opening and along the ceiling lights.
- **GI-absent zones**: The right wall (the long orange surface filling the right half) shows almost identical brightness in mode 0 and mode 6 — meaning the warm orange tone is essentially **all indirect bounce** from off-camera light. There is no direct light striking it.
- **Floor**: Nearly black in mode 6, slightly less black in mode 0. The floor is receiving very little indirect light — it reads almost purely as direct contribution (and there is little of that either, hence the dark corridor floor).
- **Vanishing point** (center, ~x=500, y=400): GI falls off sharply with distance — mode 6 shows the corridor end going pitch black while mode 3's 5× amplification reveals there *is* some weak GI there, just below the visibility floor.

## 2. Banding Artifacts in Mode 6

Visible banding patterns:

- **Right wall concentric arcs**: Large smooth circular/elliptical bands centered roughly off-screen right (~x=1100, y=350), spaced ~150–200 px apart. The shape (smooth curved isolines on a flat wall) is characteristic of **probe-grid interpolation** — the wall is sampling across the trilinear footprint of a sparse probe volume (`baseRes:48`, `volumeSize:4` → ~12 voxels/unit, very coarse for this scale).
- **Ceiling stairstep blockiness**: Hard pixelated chunks on the ceiling lights (~x=400–800, y=50–250). This is **directional-bin aliasing** — at `dirRes:8` (only 64 directions per probe), bright emitters get quantized into discrete cone footprints.
- **No clear cascade-boundary seam visible** in this frame (no sharp brightness discontinuity along a planar boundary), consistent with cascade 1 being completely empty (`anyPct:0`) — there's no transition to seam against.

## 3. Smoothness / Over-blur Assessment

- **Mode 3** is **noisy in the structural sense** (blocky probe footprints, directional banding) but not stochastically noisy — there's no per-pixel salt-and-pepper, just low-frequency quantization. The temporal accumulation (`temporalAlpha:0.05`) is doing its job on noise but cannot fix undersampling.
- **Mode 0**: The bilateral blur has clearly **over-smoothed the right wall** — the GI bands that are visible in mode 6 are *also* still visible in mode 0 (compare the orange arcs at x>800). Blur reduced high-frequency noise but cannot remove low-frequency probe-grid structure. Meanwhile, the floor reflections (the bright vertical streaks at x≈430, y=400–700) appear slightly smeared into the floor.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Fair** | Reads plausibly but inherits all the GI banding; blur masks noise but not structure |
| 3 (Indirect ×5) | **Fair** | Useful diagnostic; clearly exposes probe footprints and dir-bin chunks |
| 6 (GI only) | **Poor** | Right-wall arcs and ceiling blockiness are obvious artifacts a viewer would notice |

## 5. Stats Cross-check & Recommendation

Key red flags in stats:
- **Cascade 1: `anyPct=0`** — completely empty. Wasted cascade.
- **Cascade 0: `anyPct=0.09%`** — extremely sparse hits.
- **Cascade 2: `anyPct=17%` but `surfPct=0`** — probes are tracing but not landing on surfaces (likely all-miss or sky).
- **Cascade 3: `surfPct=0.46%`, `skyPct=50%`** — mostly sky-hits, almost no surface hits.
- **`meanLum=-nan`** in c0/c2 indicates a divide-by-zero in the variance estimator — should be guarded.
