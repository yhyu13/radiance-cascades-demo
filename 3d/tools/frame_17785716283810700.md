# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785716283810700_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785716291595800_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785716298968856_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17785716283810700.json`  
**Analyzed:** 2026-05-12T15:41:03  
**Model:** claude-opus-4-7

---

# Visual Triage Report — Cornell Box GI

## 1. GI Contribution Analysis

Comparing mode 0 (composite) against mode 6 (GI only):

- **Color bleeding** is clearly present and correctly localized:
  - Red bleed onto the **floor near the left wall** and onto the **left face of the tall block** (visible in mode 6, faintly carried into mode 0).
  - Green bleed onto the **floor near the right wall** and onto the **right face of the small checkered cube** — the green tint on the small cube's right edge is the strongest GI signal in mode 6.
- **Ceiling** in mode 6 shows weak indirect light (mostly desaturated grey with slight pink/green gradient left-to-right) — expected since the ceiling is the light source plane and only receives bounce light.
- **Direct-only regions** (the bright lit floor center in mode 0) correspond to dim/dark areas in mode 6, which is correct — GI is bounce-only.
- The **outer "frame"** (the grey/red/green border outside the inner room) shows GI in mode 6 but appears as the textured probe-debug overlay in mode 0 — that's the cascade volume extending past the room geometry.

## 2. Banding Artifacts in Mode 6

Visible structures:

- **Checkerboard / dotted pattern** on the top, bottom, and outer side walls (the "frame" region). Dot spacing is regular and aligns with `baseRes: 32` projected through the volume — this is the **probe grid debug visualization**, not an artifact of GI itself.
- Inside the room, I can see **soft horizontal/vertical bands** on the back wall — a faint gradient discontinuity roughly mid-height. Spacing is large (one band across the wall), consistent with a **cascade boundary** between c0 and c1 (given `c0MinRange: 0.5`, `c1MinRange: 1.0`, `volumeSize: 4`).
- I do **not** see fine directional-bin striping (which would manifest as 8 angular wedges given `dirRes: 8`); the indirect is smooth angularly.

## 3. Smoothness / Over-blur Assessment

- **Mode 3 (indirect ×5)**: Spatially **very smooth** — almost too smooth. Walls are flat gradients with no visible noise. This indicates the temporal accumulation (`temporalAlpha: 0.05` = 20-frame EMA) is doing heavy lifting.
- **Mode 0 (composite)**: The bilateral blur has **over-smoothed the contact shadow** under the tall block — the shadow edge on the floor is very soft, and the small cube's checker texture is slightly smeared. The corner between back wall and ceiling is blurred across the geometric seam.
- Edges of the colored walls themselves remain sharp, so the bilateral filter's depth/normal weights are working — only the GI term is being over-blurred onto sharp geometry.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 Final | **Good** | Plausible Cornell, correct color bleed, but soft contact shadows and texture smearing on the small cube |
| 3 Indirect ×5 | **Good** | Clean, low-noise, color-bleed direction correct; suspiciously smooth suggests heavy temporal lag |
| 6 GI only | **Fair** | Useful diagnostic but probe-grid debug overlay dominates the frame; faint cascade seam visible on back wall |

## 5. Stats Cross-check & Recommendation

Key observations from stats:
- `anyPct: 100` across all cascades — **fully covered**, no under-sampling.
- `surfPct` on c0 is only **68.9%** — ~31% of c0 probes hit nothing within 0.5 units. This is fine (small near-field range) but means c0 contributes less than it should near surfaces.
- Variance is low (0.008 → 0.0001 across cascades) — consistent with the **smooth, lag-prone** appearance in mode 3.
- `temporalAlpha: 0.05` is very aggressive smoothing; combined with `jitterHoldFrames: 2` this explains the over-soft shadows.

### Recommended change

**Raise `temporalAlpha` from 0.05 → 0.12** (≈8-frame EMA).

Justification: the cascades are well-converged (anyPct=100, low variance), so heavy temporal smoothing
