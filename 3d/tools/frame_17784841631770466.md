# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784841631770466_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784841633403863_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784841635514708_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784841631770466.json`  
**Analyzed:** 2026-05-11T15:23:15  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Renders

## 1. GI Contribution Analysis

Comparing mode 0 (final) against mode 6 (GI only), the indirect light is doing the following work:

- **Bright GI zones**: The ceiling near the camera (top-left to top-center) shows the strongest indirect bounce — visible as warm cream tones in mode 6. The left wall/door frame around y=0.4–0.6 also picks up significant bounce light.
- **Weak/absent GI**: The right wall reads almost pure brown in mode 6 with very little added illumination — it's in shadow relative to the bounce sources. The far end of the corridor (vanishing point) also collapses to near-black GI.
- **Floor**: Receives modest, fairly uniform GI, which is why it looks flat-gray in mode 0 rather than picking up colored bounce from the walls.

Mode 3 (×5 amplified) confirms the indirect signal is genuinely low-magnitude everywhere — even amplified it's a soft cream wash, not vivid color bleeding. This is consistent with `meanLum ≈ 0.016` on cascade 0.

## 2. Banding in Mode 6

Visible structures in mode 6:

- **Stair-step / blocky tiling on the ceiling** (the dark brown chevrons at top): these are coarse and rectangular, with spacing roughly matching one probe cell projected at distance. Shape (axis-aligned, ~equal horizontal and vertical extent per "tile") points to **probe-grid aliasing**, not directional bins (which would smear along view rays) and not cascade boundaries (which would form a single radial ring/seam).
- **No clear concentric ring or hard radial discontinuity** is visible, so cascade transitions appear well-blended.
- **No streaking along view direction**, so `dirRes=8` is not obviously the culprit at this distance.

Diagnosis: **probe-grid spatial undersampling on the ceiling**, where probes are sparse relative to the small high-contrast geometry (light fixtures).

## 3. Smoothness & Blur Assessment

- **Mode 3** is spatially smooth in open regions (floor, far wall) but shows clear blocky probe footprints on the ceiling and around the door jambs on the left. Not noisy in the stochastic sense — the structure is deterministic grid aliasing, consistent with the very low `variance` (1e-3 down to 9e-6 across cascades), which means temporal accumulation is converged but the underlying spatial resolution is the limit.
- **Mode 0** bilateral blur has clearly over-smoothed the **left wall/door region** (mid-left, y≈0.3–0.7) — vertical edges that should be crisp dissolve into smeared verticals. The **floor highlights** (the bright dashed specks running down the corridor) survive, suggesting the bilateral kernel is preserving high-contrast direct features but eroding low-contrast indirect-shadow boundaries.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (final) | **Fair** | Readable, but bilateral over-smooths left-wall edges; ceiling chevrons leak through. |
| 3 (indirect ×5) | **Fair** | Useful as a diagnostic; reveals grid aliasing clearly. Smoothness OK in open areas. |
| 6 (GI only) | **Poor** | Dominated by probe-grid blocking on ceiling; very little usable color bleed. |

## 5. Stats Cross-Check & Recommendation

- `surfPct` on cascade 0 is only **48.6%** — nearly half of c0 probes hit nothing useful. This is the root cause of the ceiling blockiness: small features near the camera fall between probes.
- `anyPct=100` everywhere means probes are placed, but c0's coverage of actual surfaces is poor.
- `variance` decreasing cleanly across cascades (1e-3 → 9e-6) confirms temporal convergence is fine — `temporalAlpha=0.05` is not the bottleneck.
- `cascadeTimeMs=20ms` is the dominant cost, so resolution increases need to be modest.

**Concrete change**: Increase `baseRes` from **32 → 48** (or alternatively raise `probeJitterScale` from 0.06 → 0.12 to dither the c0 grid more aggressively across the 2-frame hold). The
