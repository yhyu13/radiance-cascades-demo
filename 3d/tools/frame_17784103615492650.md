# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784103615492650_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784103618073245_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784103620888621_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784103615492650.json`  
**Analyzed:** 2026-05-10T18:53:12  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing mode 0 (final) against modes 3 and 6, indirect light is contributing primarily in the following regions:

- **Ceiling band (top of frame, ~y=0–200)**: Mode 3 shows a clear warm bounce on the ceiling near the camera and along the upper corridor walls. This carries through to mode 0 as the soft warm tint along the top edge.
- **Left wall (x=0–250)**: Strong indirect fill — mode 3 shows it nearly white-saturated, mode 6 confirms a real (un-amplified) warm wash. This is the brightest GI contribution in the scene.
- **Right wall (x=900–1280)**: Heavy warm/orange bounce, especially in the lower-right where mode 3 and mode 6 both show concentrated reddish energy. This is what gives the final composite its rust tint on that side.
- **Far end of corridor (~x=400–550, y=250–400)**: GI is *absent* here — the dark archway in mode 0 matches the dark patch in mode 6, confirming the recess receives little bounce light.
- **Floor (lower-center)**: Mode 6 shows the floor receives only weak GI; the bright streaks visible in mode 0 (the dashed highlights down the corridor) are direct-light specular/emissive features, not GI.

## 2. Banding Artifacts in Mode 6

Visible banding patterns:

- **Vertical/columnar soft bands on the left wall** (~x=100, x=250) and **right wall** (~x=750, x=950) with spacing of roughly 150–200 px. The spacing is too coarse for the 32³ base probe grid at this view distance and is consistent with **cascade-boundary transitions** — the warm-to-neutral falloff on the right wall has a visible step rather than a smooth gradient.
- **Blocky pixel-scale stippling on the ceiling near the entry arch** (~x=400–600, y=50–250). This is the characteristic 8-bin **directional-bin (dirRes=8)** quantization — discrete trapezoidal patches matching octahedral cell footprints projected onto the ceiling geometry.
- No clear regular grid-aligned squares visible at the probe-grid spacing, so probe-grid aliasing is not the dominant artifact here.

## 3. Smoothness / Over-smoothing Assessment

- **Mode 3 (indirect ×5)**: Spatially **smooth on flat walls** but with visible directional quantization on the ceiling (see above). No high-frequency Monte Carlo noise — the temporal accumulation (α=0.05) is doing its job.
- **Mode 0 (final)**: The bilateral blur has **over-smoothed the right-wall corner** — the geometric edge between the right wall and the receding corridor is completely lost in a soft brown haze (x≈700–1000, y=300–600). The arch silhouette at the far end is also softer in mode 0 than the underlying GI in mode 6 would warrant.

## 4. Ratings

| Mode | Rating | Reason |
|---|---|---|
| Mode 0 (final) | **Fair** | Plausible mood, but bilateral blur destroys the right-wall edge and the foreground geometry is mushy. |
| Mode 3 (×5 indirect) | **Good** | Reveals GI structure clearly; directional banding visible but expected at dirRes=8. |
| Mode 6 (raw GI) | **Fair** | Useful for diagnosis but reveals cascade-boundary stepping on side walls. |

## 5. Stats Cross-check & Recommendation

The stats reveal serious problems:
- **Cascade 0: anyPct=0** — fully empty. The finest cascade is contributing nothing, which is why fine-scale GI detail is missing and the bilateral blur has nothing crisp to preserve.
- **Cascade 1: anyPct=1.5%** — almost empty.
- **Cascade 2: anyPct=36%, maxLum=32618** — carrying nearly all the GI energy, with extreme outliers (NaN mean/variance indicates a fireflies/Inf contamination).
- **Cascade 3: skyPct=6.25%, negative meanLum, variance=17051** — numerically broken (negative luminance, near-zero maxLum with huge variance).

The visible banding on side walls correlates with cascade 2 carrying everything while c0/
