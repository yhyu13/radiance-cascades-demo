# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784849979810391_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784849980651739_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784849981629000_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784849979810391.json`  
**Analyzed:** 2026-05-11T15:37:10  
**Model:** claude-opus-4-7

---

## Visual Triage

### 1. GI contribution (isolated by comparing modes)

- **Right wall (large vertical surface, mid-to-foreground):** Strong warm brown indirect contribution. Visible in mode 6 as a saturated rust/copper bloom, in mode 3 as the dominant amplified region. In mode 0 this lifts the right wall significantly — without GI it would read near-black.
- **Floor (foreground center, ~y=500–700):** Receives moderate indirect from the right wall (warm tint), visible in mode 6. Mode 0 shows the floor as neutral grey because direct light dominates here.
- **Left wall (x<300):** Almost no indirect contribution in mode 6 — appears dark olive/grey. Mode 3 confirms it is dim even at 5× amplification. The left wall in mode 0 is bright purely from direct light.
- **Ceiling area near doorway (top-center, y<200):** Brightest patches in mode 3, indicating ceiling receives bounce off the floor / back room.
- **Back doorway aperture (~x=420, y=300):** Black silhouette in all three modes — geometry occlusion / unsampled probes behind it.

### 2. Banding in mode 6 (GI only)

- **Right wall:** Smooth horizontal gradient, no clear stair-stepping. Color is uniform — this is **not** probe-grid banding.
- **Floor:** A faint diagonal seam runs from lower-left to mid-right around y≈500. Spacing roughly matches a single probe cell at the camera distance — likely a **cascade-boundary transition** between c0 and c1 (c0MinRange=0.5, c1MinRange=1 places the boundary right around the foreground floor).
- **Ceiling tiles (top of frame):** The pixelated dark/light speckle in modes 0 and 3 is geometry/voxelization, not GI banding — mode 6 shows the underlying GI there is smooth.
- No visible **directional-bin** ringing (which would appear as angular wedges around concave corners). dirRes=8 looks adequate for this scene.

### 3. Smoothness assessment

- **Mode 3 (indirect ×5):** Spatially smooth across all large surfaces. No salt-and-pepper noise, no temporal sparkle visible in a still. Temporal accumulation (alpha=0.05) is doing its job.
- **Mode 0 (final):** Bilateral blur has **over-smoothed the right edge** — the gradient from bright foreground wall into the receding hallway is mushy, and the silhouette of the dark doorway pillar (~x=400) bleeds GI color into the black void. The floor's specular streaks survive, so the blur is normal-aware but the range sigma may be too generous on color.

### 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (final) | **Fair** | Heavy right-wall color cast looks unphysical; blur eats silhouette edges |
| 3 (indirect ×5) | **Good** | Smooth, plausible bounce distribution; useful diagnostic |
| 6 (GI only) | **Fair** | Confirms strong directional bias toward right wall; faint cascade seam on floor |

### 5. Stats cross-check & recommendation

- `surfPct` climbs 48 → 85 → 100 → 100: **c0 is under-sampled** (only ~49% of c0 probes hit a surface). That explains why fine-scale GI on the right wall looks blocky/oversaturated — c0 is the cascade responsible for near-field detail and it's missing half its data.
- `maxLum` 0.54 in c0 vs 0.26 in c1: a few c0 probes are firing very hot, consistent with the warm right-wall bloom being driven by a small number of bright samples rather than a converged average.
- `variance` is low overall (0.001), matching the smooth appearance — banding is minimal, agreeing with the visual.

**Concrete change:** Increase `c0MinRange` from 0.5 → 0.8 (or raise `baseRes` from 32 → 48). This gives c0 probes longer rays so more of them hit a surface, lifting `surfPct` toward c1's 85% and reducing the hot-sample dominance that's painting the right wall orange. If you'd rather not touch range, alternatively bump `temporalAlpha` down to 0.03 to let the few good c0 samples integrate longer.
