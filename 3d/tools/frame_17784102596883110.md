# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784102596883110_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784102599292264_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784102601835451_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784102596883110.json`  
**Analyzed:** 2026-05-10T18:51:29  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Renders

## 1. GI Contribution Analysis

Comparing the three modes, the indirect light is concentrated in specific zones:

- **Bright GI zones (visible in modes 3 & 6):** The ceiling strip running down the center, particularly the orange/red diagonal stripes from ~x=400–700, y=0–250. These are clearly receiving bounce light from the warm-toned ceiling fixtures or far wall.
- **Far end of corridor (mode 6, ~x=400–500, y=270–360):** A reddish glow showing color bleed from the back wall — this is the strongest GI contribution and reads correctly in the final composite.
- **GI absent zones:** The left wall (x=0–250) and right wall (x=900–1280) show almost no bounce contribution in mode 6 — they appear nearly flat-shaded brown/grey. The floor (y=400+) also has minimal indirect light, only a weak warm tint near the vanishing point.
- **Mode 0 vs Mode 6 delta:** The direct lighting contribution is mostly the bright specular streaks on the floor (the white slashes at y=400–650) — these are absent in modes 3 and 6, confirming they are direct-only.

## 2. Banding Artifacts in Mode 6

- **Ceiling (y=0–250, center):** Discrete blocky/staircase patterns on the orange diagonal bands. Spacing is roughly 30–50 px wide, irregular along the perspective line. Shape is **rectangular blocks aligned to world-space axes** → this is **probe-grid banding** (baseRes=32, volumeSize=4 means ~8cm probe spacing, consistent with these block sizes at this depth).
- **Far wall opening (x=420–520, y=260–380):** Soft horizontal bands stepping in luminance toward the floor. Spacing is coarse (~40 px) and aligned with depth → consistent with **cascade-boundary transition** between c1 and c3 (note c0 and c2 have anyPct=0 — they're empty).
- **No clear angular pinwheel/star artifacts** are visible, so directional-bin (dirRes=8) aliasing is not the dominant issue here.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5):** Very smooth — almost too smooth. The amplified view shows broad gradients with no high-frequency noise, suggesting heavy temporal accumulation (consistent with temporalAlpha=0.05) or aggressive spatial filtering upstream. The trade-off is visible **ghosting** on the ceiling fixtures where the orange tone smears beyond geometry edges.
- **Mode 0 (final):** Bilateral blur appears **over-smoothed on the right wall** (x=800–1280) — the wall reads as a featureless blurry brown gradient with no surface detail, and the blob in the upper right has no clear silhouette. The left wall edge (x=200–300) is also softened to the point that the railing/ledge geometry is barely discernible.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| Mode 0 (Final) | **Fair** | Plausible look, but bilateral blur destroys edge detail on side walls |
| Mode 3 (Indirect ×5) | **Fair** | Smooth, but ghosting and missing GI on side walls reveals under-sampling |
| Mode 6 (GI only) | **Poor** | Visible probe-grid banding on ceiling; large zones with zero indirect contribution |

## 5. Stats Cross-Check & Recommendation

The probe stats confirm the visual diagnosis:
- **c0 and c2 are completely empty** (anyPct=0) — only c1 (6.57%) and c3 (17.19% surf) are contributing. That's poor cascade utilization.
- **c1 has `meanLum=-nan` and `maxLum=16213.5`** — a NaN-producing fireflies in c1 is a serious red flag and likely propagates as the over-bright orange ceiling streaks.
- **c3 variance=27468** with `maxLum=6.36e-06` is nonsensical — luminance values near zero shouldn't produce huge variance unless there's a sign/precision bug or division artifact.

**Concrete recommendation:** **Raise `temporalAlpha` from 0.05 → 0.12** *and* **clamp/sanitize c
