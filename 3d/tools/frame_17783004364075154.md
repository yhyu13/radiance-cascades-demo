# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783004364075154_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783004365125104_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783004366490748_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17783004364075154.json`  
**Analyzed:** 2026-05-09T12:21:07  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Render

## 1. GI Contribution Analysis

Comparing the three modes, indirect light is contributing meaningfully in these locations:

- **Back wall (center, mode 6)**: Picks up a faint reddish tint on the left half and greenish tint on the right half — classic Cornell color bleed, visible as the mid-grey gradient instead of pure white.
- **Floor between/under the boxes (mode 6)**: Shows a soft greenish wash on the right-facing surfaces and reddish on the left-facing surfaces of the tall box.
- **Pumpkin (orange sphere)**: Appears brightly in mode 6, indicating it is an emissive source contributing significant indirect light to the right wall area.
- **Indirect is absent / very weak**: The ceiling (already near the light) and the floor area furthest from the light show almost no GI contribution — the floor reads near-black in mode 6.

## 2. Banding Artifacts in Mode 6

- **Concentric arc bands on the ceiling and floor**: Tight, regularly spaced curved black/grey stripes wrapping around what looks like the camera-relative origin. The spacing is uniform — this is **probe-grid aliasing** (baseRes=32 voxels across volumeSize=4 → ~0.125 unit probe spacing), where the raymarch is sampling between probes that haven't converged.
- **Diagonal hard edge on the left (red) wall**: A sharp linear discontinuity cutting across the lower-left wall. The straight, axis-aligned shape and its position roughly midway through the volume suggests a **cascade-boundary seam** between c0 and c1.
- **Vertical streaks on the tall box**: Spaced regularly, matching the box's voxel resolution rather than directional bins — likely surface self-occlusion at probe sample points, not dirRes (8) banding.

No clear directional-bin pinwheel/star artifacts visible, so dirRes=8 appears adequate for this scene.

## 3. Smoothness Assessment

- **Mode 3 (5× indirect)**: Spatially **smooth on the large wall surfaces** (good temporal accumulation from temporalAlpha=0.05), but the arc banding is still visible on the ceiling and the cascade seam is preserved on the red wall. Not noisy — structured artifacts dominate over stochastic noise.
- **Mode 0 (final)**: The bilateral blur has **not over-smoothed** geometric edges — the box silhouettes, pumpkin, and wall corners remain crisp. However, the blur has **failed to hide the arc bands and the diagonal cascade seam** on the red wall, which still bleed through into the composite.

## 4. Mode Ratings

| Mode | Rating | Reasoning |
|------|--------|-----------|
| 0 (Final) | **Fair** | Composite is readable and color-bled correctly, but cascade seam on red wall is visible in the final image. |
| 3 (Indirect ×5) | **Fair** | Smooth where it should be, but reveals probe-grid arcs and cascade seam clearly. |
| 6 (GI only) | **Good** as a diagnostic | Exactly the dynamic range you want for inspection; reveals all the structural problems. |

## 5. Stats Cross-Check & Recommendation

Key observations from stats:
- **anyPct=100 across all cascades**: probes are all hitting something — no empty regions.
- **c0 surfPct=74.8%**: ~25% of c0 rays miss surfaces (escape the small c0 range). c0MinRange=1 may be too short for this 4-unit volume.
- **Variance drops sharply** c0(0.0056) → c3(0.00008): higher cascades are well-converged; the **visible banding is dominated by c0/c1**, matching the cascade-seam observation on the red wall.
- **temporalAlpha=0.05** is already quite low (heavy temporal smoothing), so the persistent bands are *spatial*, not temporal noise.

**Concrete recommendation: Increase `c0MinRange` from 1 to ~1.5–2.0** (and proportionally bump `c1MinRange`). This will:
- Push the c0→c1 transition further from camera, hiding the diagonal seam on the red wall.
- Raise c0 surfPct closer to c1's 81%, reducing the under-sampled fraction that's causing the structured banding.

If the seam pers
