# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782524226904480_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782524227531019_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782524228142241_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17782524226904480.json`  
**Analyzed:** 2026-05-08T23:00:52  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing the three modes is difficult because **mode 0 (final) appears to show no scene geometry at all** — it's a flat dark grey (~#2C2C2C) with no visible Cornell Box walls, no color bleeding, no shadows. This suggests either the direct lighting pass is not contributing, or the camera/scene is not set up as expected for this frame.

Modes 3 and 6 do show the GI structure: a series of **concentric soft rings centered slightly below the image center (~640, 360)**, with a darker central disk (~radius 100px) and progressively fainter outer rings extending to the corners. Mode 3 (5× amplified) shows these clearly as grey-on-grey gradients; mode 6 confirms they are real (not amp noise) but extremely dim — peak indirect luminance is on the order of 0.01–0.05.

Indirect light is **brightest in the outer mid-field** (rings around radius 300–500px) and **darkest in the central disk** and far corners. There is no recognizable Cornell wall/floor color bleed — only radial probe structure.

## 2. Banding in Mode 6

The bands are:
- **Shape:** concentric circular/elliptical rings, not axis-aligned grids
- **Spacing:** roughly 100–150 px between visible ring boundaries, with 3–4 nested rings visible
- **Center:** offset slightly below frame center

This pattern is **not consistent with probe-grid aliasing** (which would produce rectilinear/axis-aligned blockiness) and **not directional-bin banding** (which would show angular wedges). The concentric, view-centered rings are most consistent with **cascade-boundary transitions** — each cascade's spatial range ends at a roughly spherical shell around the camera, and the abrupt change in probe density between cascades creates ring artifacts.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5):** Spatially **smooth, not noisy**. No high-frequency speckle is visible; the rings are gradients, not stochastic. This is consistent with `temporalAlpha: 0.05` (heavy temporal smoothing) and `variance` values in the 1e-4 range.
- **Mode 0 (final):** Cannot assess bilateral over-smoothing because no edges are visible at all. The image is a near-uniform color.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Poor** | No visible scene content; cannot validate composite |
| 3 (Indirect ×5) | **Fair** | Smooth, but reveals clear cascade-boundary rings |
| 6 (GI only) | **Fair** | Confirms rings are real GI structure, not amp artifacts |

## 5. Stats Cross-Check & Recommendation

Stats observations:
- `anyPct: 100` across all cascades — probes are fully populated ✓
- `surfPct` rises 81.6 → 85.2 → 100 → 100 across cascades C0→C3, indicating outer cascades hit only surfaces (no sky escape, `skyPct: 0` everywhere)
- `meanLum` falls monotonically 0.0158 → 0.0149 → 0.0127 → 0.0068 — **C3 contributes ~half the radiance of C0**, so cascade blending weight matters a lot
- `variance` drops 7.1e-4 → 4.8e-4 → 2.4e-4 → 6.6e-6 — C3 is essentially flat, but C0/C1 have enough variance to produce the visible rings

The C0→C1 luminance step (0.0158 → 0.0149, ~6%) and the C2→C3 step (0.0127 → 0.0068, ~46%) are uneven — the **C2/C3 boundary is the dominant ring source**.

**Concrete recommendation:** Increase `c0MinRange` and `c1MinRange` from `1` to `~1.5–2.0`, and/or widen the cascade overlap/blend region. This will smooth the radiance falloff between cascades and dissolve the concentric rings without changing temporal behavior. As a secondary check, investigate why mode 0 shows no direct lighting — the GI tuning is moot if the composite is broken.
