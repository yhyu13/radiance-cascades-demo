# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785568510996746_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785568513639328_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785568516529514_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17785568510996746.json`  
**Analyzed:** 2026-05-12T11:34:41  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing mode 0 (final) against mode 6 (GI only) and mode 3 (×5 amplified):

- **Left wall (light-colored)**: Strong indirect contribution visible in all three modes. The carved/relief structure on the left receives the brightest GI — visible as warm cream tones in mode 6, exaggerated to near-white in mode 3. This is the primary bounce surface.
- **Right wall (red/brown)**: Receives moderate indirect light, dominated by red color bleed from itself. In mode 3, you can see the smooth red gradient falling off toward the back — confirming the wall is lit almost entirely by GI (no direct light reaches it strongly).
- **Floor**: Almost completely dark in mode 6 and mode 0. Mode 3 reveals only a faint warm glow near the back doorway. Indirect light is **absent on the floor** — this is the clearest deficit.
- **Ceiling**: Bright direct-lit strips (the light fixtures) in mode 0 are absent in mode 6 — confirming those are direct sources, not GI.
- **Back doorway**: Visible warm glow in mode 3 indicates GI is leaking/propagating through the opening correctly.

## 2. Banding in Mode 6

- **Right wall**: Faint **horizontal/diagonal bands** following the perspective of the wall, spaced roughly every 30–50 px in screen space at mid-distance. The spacing increases with distance, consistent with a **fixed world-space probe grid** (baseRes=16, volumeSize=4 → ~0.25 unit spacing) seen in perspective. This is **probe-grid origin**, not directional.
- **Left wall lower region**: A soft vertical seam around x≈250 where bright GI abruptly transitions to darker floor — likely a **cascade boundary** between c0 (0.5 range) and c1 (1.0 range).
- **No visible angular/pinwheel artifacts** that would indicate directional-bin aliasing from dirRes=8.

## 3. Smoothness Assessment

- **Mode 3** looks **very smooth** — almost over-smoothed. No high-frequency noise, which is consistent with temporalAlpha=0.05 (heavy temporal accumulation). The blur is so strong that the doorway glow is a soft blob with no shape.
- **Mode 0** shows mild edge preservation on the left-wall reliefs but the bright vertical highlights in the lower-left look **smeared horizontally** — bilateral blur appears to have crossed a depth/normal edge near the window-like structure. The floor reflection streaks are sharp (direct), so the over-smoothing is GI-channel only.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| Mode 0 | **Fair** | Plausible look, but floor lacks bounce, left-wall GI is over-smoothed |
| Mode 3 | **Fair** | Too blurry to diagnose detail; useful only for gross distribution |
| Mode 6 | **Good** | Clearly reveals probe-grid banding and GI absence on floor |

## 5. Stats Cross-Check

The cascade stats show serious problems:
- **c0**: anyPct=2.1% — almost no probes hit anything (under-sampled near-field)
- **c1**: anyPct=95.7%, **meanLum=NaN, variance=NaN** — NaN propagation, likely from a divide-by-zero or bad ray. This explains the smooth-but-wrong indirect term.
- **c2**: anyPct=0 but variance=27468 with negative meanLum (-275) — **corrupt accumulator state**.
- **c3**: anyPct=0 but surfPct=87.5% — inconsistent (surfPct should be ≤ anyPct).

The **NaN in c1** is the smoking gun: it explains why the floor receives no GI (NaNs being clamped/rejected downstream) and why mode 3 looks unnaturally smooth (heavy temporal carry-over of the last valid frame).

## 6. Concrete Parameter Change

**Raise `temporalAlpha` from 0.05 → 0.15** *and* add a NaN guard before accumulation.

Rationale: with anyPct fluctuating between 2% (c0) and 96% (c1), the current alpha=0.05 is locking in stale/NaN values for ~20
