# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782248827480207_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782248835009113_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782248842489174_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17782248827480207.json`  
**Analyzed:** 2026-05-08T15:21:58  
**Model:** claude-opus-4-7

---

## Visual Triage

### 1. GI contribution isolation
Comparing mode 0 (final) against mode 6 (GI only):
- **Strong indirect contribution** on the ceiling near the center-front (bright warm/neutral hotspot in mode 6 — this is light bouncing back up off the floor and the two blocks toward the ceiling/light-source area).
- **Color bleed** is visible but subtle: the tall block's right face picks up a faint green tint from the right wall, and its left/shadow side picks up a faint red tint from the left wall (most obvious in mode 6, confirmed in mode 3 amplified).
- **Floor between the blocks** receives noticeable indirect fill — it is not black in mode 6, indicating multi-direction probe coverage there.
- **GI is essentially absent outside the box** (the gray top/bottom slabs and the lavender side bars contribute no indirect light — black halo around the box in mode 6), which is correct since they are outside the lit volume.
- **Back wall** has a smooth vertical gradient in mode 6 — brighter at top (near light) fading down — consistent with expected falloff.

### 2. Banding in mode 6
- A **soft rectangular halo** outlines the entire box silhouette (a 1–2 probe-cell-wide bright fringe just inside the black region, and a corresponding dim band just inside the colored walls). Spacing ≈ one cell of a 32³ base grid → **probe-grid leakage at the volume/empty boundary**, not directional bins.
- **No fine angular streaks or pinwheel patterns** are visible on the walls → directional binning (dirRes=8) is adequate for this smooth diffuse scene.
- **No abrupt luminance step across mid-frame** → cascade boundaries (c0/c1/c2/c3) are blending cleanly; consistent with the gentle variance falloff in stats (0.0085 → 0.00016).

So the banding is **probe-grid sourced (trilinear leak at occluder/empty transitions)**, not directional or cascade.

### 3. Smoothness assessment
- **Mode 3 (indirect ×5):** spatially very smooth — almost no high-frequency noise. The walls show clean gradients; if anything it looks **over-filtered/under-resolved** rather than noisy. Temporal alpha of 0.05 plus large probe footprint is doing heavy lifting.
- **Mode 0 (final):** bilateral blur has **over-softened the contact shadow** under the tall block (the shadow's leading edge on the floor is mushy, and the block's silhouette against the back wall is slightly haloed). The cube on the right also shows a soft shadow boundary rather than a crisp one.

### 4. Ratings
- **Mode 0 (final):** **Good** — pleasing, plausible Cornell box, but contact shadows are blurred.
- **Mode 3 (indirect ×5):** **Fair** — too smooth/washed; geometry detail (block edges, corner creases) barely survives the GI term.
- **Mode 6 (GI only):** **Fair** — correct color bleed direction and ceiling bounce, but probe-grid halo around the box silhouette is clearly visible.

### 5. Stats cross-check & recommended change
- `surfPct` 98.3 / 99.2 / 100 / 100 → all cascades well-converged, no under-sampling.
- Variance ladder 0.0085 → 0.0083 → 0.0044 → 0.00016 → healthy decay across cascades; c3 is essentially flat (no spatial structure left), consistent with no visible cascade seams.
- `temporalAlpha = 0.05` is very low → explains the smoothness in mode 3 and likely contributes to leak persistence at the box boundary (stale probes near silhouettes get blended slowly).
- `probeJitterScale = 0.06` with `jitterHoldFrames = 2` — small jitter held for 2 frames; combined with low alpha, boundary probes barely move.

**Concrete change:** raise **`temporalAlpha` from 0.05 → ~0.10–0.12**. This will let the silhouette/boundary probes update faster and reduce the rectangular halo in mode 6 without sacrificing much smoothness (variance is already low). If the contact-shadow softness in mode 0 persists, **also reduce the bilateral blur radius** (or its range sigma) so the indirect term doesn't bleed across the block-floor boundary. Do not
