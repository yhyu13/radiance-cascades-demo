# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782273616319496_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782273618486212_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782273620489285_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17782273616319496.json`  
**Analyzed:** 2026-05-08T16:03:12  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Render

## 1. GI Contribution Analysis

**Where indirect light is present (visible in modes 3 & 6):**
- **Floor**: Strong fill in the front portion of the floor, especially under/around the tall left block — clearly catching bounced light from the white walls and ceiling.
- **Back wall (center)**: Bright bloom directly under the ceiling light position, indicating the light bouncing off the back wall back toward the camera.
- **Left wall inner face**: Faint red wash from the red wall bouncing onto the front of the tall block (visible in mode 6 as a pinkish tint on the block's left side).
- **Right wall inner face**: Symmetrical green tint on the short cube's right face.
- **Ceiling corners**: The mode 6 image shows the ceiling reddening on the left half and greening on the right — classic Cornell color bleed from the side walls.

**Where indirect is absent / weak:**
- The **outer black region** correctly receives no GI — good (no leakage outside the volume).
- The **gray frame bars** (top/bottom outside the box) show GI bleed in mode 3 — they are receiving indirect light through the volume bounds, slightly suspicious.

---

## 2. Banding Artifacts in Mode 6

Looking carefully at mode 6:
- **Soft circular falloff on the back wall** around the ceiling light reflection — this is smooth, not banded. Consistent with `dirRes: 8` directional resolution being adequate for diffuse GI.
- **No sharp probe-grid checkerboard** is visible. With `baseRes: 32` over `volumeSize: 4`, probe spacing is fine enough relative to image scale.
- **Slight blocky transition** at the boundary between the lit floor and the shadowed area under the tall block — this looks like a **probe-grid** artifact (axis-aligned, ~1 probe-cell wide) rather than directional binning (which would appear as angular wedges).
- **No cascade-boundary ring** visible — cascade transitions are well-blended (corroborated by the smooth variance falloff: 0.0085 → 0.0083 → 0.0044 → 0.00016 across cascades).

---

## 3. Mode 3 Smoothness & Mode 0 Blur Assessment

**Mode 3 (indirect ×5)**: Spatially **very smooth**, almost too smooth — no visible noise grain. This is consistent with `temporalAlpha: 0.05` (heavy temporal accumulation, ~20-frame effective history). The amplified view shows soft gradients with no fireflies or speckle.

**Mode 0 bilateral blur**: 
- Cube edges remain crisp (good — bilateral edge-stopping is working on the geometry).
- However, the **shadow under the tall block** has soft penumbra that looks slightly over-blurred — the contact shadow at the block's base is less defined than expected for direct lighting.
- The **gray frame bars** show light leakage from the box interior, suggesting the bilateral filter is bleeding GI across the box-frame boundary (depth/normal weights too loose there).

---

## 4. Ratings

| Mode | Rating | Notes |
|------|--------|-------|
| Mode 0 (Final) | **Good** | Convincing color bleed, crisp geometry; minor leak onto frame bars |
| Mode 3 (Indirect ×5) | **Good** | Very smooth, plausible distribution; reveals frame-bar leak |
| Mode 6 (GI only) | **Fair** | Readable but mildly blocky on floor; cascade 3 is nearly flat (maxLum 0.054) — under-contributing |

---

## 5. Stats Cross-Check & Parameter Recommendation

**Convergence**: All cascades report `anyPct: 100` and `surfPct ≥ 98%` — probes are well-populated. No under-sampling.

**Variance vs banding**: Cascade 0–1 variance (~0.0085) is moderate and matches the soft floor blocking I see. Cascade 3 variance is essentially zero (0.00016) and meanLum is half of cascades 0–2 — **the outermost cascade is contributing almost nothing useful** but is still being sampled.

### Concrete recommendation:
**Reduce `cascadeCount` from 4 → 3**, OR **increase `c1MinRange`** so cascade 2/3 cover more world space.
