# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784776952525190_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784776954116190_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784776956221702_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784776952525190.json`  
**Analyzed:** 2026-05-11T13:35:26  
**Model:** claude-opus-4-7

---

# Visual Triage Report — Cornell Hallway

## 1. GI Contribution Isolation

Comparing mode 0 (final) vs mode 6 (GI only):

- **Right wall (entire right half of frame)**: GI is delivering most of the warm reddish-brown tint. In mode 6 this wall is the brightest indirect contributor (saturated red-brown bounce), and in mode 0 it dominates the right side coloration. Direct light alone would not produce that warmth.
- **Left wall (foreground, lower-left quadrant)**: Indirect is dim and cool/grey in mode 6 — almost no bounce energy reaches here. The mode 0 brightness on the left is therefore mostly direct.
- **Ceiling slats (top of frame)**: Direct-lit in mode 0 (bright with sharp dark bin patterns visible), but in mode 6 the ceiling reads as muddy red-brown — GI is picking up bounce from the red wall and spilling it onto the ceiling.
- **Floor (center vanishing strip)**: Mode 6 shows a smooth grey-brown gradient receding into a darker red pocket near the far doorway. The bright specks/dashes along the floor in mode 0 are direct-only (light leaks through ceiling gaps).
- **Far doorway (vanishing point)**: Mode 6 shows a distinct dark red "well" — GI correctly darkens because the far end has reduced sky/aperture contribution.

## 2. Banding in Mode 6

Visible structure:
- **Ceiling stripe pattern** (top center, repeating dark-light bands roughly every 40–60 px horizontally): These are **directional-bin discretization** artifacts. With `dirRes: 8` (only 8 directions per probe), the ceiling — which samples nearly-grazing bounce rays from the red wall — quantizes into discrete angular bins. The shape is curved/arched, following the perspective of the corridor, which is characteristic of dir-bin aliasing rather than probe-grid (probe-grid would show axis-aligned blocky cells).
- **Right wall vertical streaks** (mid-right, faint vertical bands): Likely **probe-grid interpolation** at `baseRes: 32` over `volumeSize: 4` (i.e., probes ~0.125 units apart). The streaks track world-space rather than screen-space.
- **No clear cascade-boundary ring** is visible — transitions between c0/c1/c2/c3 appear smooth, consistent with all four cascades reporting `anyPct: 100`.

## 3. Smoothness Assessment

- **Mode 3 (5× amplified indirect)**: Looks **spatially smooth, almost too smooth** — barely any high-frequency noise, but the doorway depth cues collapse into a soft gradient. This indicates the temporal accumulation is doing heavy lifting (`temporalAlpha: 0.05` = ~20-frame EMA).
- **Mode 0 (final)**: Bilateral blur has **over-softened the right wall**, where the red bounce bleeds into a continuous gradient with no surface detail. The ceiling slat edges, however, are preserved — bilateral is doing its job there. The most concerning over-blur is the foreground left wall corner (~x=300, full vertical), where geometry edges are smeared into a milky gradient.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (final) | **Fair** | Composite reads correctly but right-side over-blur and floor speckle hurt |
| 3 (indirect ×5) | **Good** | Smooth, low-noise, useful for debugging |
| 6 (GI only) | **Fair** | Useful but directional banding on ceiling is obvious |

## 5. Stats Cross-Check & Recommendation

Key stats observations:
- `surfPct` climbs 48.6 → 84.8 → 100 → 100 across cascades: **c0 is under-sampling surfaces** (only ~49% of c0 probes hit geometry). This explains the highest variance (0.00104) and highest maxLum (0.545) in c0 — sparse hits create hot spots.
- `dirRes: 8` is low — directly visible as ceiling banding.
- `temporalAlpha: 0.05` is very aggressive smoothing, consistent with the over-soft mode 3.
- `cascadeTimeMs: 21.55` is the dominant cost; ra
