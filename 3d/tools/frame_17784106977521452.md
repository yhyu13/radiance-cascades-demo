# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784106977521452_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784106979996840_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784106982422915_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784106977521452.json`  
**Analyzed:** 2026-05-10T18:58:46  
**Model:** claude-opus-4-7

---

## Visual Triage Report

### 1. GI Contribution Analysis

Comparing mode 0 (final) against mode 6 (GI-only) shows that **indirect light dominates almost the entire frame** — the corridor has no visible direct-lit surfaces aside from the bright streaks on the floor (likely emissive/sky leak through the ceiling slats around image-center top). The GI-only pass reproduces nearly all the warm reddish tone on the right wall and ceiling, while the left wall stays cool/neutral, suggesting the bounce light is sourced from the right-side ceiling apertures.

The bright far-end "doorway" at image center (~x=460, y=300) is present in all three modes, indicating it's a **direct light source / emissive**, not a GI hotspot. The floor streaks running from y=400 down to y=650 are present in mode 0 and mode 6 but absent/dim in mode 3 — these are **direct contributions**, not GI.

### 2. Banding in Mode 6

I can see:
- **Soft horizontal/vertical block boundaries** on the right wall around x=900–1100, y=200–500 — roughly square blocks ~80–120 px across. This spacing is consistent with **probe-grid voxel boundaries** at the coarsest cascade (volumeSize=4, baseRes=32 → fairly large world-space cells).
- **Stair-step "ribbons"** on the ceiling around x=400–700, y=0–250 — these are short, bin-aligned arcs that look like **directional-bin discretization** (dirRes=8 gives only 8×8=64 directions, which is coarse for specular-ish ceiling response).
- No clear ring-shaped cascade-boundary discontinuity is visible (consistent with stats showing only cascade 0 has any data).

### 3. Smoothness / Over-blur

- Mode 3 (×5 amplified) looks **spatially smooth, low-noise**, almost milky — temporalAlpha=0.05 is doing heavy accumulation. No speckle, but the directional-bin ribbons on the ceiling are still visible after amplification, confirming they're structural, not noise.
- Mode 0's bilateral blur has **over-smoothed the left wall and right foreground** into featureless gradients (x<200 and x>900). The floor streaks survived (high-contrast guide), but any subtle GI gradient on the lateral walls is washed out.

### 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Fair** | Plausible look but bilateral blur is too aggressive on side walls |
| 3 (Indirect ×5) | **Fair** | Smooth but reveals dir-bin ribbons on ceiling; useful diagnostic |
| 6 (GI only) | **Poor–Fair** | Probe-grid blocks visible on right wall; carries the whole image |

### 5. Stats Cross-check

The stats are alarming: **only cascade 0 has any populated probes (anyPct=0.3%)**, cascades 1–2 are empty, and **cascade 3 has negative meanLum (-375) and negative variance (-0.0156)** — that's a NaN/accumulator-corruption signature, not a valid measurement. surfPct=0 everywhere means **no probe is registering a surface hit**, yet maxLum on c0 is 21781 (a single fireflies-tier sample). The visible GI is being carried by an extremely sparse set of probes, which explains the blocky banding.

### Concrete Parameter Change

**Increase `dirRes` from 8 → 16** *and* raise `probeJitterScale` from 0.06 → 0.15.

Rationale: the ceiling ribbons in mode 3 are directional-bin aliasing (8 dirs is too few when one bright aperture dominates), and the right-wall blocks in mode 6 are spatial-probe aliasing that more jitter (combined with the existing low temporalAlpha=0.05) will dither out. Secondarily, **investigate the cascade 3 negative-luminance/NaN** before tuning further — that suggests an uninitialized-probe read path that's likely also poisoning c0's max value.
