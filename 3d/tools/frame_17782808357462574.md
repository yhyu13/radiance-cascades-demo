# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782808357462574_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782808365032655_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782808372464762_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17782808357462574.json`  
**Analyzed:** 2026-05-09T06:54:30  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Isolation

Comparing mode 0 (final) vs mode 6 (GI only):

- **Color bleed onto floor & ceiling**: Mode 6 shows clear red wash on the left half of the floor and faint green on the right half, plus a reddish/greenish tint creeping onto the ceiling near the side walls. This bleed is *present but very subtle* in mode 0 — direct light dominates.
- **Back wall**: Mode 6 reveals a bright hotspot high-center on the back wall (light fixture's indirect bounce off the ceiling). The lower-center back wall is noticeably darker.
- **Tall block's right face** in mode 6 shows a faint **green tint** — bounce from the green wall — which is a correct GI signature.
- **Shadow regions** (behind tall block on the floor, behind short block) receive almost no indirect fill — they remain dark in mode 6, meaning GI is not adequately filling occluded pockets.

## 2. Banding in Mode 6

- **Block-shaped halos** extend ~20–30 px *outside* the geometry silhouette on all four sides (visible as a soft gray rectangular fringe against the black background). Spacing matches the **base probe grid** (`baseRes=32` over `volumeSize=4` → ~0.125 unit probe spacing), and the offset corresponds to one probe cell projected to screen.
- The **stepped corners** at the top and bottom edges of the gray frame are classic **cascade-boundary** artifacts — you can see the outer cascade (coarser) extending past the inner cascade footprint.
- No clear angular streaking from the light source on the back wall — directional-bin (`dirRes=8`) banding is **not** the dominant artifact here; it's spatial probe-grid + cascade transitions.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: Spatially **very smooth**, almost over-blurred. No high-frequency noise, but the silhouettes of both blocks are washed out and bleed into the floor. Cascade-boundary halo is also visible here as gray fringes.
- **Mode 0 (final)**: The bilateral blur has **over-smoothed the contact shadow** under the short block (right) — there's no crisp ground-contact darkening. The tall block's left-side shadow on the floor survives because it's direct-shadow driven. Block edges themselves are preserved well.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 Final | **Good** | Clean, plausible; mild loss of contact shadow detail. |
| 3 Indirect×5 | **Fair** | Reveals cascade halo + probe-extent leak around geometry. |
| 6 GI only | **Fair** | Shows correct color bleed but obvious cascade-boundary stepping and weak occlusion fill. |

## 5. Stats Cross-Check & Recommendation

- `surfPct` is 98–100% across all cascades and `anyPct=100` → **probes are well-populated**, no under-sampling.
- Variance drops sharply from cascade 0 (0.0085) to cascade 3 (0.00016) — a **63× falloff**, consistent with the visible *outer halo being uniform/flat* while the inner cascade carries all structure. This confirms the rectangular fringe is a **cascade-blend** issue, not noise.
- `temporalAlpha=0.05` is already heavily smoothed (matches the lack of noise in mode 3).

**Concrete change**: Increase **`c0MinRange`** (and/or `c1MinRange`) from `1` to `2`. This extends the inner, high-resolution cascade's footprint so it covers the visible geometry instead of handing off to the coarse cascade right at the block silhouette — which is what's producing the gray rectangular halo and stepped corners. If the halo persists, secondarily tighten the cascade-blend transition width rather than touching `temporalAlpha` or `dirRes` (neither is the bottleneck given current stats).
