# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785583583878695_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785583587312845_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785583590735693_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17785583583878695.json`  
**Analyzed:** 2026-05-12T11:59:50  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box Corridor

## 1. GI Contribution Analysis

Comparing mode 0 (final) against mode 6 (GI only), the two are **nearly identical** — the direct lighting contribution appears minimal or absent in this frame. The visible illumination is almost entirely indirect:

- **Bright GI zones**: ceiling lamp blocks (right side, receding into distance), the bright doorway/opening at the far end of the corridor (~center, vanishing point), and the warm orange bounce on the right-side baseboard/ledge running the length of the corridor.
- **Absent/weak GI**: left wall mid-height (very dark), the right wall above the baseboard (falls off rapidly into shadow), and the floor center-line which should have received a stronger ceiling-lamp bounce.
- The mode 3 (×5) view confirms the indirect field is actually present and structured across the entire corridor — the right wall has clear smooth gradient falloff — meaning mode 0/6 are simply too dim, not missing data.

## 2. Banding in Mode 6

Looking at the floor in the mid-distance (around x≈400–550, y≈450–600 px), there are **discrete blocky luminance steps** roughly aligned with world-space block boundaries — these look like **probe-grid** banding (volumeSize=4, baseRes=64 → ~16 cm probe spacing), not directional bins. Spacing is regular and axis-aligned.

On the receding ceiling lamps (right side, near vanishing point) you can see two faint **isolated bright pixels** (~x=470, y=270) that don't appear in mode 0 — these are probably **fireflies from the inf/NaN luminance** in cascades 1 and 3 leaking through before clamp.

I do **not** see clear ring-shaped directional-bin artifacts or sharp cascade-boundary discontinuities in this frame.

## 3. Smoothness / Over-blur

- **Mode 3** is spatially **smooth on large surfaces** (right wall gradient is clean) but shows mild blocky structure on the floor — consistent with probe-grid resolution rather than per-pixel noise. Temporal accumulation (α=0.05) is doing its job.
- **Mode 0 bilateral blur**: the right baseboard edge remains crisp (good), but the **left-side window highlights** (~x=250, y=300–550) look smeared/streaked vertically, and the lamp hotspots on the ceiling have lost their hard edges. Mild over-smoothing on small bright features.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (final) | **Fair** | Readable, but direct term seems missing; over-blurred highlights |
| 3 (indirect ×5) | **Good** | Smooth, plausible bounce; minor probe-grid blockiness on floor |
| 6 (GI only) | **Fair** | Visible probe-grid banding on floor; firefly pixels suggest NaN/inf leakage |

## 5. Stats Cross-Check & Recommendation

The stats confirm what's on screen:
- **Cascades 0 and 2 are entirely empty** (anyPct=0, all zeros) — only c1 (23.9%) and c3 (93.9%) have data. This is a serious convergence problem: half your cascades aren't contributing.
- **NaN/inf luminance** in c1 and c3 (maxLum=29653 in c3, inf in c1) — that's the source of the firefly pixels and likely poisoning the variance estimate, which prevents the bilateral filter from making good edge decisions.

### Concrete change

**Add a hard luminance clamp before temporal accumulation** (e.g. `clamp(L, 0, 50.0)` per probe sample). The inf/NaN values are the root cause of: (a) the fireflies in mode 6, (b) the broken variance metric, and (c) likely the reason cascades 0/2 are reading as empty (a single NaN can poison reductions).

If you want a parameter knob instead: **raise `temporalAlpha` to 0.1** *only after* fixing the NaN — right now α=0.05 is locking in bad values for ~20 frames, so when an inf appears it persists. Faster blend-out will hide residual fireflies until the clamp lands.
