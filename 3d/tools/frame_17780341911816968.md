# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17780341911816968_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17780341914033223_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17780341916262253_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17780341911816968.json`  
**Analyzed:** 2026-05-06T10:23:44  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Render

## 1. GI Contribution Analysis

Comparing direct-only expectation (mode 0 minus what mode 6 shows) against the indirect terms:

- **Color bleed onto floor/ceiling**: Mode 6 shows clear red wash on the left half of the floor and faint green on the right half — classic Cornell color bleed. Mode 0 retains very little of this; the floor reads near-neutral white.
- **Tall block (left)**: Its right-facing side in mode 6 picks up warm/red bounce; the shadow-side of both blocks in mode 3 is filled in by GI rather than going black.
- **Ceiling around light**: Mode 6 shows a bright disc of bounce around the ceiling light footprint, indicating second-bounce return from the floor.
- **Missing GI**: The back wall in mode 6 is surprisingly dim in the upper-center given how bright the floor is — bounce from floor → back wall is under-represented.

## 2. Banding Artifacts in Mode 6

- **Soft halo ring around the ceiling light** on the back wall — round, ~1 probe-cell wide. Consistent with a probe-grid footprint at `baseRes=32` over `volumeSize=4` (probe spacing ≈ 0.125 units).
- **Vertical soft seam** roughly at the midline of the back wall where red/green bleed transitions through grey — this is a **directional-bin** signature (`dirRes=8` = only 64 directions per probe, so opposing-wall contributions discretize).
- **Blocky falloff on the floor** in front of the tall block — square-ish patches ~uniform in size across the floor, no abrupt scale change → I do **not** see a clear cascade-boundary seam (which would appear as a sudden ring at a fixed radius from camera).

Net: artifacts read as **directional-bin + probe-grid**, not cascade transitions.

## 3. Smoothness / Over-blur Assessment

- **Mode 3 (×5 indirect)**: Looks spatially smooth, almost too smooth — heavy temporal integration is doing the work (`temporalAlpha=0.05` ⇒ ~20-frame EMA). No high-frequency noise visible, but contact regions (block bases) are blurred into the floor.
- **Mode 0 final**: Bilateral blur has **over-smoothed contact shadows** — the tall block's shadow on the floor is a soft gradient with no crisp ground-contact line. The right block's base is better preserved. Wall/ceiling corners are slightly bleeding (red tint creeps onto the ceiling above the red wall edge).

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 Final | **Fair** | Image is plausible but contact shadows are washed out; color bleed is nearly gone. |
| 3 Indirect ×5 | **Good** | Smooth, low-noise, readable bounce structure. |
| 6 GI only | **Fair** | Visible directional-bin seam on back wall and probe-ring around ceiling light. |

## 5. Stats Cross-check & Recommendation

- `surfPct` 98–100% across all cascades → probes are well-populated, not under-sampled.
- `anyPct=100`, `skyPct=0` → fully enclosed scene, expected.
- **Variance drops 50× from C0 (0.0085) to C3 (0.00016)** — far cascades are extremely flat. Mean luminance also drops (0.053 → 0.029), suggesting the **outer cascades are under-contributing** rather than smoothing the inner ones. The visible banding is dominated by C0/C1 directional discretization, matching the high C0 variance.
- `cascadeTimeMs=0.085`, `raymarchTimeMs=0.197` — plenty of headroom.

**Concrete change**: **Increase `dirRes` from 8 → 12 (or 16)**. The mid-wall directional seam and the ringing around the ceiling-light footprint both point to angular under-sampling, not spatial under-sampling. C0 variance (0.0085) with `meanLum` 0.053 is a ~17% relative variance — directional bins are the cheapest place to spend the available 0.2 ms budget.

Secondary: if blur over-smoothing in mode 0 persists after the dirRes bump, **tighten the bilateral range sigma** rather than reducing kernel rad
