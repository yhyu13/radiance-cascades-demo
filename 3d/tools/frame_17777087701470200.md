# AI Visual Triage

**Image:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777087701470200.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17777087701470200.json`  
**Analyzed:** 2026-05-02T15:59:53  
**Model:** claude-opus-4-7  
**Note:** Phase 12a triage (image + probe stats) — geometry-coupled causes stated; software-level root causes require Phase 6b pipeline inspection.

---

## Triage Report

**1. Overall:** A Cornell Box rendered from outside/through the front opening, showing the classic red-left/green-right walls, white ceiling/floor/back wall, and two white block occluders on the floor, with the box framed by dark grey bars (top/bottom) and the colored exterior wall slabs visible on either side.

**2. Visible artifacts:**

- **Color bleeding error (offset/misaligned bleed):** The red and green tints appear *outside* the box on the left and right exterior wall slabs (the dark red and dark green rectangles flanking the interior), while the *interior* side walls show only mild bleed onto the floor — indirect color is leaking onto the wrong side of the wall geometry. This is geometry-coupled to either back-face contribution or probes sampling through the thin wall slabs.
- **Missing/weak contact shadows:** The base of both white blocks meets the floor with a soft gradient rather than a firm contact shadow; the right block in particular has almost no grounding shadow on its right side facing the green wall.
- **Mild probe-grid softness on the back wall:** A faint low-frequency luminance blob is centered on the upper back wall — consistent with sparse C0 probe interpolation rather than true banding (no hard cell edges visible).
- **No visible directional bin banding, no cascade ring seam, no shadow acne.**

**3. Not clean — but no severe banding/seams.** Primary issue is the exterior-slab color leak.

**4. Quality rating: Fair.**

---

### Stats cross-check
- `surfPct` climbs 29 → 75 → 100 → 100 across C0–C3: C0 is **under-sampled** (only ~29% of near probes hit a surface), which matches the soft, low-frequency look of the indirect lighting and the weak contact shadows.
- C0 `variance` (0.0057) is the highest of all cascades, but there's no visible hard banding — consistent with spatial smoothing dominating, not directional aliasing (so `dirRes=8` is adequate).
- C3 `meanLum` and `maxLum` collapse (0.029 / 0.053), so the far cascade is contributing very little — fine for this small scene.

### Recommended single change
Increase **`probeJitterScale`** from `0.05` → ~`0.25` (or alternatively raise C0 probe density). The low C0 `surfPct` of 29% indicates many near-field probes are missing the close geometry (block bases, wall-floor seams); more jitter will let temporal accumulation cover those gaps and tighten the contact shadows without touching directional resolution. Keep `temporalAlpha` at 0.05 so the added jitter integrates cleanly.
