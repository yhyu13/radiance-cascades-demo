# AI Visual Triage — Sequence (8 frames)

**f0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777681736725594_f0.png`  
**f1:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777681737517471_f1.png`  
**f2:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777681738232634_f2.png`  
**f3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777681739118425_f3.png`  
**f4:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777681739839800_f4.png`  
**f5:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777681740633657_f5.png`  
**f6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777681741348692_f6.png`  
**f7:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777681742286738_f7.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17777681736725594.json`  
**Analyzed:** 2026-05-03T08:30:00  
**Model:** claude-opus-4-7

---

## Cornell Box Temporal Stability Analysis

### 1. What is visually stable
The overall scene geometry, camera, red/green wall colors, the two white blocks, the cast shadows, and the broad lighting distribution (bright ceiling, color bleed onto floor) remain consistent across all 8 frames.

### 2. Regions that change

- **Red left wall (interior face)**: Subtle brightness/saturation shimmer. The red appears slightly more pink/desaturated in f0–f2 and slightly deeper/redder in f4–f7. Magnitude: small tonal drift, not a hard jump.
- **Green right wall (interior face)**: Mirror behavior to the red wall — slightly brighter/lighter green in f0–f3, marginally darker in f4–f7. Subtle.
- **Back wall near top-center and ceiling crease**: Very faint luminance wobble in the indirect-lit highlight band. Subtle shimmer rather than banding.
- **Floor in front of the taller block (shadow penumbra)**: Slight softening/hardening of the shadow edge between frames — consistent with probe jitter offsetting the GI sample point.
- **Outer dark frame edges (red/green slabs visible behind the box opening)**: Almost no change — these are direct-lit and not GI-driven.

**Temporal pattern**: The wall-tint changes track loosely with a **2-frame cadence** (f0/f1 similar, f2/f3 similar, etc.), which matches `jitterHoldFrames: 2`. This indicates the jitter cycle is *visible through* the EMA — alpha is low enough that history dominates, but the held-jitter pairs still produce a faint periodic pulse. There is also a slow monotonic drift component (red deepening over the 8-frame window) consistent with EMA still converging.

### 3. Stability rating
**Stable** (bordering on Excellent). The changes are subtle tonal shimmer on the colored walls, not flicker or swimming. Cascade stats support this: c0 variance 0.0085 is modest, c3 variance is essentially zero (well-converged coarse cascade), and surfPct is 98–100% across all cascades — probes are well-sampled, no under-coverage.

### 4. Recommended parameter change
The dominant residual artifact is the **2-frame periodic pulse** from the jitter hold aligning with a low EMA alpha that can't fully damp it. Two good options, pick one:

- **Preferred: increase `jitterPatternSize` decorrelation by reducing `jitterHoldFrames` from 2 → 1.** This breaks the 2-frame coherence so EMA averages 4 distinct jitter positions per 4 frames instead of 2 pairs, eliminating the periodic component without raising noise.
- Alternatively, **reduce `probeJitterScale` from 0.06 → 0.03**. Stats show meanLum ≈ 0.05 with maxLum 0.56 in c0 — the per-sample variance the jitter introduces on colored walls is the visible component; halving jitter amplitude directly halves the residual shimmer while keeping the same anti-aliasing benefit given the already-high coverage (surfPct ≥ 98%).

Do **not** raise `temporalAlpha` — at 0.05 it is correctly low for this scene; raising it would amplify the periodic pulse you can already see.
