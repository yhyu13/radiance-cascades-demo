# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784077466643993_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784077474368015_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784077482036349_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784077466643993.json`  
**Analyzed:** 2026-05-10T18:09:42  
**Model:** claude-opus-4-7

---

## Visual Triage: Cornell Box GI Cascade

### 1. GI Contribution Isolation

Comparing mode 0 (final) against mode 6 (GI only):

- **Color bleed onto floor**: Mode 6 shows clear red tint on the floor near the left wall and green tint near the right wall — classic Cornell bleed. This carries through to mode 0 subtly (floor is slightly warmer on the left, cooler on the right).
- **Ceiling near the light**: Mode 6 shows the bright disc on the ceiling around the area light — this is the GI feedback from the floor/walls reflecting back up. It reads correctly in mode 0.
- **Tall block's right face** (facing the green wall): mode 6 shows green spill; in mode 0 you can faintly see this on the block's right edge.
- **Short block's left face**: receives some red bounce in mode 6, visible as a warm tint.
- **Behind / outside the box (the gray "halo")**: Modes 0/3/6 all show indirect light leaking onto the *outer* faces of the box walls (the gray top/bottom bars and the saturated red/green outer slabs). This is GI bleeding through the wall — a leak, not a real bounce.

### 2. Banding in Mode 6

- **Outer frame "halo"**: Hard rectangular bands around the box silhouette (top gray bar, bottom gray bar, red slab on the left edge, green slab on the right). Spacing ≈ one probe cell at base resolution. This is **probe-grid leakage** — probes sitting outside the box wall are sampling interior radiance because the wall is thinner than one probe spacing (`baseRes 32`, `volumeSize 4` → ~0.125 unit spacing; wall thickness is comparable).
- **Soft blocky gradient on the back wall and floor in mode 6**: low-frequency, cell-sized blotches consistent with the **probe grid** (not directional bins — directional artifacts would appear as angular streaks, which I don't see).
- **No visible cascade-boundary ring**: I don't see a discrete radius where appearance changes, so cascade transitions look well-blended.

### 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: Spatially **smooth**, almost creamy — no high-frequency noise, no speckle. Consistent with `temporalAlpha 0.05` (heavy temporal integration) plus bilateral blur. Possibly *too* smooth: the tall block in mode 3 has lost most of its silhouette definition and bleeds into the back wall.
- **Mode 0 (final)**: Edges of the blocks and the wall corners are crisp — direct lighting carries the geometry. **No obvious over-blur** of primary edges. However, the contact shadow under the short block looks soft/raised, suggesting the indirect term is filling in occluded crevices (light leak through the block's base).

### 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| Mode 0 | **Good** | Clean composite, plausible bleed, but light leak halo around exterior is visible |
| Mode 3 | **Fair** | Smooth but indirect term leaks well past wall geometry; over-soft |
| Mode 6 | **Fair** | Plausible bounce pattern, but probe-grid banding on the exterior halo is the dominant artifact |

### 5. Stats Cross-check & Recommendation

Stats look healthy on paper:
- `surfPct` 98–100% across all 4 cascades → probes are finding surfaces, not under-sampled.
- `anyPct` 100% everywhere → no empty probes.
- `variance` decreases monotonically across cascades (0.0085 → 0.00016) → cascades are converging properly, with the outer cascade acting as a smooth bias term.
- `meanLum` is stable (~0.05) across c0–c2, dropping at c3 — expected.

The visible banding is **not from under-sampling** (surfPct is high) — it's from **probe spacing being coarser than wall thickness**, causing interior radiance to leak to exterior probes.

**Concrete change**: Increase `baseRes` from 32 → 48 (or shrink `volumeSize` to 3) to make probe spacing finer than the Cornell wall thickness. This directly addresses the exterior halo leak in all three modes. If memory is a concern, the cheaper alternative is to **reduce `probeJitterScale`
