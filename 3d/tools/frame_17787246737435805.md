# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17787246737435805_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17787246738165754_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17787246739575861_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17787246737435805.json`  
**Analyzed:** 2026-05-14T10:11:46  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Renders

## 1. GI Contribution Analysis

Comparing mode 0 (final) vs mode 6 (GI only) isolates the indirect term:

- **Color bleed onto floor/ceiling near walls**: The mode 6 view shows clear red wash on the ceiling-left and floor-left regions, and green wash on the ceiling-right/floor-right — classic Cornell color bleed. This is the GI doing its job.
- **Tall box's right face** picks up faint green from the right wall (visible in mode 6 around the upper-right edge of the box).
- **Short box's left face** has subtle red bleed.
- **Back wall center** in mode 6 shows a mild greenish-red mix — indirect bounce from both colored walls.
- **Areas where GI is absent**: large black bands on the ceiling and floor (concentric arcs). These are not "no light" — they're holes in the probe sampling/raymarch (see §2).

## 2. Banding Artifacts in Mode 6

The most prominent artifacts:

- **Concentric arc bands on ceiling and floor** — curved striping with spacing that grows with distance from the camera. Shape (curved, view-aligned, distance-dependent) is consistent with **raymarch step / cone-trace stride aliasing**, not probe-grid stair-stepping. The curvature follows the screen-space ray fan, which rules out cascade boundaries (those would be axis-aligned planar transitions).
- **Vertical streaks down the box sides** (visible in all three modes) — uniform spacing aligned with world axes. These look like **probe-grid sampling** at `baseRes:32` over `volumeSize:4` (≈8 cm spacing), aliased against the thin vertical box geometry.
- **No clear cascade-boundary discontinuity** is visible (no sudden luminance step at a fixed distance), which agrees with `surfPct` rising smoothly 36→82→100→100 across cascades.

## 3. Smoothness / Blur Assessment

- **Mode 3 (indirect ×5)**: spatially **smooth** on the large wall surfaces — pink/green gradients are clean, no high-frequency noise. This indicates `temporalAlpha:0.05` is doing heavy temporal integration successfully (variance in cascades is low: 0.005 → 7.6e-5).
- **Mode 0 (final)**: the bilateral blur has **not over-smoothed** the colored-wall edges (red/green wall corners stay crisp). However, the **black arc bands persist into mode 0**, meaning the blur is preserving them as edges — they're being treated as valid signal, not noise.
- The shadow under the short box and the contact between box and floor remain sharp — good edge preservation.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 Final | **Fair** | Direct lighting & color crisp, but ceiling/floor arc holes leak through |
| 3 Indirect ×5 | **Good** | Smooth, plausible bleed, no temporal noise |
| 6 GI only | **Fair** | Bleed is correct in direction but arc banding dominates dark regions |

## 5. Stats-Informed Diagnosis & Recommendation

Key stats:
- Cascade 0 `surfPct: 36%` — **only a third of c0 probes hit a surface**. This is the under-sampled cascade and likely the source of the near-field arcs (c0 covers `c0MinRange:0.5`, exactly the floor/ceiling near-camera zone where the arcs are worst).
- Cascade 0 `variance: 0.0052` is ~70× cascade 3's variance — confirms spatial structure (banding) at the finest level.
- `cascadeTimeMs: 47.6` is the bottleneck; `raymarchTimeMs: 0.15` is trivial.

**Concrete change**: increase `probeJitterScale` from **0.06 → 0.15** (and optionally drop `jitterHoldFrames` from 2 → 1). With `temporalAlpha:0.05` you already have ~20-frame integration, so larger jitter will average out the c0 arc pattern without introducing visible flicker. This directly attacks the low `surfPct` in c0 by spreading sample positions over more of the empty 64% of probes per frame.

If that's insufficient, the second lever is **`dirRes: 8 → 12`** to reduce
