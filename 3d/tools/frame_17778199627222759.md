# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17778199627222759_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17778199629209381_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17778199631350405_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17778199627222759.json`  
**Analyzed:** 2026-05-03T22:53:11  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Renders

## 1. GI Contribution Analysis

Comparing the three modes, indirect light is **clearly present** in expected locations:
- **Color bleed onto the floor**: Mode 6 shows reddish tint on the left floor and greenish tint on the right floor — classic Cornell signature.
- **Ceiling bounce**: Mode 6 reveals a bright spot on the back ceiling — light bouncing off the floor and back walls.
- **Tall block's right face** (facing camera-right): picks up green tint from the green wall in mode 6.
- **Short block's left face**: subtle red tint from the red wall.
- **GI is absent/weak** behind/underneath the blocks (correct — occluded), and on the gray frame bars (those appear unlit by GI, only direct).

The final composite (mode 0) looks plausibly lit, with soft contact shadows under both blocks.

## 2. Banding Artifacts in Mode 6

I can see the following structured artifacts:
- **Soft rectangular halo around the room opening** — the gray frame bars at top/bottom appear darker than their surroundings, with a visible discontinuity at the cascade/volume boundary. This is consistent with a **cascade-boundary transition** where the outer cascade (c3, very low maxLum 0.054) takes over and dims abruptly.
- **Low-frequency blobby gradients** on the back wall and floor, with cell sizes roughly matching a **32³ base probe grid** projected to screen — these are probe-grid interpolation artifacts, not directional bins.
- **No fine pinwheel/spoke pattern** is visible that would indicate dirRes=8 directional aliasing — the gradients are smooth angularly.

So: dominant artifact is **probe-grid spacing + cascade boundary**, not directional bin count.

## 3. Smoothness Assessment

- **Mode 3 (×5 indirect)**: Looks **spatially smooth**, almost too smooth — no high-frequency noise, but block edges are very soft. This indicates heavy temporal accumulation (consistent with `temporalAlpha: 0.05`) plus probe interpolation blur.
- **Mode 0 (final)**: The bilateral blur appears **well-tuned** — block silhouettes remain crisp, contact shadows are preserved. No obvious over-smoothing on geometric edges. The slight color bleed on the left wall edge of the tall block looks correct.

## 4. Ratings

| Mode | Rating | Notes |
|------|--------|-------|
| Mode 0 (final) | **Good** | Plausible Cornell, crisp edges, correct shadows |
| Mode 3 (indirect ×5) | **Fair** | Smooth but lacks detail; cascade seam visible at frame edge |
| Mode 6 (GI only) | **Fair** | Color bleed correct, but cascade boundary darkening is obvious |

## 5. Stats Cross-Check & Recommendation

Stats show:
- `surfPct` 98–100% across all cascades → **probes are well-fed**, not under-sampled.
- `variance` drops 50× from c0 (0.0085) to c3 (0.00016), and `maxLum` drops 10× — c3 is essentially **flat and dim**, which explains the dark halo where c3 dominates.
- `anyPct: 100` everywhere → no missing data.
- `temporalAlpha: 0.05` is very low → explains the over-smoothed look in mode 3.

**Concrete suggestion**: **Raise `c1MinRange` (and/or extend cascade overlap) so cascade 3's dim contribution doesn't show through as a boundary halo around the frame.** Alternatively, if cascade range is fixed, **increase `temporalAlpha` to ~0.1** — this would let new lighting integrate faster and slightly sharpen indirect detail without sacrificing stability (current 0.05 is over-damping the mode-3 view).

If forced to pick one: **`temporalAlpha: 0.05 → 0.1`** — addresses the visible over-smoothing in mode 3 with no risk to the already-converged probe coverage.
