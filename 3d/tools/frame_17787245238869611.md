# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17787245238869611_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17787245239763958_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17787245241304734_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17787245238869611.json`  
**Analyzed:** 2026-05-14T10:09:16  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing mode 0 (final) against modes 3 and 6, indirect light is doing the following work:

- **Brightest indirect contribution**: along the **left wall** (upper-left quadrant), where mode 3 shows a strong warm/white wash and mode 6 confirms it as the dominant non-direct source. This is consistent with bounced light from the ceiling emitters spilling onto the wall.
- **Floor (lower-center)**: mode 3 shows a clear soft gradient brightening toward the camera; in mode 6 this reads as a muted warm tint coming off the right-side red rail.
- **Indirect absent / dark**: the **far end of the corridor** (centered dark rectangle around the doorway/recess at ~x=460, y=290) stays dark in all three modes — GI is not reaching that pocket. Also the **right wall lower half** has almost no indirect lift; mode 6 shows it taking direct red bleed only from the rail, not from bounced fill.

## 2. Banding in Mode 6

Visible banding patterns:
- **Vertical/diagonal soft bands on the left wall** (around x=100–300), spaced roughly 40–60 px apart, aligned with the corridor's perspective. Spacing roughly doubles as you move toward the far end — classic **cascade-boundary** signature (c0→c1→c2 transitions along view depth).
- **Blocky chunks on the ceiling** near the lights — these are **probe-grid** footprints (baseRes=32 over volumeSize=4 → ~12.5 cm probe spacing, clearly visible at this camera distance).
- I do **not** see clean angular pinwheel/star artifacts that would indicate dirRes=8 directional-bin aliasing dominating; the structure is positional, not directional.

## 3. Smoothness of Mode 3 vs Edge Preservation in Mode 0

- **Mode 3** is mostly smooth across large surfaces (left wall, floor) but shows **low-frequency blotching** near the ceiling lights and a smeared halo around the dark doorway. Not noisy per-pixel — temporal accumulation (alpha=0.05) is doing its job — but spatially under-resolved.
- **Mode 0**: the bilateral blur has **over-smoothed** the geometric silhouettes on the left side (the white shapes at ~x=200–350 lose their edges into the wall) and the near rail on the right is bleeding red into the floor. Sharp depth discontinuities at the doorway are preserved, however.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Fair** | Readable, but bilateral kernel is too aggressive; left-side detail mush. |
| 3 (Indirect ×5) | **Fair** | Smooth on flats, but cascade banding and probe blockiness exposed. |
| 6 (GI only) | **Good (as a diagnostic)** | Clearly reveals where GI is and isn't contributing. |

## 5. Stats Cross-Check & Parameter Recommendation

Stats observations:
- All cascades at `anyPct=100` — good coverage.
- **c0 surfPct = 48.6%** is low — half of cascade-0 probes aren't hitting surfaces, which explains the **near-field blotching** and the dark doorway pocket (c0 is responsible for short-range detail there).
- **c0 variance (0.00116) is ~60× c3 variance** — high spatial structure in c0 matches the visible banding I called out on the left wall.
- `cascadeTimeMs = 43.35` is the bottleneck; raymarch is negligible.
- `temporalAlpha = 0.05` is already low (heavy accumulation), so noise isn't the issue — under-sampling is.

**Concrete change**: **Increase `c0MinRange` from 0.5 → ~0.9** (or alternatively bump `baseRes` from 32 → 48 for cascade 0 only). The current 0.5 m c0 range under-covers near-field surfaces in this corridor (hence surfPct=48%), forcing c1 to fill in and producing the cascade-boundary bands on the left wall. Widening c0's reach should push surfPct toward 70–80% and reduce both the banding and the dark doorway pocket without touching temporal stability.

If perf budget permits a second tweak: drop the bilateral blur rad
