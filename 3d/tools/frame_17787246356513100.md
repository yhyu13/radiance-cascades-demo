# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17787246356513100_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17787246357524028_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17787246358324607_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17787246356513100.json`  
**Analyzed:** 2026-05-14T10:11:08  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing mode 0 (final) against mode 6 (raw GI) and mode 3 (indirect ×5):

- **Color bleeding is present and correctly directional.** Mode 6 shows clear red tint on the back wall's left half and on the left side of the front-facing tall block. Green bleed is visible on the back wall's right portion and the right face of the short block (foreground).
- **Ceiling near the light** receives almost no indirect contribution (it's already lit directly and faces away from bounce sources) — correct.
- **Floor area** (the dotted/banded region at bottom) shows essentially zero indirect light in mode 6, even though it should be receiving bounce light from both colored walls. This is suspicious — likely the floor isn't being sampled by probes properly (see banding below).
- **The shadowed left face of the tall block** (in mode 0 it's nearly black) gets only weak red GI in mode 6 — indirect fill there is under-powered.

## 2. Banding Artifacts in Mode 6

Three distinct banding patterns are visible:

- **Concentric/arced dotted bands on the floor and ceiling**, spaced roughly every 8–15 px and curving around the camera nadir/zenith. Shape (radial arcs centered on view axis) + the fact they appear on near-grazing surfaces strongly suggests **raymarch step aliasing against the probe grid** — i.e. the cone/ray intersects probe cells at oblique angles and samples sparse directional bins. With `dirRes: 8` (only 64 directions per probe), grazing surfaces alias heavily.
- **Vertical streaks down the tall block** (visible in all three modes) — these align with cascade-0 probe spacing (`baseRes:32`, `volumeSize:4` → ~0.125 unit probe spacing). Spacing of streaks looks consistent with probe-column projection.
- **No obvious cascade-boundary ring** is visible in the mid-room — cascade transitions appear smooth.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5):** Spatially **smooth** on the walls and back wall — temporal accumulation (`alpha:0.05`) is doing its job. No high-frequency noise. The pink/green washes are even.
- **Mode 0 (final):** The bilateral blur has **not over-smoothed** wall edges — the red/green wall boundaries against the back wall remain crisp. However, the floor banding survives into the composite, meaning the bilateral filter isn't catching that low-frequency structured artifact (as expected — bilateral preserves it because it's not noise).
- The shadow edge on the left wall (cast by the tall block) is preserved cleanly in mode 0.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Good** | Clean composite, correct bleed, crisp shadows; floor banding the only blemish |
| 3 (Indirect ×5) | **Good** | Smooth, well-converged on walls; reveals weak floor/ceiling sampling |
| 6 (GI only) | **Fair** | Banding clearly visible; useful as a diagnostic but shows the dirRes ceiling |

## 5. Stats Cross-Check & Parameter Recommendation

Key observations from stats:
- **Cascade 0: `surfPct: 36.5%`** — only a third of c0 probes hit a surface. This is the under-sampled cascade and matches the floor/ceiling arc banding (grazing rays from near-camera probes miss geometry within c0's 0.5-unit range).
- **Cascade 0 variance (0.0052) is ~70× cascade 3's** — high spatial structure in c0 = the banding we see.
- **`dirRes:8`** (64 dirs) is the likely root cause of directional aliasing on grazing surfaces.

**Concrete recommendation:** **Increase `dirRes` from 8 → 12** (144 directions, ~2.25× cost on the 21.5 ms raymarch → ~48 ms; if that's too steep, try 10 first).

Rationale: the arc-shaped floor/ceiling bands are directional-bin aliasing, not temporal noise (mode 3 is already smooth) and not probe spacing (streaks are a separate, milder artifact). More directions per probe will dissolve the arcs without needing more probes or lower tem
