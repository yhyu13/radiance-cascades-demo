# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784848402121599_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784848403213961_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784848403928599_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784848402121599.json`  
**Analyzed:** 2026-05-11T15:34:30  
**Model:** claude-opus-4-7

---

## Visual Triage

### 1. GI contribution isolation

The camera is pressed against / inside geometry — the large black silhouette dominating the centre-left is a back-facing or unlit surface (no direct light, no GI hit). The interesting GI signal lives in the periphery:

- **Right wall (right ~40% of frame):** strong warm reddish-brown wash, brightest in the upper-right quadrant. Mode 6 confirms this is genuine indirect (red-wall bounce), not direct. Mode 3 shows the same region as a uniform pale tan once amplified — the colour tint in mode 0/6 comes from albedo modulation, not from the probe itself.
- **Left wall (left ~15%):** much dimmer warm grey, with a small bright lobe near the mid-left edge (visible in all three modes) — likely a light-leak or a probe that sees the light source through the thin wall.
- **Floor (bottom-right):** smooth falloff from warm to dark moving away from the right wall — consistent indirect bounce.
- **Black region:** GI is correctly zero there (mode 6 is also black) — not a GI bug, just a back-face / occluded sample.

### 2. Banding in mode 6

- **Right wall, upper-right:** broad, low-frequency horizontal/diagonal **luminance bands** roughly 80–120 px apart. Spacing matches probe-grid spacing at this distance (baseRes 32 over volumeSize 4 ⇒ ~0.125 u between probes, projected). This is **probe-grid trilinear interpolation banding**, not directional-bin.
- **Left wall:** a small bright **blob near mid-height** with a sharp edge — single-probe leak through wall, classic cascade-0 boundary issue.
- I do **not** see ring-shaped or wedge-shaped artifacts that would indicate directional-bin (dirRes=8) aliasing, nor abrupt brightness steps that would indicate cascade-boundary seams.

### 3. Smoothness / over-blur

- **Mode 3** is extremely smooth — almost flat across the right wall. With a 5× amplification and still no visible noise, the temporal accumulation (alpha 0.05) is doing its job; variance stats agree (1e-3 → 9e-6 across cascades).
- **Mode 0** matches mode 6 closely; the bilateral blur has not visibly destroyed edges (the silhouette of the black geometry is crisp). However, the small bright leak on the left wall has been **smeared outward** in mode 0 vs mode 6 — the blur is propagating the leak rather than suppressing it.

### 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 Final | **Fair** | Crisp edges, plausible colour, but light-leak smeared by blur |
| 3 Indirect ×5 | **Good** | Very smooth, low noise, confirms temporal stability |
| 6 GI only | **Fair** | Visible probe-grid banding on right wall, leak on left |

### 5. Stats cross-check & parameter recommendation

- `surfPct` for cascade 0 is only **48.6%** — nearly half of c0 probes never hit a surface. That is the proximate cause of the right-wall banding: c0 should be the high-detail cascade here, but it's sparsely populated, so interpolation reaches further than it should.
- Variance drops cleanly across cascades (1e-3 → 9e-6), consistent with the smooth low-frequency banding rather than high-frequency noise.
- `maxLum` 0.54 in c0 vs 0.26 in c1 → a few c0 probes are very bright (the leak), and temporalAlpha 0.05 is slow to wash them out.

**Concrete change:** raise `c0MinRange` from 0.5 → ~0.9 (or increase `baseRes` from 32 → 48). Either gives c0 probes longer rays so `surfPct` climbs from ~49% toward the ~85% of c1, which should eliminate the right-wall banding without touching the smooth temporal behaviour. If only one knob: **`c0MinRange = 0.9`** — cheapest, directly addresses the under-sampled-probe stat that matches the visible artifact.
