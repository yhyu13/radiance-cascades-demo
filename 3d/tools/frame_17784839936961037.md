# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784839936961037_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784839937257462_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784839937794511_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784839936961037.json`  
**Analyzed:** 2026-05-11T15:20:24  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing mode 0 (final) with mode 6 (GI only) against expected direct lighting:

- **Ceiling strip (top center/right)**: The bright orange-brown "stair-step" pattern visible across the ceiling in all three modes is the dominant GI source — likely emissive geometry or strongly lit blocks bouncing warm light down the corridor.
- **Right wall**: Receives strong warm indirect bounce (visible orange/brown tint in modes 0 and 6), consistent with the ceiling emitter casting sideways.
- **Left wall**: Noticeably darker/cooler in mode 6, indicating the GI is asymmetric — left wall is in indirect shadow relative to the right.
- **Floor**: The bright streaks down the floor center (mode 0) are mostly direct lighting; mode 6 shows the floor is fairly dark in GI terms, only picking up warm bounce near the back of the corridor.
- **Far end of corridor**: A bright warm "doorway" glow appears in all three modes — this is the GI carrying the emitter's color down the depth axis.

## 2. Banding in Mode 6

- **Ceiling**: Sharp rectangular blocky patches with ~16–24 px spacing — these align with **probe grid spacing** (baseRes 32 over volumeSize 4 ≈ 8 units/probe, then projected through perspective). The blocks are axis-aligned to world space, not screen space.
- **Right wall (mid-depth)**: Broad soft orange smear with no sharp internal banding — this looks like cascade interpolation rather than directional bins.
- **Far wall transition**: A faint vertical seam roughly 2/3 across the image where warm tone drops to neutral — consistent with a **cascade boundary** (likely C0→C1, given c0MinRange=0.5).
- I do **not** see clear directional-bin pinwheel artifacts (which would manifest as 8-fold rotational patterns), so dirRes=8 appears adequate.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: Spatially smooth overall — no visible per-pixel noise, but coarse blocky structure on the ceiling and a soft halo around the far doorway. Variance stats confirm this (C0 variance 0.001 is low).
- **Mode 0 (final)**: The bilateral blur has **over-smoothed the right wall**, producing the smeared brownish silhouette that bleeds across what should be a defined wall edge. The floor highlights remain crisp, so the blur is preserving direct-lighting edges but losing GI edge detail.
- The "ghost" dark blob on the right side of mode 0 looks like blur-leakage from an out-of-frame occluder being smeared across the wall.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| Mode 0 (final) | **Fair** | Over-blurred GI on right wall, ghosting artifact |
| Mode 3 (×5) | **Good** | Clean and smooth; reveals cascade structure clearly |
| Mode 6 (GI only) | **Fair** | Probe-grid blocking on ceiling clearly visible |

## 5. Stats Cross-Check & Recommendation

**Convergence**: anyPct=100 across all cascades is healthy. surfPct rises 48→85→100→100, which is normal (outer cascades cover larger volumes with more surface hits). C0's surfPct of 48% combined with its **highest variance (0.00103)** and **highest maxLum (0.54)** matches the visible ceiling blockiness — C0 is under-sampled where it matters most.

**Concrete change**: **Increase `probeJitterScale` from 0.06 to ~0.15–0.20** and consider **raising `temporalAlpha` from 0.05 to ~0.08**.

Rationale: The ceiling banding is spatial (probe grid aliasing), and the current jitter (6% of probe spacing) is too small to break up the grid pattern. A larger jitter combined with slightly faster temporal accumulation will dither the C0 hits across probe cells and average out the blocking — without needing to double baseRes (which would also double the 0.215 ms cascade cost). If ceiling blocks persist after that, then bump `baseRes` from 32→48 as a second step.
