# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785502452007991_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785502453998932_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785502456262096_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17785502452007991.json`  
**Analyzed:** 2026-05-12T09:44:34  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing mode 0 (final) vs mode 6 (GI only) isolates the indirect bounce:

- **Color bleeding onto floor/ceiling**: Mode 6 shows clear red wash on the left side of the floor and faint green on the right. In mode 0 this is barely perceptible — the bilateral blur and direct term dominate.
- **Back wall**: Mode 6 reveals a bright bloom-like hotspot near the upper-center of the back wall, suggesting a strong indirect contribution from light bouncing off the ceiling/floor toward the camera-facing wall. This is consistent with mode 3's hotspot in the same region.
- **Tall block (left)**: Receives noticeable red bleed on its left face in mode 6; the right face shows faint green. The shorter block (right) is mostly lit from the green side.
- **Floor under/between blocks**: GI is present but dim — no contact darkening artifact, AO-like falloff looks plausible.
- **Outside the box (black region)**: Mode 6 correctly shows zero GI outside the volume — no light leakage.

## 2. Banding in Mode 6

- **Ceiling**: A soft circular/elliptical bright spot near the top-center back. This is the light-source footprint, not banding per se, but its edge is slightly **stepped** — consistent with `dirRes=8` directional bins (only 8² = 64 directions sampled).
- **Back wall**: The transition from bright (top) to dim (bottom) shows **smooth gradients**, no visible probe-grid checkerboard. With `baseRes=32` and `volumeSize=4`, probe spacing is ~0.125 units, fine enough to be invisible at this view distance.
- **Cascade boundaries**: No visible discontinuity rings. The four cascades blend cleanly (also supported by stats — meanLum is consistent 0.029–0.056 across cascades).
- **Floor**: A faint horizontal soft band near the front edge of the floor in mode 6 — likely the c0/c1 cascade transition (`c0MinRange=0.5`), but it is very subtle.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: Spatially **smooth**, no high-frequency noise. Slightly washed/milky which is expected at 5× amplification. No fireflies. Temporal accumulation (`temporalAlpha=0.05`) is doing its job.
- **Mode 0 (final)**: The bilateral blur looks **slightly aggressive** on the colored walls — the red and green panels lose some of their crisp corner definition near the ceiling/floor seams. Block edges are preserved well, however. No obvious over-smoothing of geometric edges.

## 4. Ratings

| Mode | Rating | Notes |
|------|--------|-------|
| Mode 0 (Final) | **Good** | Clean composite, slight blur softening on wall corners |
| Mode 3 (Indirect ×5) | **Good** | Smooth, low-noise, but somewhat washed/low-contrast |
| Mode 6 (GI only) | **Fair** | Reveals dim GI; faint stepping in ceiling hotspot edge hints at low `dirRes` |

## 5. Stats Cross-Check & Recommendation

Key observations from stats:
- `surfPct` for **cascade 0 is only 68.9%** — nearly a third of c0 probes hit no surface. This is the under-sampled cascade.
- `variance` is highest in c0/c1 (~0.008) — matches where banding/stepping would first appear.
- Cascades 2 & 3 are fully converged (100% surfPct, low variance).
- `dirRes=8` → 64 directions per probe is on the low side for a Cornell-box-like scene with strong directional bounce light.

### Concrete recommendation

**Increase `dirRes` from 8 → 12 (or 16).**

Rationale: The stepped edge on the ceiling hotspot in mode 6 and the relatively high c0 variance (0.008 with maxLum 0.56) both point to **directional under-sampling** rather than spatial probe density issues. Bumping `dirRes` to 12 gives 144 directions/probe, which should smooth the hotspot footprint and reduce c0 variance without needing more probes. If frame budget is tight (current raymarch 0.19 ms is cheap), this is the highest
