# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784103053187425_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784103055990473_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784103058501331_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784103053187425.json`  
**Analyzed:** 2026-05-10T18:52:14  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing modes, the indirect light is **strongly biased toward warm/reddish tones** (visible in mode 3 as orange-pink wash on the ceiling beams and the bright doorway at end-of-corridor, ~x=460,y=300). The right wall in mode 0 shows a large diffuse reddish smear (x=800-1200, y=200-700) that appears in mode 6 but not as direct lighting — this is GI bleeding from an off-screen warm surface (likely a hand/arm or red wall to camera-right).

Indirect light is **largely absent** on the floor's far half (x=400-700, y=400-650) — mode 6 shows it nearly black there, while mode 3 (×5) reveals only faint contribution. The left wall (x=0-300) receives almost no GI in mode 6, appearing flat-lit only by direct light in mode 0.

## 2. Banding Artifacts in Mode 6

- **Ceiling beams (y=0-180)**: Blocky, ~32-40px wide rectangular patches with hard edges between lit (orange) and unlit (dark) cells. Spacing matches a **probe-grid footprint** at baseRes=32 / volumeSize=4 — these are voxel-sized probe cells, not directional bins.
- **Right wall smear**: Soft, large-radius blobs (~200px) — consistent with **cascade-boundary blending** between an active far cascade and an inactive near one.
- **Far doorway (x=440, y=290)**: Square halo with stair-stepped edge — classic **dirRes=8 directional aliasing** at a high-contrast occlusion edge.

## 3. Smoothness / Over-blur

- **Mode 3** is **noisy and patchy**, not smooth — the ×5 amplification reveals significant spatial discontinuities especially on the ceiling and far wall.
- **Mode 0's bilateral blur** has over-smoothed the floor's specular highlight streaks (y=450-650) into elongated comet shapes, and the right-wall GI bleed into a featureless gradient that hides the underlying probe structure (which is why mode 6 is needed to diagnose it).

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 Final | **Fair** | Plausible at a glance, but blur masks real GI defects |
| 3 Indirect ×5 | **Poor** | Visible patchiness, color cast suggests single-bounce dominance |
| 6 GI only | **Poor** | Clear banding, missing GI on left wall and far floor |

## 5. Stats Cross-Check & Recommendation

The stats are alarming:
- **Cascade 0**: 3.6% anyPct, NaN/Inf luminance → numerical blow-up from undersampled probes
- **Cascade 1**: completely empty (0% everything)
- **Cascade 2**: 92% anyPct but maxLum=29653 and NaN variance → **firefly/overflow**
- **Cascade 3**: negative meanLum (-371) with maxLum=0 → **sign error or uninitialized memory read**

The Inf/NaN in cascade 0 and the 29653 max in cascade 2 are almost certainly the source of the warm color cast (a single firefly probe being temporally accumulated at α=0.05 means it persists ~20 frames).

**Concrete recommendation**: **Clamp cascade radiance to a sane max (e.g., 10.0) before accumulation, and raise `temporalAlpha` to 0.15** to flush the existing NaN/Inf-contaminated history faster. As a secondary fix, investigate why cascade 1 is empty — likely a range-overlap bug between c0MinRange and c1MinRange (both =1 means cascade 1 has zero coverage band). Setting `c1MinRange = 2` should restore cascade 1 population and reduce reliance on the broken cascade 2.
