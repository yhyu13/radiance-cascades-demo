# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784849124276856_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784849125050316_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784849125981815_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784849124276856.json`  
**Analyzed:** 2026-05-11T15:35:43  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Isolation

Comparing mode 0 (final) against mode 6 (GI only), the indirect light is doing **most of the heavy lifting** in this frame — the two images are nearly indistinguishable in tone and structure. This suggests direct lighting contribution is minimal in the visible region (the camera appears to be inside or behind an occluding mesh — the large black silhouette in the center-left of all three images).

**Bright GI zones:**
- Right wall and floor (right half of frame): warm orange/brown bounce, peaking mid-right around (900, 400). Consistent with a red/orange surface bouncing onto neutral geometry.
- Ceiling strip near top-right: subtle warm wash.
- Small bright "hotspot" visible through the central opening at ~(480, 320) — likely a direct view of an emitter or strongly-lit surface.

**Dark/absent GI zones:**
- Left wall reads cooler and dimmer — receives less bounce.
- The black silhouette in the center is **not GI absence** — it's geometry with no probe coverage or backface (see §2).

## 2. Banding Artifacts in Mode 6

- **Right wall (≈ x=700–1100):** soft, low-frequency vertical/diagonal gradients. Spacing is large (~150–200 px), consistent with **probe-grid spacing at cascade 0** (baseRes 32, volumeSize 4 → ~0.125 unit probe spacing projected through perspective).
- **Ceiling near top-right (y < 100):** stair-step bands in the warm patches at (550, 30) and (700, 40). Sharp, regular, ~10–15 px spacing — these are **directional-bin aliasing** (dirRes=8 = only 8² = 64 directions, too coarse for grazing-angle ceiling sampling).
- **Floor diagonal streaks (~600, 600):** dotted/dashed pattern — looks like **jitter pattern residue** that temporal accumulation hasn't dissolved (temporalAlpha 0.05 should be fine, but jitterHoldFrames=2 may be locking the pattern).
- **No clear cascade-boundary rings visible** — cascade transitions look clean.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5):** spatially **very smooth** on the right side and floor, almost too smooth — heavy temporal/spatial filtering is in effect. The dotted floor pattern at (600, 600) is the only visible noise, and that's amplified jitter, not Monte Carlo variance.
- **Mode 0 vs Mode 6:** essentially identical. The bilateral blur is **not over-smoothing** any visible edges — the silhouette of the black geometry remains crisp in both. If anything, the blur is doing nothing distinguishable here, suggesting either the GI signal is already smooth enough or the bilateral guide is working well.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Fair** | Looks plausible but black silhouette dominates — likely a mesh/occlusion bug, not a GI bug |
| 3 (Indirect ×5) | **Good** | Smooth, low-noise, reveals clean bounce structure |
| 6 (GI only) | **Fair** | Visible directional banding on ceiling, jitter residue on floor |

## 5. Stats Cross-Check & Recommendation

**Cascade health:**
- C0 surfPct = 48.6% — **half the probes in cascade 0 see no surface**. This is the smallest cascade and should have highest hit rate near geometry. Low coverage → noisy/biased near-field bounce.
- C0 variance (0.00103) is ~3× C1's and ~120× C3's — confirms the spatial structure (banding) seen in mode 6 originates in **cascade 0**.
- C3 maxLum (0.015) ≈ meanLum (0.008) → far cascade is essentially flat ambient, contributing little detail.

**Concrete recommendation:**

> **Increase `dirRes` from 8 → 12 (or 16).**

Justification:
1. The ceiling banding in mode 6 is clearly directional-bin aliasing — 64 directions is too few for the grazing geometry visible.
2. C0's low surfPct (48%) means many directions miss; more bins = better odds of catching surface hits, raising
