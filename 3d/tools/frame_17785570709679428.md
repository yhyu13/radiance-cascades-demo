# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785570709679428_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785570712921464_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785570716090064_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17785570709679428.json`  
**Analyzed:** 2026-05-12T11:38:22  
**Model:** claude-opus-4-7

---

# Visual Triage: GI Cascade Diagnostic

## 1. GI Contribution Isolation

Comparing mode 0 (final) vs mode 6 (GI only), the indirect term is doing **most of the lighting work** in this scene — the two images are nearly identical in tonality. This tells me direct lighting contribution is minimal; the corridor is essentially GI-lit from the ceiling fixtures.

**Bright GI zones** (visible in mode 3):
- Ceiling strip down the corridor center — strong bounce from the fixtures themselves
- Right wall, mid-frame — receives warm bounce, creating the orange rail-like streak that dominates mode 0's lower-right
- Floor near the vanishing point (~center frame) — secondary bounce pool

**GI-absent zones**:
- Left wall lower half — falls to near-black in mode 6, suggesting probes there receive little energy
- Far-end doorway/recess (center vanishing point) — dark void, no probes converged behind that occluder

## 2. Banding in Mode 6

Visible structure:
- **Blocky "stair-step" patches** along the right wall mid-frame, roughly 8–16 px on screen — consistent with **probe-grid spacing** at `baseRes=64` over `volumeSize=4` (one probe per 0.0625 units, projected into perspective gives exactly that footprint at mid-depth).
- **Soft halo discs** around each ceiling light (most obvious in mode 3) — these are smoothly falling off, not banded, so directional bins (`dirRes=8`) are adequate for the dominant emitters.
- No diagonal/sliced banding that would indicate **cascade boundaries** — the C0→C1→C2 transitions are not visible as seams, which is good.

So: banding source is **probe-grid spatial resolution**, not directional bins or cascade handoff.

## 3. Smoothness Assessment

- **Mode 3** (5× indirect): Smooth overall but the right wall shows faint **horizontal ripple bands** running along the depth axis — this is temporal under-convergence at `temporalAlpha=0.05` combined with `jitterHoldFrames=2` leaking pattern structure.
- **Mode 0** (final): Bilateral blur has **over-smoothed the ceiling fixtures** — individual bulb shapes are smeared into amorphous blobs, losing the geometric edge that's still crisp in mode 3. The right rail edge is also bleeding into the floor.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 Final | **Fair** | Plausible mood, but blur radius too aggressive; fixture geometry lost |
| 3 Indirect×5 | **Good** | Smooth, readable bounce structure; minor temporal ripple |
| 6 GI only | **Fair** | Probe-grid blockiness visible on right wall; dark falloff acceptable |

## 5. Stats Cross-Check & Recommendation

Critical red flags in stats:
- **C0 anyPct = 0%** — cascade 0 is completely empty. No near-field probes are getting hits.
- **C3 maxLum = 29653** with `nan` variance — a fireflied probe is poisoning the coarsest cascade.
- **C1 maxLum = inf** — another NaN/Inf injection.
- `surfPct = 0` everywhere — surface-hit classification is broken or unreported.

The Inf/NaN in C1 and C3 explains why the bilateral blur in mode 0 over-smooths: it's compensating for outlier probes by widening its kernel.

**Concrete change**: Clamp probe radiance accumulation to a finite max (e.g. `maxLum ≤ 100`) **before** temporal integration, and raise `c0MinRange` from 0.5 → 0.8 so cascade 0 actually captures near-field bounces (currently 0% hit rate means it's contributing nothing). If only one knob is allowed: **add an Inf/NaN guard + luminance clamp at probe write** — this single fix will resolve both the firefly contamination and let you reduce bilateral blur radius to recover fixture detail.
