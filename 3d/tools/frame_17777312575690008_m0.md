# AI Visual Triage

**Image:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777312575690008_m0.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17777312575690008.json`  
**Analyzed:** 2026-05-02T22:14:41  
**Model:** claude-opus-4-7  
**Note:** Phase 12a triage (image + probe stats) — geometry-coupled causes stated; software-level root causes require Phase 6b pipeline inspection.

---

## Triage Report

**1. Overall:** A Cornell Box rendered through a 4-level radiance cascade GI system, showing the classic red/green side walls, two white blocks on the floor, and what appears to be a framing/occluder geometry on the outside (dark bars top/bottom, dimmed red/green slabs left/right outside the box opening).

**2. Visible artifacts:**

- **Color bleeding error (suppressed/wrong-side tinting):** The outer slabs flanking the box (dim red on the left exterior, dim green on the right exterior) are showing the interior wall colors on surfaces that face *away* from the box interior — indirect light is leaking through or being attributed to the wrong side of these occluder panels.
- **Missing shadows / light leak around frame:** The dark grey horizontal bars at the top and bottom of the box opening cast no contact shadow onto the adjacent exterior surfaces, and the exterior red/green slabs receive interior radiance despite being on the outside of an opaque wall — consistent with probes near the wall sampling rays that pierce the thin geometry.
- **Faint soft banding on the back wall and ceiling:** Subtle smooth gradient steps visible on the rear wall behind the blocks; too soft to confidently call probe-grid banding, but worth flagging given C0 variance (~0.0057) is the highest of the cascades.
- **Otherwise:** Shadows under the two blocks read correctly, no ring-shaped cascade seam is visible, no hard 45° (dirRes=8 → ~45° bin) angular steps are apparent in the indirect lighting, and no shadow acne on directly lit surfaces.

**3. Stats cross-check:**
- All cascades report `anyPct=100` and C2/C3 hit `surfPct=100`, so probe coverage is fine; convergence is not the bottleneck.
- C0 `surfPct=29%` is expected (near-field probes often see sky/empty in their short rays) and not itself an artifact source here.
- C0 has the highest variance (0.0057) and highest maxLum (0.559) — consistent with the residual near-field noise/banding feel on the back wall.
- The exterior leak is a geometry/visibility issue (probes seeing through the thin frame walls), not a convergence issue.

**4. Suggested parameter change:** Increase `probeJitterScale` from 0.05 → ~0.15 to break up the residual C0 spatial structure on the back wall; if the exterior leak persists, that's a thickness/visibility fix, not a probe-parameter fix.

**5. Quality rating:** **Fair** — the box interior itself looks clean and well-converged, but the exterior light leak through the frame geometry is a prominent, immediately visible defect.
