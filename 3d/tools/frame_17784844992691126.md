# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784844992691126_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784844992815914_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784844993207281_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784844992691126.json`  
**Analyzed:** 2026-05-11T15:28:49  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Isolation

Comparing mode 0 against mode 6, the indirect term contributes most prominently in:
- **Far end of the corridor** (center, around the lit doorway/opening): warm orange bounce dominates here — this is where GI carries the scene since direct light is occluded.
- **Right-hand wall foreground**: a soft warm wash visible in mode 6 that gives the foreground its volumetric "air" in mode 0.
- **Floor mid-corridor**: subtle warm fill bouncing up from the lit far surface.

GI is essentially **absent on the left wall** in the near foreground (it reads near-white/empty in mode 6), meaning the foreground left wall in mode 0 is carried almost entirely by direct light.

## 2. Banding in Mode 6

I can see:
- **Soft horizontal/vertical blockiness on the ceiling** near the top edge — roughly 6–8 discrete patches across the ceiling width, consistent with **probe-grid spacing** (baseRes 24 over volumeSize 4 → ~0.17 unit probe spacing, projecting to a handful of cells across the visible ceiling).
- **A diffuse soft halo around the far doorway** with no sharp ring — no clear cascade-boundary seam visible.
- **No directional-bin pinwheeling** is evident (dirRes 8 = 64 directions seems sufficient for this smooth bounce).

The artifacts read as **probe-grid**, not cascade boundary or directional aliasing.

## 3. Smoothness Assessment

- **Mode 3 (5× amplified)**: Surprisingly smooth — almost milky. At 5× gain you'd expect noise to pop, but it doesn't. This suggests temporal accumulation is doing heavy lifting (consistent with temporalAlpha 0.05 = ~20-frame history). Variance stats agree: cascade 0 variance is only 9e-4.
- **Mode 0 bilateral blur**: The right-hand wall in mode 0 shows a **suspiciously soft, almost out-of-focus falloff** — geometric edges between the right wall and ceiling are blurred away. The bilateral filter is **over-smoothing the right wall/ceiling crease**. Detail texture in the foreground floor also looks washed.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 Final | **Fair** | Reads correctly but right-side over-blurred; loss of edge detail |
| 3 Indirect ×5 | **Good** | Clean, smooth, no firefly noise; probe banding subtle |
| 6 GI only | **Fair** | Visible ceiling probe-cell structure; warm bounce localized correctly |

## 5. Stats Cross-Check & Recommendation

Cascade convergence:
- C0 surfPct **51%** is low — half the cascade-0 probes hit nothing useful, while maxLum 0.46 vs mean 0.016 indicates **a few hot probes dominating** (variance 9e-4 confirms spatial structure → matches the ceiling banding).
- C3 meanLum 0.0036 with surfPct 96% — outer cascade contributes almost nothing; fine.

**Concrete change**: Increase **`baseRes` from 24 → 32** (or alternatively reduce `volumeSize` to 3). C0's 51% surface hit rate plus visible ceiling probe-cell banding both point to **under-sampled near-field probes** as the dominant artifact source. Bumping baseRes will tighten probe spacing in C0, raise surfPct, and dissolve the ceiling banding without touching the (already well-tuned) temporal filter.

Secondary tweak if perf-bound: lower the **bilateral blur range/sigma** to recover the right wall/ceiling edge in mode 0.
