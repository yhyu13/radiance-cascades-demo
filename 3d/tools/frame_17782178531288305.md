# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782178531288305_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782178538833082_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782178546323333_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17782178531288305.json`  
**Analyzed:** 2026-05-08T13:24:45  
**Model:** claude-opus-4-7

---

# Visual Triage Report — Cornell Box GI

## 1. GI Contribution Analysis

Comparing the three modes, indirect light is **clearly present** in the expected locations:

- **Color bleeding onto the floor**: The mode 6 (GI only) view shows a faint reddish tint on the floor near the left wall and a green tint on the floor near the right wall, plus on the facing sides of both blocks (left side of tall block = pinkish; right side of short block = greenish). This is correct Cornell behavior.
- **Ceiling near the light**: Mode 6 shows the bright hotspot on the ceiling around the emitter, with falloff toward the corners — direct emission contribution captured in the cascade.
- **GI absent / weak**: The shadow cast by the tall block on the floor (visible in mode 0) does **not** receive a clear bounce fill from the red wall in mode 6 — the shadowed region just behind the tall block stays quite dark. Indirect fill there is under-represented.
- **Outside the box** (the gray frame visible in modes 0/3): These exterior surfaces show essentially no GI in mode 6 — expected, since they face away from the emitter.

## 2. Banding in Mode 6

Looking carefully at mode 6:

- **Blocky low-frequency patches** are visible on the back wall and ceiling — roughly square cells whose size matches the **probe grid spacing**. With `baseRes: 32` and `volumeSize: 4`, that's ~0.125 units per probe, which projects to ~8–10% of the back-wall image width. The visible patch size is consistent with **probe-grid origin**, not directional bins.
- **No fine pinwheel/striping** that would indicate `dirRes: 8` directional aliasing — the 8 bins appear adequate for this diffuse-only scene.
- **No sharp discontinuity lines** at concentric distances from the camera, so cascade boundaries are not producing visible seams (consistent with `surfPct ≥ 98%` across all 4 cascades).

The dominant artifact is **probe-grid blockiness**, mildly softened by temporal accumulation.

## 3. Smoothness Assessment

- **Mode 3 (5× indirect)**: Looks **spatially smooth**, almost over-soft. Edges of the blocks are barely distinguishable from background — this is the bilateral filter (or low effective resolution of the GI term) doing heavy work. No salt-and-pepper noise is visible, suggesting `temporalAlpha: 0.05` is producing good temporal stability.
- **Mode 0 (final)**: Edges of the blocks and walls are crisp; the bilateral blur has **not** over-smoothed geometry edges. However, the **shadow boundary** behind the tall block looks slightly soft/blurred in a way that suggests the indirect term's blur is bleeding across the shadow contact line.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| Mode 0 (final) | **Good** | Crisp direct, plausible color bleed, minor shadow-edge softening |
| Mode 3 (indirect ×5) | **Fair** | Smooth but lacks any high-frequency detail; reads as low-res upsample |
| Mode 6 (GI only) | **Fair** | Correct color directions, but visible probe-grid blockiness on back wall/ceiling |

## 5. Stats Cross-Check & Recommendation

- `surfPct` 98–100% and `anyPct` 100% → **probes are well-fed**, no under-sampling problem.
- Cascade 0 variance (0.0085) is ~54× cascade 3 variance (0.00016) → expected, but the **mid-cascades (1, 2) carry most of the visible energy** (`meanLum` ~0.05) and their variance (~0.008, 0.004) is consistent with the blockiness seen in mode 6.
- `cascadeTimeMs: 0.071` and `raymarchTimeMs: 0.149` → **plenty of headroom** for a resolution bump.

**Concrete recommendation**: Increase `baseRes` from **32 → 48** (or 64). The visible banding is probe-grid spatial aliasing, not directional or temporal. `dirRes: 8` is sufficient for diffuse, and `temporalAlpha: 0.05` is already producing stable, noise-free output (mode 3 is smooth). Spending the available GPU budget on finer prob
