# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785807366227506_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785807373686536_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785807381266884_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17785807366227506.json`  
**Analyzed:** 2026-05-12T18:12:50  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Isolation

Comparing mode 0 (final) with mode 6 (GI only) shows where indirect light is doing real work:

- **Strong GI presence**: The ceiling near the area light shows soft warm bounce. The floor has a faint reddish bias on the left and greenish bias on the right — classic Cornell color bleed visible in mode 6 and contributing subtly to mode 0.
- **Color bleed onto occluders**: The tall block's right face shows a faint green tint in mode 6; the short block's left face shows a faint red tint. Both survive into the final composite.
- **GI absent / weak**: The shadow under/behind the tall block in mode 0 is dark — mode 6 confirms little indirect fill reaches the floor between the blocks and the back-left corner. The walls themselves in mode 6 are also weakly lit on their lower halves, meaning second-bounce floor→wall transport is missing.
- **Outer "frame" (the dark gray border around the box in mode 0)**: in mode 6 this region is nearly black, confirming it is unlit backside geometry, not a GI artifact.

## 2. Banding in Mode 6

Looking at mode 6 carefully:

- **Soft rectangular halos** appear around the box silhouette and around both blocks — edges look "puffy" / dilated by roughly 8–12 px. Spacing matches the **probe footprint of the coarse cascade** (baseRes=32 over volumeSize=4 → ~0.125 units/probe at C0, doubling per cascade).
- **No directional pinwheel/star pattern** is visible on the walls, so dirRes=8 is adequate for this lighting — not a directional-bin issue.
- **A faint brightness step** is visible where the ceiling meets the back wall and on the floor mid-depth — consistent with a **cascade-boundary transition** (C0→C1 around the c0MinRange=0.5 shell). It is subtle but present.
- **No probe-grid checkerboard** stippling is visible, so temporal accumulation is doing its job spatially.

Primary banding source: **probe footprint dilation + one mild cascade seam**, not directional aliasing.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: Looks **spatially smooth, almost too smooth** — basically blurry with no high-frequency noise. Variance stats (0.004–0.008) confirm this. The trade-off is loss of contact detail: the base of the tall block bleeds into the floor and the small block's contact shadow is gone.
- **Mode 0 (final)**: The bilateral blur has clearly **over-smoothed contact shadows**. The tall block's bottom edge against the floor is feathered, and the shadow cast onto the floor by the tall block looks softer than it should given a small area light. Direct lighting edges (block silhouettes against back wall) are preserved well, so the bilateral guide is working — but the indirect smoothing radius is too aggressive.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| Mode 0 (final) | **Good** | Reads correctly as a Cornell box; over-smoothed contacts hurt it |
| Mode 3 (indirect ×5) | **Fair** | Useful for visualization but very blurry; probe footprint dominates |
| Mode 6 (GI only) | **Fair** | Color bleed reads correctly, but probe halos and a cascade seam are visible |

## 5. Parameter Recommendation

**Cross-checking stats with images:**
- C0 surfPct = 68.9% — **only ~69% of coarsest probes hit a surface**. This is the root cause of the puffy probe halos around silhouettes: empty C0 probes fall back to coarser cascades, producing the dilated bleed.
- C3 variance = 0.00016 (basically flat) — the deepest cascade is fully converged and not contributing structure.
- temporalAlpha=0.05 with jitterHoldFrames=2 is fine; no temporal flicker visible.

**Concrete change:** **Reduce `c0MinRange` from 0.5 to ~0.3** (and correspondingly tighten `c1MinRange` to ~0.6). This shrinks C0's footprint so more probes land on/near surfaces, which should push C0 surfPct above ~85% and tighten the silhouette halos in mode 6.

**Secondary t
