# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784852745917731_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784852746741289_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784852747783527_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784852745917731.json`  
**Analyzed:** 2026-05-11T15:41:47  
**Model:** claude-opus-4-7

---

# Visual Triage Report — Corridor Frame

## 1. GI Contribution Isolation

Comparing mode 6 (GI only) against the implied direct-only baseline (mode 0 minus mode 6):

- **Brightest indirect**: the left wall/pillar region in the near-to-mid foreground, plus the ceiling slabs running down the corridor. The left wall picks up a warm bounce that is clearly absent on the right wall.
- **Strong asymmetry**: right wall and right-side curved structure receive almost no indirect — they sit in a deep red-brown that essentially equals their albedo × ambient. The light source(s) must be on the left / above-left.
- **Indirect absent**: the floor down-corridor (mid-distance gray strip) shows almost no GI lift; brightness there in mode 0 comes from direct light spill (the bright specks along the floor centerline).
- **Far doorway** (vanishing point): mode 6 shows a warm reddish glow — that is indirect bouncing off the back wall, since direct contribution there is small in the final.

## 2. Banding in Mode 6

Visible banding patterns:

- **Concentric arc bands on the right wall and right curved surface** — broad, smooth, roughly 80–120 px apart, centered loosely on the camera axis. Shape and spacing are consistent with **cascade-boundary transitions** (4 cascades, c0/c1 minRange 0.5/1.0 → boundaries at a few discrete radii from the camera). Not probe-grid (those would be axis-aligned blocks) and not directional-bin (those would be hard angular wedges).
- **Blocky stair-stepping along the ceiling beams and far doorway frame** — ~8–16 px aligned to world axes. That is the **probe grid** (baseRes 32, volumeSize 4 → ~12.5 cm probe spacing) showing through where geometry is thin.
- No clear directional-bin wedges visible (dirRes 8 is coarse but the scene is diffuse enough to hide it).

## 3. Smoothness & Over-Blur Assessment

- **Mode 3 (indirect ×5)**: spatially **very smooth**, almost too smooth — looks low-frequency and washed. No speckle noise, which is consistent with temporalAlpha 0.05 (heavy temporal accumulation). Variance numbers confirm this (c0 variance 1e-3, dropping to 9e-6 by c3). The cost is visible **ghosting/smearing** on the left pillar in mode 0 and a soft halo around the far doorway.
- **Mode 0 bilateral blur**: has **over-smoothed the floor lane markings** (the white dashes near the bottom of the frame are smeared into streaks) and **softened the left pillar edge** into the wall. Door-frame silhouette at the vanishing point is also mushy. Geometric edges that should be crisp are losing their normal/depth discontinuity.

## 4. Ratings

| Mode | Rating | Reason |
|---|---|---|
| 0 Final | **Fair** | Plausible lighting balance, but bilateral blur is too aggressive on small features (floor markings, pillar edge). |
| 3 Indirect ×5 | **Good** | Clean, smooth, no noise — confirms GI is converging. Slight ghosting from low temporalAlpha. |
| 6 GI only | **Fair** | Useful for diagnosis, but cascade-boundary arcs and probe-grid stair-stepping are obvious. |

## 5. Stats Cross-Check & Recommendation

Stats show **healthy convergence**: anyPct 100% on every cascade, surfPct climbs 48→100%, variance drops monotonically. The c0 maxLum 0.54 vs meanLum 0.017 ratio (~32×) indicates a few hot probes near a light, which matches the bright spill on the left. No under-sampling problem.

The arcs in mode 6 align with cascade boundary radii, and c0→c1 has the largest variance jump (1.03e-3 → 7.4e-4, ~28% drop). That transition is where the visible ring sits.

**Concrete parameter change**: **increase `c0MinRange` from 0.5 → 0.75** (or equivalently widen the cascade blend region). This pushes the c0/c1 boundary outward and softens the most visible arc on the right wall. As a secondary change, **reduce the bilateral blur strength** (or raise its depth/
