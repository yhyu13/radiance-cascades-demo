# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782523864165893_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782523865131796_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782523865663602_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17782523864165893.json`  
**Analyzed:** 2026-05-08T23:00:17  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing the three views, the scene appears to be a **near-empty or featureless frame** — there is no visible Cornell Box geometry (no walls, no colored bleed, no shadowed corners). All three modes show only a soft, roughly radial gradient with a faint darker disc near image center (~640, 360) and concentric rings extending outward.

- **Mode 0 (final)**: Uniform mid-dark gray (~#4a4a4a). No discernible direct lighting features — no wall albedo, no light source highlight.
- **Mode 3 (indirect ×5)**: Lifts to ~#9a9a9a, revealing the same radial structure more clearly. This means indirect GI is essentially **uniform across the frame** — there is no localized bounce (e.g., no bright patch under a light, no color bleed on a wall).
- **Mode 6 (GI only)**: Very dark (~#1e1e1e), confirming raw indirect is low-magnitude (~0.02 luminance, matches `meanLum` 0.011–0.027).

**Conclusion**: Either the camera is pointed at empty volume, or the geometry is outside the probe volume. The GI signal exists but carries no scene-structure information.

## 2. Banding in Mode 6

The visible structure in mode 6 consists of **smooth concentric elliptical rings** centered roughly on the image center, with a distinct **inner disc ~150 px radius** and outer rings spaced ~200–300 px apart reaching the corners.

- **Shape**: Round/elliptical, centered on the view — *not* axis-aligned.
- **Spacing**: Coarse, low-frequency, monotonic falloff.

This pattern is **inconsistent with probe-grid banding** (which would be axis-aligned rectilinear) and **inconsistent with cascade boundaries** (which would appear as sharp concentric *steps* at fixed world-space radii). The smooth radial falloff is most consistent with **directional-bin aliasing** from the low `dirRes: 8` (only 64 directions per probe) projected through a roughly view-aligned probe — the fixed direction set produces a soft cosine-like envelope when geometry doesn't break it up.

## 3. Smoothness Assessment

- **Mode 3**: Spatially **very smooth**, no high-frequency noise or speckle. Given temporalAlpha=0.05 (heavy temporal accumulation) and variance of 0.0007–0.002 across cascades, this matches expectation.
- **Mode 0 bilateral blur**: Cannot evaluate edge preservation — **there are no edges in this frame to over-smooth**. The blur is doing nothing visible because the input has no high-frequency content.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| Mode 0 (Final) | **Poor** | No visible scene content; can't validate composite. |
| Mode 3 (Indirect ×5) | **Fair** | Smooth, stable, but reveals only directional envelope, no scene response. |
| Mode 6 (GI only) | **Fair** | Cleanly shows raw GI is low and structureless; useful diagnostically. |

## 5. Stats Cross-Check & Recommendation

- `surfPct` rises 81% → 100% across cascades — **C0 is under-sampling surfaces** (~18% of C0 probes hit nothing). This suggests probes near the camera are in empty space.
- `skyPct: 0` everywhere — rays neither hit surfaces nor sky in C0, consistent with **probes inside/behind geometry or volume too small**.
- `maxLum` falls 0.65 → 0.02 across cascades, variance drops 100×: outer cascades are essentially flat ambient. Healthy falloff, but C0 is the only one carrying signal.
- `volumeSize: 4` with `baseRes: 32` → C0 probe spacing 0.125 units; if the box is larger than 4 units the camera viewpoint may simply be outside meaningful coverage.

### Concrete parameter change

**Increase `dirRes` from 8 → 16** (256 directions/probe). The smooth radial banding in mode 6, combined with the low C0 `surfPct` of 81.6% (rays missing surfaces despite being in a closed box), points to insufficient angular resolution producing a coarse directional envelope. This will roughly 4× the raym
