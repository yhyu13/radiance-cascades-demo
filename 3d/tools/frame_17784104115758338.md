# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784104115758338_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784104118484143_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784104121404530_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784104115758338.json`  
**Analyzed:** 2026-05-10T18:54:04  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Render Modes

## 1. GI Contribution Analysis

Comparing the three modes, indirect light is doing the following work:

- **Bright GI zones**: Left wall (lower-left third of frame) shows the strongest indirect contribution — visible as the warm cream/beige bounce in mode 6 and amplified in mode 3. The ceiling near the back of the corridor also picks up bounce from the bright far-end light source.
- **Absent/weak GI zones**: The right wall and the foreground hand/object on the right are almost entirely lit by indirect (mode 6 shows them as a muddy red-brown), but the GI there is flat and lacks any spatial variation — suggesting probes there are saturated by a single dominant bounce direction.
- **Floor**: Mode 6 shows the floor going nearly black past the midline, while mode 0 retains floor detail from the bright specular streaks at the far end — those streaks are direct light, not GI.

## 2. Banding Artifacts in Mode 6

Visible artifacts:
- **Stepped horizontal bands on the right wall** running along the corridor's depth axis. Spacing roughly doubles as you move away from the camera — classic **cascade-boundary** signature (each cascade halves angular resolution / doubles probe spacing).
- **Blocky ~16–24 px patches** on the ceiling fixtures and back wall around the lit doorway — consistent with the **baseRes=32, volumeSize=4** probe grid being interpolated at screen scale.
- **No rotational/pinwheel banding** visible, so `dirRes=8` directional bins do not appear to be the dominant artifact source here.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: Spatially **smooth to the point of mush** on the side walls — almost no high-frequency noise, but you can see the cascade transitions as soft luminance steps along the corridor. No speckle, indicating heavy temporal accumulation (consistent with `temporalAlpha=0.05`).
- **Mode 0 (final)**: Bilateral blur has **over-smoothed the right-hand foreground object** — its silhouette bleeds into the wall. The ceiling fixtures retain crisp pixel-stepped edges (those are direct), but the GI underneath them is flattened, losing contact-shadow definition where ceiling meets wall.

## 4. Mode Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| Mode 0 (final) | **Fair** | Usable image, but bilateral blur is too aggressive; GI edges lost on right side. |
| Mode 3 (indirect ×5) | **Fair** | Smooth but reveals cascade banding; useful as a debug view. |
| Mode 6 (GI only) | **Poor** | Heavy banding, large dark regions, and the stats below confirm under-sampling. |

## 5. Stats Cross-Check & Parameter Recommendation

The cascade stats are alarming:
- **Cascade 0 & 1**: `anyPct` only 3.5% / 4.8%, and `surfPct=0` everywhere — the near cascades have almost no valid surface hits. `meanLum=NaN` confirms divide-by-zero from empty probes.
- **Cascade 2**: 99% filled but `maxLum=29504` and NaN variance — a single firefly probe is poisoning the mean.
- **Cascade 3**: 100% sky, negative meanLum — sign/accumulator bug or uninitialized memory.

The NaN/inf propagation plus the visible cascade-boundary banding both point at the same root cause: **near-cascade probes are not being seeded with enough rays to converge before temporal blending freezes them in.**

**Concrete change**: Raise `temporalAlpha` from `0.05` → `0.15` **and** increase `probeJitterScale` from `0.06` → `0.15`. The current 5% blend with tiny jitter means under-sampled probes (cascades 0/1 at ~4% coverage) take ~60 frames to refresh — long enough for fireflies in cascade 2 to dominate the composite. Faster blending plus wider jitter will both fill empty probes and dilute the `maxLum=29504` outlier driving the right-wall flatness.

If only one knob can move: **`temporalAlpha = 0.15`** — it directly addresses the NaN-stuck probes and the casc
