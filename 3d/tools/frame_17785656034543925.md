# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785656034543925_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785656038167715_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785656041734740_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17785656034543925.json`  
**Analyzed:** 2026-05-12T14:00:35  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing mode 0 (final) against mode 6 (GI only), the two images are **nearly identical** in brightness — this immediately tells me the direct lighting contribution is negligible or missing. The corridor is being lit almost entirely by indirect GI from the ceiling lamp fixtures.

**Bright GI zones:**
- Ceiling lamps (top of frame, receding into distance) — saturated white spheres
- Left wall near the window/opening (mid-left) — strong bounce highlight
- Floor strip directly under the lamps — visible light pools spaced regularly

**Dark GI zones:**
- Right wall (entire right half of frame) — almost pure ambient brown, no bounce
- Floor far end of corridor — falls off to black
- Left foreground archway interior — black

The asymmetry (right wall dark, left wall lit) suggests probes on the right side are not receiving the lamp contribution that the left side is.

## 2. Banding Artifacts in Mode 6

The **right wall** shows the most diagnostic artifact: large **overlapping circular/spherical blobs** of brown light, roughly 80–150 px in diameter at screen scale, increasing in size with distance. These are clearly **probe-volume splats** — each visible disc corresponds to one probe's irradiance being sampled by the raymarcher with a wide spatial footprint.

The spacing (and the fact that the blobs grow with distance from the camera) is consistent with **cascade boundary transitions**: the discs on the right wall look like cascade 2 or 3 probe footprints (`volumeSize: 4`, `baseRes: 64` → coarse outer cascades). I do not see clear directional-bin striping (no angular wedges around the lamps).

The **floor** shows a different artifact: regular rectangular light/dark tiles aligned with the corridor axis — these match the lamp spacing above, so they are physically correct caustic-like spots, not a probe artifact.

## 3. Smoothness / Bilateral Blur Assessment

**Mode 3 (indirect ×5):** The amplified indirect is **smooth, almost over-smooth** — broad gradients on walls, no high-frequency noise. The lamp fixtures are blown out to white but their edges are soft. No salt-and-pepper noise visible, which suggests temporal accumulation is doing its job (consistent with `temporalAlpha: 0.05`).

**Mode 0 vs Mode 6:** They look essentially identical, meaning bilateral blur is either disabled or has nothing to do because the raw GI is already low-frequency. No edge over-smoothing is visible — geometric edges (wall/floor seam, lamp silhouettes) remain crisp.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 Final | **Fair** | Looks like GI-only; missing or unbalanced direct term |
| 3 Indirect ×5 | **Good** | Smooth, temporally stable, useful for debugging |
| 6 GI only | **Poor** | Heavy probe-splat blobbing on right wall, asymmetric coverage |

## 5. Stats Cross-Check & Recommendation

The cascade stats are alarming:
- **Cascades 0 and 1: all zeros** — these near-field cascades are not being populated at all. That explains the lack of fine detail and the dominance of coarse splats from cascade 2/3.
- **Cascade 2: `anyPct = 4.69%`, `meanLum = -nan`, `variance = -nan`** — NaN propagation, very few probes hit anything, and the ones that do produced invalid luminance.
- **Cascade 3: `meanLum = -374.5`** — negative mean luminance is non-physical; a sign bit or accumulator is broken.
- **`maxLum = 340`** in cascade 2 with only 4.69% coverage matches the visible blown-out lamp blobs — a handful of probes are catching the lamp and getting splatted across huge areas.

### Concrete parameter change

**Increase `c0MinRange` and fix cascade 0/1 population first** — but the single most impactful one-line change:

> **Set `dirRes: 8 → 16` AND `c0MinRange: 0.5 → 0.25`**, or if only one: **lower `c0MinRange` to 0.25** so cascade 0 actually covers the corridor's near-field ge
