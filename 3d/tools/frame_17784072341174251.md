# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784072341174251_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784072342075958_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784072343353917_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784072341174251.json`  
**Analyzed:** 2026-05-10T18:01:04  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing the three views, indirect light is contributing meaningfully across the scene:

- **Bright GI zones**: Ceiling near the strip lights (top center), and the far doorway/end-of-corridor area where direct light from the source bounces back. The right wall in mode 6 shows a warm reddish bounce — clearly picking up the floor/wall tint.
- **Absent/weak GI**: The left wall in mode 6 is noticeably darker than the right wall, despite symmetric geometry visible in mode 0. The floor in the foreground (bottom of frame) receives almost no indirect light — visible as a dim grey band in mode 6.
- **Mode 0 vs Mode 6**: Direct lighting is responsible for the bright specular streaks on the floor (the white "dashes" running down the corridor center). Those vanish entirely in mode 6, confirming they are direct-only contributions.

## 2. Banding Artifacts in Mode 6

Visible banding patterns:

- **Ceiling (top of frame)**: Stepped/blocky brightness changes around the light fixtures — these align with the geometry of the lights themselves rather than a probe grid, so likely faithful.
- **Right wall**: A soft horizontal seam roughly 1/3 down from the top — consistent **cascade-boundary** transition (cascade 0→1 falloff, given `c0MinRange=1`, `c1MinRange=1`).
- **Far doorway**: Visible blocky pixelation in the recessed area — spacing matches `baseRes=32` over `volumeSize=4` (≈8cm probe spacing), pointing to **probe-grid** undersampling at the cascade-0 scale.
- No clear directional-bin pinwheel/streak pattern is visible, so `dirRes=8` appears adequate for this scene.

## 3. Smoothness & Over-blur Assessment

- **Mode 3 (indirect ×5)**: Looks **spatially smooth** with very little high-frequency noise. The 5× amplification is not exposing speckle, which suggests temporal accumulation is working (consistent with `temporalAlpha=0.05`). However, the smoothness is almost *too* uniform — the indirect term has lost detail at material boundaries (the strip lights bleed into the ceiling without crisp edges).
- **Mode 0**: The bilateral blur has over-smoothed the **left wall and right wall** — they appear as featureless gradients with no surface texture detail. The floor specular highlights survive (because direct light bypasses the blur), but everything in the periphery looks like soft-focus paint. The far doorway also has its silhouette softened.

## 4. Ratings

| Mode | Rating | Reasoning |
|------|--------|-----------|
| Mode 0 (Final) | **Fair** | Readable, but bilateral blur is too aggressive on walls; loses material identity. |
| Mode 3 (Indirect ×5) | **Good** | Clean, smooth, useful for diagnosis. Confirms GI is converged. |
| Mode 6 (GI only) | **Fair** | Reveals cascade seam on right wall and probe blockiness near doorway. |

## 5. Stats Cross-Check & Recommended Change

**Stats observations:**
- `surfPct` rises from 81.6% (C0) → 100% (C2/C3): cascade 0 has ~18% of probes that didn't hit a surface — explains the dim/blocky regions near the camera.
- Variance drops cleanly across cascades (1.1e-3 → 9.8e-6), matching the visible cascade seam on the right wall — C0 carries most of the spatial structure.
- `cascadeTimeMs=9.6` is the dominant cost; `raymarchTimeMs=0.1` is trivial.

**Recommended change**: **Increase `c0MinRange` from 1 → 2** (and optionally `baseRes` from 32 → 48).

Rationale: Cascade 0's low `surfPct` (81.6%) indicates probes are missing nearby geometry — likely because the C0 range is too short to reach the corridor walls from probe centers in open-air voxels. Extending C0's range will let it capture the dominant near-field bounces, which should:
- Eliminate the visible cascade-0/1 seam on the right wall,
- Reduce the over-reliance on bilateral blur to hide blockiness (allowing you to soften
