# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784681308574433_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784681316180315_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784681323602581_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784681308574433.json`  
**Analyzed:** 2026-05-11T10:56:06  
**Model:** claude-opus-4-7

---

# Visual Triage Report — Cornell Box GI

## 1. GI Contribution Analysis

Comparing mode 0 (final) vs mode 6 (GI only):

- **Color bleeding from walls**: The red wall (left) bleeds onto the floor and the left face of the tall box; the green wall bleeds onto the right face of the short box and the right portion of the floor. This is clearly visible in mode 6 — the floor near the red wall shifts pink/warm, while the floor near the green wall shifts cool/green.
- **Ceiling**: In mode 6 the ceiling is dim with a small bright hotspot directly under (above?) the light area — this is the bounce return from the bright floor.
- **Direct-only zones**: The shadow of the tall box on the floor (visible in mode 0 to the lower-left) is *not* fully black in mode 6 — GI fills it with dim warm light. Good.
- **Absent GI**: The back wall in mode 6 is uniformly dim with no clear bleed from the side walls reaching its corners — GI falloff looks correct but slightly flat.

## 2. Banding Artifacts in Mode 6

- **Soft horizontal/vertical blockiness on the back wall and ceiling** at roughly 1/8 of the cube interior — consistent with the **base probe grid (baseRes=32, volumeSize=4)** trilinear footprint, not directional bins.
- **No visible cascade boundary rings** — transitions between c0/c1/c2 ranges (0.5 / 1.0 / …) look smooth; no concentric step on the floor.
- **No directional pinwheel/spoke pattern** that would indicate dirRes=8 aliasing on the curved highlights.
- **Slight outer halo** around the box silhouette (the gray fringe outside the cube outline in modes 3 and 6) — this is probe leakage past the geometry boundary, characteristic of trilinear interpolation crossing the wall.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: Spatially **very smooth**, almost too smooth — no high-frequency noise, but the back wall is washed out and detail-free. Temporal accumulation (alpha=0.05) is doing heavy lifting.
- **Mode 0 (final)**: The bilateral blur has **slightly softened the contact shadow** under the short box (right) and the corner crease where the back wall meets the ceiling looks rounded. The shadow of the tall box on the floor is still readable, so blur isn't destructive — just on the edge of over-smoothing.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Good** | Clean composite, readable shadows, mild edge softening |
| 3 (Indirect ×5) | **Good** | Smooth, low-noise, but leakage halo around silhouette |
| 6 (GI only) | **Fair** | Color bleed correct, but probe-grid blockiness visible on back wall and outside-silhouette leak |

## 5. Stats Cross-Check & Recommendation

- **anyPct=100% all cascades** and **surfPct=99–100% on c1–c3** → probes are well-fed; c0 at 68.9% surfPct is expected (smallest range misses far surfaces).
- **Variance drops cleanly** 0.008 → 0.008 → 0.004 → 0.00015 across cascades — healthy convergence, no cascade is starved.
- **maxLum 0.56 on c0** vs **0.05 on c3** confirms near-field detail is preserved; no evidence of directional aliasing in the numbers (would expect higher c0 variance).

### Concrete change

**Increase `baseRes` from 32 → 48 (or reduce `volumeSize` to 3)**. The dominant artifact is probe-grid footprint blockiness on the back wall and silhouette leakage — both are spatial-resolution issues, not temporal or directional. Stats show convergence is fine and variance is low, so spending the budget on more probes (rather than higher `dirRes` or lower `temporalAlpha`) directly attacks the visible defect. If perf becomes tight (cascadeTimeMs would roughly 3.4×), alternatively **drop `volumeSize` to 3** to tighten probe spacing without changing probe count.
