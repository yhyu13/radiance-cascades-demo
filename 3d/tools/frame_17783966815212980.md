# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783966815212980_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783966822889656_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783966830562478_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17783966815212980.json`  
**Analyzed:** 2026-05-10T15:05:16  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Render Modes

## 1. GI Contribution Analysis

Comparing mode 0 (final) against mode 6 (GI only):

- **Color bleeding is present and correct in direction**: mode 6 shows red tint on the left side of the tall block and red wash across the left half of the floor; symmetric green tint on the right side of the short cube and right floor. This matches the expected indirect bounce from the colored side walls.
- **Ceiling in mode 6** shows a soft bright spot directly under the light, indicating the indirect term is picking up the back-wall/floor return bounce correctly.
- **Tall block's right face** in mode 6 shows a faint green rim — consistent with a single bounce off the green wall.
- **Shadow regions** (behind the tall block on the floor, behind the short cube) remain dark in mode 6, meaning GI is *not* filling in occluded zones strongly — occlusion is being respected.

## 2. Banding Artifacts in Mode 6

Visible structures:
- **Soft blocky gradients on the back wall and ceiling** — roughly 4–6 cells across the visible wall span. With `baseRes: 32` and `volumeSize: 4`, this matches the **probe grid spacing of cascade 0** (~0.125 units/probe), not directional bins.
- **No sharp ring/halo discontinuities** that would indicate cascade-boundary seams between c0/c1.
- **No angular pinwheel pattern** on the floor that would suggest `dirRes: 8` directional aliasing (8 dirs is low, but the bilateral filter and temporal accumulation are hiding it here).

Verdict: banding is **probe-grid trilinear interpolation**, not directional or cascade.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: Very smooth — almost over-smooth. No high-frequency noise, no fireflies. The walls are nearly flat-shaded, suggesting heavy temporal accumulation (`temporalAlpha: 0.05` ⇒ ~20-frame effective window) plus bilateral blur is doing most of the work.
- **Mode 0 (final)**: Edges of the blocks and wall corners remain crisp, so **bilateral blur has preserved geometric edges well** — no obvious bleed of red across the back wall or onto the front face of the tall block. The block contact shadows on the floor are intact.
- One mild concern: the soft red/green halo extends slightly *outside* the box silhouette in modes 3 and 6 (visible as colored bleed onto the black background border around the box). This is GI being sampled in empty-space probes adjacent to the wall and projected outward — not a render bug, but indicates probes near the wall exterior are accumulating wall radiance.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 Final | **Good** | Clean composite, edges preserved, plausible color bleed, mild halo outside box |
| 3 Indirect ×5 | **Good** | Very smooth, no noise, but possibly *too* smooth — losing detail |
| 6 GI only | **Fair** | Probe-grid banding visible on back wall/ceiling; correct directionality |

## 5. Stats Cross-Check & Recommendation

Stats confirm the visual read:
- `surfPct` 98–100% across all 4 cascades → **probes are well-converged**, not under-sampled.
- `variance` drops monotonically (0.0085 → 0.00016) → cascades are behaving correctly, no high-variance outlier cascade.
- `temporalAlpha: 0.05` is very low → explains the over-smoothness in mode 3.
- `dirRes: 8` is low but not the visible bottleneck here.

### Concrete recommendation: **Increase `baseRes` from 32 to 48 (or 64)**

The dominant artifact is **spatial probe-grid banding on the back wall in mode 6**, and stats show no sampling deficiency to fix via temporal or jitter changes. Increasing probe density directly attacks the visible artifact. Cascade 0's variance (0.0085) has the spatial structure to support more probes, and raymarch time (0.086 ms) has plenty of headroom.

If raising `baseRes` is too costly, the second-best option is **raise `probeJitterScale` from 0.06 → 0.12** to break up the static grid pattern via temporal d
