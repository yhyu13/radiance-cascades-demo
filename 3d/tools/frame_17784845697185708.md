# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784845697185708_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784845697396171_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784845697616069_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784845697185708.json`  
**Analyzed:** 2026-05-11T15:29:58  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing mode 0 (final) against mode 6 (GI only), the indirect light is concentrated in:
- **Far end of the corridor** (center vanishing point): warm reddish-orange glow, clearly visible in all three modes — this is bounce light from a distant source down the hallway.
- **Ceiling/upper walls near camera**: receives indirect bounce, visible as the brownish patches at the top of mode 6.
- **Floor**: relatively dark in mode 6, meaning the bright floor highlights in mode 0 are mostly **direct** light contribution, not GI.
- **Right wall (mid-frame)**: dark blob in mode 6 — this is a near-field occluder/probe shadow, not a GI hotspot.

Indirect light is **absent** along the lower-left wall and floor plane near camera, where mode 0's brightness is direct-light driven.

## 2. Banding in Mode 6

Visible artifacts:
- **Horizontal/diagonal patchy stripes along the ceiling** running toward the vanishing point — irregular, blob-like rather than gridded. Spacing is uneven (~10–30 px), inconsistent with a uniform probe grid.
- **A large dark bilobed blotch on the right wall** mid-frame — this looks like a single probe or directional bin under-sampled relative to its neighbors. Its size (~60 px) and isolation suggest a **directional-bin discontinuity** (dirRes=8 = only 8 dirs/probe → coarse angular sampling), not a cascade boundary.
- No clear concentric cascade-ring boundary is visible, so cascade transitions appear well-blended.

Most likely source: **directional binning** (dirRes=8) combined with low temporalAlpha producing slow convergence per direction.

## 3. Mode 3 Smoothness & Mode 0 Blur

- **Mode 3** (5× amplified) looks **spatially smooth overall** — the walls are clean cream tones with no high-frequency speckle. Noise level is good. The dark patches on the ceiling persist however, confirming they are **structural** (probe/dir bin) rather than stochastic noise.
- **Mode 0** shows the bilateral blur has **slightly over-smoothed the right wall**, where the GI shadow-blob bleeds into a soft brown smear. The corridor depth cue is preserved, but the right-wall geometry detail is muddy.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Fair** | Pleasing depth/atmosphere but right-wall over-blurred, GI patches leak through |
| 3 (Indirect ×5) | **Good** | Smooth, low noise, reveals structural artifacts cleanly |
| 6 (GI only) | **Fair** | Banding/blob artifacts visible, dark right-wall patch is unphysical |

## 5. Stats Cross-Check & Parameter Recommendation

Stats confirm:
- `anyPct=100` everywhere → no under-sampled probes (good).
- `surfPct` rises 48→100 across cascades — expected.
- **C0 variance (0.00103) is ~3× C1 and ~100× C3** → spatial structure concentrated in the finest cascade, matching the visible blob/banding in mode 6.
- `maxLum=0.54` in C0 vs `meanLum=0.017` → very high dynamic range per probe = **a few directional bins are catching bright hits while neighbors miss them**. Classic dirRes-too-low signature.

### Concrete change: **increase `dirRes` from 8 to 12 (or 16)**

This directly addresses the C0 variance and the isolated dark blob on the right wall. Increasing angular resolution will smooth the directional sampling without needing to change `temporalAlpha` (0.05 is already conservative and mode 3 confirms temporal stability is fine).

If GPU budget is tight (cascadeTimeMs=0.347 leaves headroom), pair with reducing `c0MinRange` slightly to keep the finest cascade tight around the camera where the artifacts live.
