# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784850806775870_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784850807682884_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784850808559103_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784850806775870.json`  
**Analyzed:** 2026-05-11T15:38:34  
**Model:** claude-opus-4-7

---

## Visual Triage

### 1. GI contribution (cross-mode comparison)

Comparing mode 0 (final) against mode 6 (GI only), the indirect term is doing most of the visible shading work in this frame — there is no strong direct-lit hotspot anywhere except the bright window/door slot at the end of the corridor (center, around the vanishing point) and the row of bright specks running down the floor (~y=400–650, center). Mode 6 confirms those bright floor specks are *not* GI — they vanish in mode 6 and mode 3, so they are direct light leaks (probably emissive/sun samples reaching the floor through the ceiling slats).

Indirect light is **brightest**:
- On the right wall mid-height (x≈900–1100, y≈300–500), warm orange bounce — clearly visible in mode 6.
- On the left wall near the foreground (x≈100–300, y≈200–500), cooler/whiter bounce.
- Around the doorway opening at the end of the corridor (center, y≈280–380), reddish bounce from the back wall.

Indirect light is **absent / very dark**:
- Ceiling between the slat fixtures (top center) reads near-black in mode 6, even though those are the actual light sources — they are self-occluded from probes below.
- Lower-right foreground (x≈1000–1280, y≈500–720) goes muddy brown with almost no detail.

### 2. Banding in mode 6

I can see two distinct artifact families in mode 6:

- **Large soft "blocky" patches** on the right wall (roughly 80–120 px across, e.g. the dark lobe at x≈1100, y≈550 and the lighter lobe at x≈900, y≈350). Spacing is consistent with the **probe grid at cascade 0/1** (baseRes 32 over volumeSize 4 ≈ a probe every ~30–40 screen px at this depth). These are *probe-grid* artifacts, not directional bins.
- **Stair-stepped luminance jumps** along the corridor floor as it recedes (visible as horizontal-ish bands at y≈500, y≈430, y≈380). The spacing roughly doubles with depth, which is the signature of **cascade boundaries** (c0→c1→c2 transitions).

I do *not* see ringed/pinwheel patterns that would indicate dirRes=8 directional aliasing — the 8-bin SH/octa basis appears adequate for this low-frequency lighting.

### 3. Smoothness of mode 3 vs over-blur in mode 0

Mode 3 (5× amplified) is **spatially smooth** — almost too smooth. There is no high-frequency Monte-Carlo noise; the cascades are clearly converged (variance 1e-3 → 9e-6 across c0–c3). What's left is the structured banding noted above, not stochastic noise.

Mode 0 shows signs of the bilateral blur **over-smoothing**:
- The right-wall corner (x≈700, y≈100–700) has lost geometric definition — the wall/ceiling seam is barely distinguishable.
- The foreground-right blob (x≈1000+, y≈500+) is smeared into a single brown gradient with no surface cue.
- Conversely, edges that *should* be soft (the bright floor specks) are kept sharp because direct light isn't blurred — that part is fine.

### 4. Ratings

- **Mode 0 (final):** **Fair** — usable, but bilateral blur is too aggressive on the right side.
- **Mode 3 (indirect ×5):** **Good** — smooth, low noise, banding is structural not stochastic.
- **Mode 6 (GI only):** **Fair** — clearly reveals probe-grid and cascade-boundary structure that the blur is hiding.

### 5. Stats cross-check & recommended change

Stats say:
- All cascades at `anyPct=100`, surfPct ramps 48 → 85 → 100 → 100. **c0 is undersampling surfaces** (only 48.6% of c0 probes hit a surface), which matches the blocky probe artifacts I see at near-field on the right wall.
- Variance drops cleanly per cascade (1e-3 → 7e-4 → 3e-4 → 9e-6), so higher cascades are well-converged — the visible banding is dominated by **c0**, consistent with the variance ratio.
- temporalAlpha=0.05 is already very smoothing, so noise is not the issue; structure is.

**Concrete
