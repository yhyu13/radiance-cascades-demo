# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785676404460406_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785676406941931_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785676408730003_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17785676404460406.json`  
**Analyzed:** 2026-05-12T14:34:28  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

All three images show a **uniform dark slate/navy field** (~RGB 22,22,32) with no visible scene content — no Cornell Box walls, no light source, no color-bleed gradient. I cannot isolate GI contribution by location because there is no direct-light reference visible in mode 0, no amplified indirect signal visible in mode 3, and no raw GI visible in mode 6. The frame appears to be either pre-first-bounce, mis-cleared, or rendering into an off-screen target.

## 2. Banding Artifacts (Mode 6)

**None visible.** I cannot identify probe-grid stripes, directional-bin facets, or cascade-boundary seams in the supplied image — the field is flat. If banding exists it is below the ~1 LSB display threshold of the dark background color.

## 3. Smoothness Assessment

- **Mode 3 (Indirect ×5):** Appears spatially smooth, but this is uninformative — a zero signal multiplied by 5 is still zero. No noise structure is observable.
- **Mode 0 bilateral blur:** Cannot evaluate edge preservation; no edges are present in the frame to over-smooth.

## 4. Mode Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 Final | **Poor** | No scene content reaching composite |
| 3 Indirect ×5 | **Poor** | Even at 5× gain, indirect is invisible |
| 6 GI only | **Poor** | Raw GI buffer reads as empty |

## 5. Stats Cross-Check (the real story)

The probe statistics confirm the visual diagnosis:

- **Cascade 0: `anyPct = 0`** — the finest cascade has **zero** probes with any hit. This is the cascade that contributes most to near-field GI; its emptiness explains the black frame.
- **Cascade 1: `anyPct = 2.1%`**, Cascade 2: `4.9%` — extremely sparse.
- **Cascade 3: `anyPct = 58.6%` but `meanLum = NaN`, `variance = NaN`** — NaN propagation in the coarsest cascade is almost certainly poisoning downstream sampling (NaN × anything → NaN → clamped to 0 in composite).
- **`surfPct = 0` across all cascades** — no probe is registering a surface hit anywhere. Either the raymarch is missing geometry entirely, or the SDF/voxel volume is empty/mis-transformed.
- **`maxLum = 21.3` in C3** with zero surface hits implies sky/emissive hits only, but `skyPct = 0` too — contradictory, reinforcing the NaN-corruption hypothesis.

## Concrete Parameter Change

**Do not tune `temporalAlpha`, `probeJitterScale`, or `dirRes` yet — they cannot fix a zero-input pipeline.**

The actionable fix: **investigate the raymarch/scene binding before any probe-tuning.** As a single parameter change to make the failure visible and bounded:

> **Increase `c0MinRange` from 0.5 → 2.0** (matching `volumeSize`-relative scale) and add a NaN-guard clamp on cascade-3 accumulation.

Rationale: `c0MinRange = 0.5` against a `volumeSize = 4` scene means C0 rays terminate within 12.5% of the volume — likely missing all geometry from most probe origins, which explains `surfPct = 0` at C0 and starves the cascade chain. Extending C0 reach should produce non-zero `surfPct` and let the NaN in C3 be isolated independently.
