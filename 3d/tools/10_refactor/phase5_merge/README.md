# Phase 5 Upper-Cascade Merge

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\10_refactor\phase5_merge\run_phase5_merge.ps1
```

Validates G6 weighted upper-cascade merge with independently generated address/visibility fixtures, candidate-clamped chart edges, distinct look-back alpha versus radiance addresses, bilinear visibility renormalization, finite no-visible behavior, and locked transition blending. Temporal feedback remains disabled.
