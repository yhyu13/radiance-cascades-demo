# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782196557833715_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782196558725716_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782196559709850_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17782196557833715.json`  
**Analyzed:** 2026-05-08T13:54:50  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing the three views, the indirect light is concentrated almost exclusively at the **top rim of the box** (the bright horizontal band at ~y=200), with a faint vertical falloff down the inner walls. This is consistent with bounce light from the lid/ceiling region.

- **Inside the box body** (the large gray area): mode 6 shows it nearly black, mode 3 shows it as a smooth mid-gray after 5× amplification → actual GI here is very dim (~0.15–0.2 linear).
- **Floor region** (bottom of frame): completely dark in all three modes — no indirect light is reaching the lower interior, even though geometrically it should receive bounces from the lit rim.
- **Outside the box** (upper black region): correctly zero in all modes.

The direct-only expectation would have a sharply lit rim and a darker interior; mode 0 reproduces this but the interior is flatter/grayer than the GI-only image suggests it should be — meaning **direct light dominates** the final composite and GI is barely contributing outside the rim.

## 2. Banding in Mode 6

The GI-only image shows two distinguishable artifacts:

- **Horizontal band at y≈195–215**: a bright stripe with a softer halo just below it (y≈220–260). The spacing (~25 px between peak and halo edge) and its alignment with the lid geometry suggest this is **probe-grid aliasing** — the top row of probes in cascade 0 sits right at the rim and the next row down is too far below to interpolate smoothly.
- **Faint vertical seam near x≈640** (very subtle, center of frame): looks like a **cascade boundary** between c0 and c1 footprints, since `volumeSize=4` and `baseRes=32` would place a cascade transition near the view center.
- No directional-bin rosettes are visible (which would appear as radial spoke patterns); `dirRes=8` seems sufficient for this low-frequency scene.

## 3. Smoothness Assessment

- **Mode 3** (5× indirect): looks **spatially smooth**, almost too smooth — there is no visible noise grain, suggesting heavy temporal accumulation (consistent with `temporalAlpha=0.05`). The rim halo bleeds ~40 px into the black region above the box, which is **probe leakage past geometry**.
- **Mode 0**: the bilateral blur has **not over-smoothed** the rim highlight — it's still crisp. However the interior gradient is so featureless that any GI structure has been washed into the direct term; can't tell if blur is hiding detail or there simply is none.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Fair** | Looks plausible but interior under-lit; GI barely contributes |
| 3 (Indirect ×5) | **Fair** | Smooth but reveals leak above rim and dead floor |
| 6 (GI only) | **Poor** | Visible probe-row band; floor receives zero bounce light |

## 5. Stats Cross-Check & Recommendation

The probe stats confirm the visual diagnosis:

- **Cascade 0**: `anyPct = 0.6%` — almost no probes have valid hits. This is why the interior/floor is black in mode 6.
- **Cascade 1**: `anyPct = 59.7%` but `meanLum = NaN`, `variance = NaN` — **NaN propagation**, likely from a divide-by-zero in the luminance accumulator when `surfPct=0` but `anyPct>0` (rays hit something but neither surface nor sky classification fired). This NaN is almost certainly poisoning downstream cascades.
- **Cascade 2**: completely empty (`anyPct=0`).
- **Cascade 3**: 42% any, 100% surface, but `maxLum=1.3` — the far cascade is working but contributes negligibly.

The high `variance=5168` in cascade 0 with only 0.6% hits matches the visible **horizontal band**: a tiny number of probes at the rim carry huge luminance (`maxLum=686`) and dominate interpolation.

**Concrete change**: Increase **`c0MinRange` from 1 → 2** (or raise `volumeSize` from 4 → 6). Cascade 0's footprint is too small to reach the box interior, leaving the floor unsampled and forcing the rim probes to do all the work (
