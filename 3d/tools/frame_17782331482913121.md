# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782331482913121_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782331483998583_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782331484629202_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17782331482913121.json`  
**Analyzed:** 2026-05-08T17:39:39  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Cascade

## 1. GI Contribution Isolation

Comparing modes 0 and 6 against the direct-only expectation:

- **Top rim (above the box, ~y=210–235)**: The brightest GI band sits on the upper lip/ceiling edge — visible as a warm-white horizontal strip in all three modes. This is where indirect bounce concentrates (likely ceiling catching light from interior).
- **Box front face (interior, ~x=440–840, y=240–540)**: Indirect contribution is very dark (mode 6 shows near-black flat gray). The face is lit almost entirely by direct light in mode 0.
- **Lower-left and lower-right corners of the front face**: subtle GI brightening visible in mode 3 (the amplification reveals a faint vignette of indirect light hugging the bottom edge ~y=510).
- **Outer purple bars (x<200, x>1080)**: Identical across all modes — these are background/skybox, unaffected by GI.

The scene is **direct-light dominated**; GI is contributing maybe 5–10% of final luminance on the visible front face.

## 2. Banding Artifacts in Mode 6

Looking carefully at mode 6:
- **Top rim band (~y=215–235)**: A single horizontal bright stripe with slight intensity variation along its length — consistent with a **probe-grid row** at the ceiling height. Spacing isn't repeated vertically, so it reads as one probe layer rather than directional binning.
- **Front face**: I do **not** see clear discrete bands, ringing, or stair-stepped luminance steps. The gradient is smooth-ish but extremely low amplitude.
- **Faint darkening near image center (~x=640, y=400)**: very subtle radial falloff, no hard cascade-boundary ring visible.

No strong evidence of directional-bin (`dirRes=8`) striping or cascade-boundary discontinuities at this exposure.

## 3. Smoothness Assessment

- **Mode 3 (5× indirect)**: Spatially **smooth**, no temporal noise visible, no fireflies. The 5× amplification would expose Monte Carlo noise if present — it doesn't. This suggests temporal accumulation (`temporalAlpha=0.05`) is working well, possibly *too* well (over-smoothed, slow to respond).
- **Mode 0 (final)**: Bilateral blur has not over-smoothed — the top rim highlight is still crisp, and the box silhouette edges are sharp against black background. No haloing visible.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| Mode 0 (final) | **Good** | Clean composite, edges preserved, but indirect contribution barely visible — scene looks under-lit by GI. |
| Mode 3 (×5) | **Good** | Smooth, noise-free, confirms GI signal is stable. |
| Mode 6 (raw GI) | **Fair** | Signal is so dim (meanLum ~0.01–0.03) that the GI term contributes little to the final image; top-rim probe row faintly visible. |

## 5. Stats Cross-Check & Recommendation

Key observations from stats:
- `surfPct` drops to **81.6%** at cascade 0 — ~18% of finest-level probes hit nothing useful. This is the under-sampling signature.
- `variance` decreases monotonically across cascades (0.0021 → 0.00002), consistent with the smooth appearance — **no high-variance band matches a visible artifact**, confirming GI is stable but weak.
- `maxLum` at C0 = 0.65 vs `meanLum` = 0.027 → strong outliers (the top-rim hotspot) but average energy is tiny.
- `cascadeTimeMs=0.25` — plenty of headroom.

### Concrete parameter change:

**Increase `c0MinRange` from 1 → 2 (or raise `baseRes` from 32 → 48).**

Rationale: cascade 0's `surfPct=81.6%` indicates many near-field probe rays are missing surfaces — they're ranging too short to hit the box interior walls that should be bouncing light onto the front face. Extending C0's range (or densifying its grid) will let the finest cascade actually capture the short-bounce energy from the box's own walls, which is exactly the missing GI on the front face. This should noticeably brighten the indirect term in m
