# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17799612276950685_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17799612278907986_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17799612280986613_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17799612276950685.json`  
**Analyzed:** 2026-05-28T17:40:51  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Render Modes

## 1. GI Contribution Isolation

Comparing mode 0 (final) against mode 6 (GI only):

- **Color bleeding** is visible on the floor near the red wall (left side, warm tint) and near the green wall (right side, cool tint). Mode 6 shows this most clearly as red/green wash extending ~30% across the floor from each wall.
- **Back wall** in mode 6 picks up a desaturated reddish-green gradient from both side walls — strongest near the wall-floor junction.
- **Tall block (left)** receives noticeable red bounce on its left-facing side; **short block (right)** picks up green on its right face. Both visible in modes 3 and 6.
- **Ceiling** in mode 6 is dim and warm-tinted (bounced light from floor), as expected since the light source illuminates downward.
- **Shadow regions** (behind/under blocks in mode 0) are clearly lifted by GI — visible by comparing the soft fill in mode 0 shadow areas against what would be pure black under direct-only.

## 2. Banding Artifacts in Mode 6

- **Blocky low-frequency patches** are visible on the back wall and floor — roughly square cells ~80–100 px wide at this resolution. With `baseRes: 32` and `volumeSize: 4`, this corresponds well to **probe-grid spacing in cascade 0** (one probe per ~0.125 world units, projecting to roughly that screen size at this camera distance).
- **No fine directional-bin striping** is visible — `dirRes: 8` (64 directions) appears sufficient; I don't see octahedral seam patterns.
- **Cascade boundary**: a faint brightness step is visible roughly at the midground floor depth — consistent with c0→c1 handoff (`c0MinRange: 0.5`).
- **No temporal flicker can be assessed from a still frame.**

## 3. Smoothness Assessment

- **Mode 3** (×5 indirect): looks **spatially very smooth**, almost too smooth — no high-frequency noise but also no contact detail. The blocks read as soft blobs with no AO-like darkening at their bases.
- **Mode 0** (final): bilateral blur appears **well-tuned overall** — block edges are crisp, wall corners are preserved. However, the shadow under the tall block on the floor looks slightly **smeared leftward**, suggesting the bilateral kernel is leaking GI across the shadow boundary.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Good** | Clean composite, minor shadow leak |
| 3 (Indirect ×5) | **Fair** | Smooth but lacks contact/structural detail |
| 6 (GI raw) | **Fair** | Visible probe-grid blocking on back wall/floor |

## 5. Stats Cross-check & Recommendation

- `surfPct` for cascade 0 is only **84.86%** — ~15% of c0 probes failed to find a surface hit. This is the likely source of the blocky patches in mode 6, since those probes fall back to coarser cascades.
- `variance` in c0 (0.0080) is ~50× higher than c3 (0.00016), consistent with the visible spatial structure being concentrated in the near-field cascade.
- `maxLum` 0.56 in c0 vs `meanLum` 0.05 indicates strong outliers — a few probes catching the light source directly, contributing to patch brightness variance.

### Concrete suggestion

**Increase `probeJitterScale` from 0.06 → 0.12** (and optionally raise `jitterHoldFrames` from 2 → 4 to keep temporal stability). The under-sampling in c0 (`surfPct 84.8%`) plus the visible grid-aligned blockiness suggests probes are landing in correlated positions; more aggressive jitter will diffuse the grid pattern and improve `surfPct` as probes sample a wider neighborhood. Given `temporalAlpha: 0.05` already provides heavy temporal smoothing, the extra jitter should integrate cleanly without flicker.

If blockiness persists after that, the next lever is **`baseRes: 32 → 48`** to physically densify c0 probes.
