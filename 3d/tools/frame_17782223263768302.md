# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782223263768302_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782223270466902_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17782223277275396_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17782223263768302.json`  
**Analyzed:** 2026-05-08T14:39:21  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Renders

## 1. GI Contribution Isolation

Comparing the three views, the indirect light contribution is **strongly concentrated in a horizontal band along the upper rim** of the box (roughly the top ~10% of the visible geometry). The lower ~80% of the front wall shows essentially **flat, uniform GI** with no visible bounce gradient.

- **Mode 6 (raw GI)**: rim band reads ~0.3–0.4 luminance, body reads ~0.05 (matches `meanLum ≈ 0.05` in stats).
- **Mode 3 (×5)**: confirms the rim is genuinely the brightest GI region — it saturates near white while the body lifts only to mid-grey.
- **Mode 0 (final)**: the rim highlight survives but the body is identical to mode 6 scaled — meaning **the visible image is dominated by indirect light**; there is little or no direct lighting reaching the front wall in this view.

The expected Cornell color-bleed gradient (brighter near floor/wall junctions, darker in the center of large flat panels) is **absent** — the body is suspiciously uniform.

## 2. Banding in Mode 6

Visible structure in mode 6:
- A **single bright horizontal band** at the top rim, ~30–40 px tall, with soft fade-off downward over ~80 px. This aligns with a **probe row at the top of the volume** — consistent with `baseRes: 32` / `volumeSize: 4` giving probe spacing of ~1/8 of the box height in screen space.
- A **very faint vertical seam** near the left and right edges of the front face (~5% in from each side), most visible in mode 3. Spacing and verticality suggest a **cascade boundary** (cascade 0 → 1 transition), not a directional bin (which would produce angular wedges) and not the probe grid (which would tile more frequently).
- No visible directional-bin banding (no wedge/star artifacts), so `dirRes: 8` is adequate for this smooth surface.

## 3. Smoothness Assessment

- **Mode 3** (indirect ×5): genuinely smooth across the body, no temporal sparkle visible in a still frame. The rim transition is soft. Variance stats (0.004–0.008) are low and consistent with the smooth appearance.
- **Mode 0** (final): the bilateral blur has **not over-smoothed** — the rim highlight retains its sharp upper edge against the black background. However, given how flat the body already is, the blur is doing very little useful work here.
- The ragged silhouette edges visible at the top rim and bottom corners are **geometric/MSAA aliasing**, not GI artifacts.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (final) | **Fair** | Plausible but flat; no color bleed, no spatial variation across body |
| 3 (indirect ×5) | **Good** | Clean, smooth, no noise; reveals rim concentration clearly |
| 6 (raw GI) | **Fair** | Confirms GI is present but body energy is suspiciously uniform |

## 5. Stats-Informed Diagnosis & Recommendation

Cascade stats are healthy on coverage (`anyPct: 100`, `surfPct: 98–100`), so probes are not under-sampled. But note:
- `meanLum` is nearly identical across cascades 0–2 (~0.05) but **cascade 3 collapses to 0.029 with variance 0.00015** — cascade 3 is essentially a DC term, contributing almost no spatial detail.
- `temporalAlpha: 0.05` is very low (20-frame effective history), which would explain the lack of fine spatial variation if the camera or lights moved recently — the history is dragging the field toward uniformity.

**Concrete recommendation:** Raise **`temporalAlpha` from 0.05 → 0.15**. The variance numbers are already low enough that you don't need such aggressive temporal smoothing, and the current value is likely the cause of the over-uniform body. If banding then reappears at the rim, compensate with `probeJitterScale: 0.06 → 0.10` rather than dropping alpha back down.

Secondary suggestion if rim banding persists: investigate why cascade 3's `maxLum` (0.054) is an order of magnitude below cascades 0–2 — the far cascade may not be receiving proper light injection, which would explain the abs
