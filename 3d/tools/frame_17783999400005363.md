# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783999400005363_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783999406874903_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17783999413789846_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17783999400005363.json`  
**Analyzed:** 2026-05-10T15:59:32  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Renders

## 1. GI Contribution Analysis

Comparing the three views, the indirect light contribution is concentrated almost entirely along the **top edge** of the frame (the ceiling/upper rim) and shows faint warm bleed wrapping down the **left and right vertical edges** in the upper third. The center and lower portions of the box interior are essentially black in mode 6, meaning GI is contributing very little there — the final composite (mode 0) in those areas is almost pure direct lighting plus a flat, very dim ambient term.

The brightest GI hotspot is a small notch at the **top-left corner** (~x=320, y=180), with a matching but weaker spot at the **top-right corner**. This is consistent with light bouncing off the top inner lip of the frame.

## 2. Banding in Mode 6

Visible banding/structure in the GI-only pass:
- **Horizontal stripe** along the top rim (~60 px tall) with a soft but clearly stepped lower boundary around y≈260. The transition from lit→dark is a single hard step, not a gradient — characteristic of a **cascade-boundary discontinuity** between c0 and c1.
- **Vertical falloff streaks** down the left edge (~x=380–420) and right edge (~x=2180), spaced as single wide bands rather than repeating cells. These align with the volume bounds of the innermost cascade — again a **cascade transition**, not probe-grid aliasing.
- I do **not** see repeating square cells that would indicate `baseRes=32` probe-grid aliasing, nor pinwheel/star patterns that would indicate `dirRes=8` directional-bin undersampling.

## 3. Smoothness & Blur Assessment

- **Mode 3 (indirect ×5):** Spatially **smooth** — no speckle, no fireflies, no temporal shimmer evident in a still. The corner highlights are soft gradients. Variance stats (0.0085, 0.0083, 0.0044, 0.00016) confirm low spatial noise, which matches the visual.
- **Mode 0 (final):** The bilateral blur has **not over-smoothed** any visible edges; the top rim's bright lip is still crisp against the dark interior. If anything, the composite looks *under-lit* in the interior rather than smeared.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 Final | **Fair** | Clean but interior is too dark; GI barely contributes outside the top band. |
| 3 Indirect ×5 | **Good** | Smooth, low-noise, clearly readable falloff. Reveals the cascade-boundary issue. |
| 6 GI only | **Fair** | Confirms GI is converging (surfPct 98–100%) but exposes hard cascade seams. |

## 5. Stats Cross-Check & Recommendation

- `surfPct` is 98.3 / 99.2 / 100 / 100 → cascades are **well-converged**, no under-sampling.
- `meanLum` drops sharply from c2 (0.049) to c3 (0.029) and `maxLum` collapses from 0.37 → 0.054 — a **6.8× luminance discontinuity** between cascades 2 and 3. This is exactly the cascade seam visible at the top-band lower boundary in mode 6.
- `variance` in c3 (0.00016) is two orders of magnitude lower than c0–c2, confirming c3 is contributing almost nothing useful to the outer regions.

**Concrete change:** Increase `c1MinRange` (and/or the per-cascade range scale) so cascade 1's footprint overlaps cascade 0's more generously — currently both are at `MinRange=1` with `volumeSize=4`, leaving a hard handoff. Alternatively, raise `baseRes` from 32 → 48 to extend cascade 0's coverage before the seam occurs. This will smooth the visible top-band cliff without touching temporal or jitter settings (which are already producing clean, low-variance output).
