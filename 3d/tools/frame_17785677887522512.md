# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785677887522512_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785677891777975_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785677895566293_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17785677887522512.json`  
**Analyzed:** 2026-05-12T14:37:09  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing mode 0 (final) against mode 6 (raw GI) and mode 3 (×5 amplified):

- **Right wall (mid-distance, around the lantern row)**: Strong indirect contribution visible as warm orange/peach bounce — clearly the dominant GI source. In mode 3 it saturates almost to white, confirming the lanterns are the primary emitter and the right wall's reddish material is bouncing warm light back into the corridor.
- **Left wall / pillar bases**: Receives noticeably less indirect light (darker in mode 6, dimmer in mode 3). This is plausible — the pillars self-occlude the bounce path.
- **Floor near camera (bottom-center)**: GI is essentially absent in mode 6; the floor reads almost pure black. Direct light from lanterns is what makes it visible at all in mode 0.
- **Far end of corridor (vanishing point doorway)**: Both mode 3 and mode 6 show minimal indirect — the dark rectangle there is correctly unlit by GI.

## 2. Banding Artifacts in Mode 6

Visible structured artifacts:
- **Large overlapping circular discs on the right wall and ceiling** (most obvious in the right third of the frame, growing in apparent size with distance). These are *probe footprint* artifacts — spherical interpolation kernels of individual probes becoming visible because neighboring probes disagree. Spacing is consistent with the **base 64³ / 4-unit volume** giving a coarse probe grid in screen space at distance.
- **Concentric ring pattern on the right wall** (very clear in mode 3) — these are the same probe spheres but smoothed; spacing roughly matches one probe per ~8–12 px at mid-depth, doubling at the far wall, which is the classic **cascade-boundary transition** between c1 and c2.
- No obvious directional-bin banding (no angular wedge/pinwheel pattern), so `dirRes=8` is adequate angularly.

The dominant artifact is **probe-grid (spatial)**, secondarily **cascade transitions**, not directional.

## 3. Smoothness / Over-blur Assessment

- **Mode 3** is *not* smooth — it shows clear probe-disc structure and ring banding, indicating low temporal convergence and visible interpolation footprints. Not noisy in a stochastic sense (no per-pixel grain), but spatially structured.
- **Mode 0** the bilateral blur has hidden most high-frequency probe noise, but it has **over-smoothed the floor light shafts** near the bottom-center — the rectangular sunbeams from the lanterns are smeared into ragged, melting shapes rather than crisp rectangles. The ceiling lantern silhouettes are also blurred into bokeh-like blobs.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Fair** | Lighting reads correctly but blur destroys lantern/shaft edges |
| 3 (Indirect ×5) | **Poor** | Probe discs and cascade rings dominate; under-converged |
| 6 (GI only) | **Poor** | Same artifacts visible without amplification, plus near-black floor |

## 5. Stats Cross-Check & Recommendation

The stats confirm the visual diagnosis:
- **c0 anyPct = 0** — the finest cascade is contributing nothing. This explains the lack of crisp near-field GI on the floor.
- **c1 = 3.3%, c2 = 7.4%, c3 = 0.4%** — extremely low surface-hit coverage across all cascades; `surfPct=0` everywhere means probes are barely registering surface hits.
- **`meanLum = -nan`, `maxLum = inf`** in c1 — a NaN/Inf is poisoning the cascade, which is consistent with the bright over-saturated discs in mode 3.
- **`temporalAlpha = 0.05`** with **`probeJitterScale = 0.06`** and `jitterHoldFrames=2`: jitter is small and temporal blend is slow, so probe footprints don't average out — exactly matching the visible static disc pattern.

### Concrete recommendation

**Raise `probeJitterScale` from 0.06 to ~0.25–0.4** (and optionally drop `jitterHoldFrames` to 1).
