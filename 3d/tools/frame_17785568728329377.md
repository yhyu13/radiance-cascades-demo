# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785568728329377_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785568730996041_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17785568734245164_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17785568728329377.json`  
**Analyzed:** 2026-05-12T11:35:03  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing mode 0 (final) against mode 6 (raw GI) and mode 3 (5× indirect):

- **Bright GI zones**: Ceiling near the light fixtures (top-center, upper-left strip) shows the strongest indirect contribution — visible in mode 3 as bright cream tones bouncing onto the upper walls and the left column faces.
- **Mid-strength GI**: The right wall (warm orange/copper) receives substantial indirect bounce — clearly visible in mode 6 as the dominant warm tint, since the right wall has almost no direct light reaching it at this angle.
- **GI-absent zones**: The far end of the corridor (center vanishing point) is nearly black in all three modes — indirect light fails to propagate down the hallway. The floor is also surprisingly dim in mode 6, suggesting the floor probes are starved.
- **Direct-only contribution** (mode 0 minus mode 6): The bright vertical streaks on the left wall windows and the small floor highlights are pure direct lighting; GI contributes little there.

## 2. Banding Artifacts in Mode 6

- **Concentric arc bands on the right wall**: Large, smooth, roughly circular rings centered near the upper-right of the frame. Spacing increases with distance — this is the classic signature of **cascade boundary transitions** (cascades 0→1 with `volumeSize=4` and doubling ranges).
- **Blocky stair-step artifacts on the ceiling fixtures**: Sharp pixelated edges around the bright lights — these are **probe-grid aliasing** at `baseRes=32` where high-luminance probes (maxLum=25253 in cascade 1) are not interpolated finely enough.
- **No visible directional-bin streaks** (would appear as radial fans from bright sources); `dirRes=8` seems adequate for this scene's smoothness.

## 3. Smoothness Assessment

- **Mode 3** is **very smooth** spatially — almost too smooth, suggesting heavy temporal accumulation (consistent with `temporalAlpha=0.05`). No visible noise grain.
- **Mode 0**: The bilateral blur preserves the geometric edges of the ceiling fixtures and floor lights well, but **over-smooths the left wall** — the window/column structure looks mushy and the depth cues are lost in the mid-distance pillars.
- **Mode 6** shows the cascade arcs clearly because no blur is hiding them.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Fair** | Reasonable composite but over-blurred on left wall; far corridor unnaturally black |
| 3 (Indirect ×5) | **Good** | Smooth, plausible bounce distribution; useful for diagnosis |
| 6 (GI only) | **Poor** | Visible cascade arcs on right wall + probe aliasing on ceiling |

## 5. Stats Cross-Check & Recommendation

The probe stats confirm the visual diagnosis and reveal serious problems:

- **Cascade 0**: `anyPct=0.009%` — almost no probes hit anything. Severely under-sampled.
- **Cascade 1**: `meanLum=NaN`, `variance=NaN`, `maxLum=25253` — **NaN propagation from a divide-by-zero or Inf injection**. This explains the harsh cascade-boundary arcs on the right wall.
- **Cascades 2 & 3**: All zeros / negative variance (`-0.015625`) — these cascades are **completely dead or corrupted**. Negative variance is mathematically impossible; this is a buffer-init or accumulator underflow bug.
- `surfPct=0` everywhere — **no probe is registering a surface hit**, yet GI is appearing. The pipeline is likely falling back to sky/ambient and the surface-classification path is broken.

### Concrete recommendation

**Primary fix (bug)**: The NaN in cascade 1's mean luminance and the negative variance in cascade 3 must be addressed first — clamp/guard the accumulator (`max(0, lum)` and `isfinite()` check) before any parameter tuning is meaningful.

**Parameter change** (after the NaN guard): **Raise `temporalAlpha` from 0.05 → 0.15** and **increase `probeJitterSc
