# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784042982136163_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784042984192535_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784042986349142_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784042982136163.json`  
**Analyzed:** 2026-05-10T17:12:10  
**Model:** claude-opus-4-7

---

# Visual Triage Report — Cornell Box GI Cascade

## 1. GI Contribution Analysis

Comparing mode 0 (final) against mode 6 (GI only):

- **Color bleeding from walls**: Mode 6 shows the red wall casting a faint reddish tint onto the left side of the tall block and onto the floor near the left wall; symmetrically, the green wall tints the right block and floor-right. This bleed is visible but very subtle in mode 0 because direct white light dominates.
- **Ceiling near light**: Mode 6 shows a bright disc on the ceiling — that's the area light's back-bounce concentrated near the emitter. This region is washed out in mode 0.
- **Shadowed/occluded zones**: The floor *between* and *behind* the two blocks shows the strongest GI lift in mode 6, exactly where direct light is blocked. This is the textbook "indirect fill" role and it is functioning.
- **Absent GI**: The black background outside the box receives no GI (correct — no surfaces). The cascade-frame artifacts (gray bars top/bottom, red/green bars left/right) extending *outside* the room geometry indicate probes outside the box are still sampling the wall colors and projecting them — this is a probe-volume-vs-room-bounds mismatch.

## 2. Banding in Mode 6

- **Outer rectangular halo**: Bold gray bars top and bottom, bold red/green bars left and right, all forming a square frame *outside* the room silhouette. The spacing matches one cascade-0 probe cell (`baseRes 32`, `volumeSize 4` → ~0.125 units), and the shape is axis-aligned blocks rather than radial lobes — this is **cascade-boundary leakage / probe-volume overhang**, not directional binning.
- **Soft circular gradient on back wall**: Smooth radial falloff with no visible step rings — directional bins (`dirRes 8` = 64 directions) are adequately resolved here.
- **No diagonal hatching or 8-way star patterns** are visible, so directional aliasing is not the dominant artifact.

Conclusion: banding source is **cascade/probe-volume boundary**, consistent with cascade-3 having very low variance (0.00016) but cascades 0–1 having ~50× higher variance (0.0085) — the outer cascade is stable while inner cascades are leaking edge probes.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: Spatially very smooth — no salt-and-pepper noise, no probe-cell checkerboard. Temporal accumulation (`alpha 0.05`) is doing its job. The block silhouettes are slightly soft but still distinguishable.
- **Mode 0 (final, blurred)**: Bilateral blur has **over-smoothed the contact shadow** under the right cube (the shadow blends into the floor too gradually) and slightly haloed the left block's cast shadow on the floor. Wall corners and block edges remain crisp, so chroma/depth guidance is mostly working.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 — Final | **Good** | Plausible Cornell result; minor shadow over-blur and faint outer probe halo |
| 3 — Indirect ×5 | **Good** | Smooth, low-noise, readable bounce structure |
| 6 — GI only | **Fair** | Strong cascade-boundary frame artifact dominates outside the room |

## 5. Stats Cross-Check & Recommendation

- `surfPct` is 98–100% across all cascades and `anyPct` is 100% → **probes are fully converged**, not under-sampled.
- `skyPct = 0` everywhere → no sky leakage path; the outer halo is purely surface-sampled wall color bleeding into out-of-room probes.
- Variance drops monotonically (0.0085 → 0.00016) across cascades — healthy falloff, but cascade 0's variance combined with the visible outer frame says the **probe volume is too large relative to the room**, so outer cascade-0 probes sit in empty space and project wall radiance outward.

**Concrete change**: Reduce `volumeSize` from **4 → ~2.5** (tighten the probe volume to the room interior), or alternatively raise `c0MinRange` so cascade 0 covers a smaller world footprint. This will eliminate the rectangular outer halo seen in m
