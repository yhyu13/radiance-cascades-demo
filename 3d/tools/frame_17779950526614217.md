# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17779950526614217_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17779950528881923_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17779950530951770_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17779950526614217.json`  
**Analyzed:** 2026-05-05T23:31:26  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Renders

## 1. GI Contribution Isolation

Comparing the three modes, indirect light is contributing in expected places:

- **Color bleeding on the back wall**: Mode 6 shows a faint red wash on the left portion of the back wall and green wash on the right — classic Cornell color bleed. Mode 0 shows this only weakly because direct light dominates.
- **Floor under the tall block (left-center)**: Mode 6 shows soft brightening from bounced ceiling light; the shadow there in Mode 0 is correctly *not* black.
- **Ceiling near the light**: Mode 3 and Mode 6 both show a bright hotspot directly under/around the light position on the ceiling — this is the light's own emission leaking into the GI buffer (expected if the emitter is sampled as a surface).
- **Indirect is *absent*** on the front-facing sides of both blocks (they appear dark/flat in Mode 6), which is correct since they face the back wall and receive little bounce.

## 2. Banding Artifacts in Mode 6

I can see:
- **Soft blocky structure on the back wall** in Mode 6 — roughly 4–6 patches across the wall horizontally. Spacing is consistent with the **base probe grid (baseRes=32, volumeSize=4)** sampled at the wall — these are **probe-grid interpolation artifacts**, not directional bins.
- **A "halo" ring around the ceiling light spot** in Mode 6 — this looks like a single bright probe cell rather than a smooth falloff, again pointing to coarse spatial probe density.
- **No visible concentric/angular wedges** that would indicate dirRes=8 directional binning artifacts.
- **No sharp rectangular discontinuities** that would indicate cascade-boundary seams (consistent with the very low variance in cascade 3: 0.00016).

## 3. Smoothness & Over-blur Assessment

- **Mode 3 (indirect ×5)**: Spatially **smooth, not noisy** — temporal accumulation (α=0.05) is doing its job. No speckle. Edges of the blocks are blurry, which is expected from low-res probe interpolation.
- **Mode 0 (final)**: Bilateral blur is **slightly over-smoothing** the contact shadow under the right block (the shadow blob bleeds outward). The tall block's left-edge contact shadow on the floor is preserved well. Wall-corner edges are crisp — blur is respecting geometry normals.
- The grey bars (top/bottom light fixtures?) appear soft-edged in all three modes, suggesting they're outside the probe volume or partially clipped.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| Mode 0 (final) | **Good** | Clean composite, plausible color bleed, mild over-blur on contact shadows |
| Mode 3 (indirect ×5) | **Good** | Smooth, no noise, shows expected bounce distribution |
| Mode 6 (GI only) | **Fair** | Visible probe-grid blockiness on back wall and ceiling hotspot |

## 5. Stats Cross-check & Recommendation

- `surfPct` is 98–100% across all cascades → probes are well-fed, no under-sampling.
- `anyPct=100` everywhere, `skyPct=0` → fully enclosed scene, expected.
- Cascade 0 variance (0.0085) is **~54× higher** than cascade 3 (0.00016) — the visible blockiness is concentrated in the finest cascade, matching what we see on the back wall.
- `cascadeTimeMs=0.077` is very cheap — there's headroom.

**Concrete change**: **Increase `baseRes` from 32 → 48 (or 64)**.

Justification: The banding is spatial (probe-grid), not directional or temporal. surfPct is already saturated, so more rays per probe won't help. Variance in C0 is high relative to C3, indicating the finest cascade lacks spatial resolution where it matters most (near surfaces). With raymarch+cascade totalling ~0.18 ms, a 1.5–2× baseRes bump is affordable and will directly attack the back-wall blockiness and the ceiling-light hotspot ring.

(Secondary option if baseRes can't change: raise `probeJitterScale` from 0.06 → 0.10 to dither the grid pattern,
