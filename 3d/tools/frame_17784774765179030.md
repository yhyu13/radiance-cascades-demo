# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784774765179030_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784774766785013_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784774768887233_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784774765179030.json`  
**Analyzed:** 2026-05-11T13:31:53  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box Hallway Renders

## 1. GI Contribution Analysis

Comparing mode 0 (final) against mode 6 (GI only) isolates the indirect light:

- **Strong GI presence**: The ceiling beams/slats in the upper-center (the repeating chevron pattern) receive significant indirect bounce — visible in mode 6 as warm reddish-brown tones. The right wall in the mid-distance also picks up notable warm bounce light, giving it the saturated terracotta/rust hue seen in mode 0.
- **GI absent / weak**: The floor in the foreground and mid-ground is nearly black in mode 6 — almost no indirect contribution reaches it. The left wall (lower half) is also very dim in GI-only. This means the bright floor in mode 0 is almost entirely **direct** light.
- **Vanishing point doorway**: In mode 3 (×5), the distant doorway glows warm — this is bounce from a hidden light source down the corridor, confirming GI is propagating distance-wise through the cascades.

## 2. Banding Artifacts in Mode 6

- **Vertical soft bands** along the right wall, spaced roughly every ~150–200 px in screen space. These have the smooth, large-scale falloff characteristic of a **probe-grid boundary** (baseRes 32, volumeSize 4 → ~0.125 units/probe at C0; the spacing matches a coarser cascade footprint).
- **Horizontal seam** running across the ceiling near the top third — a faint luminance step where the warm bounce stops abruptly. This looks like a **cascade-boundary transition** (C0→C1 handoff), consistent with `c0MinRange: 0.5`.
- **No directional-bin pinwheels** are visible (dirRes 8 is enough here — no obvious 8-fold rosette structure on flat surfaces).

## 3. Smoothness Assessment

- **Mode 3** is **spatially smooth, almost too smooth** — the ×5 amplification reveals a creamy, low-frequency field with no high-frequency noise. This matches the very low variance in stats (C0 var ≈ 1e-3, C3 var ≈ 9e-6). Temporal accumulation at α=0.05 is doing heavy lifting.
- **Mode 0**: bilateral blur has **over-smoothed** the foreground floor stripes — the dashed white markings at the bottom show ragged, melted edges that should be sharper rectangles. The right wall's bounce gradient also bleeds onto the ceiling boundary.
- The trim/handrail on the left side at mid-height has lost its silhouette in mode 0 — blur kernel is too aggressive across that depth discontinuity.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| 0 (Final) | **Fair** | Plausible mood, but bilateral blur eats edge detail on floor markings & left handrail |
| 3 (Indirect ×5) | **Good** | Smooth, no fireflies, GI propagates well into distance |
| 6 (GI only) | **Fair** | Visible probe-grid bands on right wall + ceiling cascade seam |

## 5. Stats Cross-Check & Recommendation

- **Convergence is healthy**: anyPct 100% across all cascades, surfPct climbs 48→100. C0's 48.6% surfPct is fine (many probes see open corridor air).
- **Variance vs. banding**: C0 variance 1e-3 with maxLum 0.54 → contrast ratio is the source of the visible right-wall bands. The lower cascades are essentially flat (var 9e-6 at C3), so the bands originate at **C0**.
- **Cascade time 19.25 ms vs raymarch 0.14 ms** — cascade build dominates; you have headroom to spend on quality.

**Concrete change**: Increase `probeJitterScale` from **0.06 → 0.15** (and consider `jitterHoldFrames` 2 → 1). The over-smooth mode 3 + low variance + visible static probe-grid bands on the right wall indicate jitter is too small to dither the C0 grid spacing. With `temporalAlpha: 0.05` already integrating ~20 frames, larger jitter will average out the band structure without introducing flicker. If bands persist, secondary change: bump `baseRes` 32 → 48 (you have the perf budget — raymarch
