# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777339362625671_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777339370079626_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17777339377447961_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17777339362625671.json`  
**Analyzed:** 2026-05-02T22:59:30  
**Model:** claude-opus-4-7

---

# Visual Triage: Cornell Box GI Cascade

## 1. GI Contribution Analysis

Comparing mode 0 (final) against mode 6 (GI only):

- **Color bleeding** is clearly present and physically plausible: the left side of the tall block picks up faint **red bounce** from the left wall, and the right side of the short cube shows **green bounce** from the right wall (visible in mode 6, subtly preserved in mode 0).
- **Ceiling near the light** in mode 6 shows the strongest indirect return — consistent with the area-light bounce off the floor and back walls.
- **Floor between the two boxes** has noticeably warmer/greener tint in mode 6, indicating cross-bounce between the colored walls.
- The **back wall** in mode 6 is dim and neutral grey, as expected (it faces away from the strongest bounce sources).
- **Outside the room frame** (the dark grey border slabs visible in modes 0/3): GI is essentially absent — correct, since these are the exterior of the box.

## 2. Banding Artifacts in Mode 6

Looking carefully at mode 6:

- **Soft horizontal/vertical blockiness** is visible on the back wall and ceiling — roughly 6–8 wide low-frequency cells across the back wall. This spacing is consistent with **probe-grid spacing** (cascade 0/1 footprint), not directional bins.
- **No sharp angular streaks** radiating from surface points — that would indicate directional-bin (dirRes=8) aliasing, and I do **not** see it clearly here.
- **No abrupt luminance step** along a ring/shell that would mark a **cascade boundary** — transitions look smooth across the room volume.
- The dim halo just outside the room edges (modes 3 and 6) is probe-footprint leakage, not a cascade seam.

Verdict: dominant artifact is **probe-grid interpolation**, mild.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5):** Spatially **very smooth**, almost no high-frequency noise. This is consistent with the low variance reported (0.004–0.006 across cascades). Possibly *too* smooth — fine contact details around the box bases are washed out.
- **Mode 0 (final):** Direct lighting edges (box silhouettes, shadow under tall block) are crisp. The bilateral blur has **not over-smoothed** geometric edges. However, the **contact shadows** beneath both boxes look slightly soft — borderline acceptable.
- **Mode 6:** Smooth but low-contrast; the back wall's GI gradient is gentle and continuous.

## 4. Ratings

| Mode | Rating | Reason |
|------|--------|--------|
| Mode 0 (Final) | **Good** | Clean composite, plausible bleeding, edges intact |
| Mode 3 (Indirect ×5) | **Good** | Smooth, low noise, but masks subtle structure |
| Mode 6 (GI only) | **Fair** | Visible probe-grid blockiness on back wall; cascade 3 is under-energized (meanLum 0.029 vs ~0.05 for 0–2) |

## 5. Stats Cross-Check & Recommendation

Key concerns from stats:
- **Cascade 0 surfPct = 29.9%** — only ~30% of cascade-0 probe rays hit a surface. The other 70% miss into empty space (skyPct=0 confirms no sky hit, so they're terminating without contribution). This wastes the highest-resolution cascade.
- **Cascade 3** has 5× lower variance and ~60% the mean luminance of cascades 0–2 — it's contributing little, suggesting its footprint extends mostly outside the lit room.
- Variance values (~0.005) are low, matching the smooth appearance — banding is **interpolation**, not noise.

### Concrete recommendation: **Reduce cascade-0 ray length (or increase `probeJitterScale` from 0.25 → 0.4)**

The 29.9% surfPct on cascade 0 indicates probes are over-reaching into the void outside the box. Either:
- **Shrink cascade 0's interval** so it stays inside the room (raises surfPct, reduces probe-grid blockiness on the back wall), **or**
- **Bump `probeJitterScale` to ~0.4** to break up the visible grid cells on the back wall in mode 6 — temporalAlpha=
