# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784855244038389_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784855244883905_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784855245925295_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784855244038389.json`  
**Analyzed:** 2026-05-11T15:45:52  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing mode 0 (final) vs mode 6 (GI only), the indirect light is **brightest along the ceiling beams/slats** in the upper third of the frame — these red-brown horizontal stripes show clear bounce light coloring the surrounding ceiling area. The **far end of the corridor** shows a bright warm pocket (around the doorway/light source at center, ~x=480 y=320) where indirect bounce is concentrated.

GI is **noticeably absent** on the right-hand wall, which appears almost flat brown in mode 6 — this surface is in shadow from direct light AND receives little bounce, suggesting it is either back-facing to the cascade probes or the probe density is insufficient there. The **floor in the foreground** also shows weak GI contribution; the bright specks visible on the floor in mode 0 are from direct light, not bounce (they vanish in mode 6).

## 2. Banding Artifacts in Mode 6

Visible artifacts:
- **Blocky chunks on the ceiling slats** (~16 px wide, distributed every ~20 px horizontally) — these match the **probe grid spacing** at the higher cascades (baseRes 32 over volumeSize 4 → ~0.125 units/probe at C0).
- **Soft horizontal banding on the right wall** at roughly mid-height — spacing is consistent with **cascade boundary transitions** between C0 and C1 (their meanLum differs ~6%, but maxLum differs 2×, so the seam shows).
- No clear directional-bin streaking (dirRes=8 is coarse but no sharp 45°/22.5° wedges are visible — the bilateral blur in mode 0 hides this regardless).

## 3. Smoothness of Indirect Term

**Mode 3 (×5 amplified)**: spatially **smooth, almost over-smoothed** — the corridor walls and floor look like a flat wash. This is consistent with the very low cascade variance values (8.9e-6 at C3). Probably temporally over-blended (alpha=0.05 is aggressive).

**Mode 0 (final)**: the bilateral blur has **clearly over-smoothed edges**. Notice the **right wall/floor seam** in the right half of the frame — there is no crisp corner, just a continuous brown gradient. The left wall pillar (~x=200) has also lost its silhouette into a soft halo. The overhead slat shadows bleed onto the ceiling.

## 4. Mode Ratings

| Mode | Rating | Notes |
|---|---|---|
| 0 Final | **Fair** | Readable, but blur kernel too wide; geometry edges lost |
| 3 Indirect ×5 | **Good** | Smooth, no objectionable noise, useful diagnostic |
| 6 GI only | **Fair** | Probe-grid blockiness visible on ceiling; right wall under-lit |

## 5. Stats Cross-Check & Recommendation

- `surfPct` at C0 = 48.6% — **half of C0 probes failed to find a surface hit**. This is the root cause of the blocky ceiling artifacts: C0 is the finest cascade and is under-sampled.
- `variance` drops 100× from C0 (1.0e-3) to C3 (8.9e-6) — confirms the higher cascades are essentially DC, while C0 carries all the spatial structure (and all the noise).
- `temporalAlpha` = 0.05 is very low → smooth but explains the lack of crisp edges and the over-soft mode 3.

**Concrete suggestion:** Increase `c0MinRange` from 0.5 → ~0.9 (or bump `baseRes` from 32 → 48). This will raise C0 `surfPct` toward ~80%, eliminating the ceiling blockiness without needing more directional bins. If only one knob can change, **raise `c0MinRange` to 0.9** — it directly addresses the low C0 hit rate that is producing the visible probe-grid banding.
