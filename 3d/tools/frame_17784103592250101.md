# AI Visual Triage — Burst (Modes 0 / 3 / 6)

**Mode 0:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784103592250101_m0.png`  
**Mode 3:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784103594993567_m3.png`  
**Mode 6:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/frame_17784103597496014_m6.png`  
**Stats:** `D:\GitRepo-My\radiance-cascades-demo\3d\tools/probe_stats_17784103592250101.json`  
**Analyzed:** 2026-05-10T18:53:11  
**Model:** claude-opus-4-7

---

# Visual Triage Report

## 1. GI Contribution Analysis

Comparing mode 0 (final) against modes 3 and 6 reveals where indirect light is doing work:

- **Right wall (foreground, ~x=900–1280)**: In mode 6 the right wall is a saturated reddish-brown — this is the dominant GI contribution, a strong color bleed from an off-screen warm surface (likely the door/wall at corridor end). It carries through into mode 0, tinting the entire right half.
- **Left wall (foreground)**: Much cooler/grayer in all three modes. GI is present but weak — the wall reads near-neutral in mode 6, suggesting it sits in shadow with little bounced contribution.
- **Floor (center, near to mid-distance)**: Mode 3 shows the floor as nearly white when amplified, but mode 0 keeps it gray — direct light dominates here. The bright specular streaks on the floor in mode 0 are direct-only (absent in modes 3 and 6).
- **Far doorway/end of corridor**: Mode 3 shows a soft warm glow spilling out of the rear opening; mode 6 confirms this is the brightest GI region in the scene. This is the "light source" feeding the right-wall bleed.
- **Ceiling beams (top center)**: Visible in all three modes with consistent color — direct light dominates these brown stripes; GI contribution is minor.

## 2. Banding in Mode 6

- **Vertical/columnar soft bands** along both side walls, spaced roughly every ~120–160 px in screen space at mid-distance. Spacing scales with depth — characteristic of a **probe-grid footprint** (baseRes=32, volumeSize=4 → ~0.125 unit spacing), not directional bins.
- **No fan-shaped or angular discontinuities** that would indicate directional-bin (dirRes=8) aliasing — directional structure is hidden by the blur.
- **No sharp planar discontinuity** crossing the image diagonally — cascade boundaries (cascadeCount=4) are not visibly seamed in this frame, but the right-wall warm blob has a soft falloff that could be a c0→c1 transition near mid-depth.
- Blocky pixelation on the ceiling beams is texture/voxel resolution, not GI banding.

## 3. Smoothness Assessment

- **Mode 3 (indirect ×5)**: Spatially smooth — almost over-smooth. No high-frequency noise visible, but the probe-grid structure described above shows through. Temporal accumulation (alpha=0.05) is clearly aggressive.
- **Mode 0 (final)**: The bilateral blur has **over-smoothed the right-wall edge** — the warm bleed has no clean termination against the ceiling or floor; it bleeds across the geometric corner near (x≈1100, y≈300–600). Floor specular highlights survive, so the blur is depth/normal-aware but its color radius is too wide.

## 4. Ratings

| Mode | Rating | Notes |
|------|--------|-------|
| Mode 0 | **Fair** | Readable, plausible lighting, but right-wall GI smear hurts edges |
| Mode 3 | **Good** | Clean, smooth, useful diagnostic — confirms GI is working |
| Mode 6 | **Fair** | Probe-grid banding visible; the strong right-wall bleed looks physically suspect |

## 5. Stats Cross-Check & Recommendation

The stats are alarming:
- **Cascade 0**: `anyPct=0.012%` — essentially **no probes hit anything**. `maxLum=170` with `variance=0.89` means a single firefly probe is driving the result.
- **Cascades 1–3**: Negative `meanLum` values (−47, −375, −320) are **non-physical** — likely accumulator overflow, sign bug, or uninitialized memory being averaged in. `variance=20824` on c1 and `variance=-0.109` on c2 (negative variance is impossible) confirm the buffers are corrupted or being read with a wrong stride.
- `surfPct=0` on cascades 0–2 means the surface-hit classifier isn't tagging anything — the GI you're seeing in the image is being produced almost entirely by cascade 3's 1.5% surface hits and 12.5% sky hits, which explains the broad, low-frequency, warm wash with no local detail.

### Concrete change

**Fix the casc
